# `v2sim.gui.evtq`

本模块共记录 9 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gui.evtq.EventQueue | class | v2sim/gui/evtq.py#L8 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.evtq.EventQueue 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | Queue；deque；self.do；time.time_ns；self._Q.put；start；self._delegates.append；self._delegates.popleft；self._Q.get；self._Q.task_done；self._parent.update；self._parent.after；ValueError；func；self._Q.empty；print | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.evtq.EventQueue.__init__ | (self, parent, maxsize: int=0, interval_ms: int=100, max_proc_ms: int=90) | v2sim/gui/evtq.py#L9 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.evtq.EventQueue.__init__ 的实现与调用链确认。 | 输入：parent, maxsize, interval_ms, max_proc_ms；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | Queue；deque；self.do | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.evtq.EventQueue.asyncrun | (self, func: Callable, *args, **kwargs) | v2sim/gui/evtq.py#L88 | Run a no-return function asynchronously | 输入：func, *args, **kwargs；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | start；func；threading.Thread；traceback.print_exc；print | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.evtq.EventQueue.delegate | (self, func: Callable, *args, **kwargs) | v2sim/gui/evtq.py#L98 | Run a no-return function on the main thread. | 输入：func, *args, **kwargs；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._delegates.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.evtq.EventQueue.do | (self) | v2sim/gui/evtq.py#L18 | Process all events in the queue. | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | time.time_ns；self._delegates.popleft；self._Q.get；self._Q.task_done；self._parent.update；self._parent.after；func；self._Q.empty；print；max；traceback.print_exc | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.evtq.EventQueue.register | (self, name: str, callback: Callable) | v2sim/gui/evtq.py#L55 | Register an event handler for a specific event name. | 输入：name, callback；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.evtq.EventQueue.setcallback | (self, name: str, callback: Callable) | v2sim/gui/evtq.py#L61 | Set or update the callback for an event. | 输入：name, callback；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.evtq.EventQueue.submit | (self, name: str, func: Callable, *args, **kwargs) | v2sim/gui/evtq.py#L73 | Run a function asychoronously and submit the results to trigger an event. | 输入：name, func, *args, **kwargs；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | start；func；self.trigger；threading.Thread；traceback.print_exc；print；isinstance | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.evtq.EventQueue.trigger | (self, name: str, *args, **kwargs) | v2sim/gui/evtq.py#L67 | Trigger an event by its name with optional arguments. | 输入：name, *args, **kwargs；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._Q.put；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
