# `v2sim.gui.viewerbox.gridpage`

本模块共记录 6 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gui.viewerbox.gridpage.GridFrame | class(Frame) | v2sim/gui/viewerbox/gridpage.py#L16 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.gridpage.GridFrame 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；isinstance；Frame；self.panel_time2.pack；Label；self.lb_time2.grid；Entry；self.entry_time2.insert；self.entry_time2.grid；Button；self.btn_time2.grid；ScrollableTreeView；self.grbus.pack；self.grbus.heading；self.grbus.column；self.grline.pack | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.gridpage.GridFrame.__init__ | (self, parent, to_set, *args, **kwargs) | v2sim/gui/viewerbox/gridpage.py#L17 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.gridpage.GridFrame.__init__ 的实现与调用链确认。 | 输入：parent, to_set, *args, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；isinstance；Frame；self.panel_time2.pack；Label；self.lb_time2.grid；Entry；self.entry_time2.insert；self.entry_time2.grid；Button；self.btn_time2.grid；ScrollableTreeView；self.grbus.pack；self.grbus.heading；self.grbus.column；self.grline.pack | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.gridpage.GridFrame.collectgrid | (self) | v2sim/gui/viewerbox/gridpage.py#L67 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.gridpage.GridFrame.collectgrid 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.grbus.clear；self._master.set_status；int；value_at；self.grbus.insert；self.grline.insert；self.entry_time2.get；self._sta.bus_attrib_of；self._sta.line_attrib_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.gridpage.GridFrame.setSta | (self, sta: ReadOnlyStatistics) | v2sim/gui/viewerbox/gridpage.py#L64 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.gridpage.GridFrame.setSta 的实现与调用链确认。 | 输入：sta；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.gui.viewerbox.gridpage.ISupportSetStatus | class(Protocol) | v2sim/gui/viewerbox/gridpage.py#L12 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.gridpage.ISupportSetStatus 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.gridpage.ISupportSetStatus.set_status | (self, msg: str) | v2sim/gui/viewerbox/gridpage.py#L13 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.gridpage.ISupportSetStatus.set_status 的实现与调用链确认。 | 输入：msg；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
