# `v2sim.gui.viewerbox.plotpage`

本模块共记录 8 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gui.viewerbox.plotpage.PlotPage | class(Frame) | v2sim/gui/viewerbox/plotpage.py#L12 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.plotpage.PlotPage 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.accum_plotmax.get；__init__；self.columnconfigure；LabelFrame；self.lfra_head.grid；Frame；self.panel_time.pack；Label；self.lb_time.pack；Entry；self.entry_time.insert；self.entry_time.pack；self.lb_end_time.pack；self.entry_end_time.insert；self.entry_end_time.pack；BooleanVar | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.plotpage.PlotPage.AccumPlotMax | (self) -> bool | v2sim/gui/viewerbox/plotpage.py#L14 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.plotpage.PlotPage.AccumPlotMax 的实现与调用链确认。 | 输入：无显式输入；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.accum_plotmax.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.plotpage.PlotPage.__init__ | (self, parent, *args, **kwargs) | v2sim/gui/viewerbox/plotpage.py#L17 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.plotpage.PlotPage.__init__ 的实现与调用链确认。 | 输入：parent, *args, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；self.columnconfigure；LabelFrame；self.lfra_head.grid；Frame；self.panel_time.pack；Label；self.lb_time.pack；Entry；self.entry_time.insert；self.entry_time.pack；self.lb_end_time.pack；self.entry_end_time.insert；self.entry_end_time.pack；BooleanVar；Checkbutton | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.plotpage.PlotPage.disable | (self, items: List[str]=[]) | v2sim/gui/viewerbox/plotpage.py#L229 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.plotpage.PlotPage.disable 的实现与调用链确认。 | 输入：items；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；disable；getattr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.plotpage.PlotPage.enable | (self, items: Optional[List[str]]=None) | v2sim/gui/viewerbox/plotpage.py#L218 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.plotpage.PlotPage.enable 的实现与调用链确认。 | 输入：items；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | enable；getattr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.plotpage.PlotPage.getConfig | (self) | v2sim/gui/viewerbox/plotpage.py#L186 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.plotpage.PlotPage.getConfig 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | int；self.accum_plotmax.get；self.entry_time.get；self.entry_end_time.get；self.fcs_pad.accum.get；self.plot_fcs.get；self.scs_pad.accum.get；self.plot_scs.get；self.bus_pad.accum.get；self.plot_bus.get；self.gen_pad.accum.get；self.plot_gen.get；self.fcs_opts.getValues；self.scs_opts.getValues；self.plot_gs.get；self.gs_opts.getValues | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.plotpage.PlotPage.getTime | (self) | v2sim/gui/viewerbox/plotpage.py#L206 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.plotpage.PlotPage.getTime 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | int；self.entry_time.get；self.entry_end_time.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.plotpage.PlotPage.pars | (self, key: str) | v2sim/gui/viewerbox/plotpage.py#L209 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.plotpage.PlotPage.pars 的实现与调用链确认。 | 输入：key；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；ret.update；self.getConfig；int；self.entry_time.get；self.entry_end_time.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
