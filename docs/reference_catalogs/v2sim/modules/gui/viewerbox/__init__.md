# `v2sim.gui.viewerbox`

本模块共记录 33 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gui.viewerbox.ViewerBox | class(Tk) | v2sim/gui/viewerbox/__init__.py#L24 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；self.title；self.geometry；Menu；self.config；self.menu.add_cascade；self.filemenu.add_command；self.filemenu.add_separator；add_lang_menu；Notebook；self.tab.pack；Frame；self.tab.add；GridFrame；TripsFrame；StateFrame | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.__init__ | (self, project_folder: Optional[str]=None) | v2sim/gui/viewerbox/__init__.py#L28 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.__init__ 的实现与调用链确认。 | 输入：project_folder；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；self.title；self.geometry；Menu；self.config；self.menu.add_cascade；self.filemenu.add_command；self.filemenu.add_separator；add_lang_menu；Notebook；self.tab.pack；Frame；self.tab.add；GridFrame；TripsFrame；StateFrame | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.__on_win_loaded | (self, event) | v2sim/gui/viewerbox/__init__.py#L104 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.__on_win_loaded 的实现与调用链确认。 | 输入：event；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.unbind；self.load_results | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox._plot_bus | (self) | v2sim/gui/viewerbox/__init__.py#L437 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox._plot_bus 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._pp.bus_pad.get；enumerate；self._Q.delegate；self._npl.quick_bus；t.strip；x.strip；t.split；self._pp.pars；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox._plot_bus_total | (self) | v2sim/gui/viewerbox/__init__.py#L453 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox._plot_bus_total 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._pp.getTime；self._npl.quick_bus_tot | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox._plot_ess | (self) | v2sim/gui/viewerbox/__init__.py#L481 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox._plot_ess 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._pp.ess_pad.get；enumerate；self._Q.delegate；self._npl.quick_ess；t.strip；x.strip；t.split；self._pp.pars；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox._plot_ev | (self) | v2sim/gui/viewerbox/__init__.py#L408 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox._plot_ev 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | int；self._pp.ev_pad.get；self._pp.entry_time.get；self._Q.delegate；self._npl.quick_ev；t.strip；x.strip；t.split；self._pp.pars | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox._plot_fcs | (self) | v2sim/gui/viewerbox/__init__.py#L363 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox._plot_fcs 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._pp.fcs_pad.get；enumerate；self._Q.delegate；self._npl.quick_fcs；t.strip；x.strip；self._pp.pars；t.split；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox._plot_fcs_accum | (self) | v2sim/gui/viewerbox/__init__.py#L359 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox._plot_fcs_accum 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._pp.getTime；self._npl.quick_fcs_accum | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox._plot_gen | (self) | v2sim/gui/viewerbox/__init__.py#L421 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox._plot_gen 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._pp.gen_pad.get；enumerate；self._Q.delegate；self._npl.quick_gen；t.strip；self._pp.pars；x.strip；len；x.startswith；t.split | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox._plot_gen_total | (self) | v2sim/gui/viewerbox/__init__.py#L449 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox._plot_gen_total 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._pp.getTime；self._npl.quick_gen_tot | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox._plot_gs | (self) | v2sim/gui/viewerbox/__init__.py#L393 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox._plot_gs 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._pp.gs_pad.get；enumerate；self._Q.delegate；self._npl.quick_gs；t.strip；x.strip；self._pp.pars；t.split；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox._plot_line | (self) | v2sim/gui/viewerbox/__init__.py#L457 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox._plot_line 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._pp.line_pad.get；enumerate；self._Q.delegate；self._npl.quick_line；t.strip；x.strip；t.split；self._pp.pars；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox._plot_pvw | (self) | v2sim/gui/viewerbox/__init__.py#L469 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox._plot_pvw 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._pp.pvw_pad.get；enumerate；self._Q.delegate；self._npl.quick_pvw；t.strip；x.strip；t.split；self._pp.pars；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox._plot_scs | (self) | v2sim/gui/viewerbox/__init__.py#L378 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox._plot_scs 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._pp.scs_pad.get；enumerate；self._Q.delegate；self._npl.quick_scs；t.strip；x.strip；self._pp.pars；t.split；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox._plot_scs_accum | (self) | v2sim/gui/viewerbox/__init__.py#L355 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox._plot_scs_accum 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._pp.getTime；self._npl.quick_scs_accum | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.askdir | (self) | v2sim/gui/viewerbox/__init__.py#L224 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.askdir 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | p.mkdir；filedialog.askdirectory；Path；os.getcwd；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.disable_all | (self) | v2sim/gui/viewerbox/__init__.py#L149 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.disable_all 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._pp.disable | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.display_images | (self, file_name: str) | v2sim/gui/viewerbox/__init__.py#L108 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.display_images 的实现与调用链确认。 | 输入：file_name；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | os.path.join；self.resize；os.path.exists；Image.open；MB.showerror；format；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.enable_all | (self) | v2sim/gui/viewerbox/__init__.py#L153 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.enable_all 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._pp.enable；self._ava.items | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.load_results | (self, folder: str) | v2sim/gui/viewerbox/__init__.py#L238 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.load_results 的实现与调用链确认。 | 输入：folder；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | cproc.exists；self.set_status；str；self.title；self.disable_all；self._Q.submit；state_path.exists；Path；res_path.exists；self.askdir；self.tab_trip.load；res_path.iterdir；ReadOnlyStatistics；AdvancedPlot；npl.load_series；self._Q.trigger | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.on_error | (self, par) | v2sim/gui/viewerbox/__init__.py#L211 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.on_error 的实现与调用链确认。 | 输入：par；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | MB.showerror；self.set_status；self.enable_all | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.on_file_select | (self, event) | v2sim/gui/viewerbox/__init__.py#L143 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.on_file_select 的实现与调用链确认。 | 输入：event；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.pic_list.curselection；self.pic_list.get；self.display_images | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.on_loaded | (self, sta: ReadOnlyStatistics, npl: AdvancedPlot) | v2sim/gui/viewerbox/__init__.py#L174 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.on_loaded 的实现与调用链确认。 | 输入：sta, npl；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self.tab_grid.setSta；self._sta.has_FCS；self._sta.has_SCS；self._sta.has_GS；self._sta.has_GEN；self._sta.has_BUS；self._sta.has_LINE；self._sta.has_PVW；self._sta.has_ESS；self.update_file_list；self.set_status；self.enable_all；self._pp.fcs_pad.setValues；self._pp.scs_pad.setValues；self._pp.gs_pad.setValues | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.on_plot_done | (self, ex: Optional[Exception]=None) | v2sim/gui/viewerbox/__init__.py#L216 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.on_plot_done 的实现与调用链确认。 | 输入：ex；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.update_file_list；self.set_status；self.enable_all；self.on_error；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.on_resize | (self, event) | v2sim/gui/viewerbox/__init__.py#L135 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.on_resize 的实现与调用链确认。 | 输入：event；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.after；self.after_cancel | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.on_state_loaded | (self, par, mode) | v2sim/gui/viewerbox/__init__.py#L171 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.on_state_loaded 的实现与调用链确认。 | 输入：par, mode；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.tab_state.setStateInst | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.open | (self) | v2sim/gui/viewerbox/__init__.py#L233 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.open 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.askdir；self.load_results | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.plotSelected | (self) | v2sim/gui/viewerbox/__init__.py#L320 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.plotSelected 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._pp.getConfig；self.disable_all；self.set_status；self._pp.cb_ext.get；self._pp.plot_title.get；self._Q.submit；int；MB.showerror；self.enable_all；self._pp.entry_dpi.get；set；traceback.print_exc；getattr；self._Q.delegate | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.resize | (self) | v2sim/gui/viewerbox/__init__.py#L122 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.resize 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.original_image.copy；resized_image.thumbnail；ImageTk.PhotoImage；self.lb_pic.config；self.winfo_width；self.winfo_height | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.resize_end | (self) | v2sim/gui/viewerbox/__init__.py#L140 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.resize_end 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.resize | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.set_status | (self, text: str) | v2sim/gui/viewerbox/__init__.py#L157 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.set_status 的实现与调用链确认。 | 输入：text；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._sbar.configure | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.ViewerBox.update_file_list | (self) | v2sim/gui/viewerbox/__init__.py#L160 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.ViewerBox.update_file_list 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.pic_list.delete；self.lb_pic.config；os.path.exists；set；sorted；os.listdir；endswith；self.pic_list.insert；file.lower | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
