# `v2sim.gui.viewerbox.plotpad`

本模块共记录 7 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gui.viewerbox.plotpad.PlotPad | class(Frame) | v2sim/gui/viewerbox/plotpad.py#L7 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.plotpad.PlotPad 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；self.cb.pack；BooleanVar；isinstance；self.cb.get；Entry；Combobox；self.accum.set；Checkbutton；self.cb_accum.pack；self.cb.current；self.cb.set；self.cb.delete；self.cb.insert；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.plotpad.PlotPad.__init__ | (self, master, show_accum: bool=False, useEntry: bool=False, useTotalText: bool=False, **kwargs) | v2sim/gui/viewerbox/plotpad.py#L8 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.plotpad.PlotPad.__init__ 的实现与调用链确认。 | 输入：master, show_accum, useEntry, useTotalText, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；self.cb.pack；BooleanVar；Entry；Combobox；self.accum.set；Checkbutton；self.cb_accum.pack；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.plotpad.PlotPad.disable | (self) | v2sim/gui/viewerbox/plotpad.py#L39 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.plotpad.PlotPad.disable 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.plotpad.PlotPad.enable | (self) | v2sim/gui/viewerbox/plotpad.py#L43 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.plotpad.PlotPad.enable 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.plotpad.PlotPad.get | (self) | v2sim/gui/viewerbox/plotpad.py#L36 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.plotpad.PlotPad.get 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.cb.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.plotpad.PlotPad.set | (self, item: str) | v2sim/gui/viewerbox/plotpad.py#L29 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.plotpad.PlotPad.set 的实现与调用链确认。 | 输入：item；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | isinstance；self.cb.set；self.cb.delete；self.cb.insert | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.viewerbox.plotpad.PlotPad.setValues | (self, values: List[str]) | v2sim/gui/viewerbox/plotpad.py#L24 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.viewerbox.plotpad.PlotPad.setValues 的实现与调用链确认。 | 输入：values；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self.cb.current | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
