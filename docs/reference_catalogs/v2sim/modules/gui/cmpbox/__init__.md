# `v2sim.gui.cmpbox`

本模块共记录 10 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gui.cmpbox.CmpBox | class(Tk) | v2sim/gui/cmpbox/__init__.py#L11 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.cmpbox.CmpBox 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；self.title；self.geometry；self.bind；self.create_widgets；Menu；self.config；self.filemenu.add_command；self.filemenu.add_separator；self.menu.add_cascade；add_lang_menu；Frame；self.sidebar.pack；Button；self.folder1_button.pack；self.folder2_button.pack | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.cmpbox.CmpBox.__init__ | (self) | v2sim/gui/cmpbox/__init__.py#L12 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.cmpbox.CmpBox.__init__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；self.title；self.geometry；self.bind；self.create_widgets；_；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.cmpbox.CmpBox.create_widgets | (self) | v2sim/gui/cmpbox/__init__.py#L27 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.cmpbox.CmpBox.create_widgets 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | Menu；self.config；self.filemenu.add_command；self.filemenu.add_separator；self.menu.add_cascade；add_lang_menu；Frame；self.sidebar.pack；Button；self.folder1_button.pack；self.folder2_button.pack；Label；self.folder1_label.pack；self.folder2_label.pack；Listbox；self.file_listbox.pack | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.cmpbox.CmpBox.display_images | (self, file_name: str) | v2sim/gui/cmpbox/__init__.py#L134 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.cmpbox.CmpBox.display_images 的实现与调用链确认。 | 输入：file_name；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | os.path.join；self.resize；os.path.exists；Image.open；MB.showerror；_；format；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.cmpbox.CmpBox.on_file_select | (self, event) | v2sim/gui/cmpbox/__init__.py#L100 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.cmpbox.CmpBox.on_file_select 的实现与调用链确认。 | 输入：event；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.file_listbox.curselection；self.file_listbox.get；self.display_images | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.cmpbox.CmpBox.on_resize | (self, event) | v2sim/gui/cmpbox/__init__.py#L131 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.cmpbox.CmpBox.on_resize 的实现与调用链确认。 | 输入：event；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.resize | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.cmpbox.CmpBox.open_folder1 | (self) | v2sim/gui/cmpbox/__init__.py#L66 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.cmpbox.CmpBox.open_folder1 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | filedialog.askdirectory；os.path.join；os.path.exists；_；self.folder1_label.config；self.update_file_list；str；MB.showerror；format；Path；os.path.basename | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.cmpbox.CmpBox.open_folder2 | (self) | v2sim/gui/cmpbox/__init__.py#L78 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.cmpbox.CmpBox.open_folder2 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | filedialog.askdirectory；os.path.join；os.path.exists；_；self.folder2_label.config；self.update_file_list；str；MB.showerror；format；Path；os.path.basename | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.cmpbox.CmpBox.resize | (self) | v2sim/gui/cmpbox/__init__.py#L106 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.cmpbox.CmpBox.resize 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.original_image1.copy；resized_image1.thumbnail；ImageTk.PhotoImage；self.image1_label.config；self.original_image2.copy；resized_image2.thumbnail；self.image2_label.config；self.winfo_width；self.winfo_height；_ | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.cmpbox.CmpBox.update_file_list | (self) | v2sim/gui/cmpbox/__init__.py#L90 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.cmpbox.CmpBox.update_file_list 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.file_listbox.delete；set；files1.union；sorted；os.listdir；endswith；self.file_listbox.insert；file.lower | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
