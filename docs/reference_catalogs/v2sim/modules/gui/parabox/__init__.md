# `v2sim.gui.parabox`

本模块共记录 11 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gui.parabox.ParaBox | class(Tk) | v2sim/gui/parabox/__init__.py#L13 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.parabox.ParaBox 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；self.title；Menu；self.config；self.filemenu.add_command；self.filemenu.add_separator；self.menu.add_cascade；add_lang_menu；self.geometry；ScrollableTreeView；self.tr.column；self.tr.heading；self.tr.pack；Frame；self.fr.pack；Button | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.parabox.ParaBox.__init__ | (self) | v2sim/gui/parabox/__init__.py#L14 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.parabox.ParaBox.__init__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；self.title；Menu；self.config；self.filemenu.add_command；self.filemenu.add_separator；self.menu.add_cascade；add_lang_menu；self.geometry；ScrollableTreeView；self.tr.column；self.tr.heading；self.tr.pack；Frame；self.fr.pack；Button | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.parabox.ParaBox._load | (self) | v2sim/gui/parabox/__init__.py#L68 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.parabox.ParaBox._load 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | Path；filedialog.askdirectory；init_dir.exists；init_dir.mkdir；DetectFiles；str；_L；MB.showerror | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.parabox.ParaBox.check | (self) | v2sim/gui/parabox/__init__.py#L151 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.parabox.ParaBox.check 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | time2str；self.q.empty；self.q.get；t.cmd.strip；self.after；isinstance；print；len；text.startswith；time.time；self.tr.set；float；text.replace；_L | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.parabox.ParaBox.check_outpath | (self, out: str) | v2sim/gui/parabox/__init__.py#L87 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.parabox.ParaBox.check_outpath 的实现与调用链确认。 | 输入：out；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.tr.get_children；self.tr.item | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.parabox.ParaBox.disable | (self) | v2sim/gui/parabox/__init__.py#L58 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.parabox.ParaBox.disable 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.parabox.ParaBox.enable | (self) | v2sim/gui/parabox/__init__.py#L63 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.parabox.ParaBox.enable 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.parabox.ParaBox.load | (self) | v2sim/gui/parabox/__init__.py#L99 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.parabox.ParaBox.load 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._load；LoadGroupBox；lgb.wait_window；join；Path；len；MB.showerror；lgb.lip.getSelected；self.tr.insert；_L；str；self.rename_outpath；self.tr.get_children | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.parabox.ParaBox.remove | (self) | v2sim/gui/parabox/__init__.py#L120 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.parabox.ParaBox.remove 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.tr.selection；len；self.tr.delete；MB.showerror；_L | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.parabox.ParaBox.rename_outpath | (self, out: str) | v2sim/gui/parabox/__init__.py#L93 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.parabox.ParaBox.rename_outpath 的实现与调用链确认。 | 输入：out；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.check_outpath | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.parabox.ParaBox.run | (self) | v2sim/gui/parabox/__init__.py#L127 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.parabox.ParaBox.run 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.tr.get_children；len；mp.Queue；time.time；enumerate；self.disable；self.after；MB.showinfo；eval；start；_L；self.tr.item；mp.Process；RedirectStdout | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
