# `v2sim.gui.convertbox`

本模块共记录 10 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gui.convertbox.ConvertBox | class(Tk) | v2sim/gui/convertbox/__init__.py#L10 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.convertbox.ConvertBox 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；self.title；self.geometry；StringVar；self.setup_ui；Frame；input_frame.pack；pack；input_select_frame.pack；output_frame.pack；output_select_frame.pack；partition_frame.pack；partition_frame.columnconfigure；BooleanVar；grid；Spinbox | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.convertbox.ConvertBox.__init__ | (self, *args, **kwargs) | v2sim/gui/convertbox/__init__.py#L11 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.convertbox.ConvertBox.__init__ 的实现与调用链确认。 | 输入：*args, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；self.title；self.geometry；StringVar；self.setup_ui；_；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.convertbox.ConvertBox.execute_first_program | (self) | v2sim/gui/convertbox/__init__.py#L116 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.convertbox.ConvertBox.execute_first_program 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.input_folder.get；self.output_folder.get；os.path.isdir；ConvertCase；_；traceback.print_exc；int；self.auto_partition.get；self.non_passenger_links.get；self.non_scc_links.get；str；self.partition_count.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.convertbox.ConvertBox.execute_program | (self) | v2sim/gui/convertbox/__init__.py#L101 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.convertbox.ConvertBox.execute_program 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.execute_button.config；threading.Thread；thread.start；MB.showwarning；self.input_folder.get；self.output_folder.get；_ | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.convertbox.ConvertBox.first_program_completed | (self, result) | v2sim/gui/convertbox/__init__.py#L138 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.convertbox.ConvertBox.first_program_completed 的实现与调用链确认。 | 输入：result；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | MB.askyesno；MB.showerror；self.reset_ui；_；self.destroy；subprocess.run；format；str；Path | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.convertbox.ConvertBox.reset_ui | (self) | v2sim/gui/convertbox/__init__.py#L155 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.convertbox.ConvertBox.reset_ui 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.execute_button.config；_ | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.convertbox.ConvertBox.run_programs | (self) | v2sim/gui/convertbox/__init__.py#L112 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.convertbox.ConvertBox.run_programs 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.execute_first_program；self.after | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.convertbox.ConvertBox.select_input_folder | (self) | v2sim/gui/convertbox/__init__.py#L79 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.convertbox.ConvertBox.select_input_folder 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | filedialog.askdirectory；self.input_folder.set；_；self.output_folder.get；Path；name.startswith；p.exists；self.output_folder.set；str；folder.lower | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.convertbox.ConvertBox.select_output_folder | (self) | v2sim/gui/convertbox/__init__.py#L96 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.convertbox.ConvertBox.select_output_folder 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | filedialog.askdirectory；self.output_folder.set；_；str；Path；self.input_folder.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.convertbox.ConvertBox.setup_ui | (self) | v2sim/gui/convertbox/__init__.py#L21 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.convertbox.ConvertBox.setup_ui 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | Frame；input_frame.pack；pack；input_select_frame.pack；output_frame.pack；output_select_frame.pack；partition_frame.pack；partition_frame.columnconfigure；StringVar；BooleanVar；grid；Spinbox；self.entry_count_widget.grid；Checkbutton；self.check_auto_part_widget.grid；self.auto_partition.trace_add | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
