# `v2sim.gui.plgbox`

本模块共记录 2 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gui.plgbox.PlgBox | class(Tk) | v2sim/gui/plgbox/__init__.py#L10 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.plgbox.PlgBox 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；self.title；self.geometry；self.columnconfigure；self.rowconfigure；Treeview；tree.column；tree.grid；Scrollbar；tree.configure；vsb.grid；Frame；btn_frame.grid；Button；btn_import.pack；btn_import_link.pack | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.plgbox.PlgBox.__init__ | (self, *args, **kwargs) | v2sim/gui/plgbox/__init__.py#L11 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.plgbox.PlgBox.__init__ 的实现与调用链确认。 | 输入：*args, **kwargs；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；self.title；self.geometry；self.columnconfigure；self.rowconfigure；Treeview；tree.column；tree.grid；Scrollbar；tree.configure；vsb.grid；Frame；btn_frame.grid；Button；btn_import.pack；btn_import_link.pack | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
