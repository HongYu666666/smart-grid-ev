# `v2sim.gui.mainbox.cscsveditor`

本模块共记录 8 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gui.mainbox.cscsveditor.CSCSVEditor | class(Frame) | v2sim/gui/mainbox/cscsveditor.py#L10 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.cscsveditor.CSCSVEditor 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；EventQueue；self._Q.register；ScrollableTreeView；self.tree.setheadings；self.tree.pack；Label；self.lb_cnt.pack；Frame；self.panel.pack；Button；self.btn_downcs.grid；self.btn_downgs.grid；self.lb_amapkey.grid；Entry；self.entry_amapkey.grid | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.cscsveditor.CSCSVEditor.__init__ | (self, master, downcs_worker, downgs_worker, files: Optional[List[str]]=None, **kwargs) | v2sim/gui/mainbox/cscsveditor.py#L11 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.cscsveditor.CSCSVEditor.__init__ 的实现与调用链确认。 | 输入：master, downcs_worker, downgs_worker, files, **kwargs；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；EventQueue；self._Q.register；ScrollableTreeView；self.tree.setheadings；self.tree.pack；Label；self.lb_cnt.pack；Frame；self.panel.pack；Button；self.btn_downcs.grid；self.btn_downgs.grid；self.lb_amapkey.grid；Entry；self.entry_amapkey.grid | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.cscsveditor.CSCSVEditor.__load | (self, files: List[str]) | v2sim/gui/mainbox/cscsveditor.py#L72 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.cscsveditor.CSCSVEditor.__load 的实现与调用链确认。 | 输入：files；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.lb_cnt.config；self.tree.clear；enumerate；split；self._Q.delegate；showerr；format；len；print；self.__readfile；all_lines.extend；cs.strip；tuple | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.cscsveditor.CSCSVEditor.__readfile | (self, file: str, encoding: str) | v2sim/gui/mainbox/cscsveditor.py#L63 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.cscsveditor.CSCSVEditor.__readfile 的实现与调用链确认。 | 输入：file, encoding；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | open；f.readline；f.readlines | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.cscsveditor.CSCSVEditor.clear | (self) | v2sim/gui/mainbox/cscsveditor.py#L101 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.cscsveditor.CSCSVEditor.clear 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.lb_cnt.config；self.tree.clear；format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.cscsveditor.CSCSVEditor.downcs | (self) | v2sim/gui/mainbox/cscsveditor.py#L51 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.cscsveditor.CSCSVEditor.downcs 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | MB.askyesno；self.downcs_wk；open；f.write；strip；self.entry_amapkey.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.cscsveditor.CSCSVEditor.downgs | (self) | v2sim/gui/mainbox/cscsveditor.py#L57 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.cscsveditor.CSCSVEditor.downgs 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | MB.askyesno；self.downgs_wk；open；f.write；strip；self.entry_amapkey.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.cscsveditor.CSCSVEditor.load | (self, files: List[str]) | v2sim/gui/mainbox/cscsveditor.py#L98 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.cscsveditor.CSCSVEditor.load 的实现与调用链确认。 | 输入：files；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._Q.submit | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
