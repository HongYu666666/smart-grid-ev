# `v2sim.gui.mainbox.plugin`

本模块共记录 8 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gui.mainbox.plugin.PluginEditor | class(ScrollableTreeView) | v2sim/gui/mainbox/plugin.py#L13 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.plugin.PluginEditor 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | set；SelectItemDialog；f.wait_window；self.plg_pool.GetPluginType；issubclass；self.setCellEditMode；self.get_children；__init__；StaPool；PluginPool；load_external_components；self.column；self.heading；self.setColEditMode；clear；self.__elements.clear | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.plugin.PluginEditor.GetEnabledPlugins | (self) | v2sim/gui/mainbox/plugin.py#L28 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.plugin.PluginEditor.GetEnabledPlugins 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.get_children；enabled_plg.append；self.item | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.plugin.PluginEditor.__addgetter | (self) | v2sim/gui/mainbox/plugin.py#L14 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.plugin.PluginEditor.__addgetter 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；SelectItemDialog；f.wait_window；self.plg_pool.GetPluginType；issubclass；self.setCellEditMode；ConfigItem；default_value_dict；self.plg_pool.GetAllPlugins；self.item；self.get_children；plgtype.ElemShouldHave | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.plugin.PluginEditor.__init__ | (self, master, onEnabledSet: Callable[[Tuple[Any, ...], str], None]=empty_postfunc, **kwargs) | v2sim/gui/mainbox/plugin.py#L35 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.plugin.PluginEditor.__init__ 的实现与调用链确认。 | 输入：master, onEnabledSet, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；StaPool；PluginPool；load_external_components；self.column；self.heading；self.setColEditMode；ConfigItem；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.plugin.PluginEditor.add | (self, plg_name: str, interval: Union[int, str], enabled: str, online: Union[RangeList, str], extra: Dict[str, Any], elem: Optional[ET.Element]=None) | v2sim/gui/mainbox/plugin.py#L65 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.plugin.PluginEditor.add 的实现与调用链确认。 | 输入：plg_name, interval, enabled, online, extra, elem；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.insert；self.plg_pool.GetPluginType；issubclass；self.setCellEditMode；self.__onEnabledSet；str；ConfigItem；ET.Element；plg_type.ElemShouldHave | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.plugin.PluginEditor.clear | (self) | v2sim/gui/mainbox/plugin.py#L61 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.plugin.PluginEditor.clear 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | clear；self.__elements.clear；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.plugin.PluginEditor.is_enabled | (self, plg_name: str) | v2sim/gui/mainbox/plugin.py#L75 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.plugin.PluginEditor.is_enabled 的实现与调用链确认。 | 输入：plg_name；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.get_children；self.item | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.plugin.PluginEditor.save_xml | (self, filename: Union[str, Path], data: Optional[List[Tuple]]=None) | v2sim/gui/mainbox/plugin.py#L81 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.plugin.PluginEditor.save_xml 的实现与调用链确认。 | 输入：filename, data；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | ET.Element；open；f.write；self.getAllData；attr.update；attr.items；e.attrib.update；rt.append；replace；str；eval；e.append；isinstance；ol.append；ET.tostring | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
