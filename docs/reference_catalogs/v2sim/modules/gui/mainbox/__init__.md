# `v2sim.gui.mainbox`

本模块共记录 35 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gui.mainbox.MainBox | class(Tk) | v2sim/gui/mainbox/__init__.py#L32 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.sim_plglist.GetEnabledPlugins；self.sim_statistic.check_by_enabled_plugins；__init__；EventQueue；self._Q.register；self._win；Menu；self.menu.add_cascade；self.menuFile.add_command；self.bind；self.menuFile.add_separator；add_lang_menu；self.config；self.rowconfigure；self.columnconfigure；Frame | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.CSCSVDownloadWorker | (self) | v2sim/gui/mainbox/__init__.py#L1070 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.CSCSVDownloadWorker 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.setStatus；self.CsCsv_editor.entry_amapkey.get；self._Q.submit；self.__checkFolderOpened；StationQuery；self._load | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.GSCSVDownloadWorker | (self) | v2sim/gui/mainbox/__init__.py#L1084 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.GSCSVDownloadWorker 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.setStatus；self.CsCsv_editor.entry_amapkey.get；self._Q.submit；self.__checkFolderOpened；StationQuery；self._load | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.__OnPluginEnabledSet | (self, itm: Tuple[Any, ...]=(), v: str='') | v2sim/gui/mainbox/__init__.py#L33 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.__OnPluginEnabledSet 的实现与调用链确认。 | 输入：itm, v；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.sim_plglist.GetEnabledPlugins；self.sim_statistic.check_by_enabled_plugins | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.__checkFolderOpened | (self) | v2sim/gui/mainbox/__init__.py#L648 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.__checkFolderOpened 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | showerr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.__init__ | (self, to_open: str='') | v2sim/gui/mainbox/__init__.py#L37 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.__init__ 的实现与调用链确认。 | 输入：to_open；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；EventQueue；self._Q.register；self._win；Menu；self.menu.add_cascade；self.menuFile.add_command；self.bind；self.menuFile.add_separator；add_lang_menu；self.config；self.rowconfigure；self.columnconfigure；Frame；self.panel_info.grid；Label | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.__load_part2 | (self, loads: Set[str], async_: bool) | v2sim/gui/mainbox/__init__.py#L675 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.__load_part2 的实现与调用链确认。 | 输入：loads, async_；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | DetectFiles；self.title；self.update；self.rb_veh_src2.configure；self.rb_veh_src1.configure；setText；self.lb_traffic.config；self.sim_cb_load_last_state.configure；self.sim_cb_load_saved_state.configure；self.setStatus；self._Q.submit；self._load_fcs；self._load_scs；self._load_gs；self._load_cscsv；self._load_plugins | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox._load | (self, loads: Optional[List[str]]=None, async_: bool=True) | v2sim/gui/mainbox/__init__.py#L664 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox._load 的实现与调用链确认。 | 输入：loads, async_；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | LoadingBox；self.update；self.after；showerr；set | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox._load_cscsv | (self, after: OAfter=None) | v2sim/gui/mainbox/__init__.py#L934 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox._load_cscsv 的实现与调用链确认。 | 输入：after；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | files.append；len；self.CsCsv_editor.load；self.CsCsv_editor.clear；self.CsCsv_editor._Q.setcallback；after | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox._load_fcs | (self, afterx: OAfter=None) | v2sim/gui/mainbox/__init__.py#L884 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox._load_fcs 的实现与调用链确认。 | 输入：afterx；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.sim_statistic.setEnabled；self.FCS_editor.setPoly；self.FCS_editor.setCSCSV；self.FCS_editor._Q.setcallback；self.FCS_editor.load；self.FCS_editor.clear；after；afterx | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox._load_gs | (self, afterx: OAfter=None) | v2sim/gui/mainbox/__init__.py#L918 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox._load_gs 的实现与调用链确认。 | 输入：afterx；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.sim_statistic.setEnabled；self.GS_editor.setCSCSV；self.GS_editor._Q.setcallback；self.GS_editor.load；self.GS_editor.clear；after；afterx | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox._load_network | (self, tab_ret, after: OAfter=None) | v2sim/gui/mainbox/__init__.py#L946 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox._load_network 的实现与调用链确认。 | 输入：tab_ret, after；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.tabs.select；time.sleep；self.lb_gridsave.config；self._Q.submit；RoadNet.load；PowerGrid.fromFile；el.getProjectorOrNone | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox._load_plugins | (self) | v2sim/gui/mainbox/__init__.py#L824 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox._load_plugins 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | set；self.sim_plglist.clear；self.__OnPluginEnabledSet；ReadXML；et.getroot；default_value_dict；self.sim_plglist.add；plg_set.add；plg_enabled_set.add；self.sim_plglist.save；showerr；issubclass；p.find；p.attrib.pop；attr.update；self.sim_plglist.plg_pool.GetPluginType | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox._load_scs | (self, afterx: OAfter=None) | v2sim/gui/mainbox/__init__.py#L901 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox._load_scs 的实现与调用链确认。 | 输入：afterx；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.sim_statistic.setEnabled；self.SCS_editor.setPoly；self.SCS_editor.setCSCSV；self.SCS_editor._Q.setcallback；self.SCS_editor.load；self.SCS_editor.clear；after；afterx | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox._load_tg | (self, after: OAfter=None) | v2sim/gui/mainbox/__init__.py#L654 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox._load_tg 的实现与调用链确认。 | 输入：after；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | TrafficGenerator；traceback.print_exc；showerr；after | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox._set_sumo_panel_enabled | (self, val: bool) | v2sim/gui/mainbox/__init__.py#L818 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox._set_sumo_panel_enabled 的实现与调用链确认。 | 输入：val；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.sim_cb_sumo_show.config；self.sim_cb_sumo_ignore_driving.config；self.sim_cb_sumo_raise_routing_error.config | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox._set_uxsim_panel_enabled | (self, val: bool) | v2sim/gui/mainbox/__init__.py#L812 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox._set_uxsim_panel_enabled 的实现与调用链确认。 | 输入：val；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.sim_cb_ux_rand.config；self.sim_cb_ux_show_info.config；self.sim_cb_ux_no_para.config | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox._win | (self) | v2sim/gui/mainbox/__init__.py#L1101 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox._win 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.title | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.draw | (self) | v2sim/gui/mainbox/__init__.py#L1064 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.draw 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.cv_net.UnlocateAllEdges；set；self.cv_net.LocateEdges；self.__checkFolderOpened；x.strip；split；self.entry_Ledges.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.generateCS | (self, ctl: StationEditor, cscsv_mode: int, **kwargs) | v2sim/gui/mainbox/__init__.py#L977 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.generateCS 的实现与调用链确认。 | 输入：ctl, cscsv_mode, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.setStatus；kwargs.pop；self._Q.submit；showerr；self.tg._Station；self._load；print；traceback.print_exc | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.generateVeh | (self) | v2sim/gui/mainbox/__init__.py#L1023 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.generateVeh 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.setStatus；try_int；self.btn_genveh.config；self._Q.submit；self.__checkFolderOpened；self.entry_carcnt.get；self.entry_carseed.get；self.entry_daycnt.get；self.veh_pars.getAllData；self.veh_gen_src.get；float；eval；ValueError；self.tg.VTrips；self._load；self.entry_v2gprop.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.get_default_grid_path | (self) -> str | v2sim/gui/mainbox/__init__.py#L508 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.get_default_grid_path 的实现与调用链确认。 | 输入：无显式输入；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | str；Path | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.get_default_roadnet_path | (self) -> str | v2sim/gui/mainbox/__init__.py#L511 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.get_default_roadnet_path 的实现与调用链确认。 | 输入：无显式输入；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | str；Path | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.netsave | (self) | v2sim/gui/mainbox/__init__.py#L477 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.netsave 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | filedialog.asksaveasfilename；self.setStatus；self.cv_net.savefig；showerr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.onDestroy | (self) | v2sim/gui/mainbox/__init__.py#L531 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.onDestroy 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.destroy；MB.askyesnocancel；self.save | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.on_cvnet_loaded | (self, el: RoadNet, pdn: Optional[PowerGrid]=None, after: OAfter=None) | v2sim/gui/mainbox/__init__.py#L963 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.on_cvnet_loaded 的实现与调用链确认。 | 输入：el, pdn, after；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.cv_net.setRoadNet；self.cv_net.setGrid；self.lb_puvalues.configure；format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.on_load_state_changed | (self) | v2sim/gui/mainbox/__init__.py#L465 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.on_load_state_changed 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.sim_load_state.get；self.entry_start.config；self.entry_end.config；self.entry_step.config；self.entry_seed.config；self.combo_ralgo.config；self.sim_cb_ux_no_para.config；self.sim_cb_ux_show_info.config | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.openFolder | (self) | v2sim/gui/mainbox/__init__.py#L969 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.openFolder 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | Path；filedialog.askdirectory；init_dir.exists；init_dir.mkdir；str；self._load | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.save | (self) | v2sim/gui/mainbox/__init__.py#L502 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.save 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.sim_plglist.save；self.FCS_editor.save；self.SCS_editor.save；self.saveNet | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.saveNet | (self) | v2sim/gui/mainbox/__init__.py#L514 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.saveNet 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.cv_net.save；os.remove；self.get_default_grid_path；self.get_default_roadnet_path；endswith；gpath.lower；npath.lower | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.savePlugins | (self) | v2sim/gui/mainbox/__init__.py#L628 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.savePlugins 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.setStatus；self.__checkFolderOpened；self.sim_plglist.save_xml；traceback.print_exc；showerr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.saved | (self) | v2sim/gui/mainbox/__init__.py#L499 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.saved 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.setStatus | (self, text: str) | v2sim/gui/mainbox/__init__.py#L1098 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.setStatus 的实现与调用链确认。 | 输入：text；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.sbar.config | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.simulate | (self) | v2sim/gui/mainbox/__init__.py#L539 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.simulate 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | try_int；V2SimConfig；self.sim_load_state.get；self.sim_save_on_abort.get；self.sim_save_on_finish.get；self.sim_copy_state.get；self.ralgo.get；self.sim_ux_no_para.get；self.sim_ux_show_info.get；self.sim_ux_rand.get；self.sim_sumo_ignore_driving.get；self.sim_sumo_raise_routing_error.get；self.sim_sumo_show.get；vcfg.save；self.destroy；self.__checkFolderOpened | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.MainBox.veh_par_edit | (self, var: StringVar) | v2sim/gui/mainbox/__init__.py#L492 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.MainBox.veh_par_edit 的实现与调用链确认。 | 输入：var；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | PDFuncEditor；e.wait_window | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
