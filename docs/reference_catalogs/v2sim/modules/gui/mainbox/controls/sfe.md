# `v2sim.gui.mainbox.controls.sfe`

本模块共记录 8 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gui.mainbox.controls.sfe.SegFuncEditor | class(Toplevel) | v2sim/gui/mainbox/controls/sfe.py#L10 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.sfe.SegFuncEditor 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；self.title；ScrollableTreeView；self.tree.column；self.tree.heading；self.tree.pack；self.tree.setColEditMode；Frame；self.fr.pack；Button；self.btn_add.grid；self.btn_del.grid；self.btn_moveup.grid；self.btn_movedown.grid；self.btn_save.grid；self.tree.insert | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.sfe.SegFuncEditor.__init__ | (self, data: SegFunc, var: StringVar) | v2sim/gui/mainbox/controls/sfe.py#L11 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.sfe.SegFuncEditor.__init__ 的实现与调用链确认。 | 输入：data, var；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；self.title；ScrollableTreeView；self.tree.column；self.tree.heading；self.tree.pack；self.tree.setColEditMode；Frame；self.fr.pack；Button；self.btn_add.grid；self.btn_del.grid；self.btn_moveup.grid；self.btn_movedown.grid；self.btn_save.grid；self.tree.insert | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.sfe.SegFuncEditor.add | (self) | v2sim/gui/mainbox/controls/sfe.py#L43 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.sfe.SegFuncEditor.add 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.tree.insert | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.sfe.SegFuncEditor.delete | (self) | v2sim/gui/mainbox/controls/sfe.py#L46 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.sfe.SegFuncEditor.delete 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.tree.tree.selection；self.tree.delete | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.sfe.SegFuncEditor.getAllData | (self) -> SegFunc | v2sim/gui/mainbox/controls/sfe.py#L72 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.sfe.SegFuncEditor.getAllData 的实现与调用链确认。 | 输入：无显式输入；输出：SegFunc | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.tree.get_children；SegFunc；self.tree.tree.item；res.append；int；float | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.sfe.SegFuncEditor.movedown | (self) | v2sim/gui/mainbox/controls/sfe.py#L55 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.sfe.SegFuncEditor.movedown 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.tree.tree.selection；self.tree.tree.index；self.tree.tree.move | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.sfe.SegFuncEditor.moveup | (self) | v2sim/gui/mainbox/controls/sfe.py#L50 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.sfe.SegFuncEditor.moveup 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.tree.tree.selection；self.tree.tree.index；self.tree.tree.move | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.sfe.SegFuncEditor.save | (self) | v2sim/gui/mainbox/controls/sfe.py#L60 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.sfe.SegFuncEditor.save 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.destroy；self.getAllData；len；self.var.set；MB.showerror；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
