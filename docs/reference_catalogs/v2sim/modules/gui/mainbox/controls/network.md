# `v2sim.gui.mainbox.controls.network`

本模块共记录 58 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gui.mainbox.controls.network.BIDC | class | v2sim/gui/mainbox/controls/network.py#L27 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.BIDC 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | list；self._rv.pop；self._mp.pop；itemdesc；isinstance；KeyError；self.pop；self.add；TypeError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.BIDC.__getitem__ | (self, id: int) | v2sim/gui/mainbox/controls/network.py#L59 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.BIDC.__getitem__ 的实现与调用链确认。 | 输入：id；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | itemdesc | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.BIDC.__init__ | (self, categories: Iterable[str]) | v2sim/gui/mainbox/controls/network.py#L28 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.BIDC.__init__ 的实现与调用链确认。 | 输入：categories；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | list | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.BIDC.__setitem__ | (self, id: int, val: Union[itemdesc, Tuple[str, Any]]) | v2sim/gui/mainbox/controls/network.py#L62 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.BIDC.__setitem__ 的实现与调用链确认。 | 输入：id, val；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | isinstance；self.pop；self.add；TypeError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.BIDC.add | (self, id: int, cls: str, item: Any) | v2sim/gui/mainbox/controls/network.py#L37 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.BIDC.add 的实现与调用链确认。 | 输入：id, item；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | KeyError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.BIDC.classes | (self) | v2sim/gui/mainbox/controls/network.py#L34 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.BIDC.classes 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.BIDC.get | (self, id: int) | v2sim/gui/mainbox/controls/network.py#L52 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.BIDC.get 的实现与调用链确认。 | 输入：id；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.BIDC.pop | (self, id: int) | v2sim/gui/mainbox/controls/network.py#L44 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.BIDC.pop 的实现与调用链确认。 | 输入：id；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._rv.pop；self._mp.pop | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.BIDC.queryID | (self, item: Any, cls: str) | v2sim/gui/mainbox/controls/network.py#L72 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.BIDC.queryID 的实现与调用链确认。 | 输入：item；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.BIDC.remove | (self, item: Any) | v2sim/gui/mainbox/controls/network.py#L48 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.BIDC.remove 的实现与调用链确认。 | 输入：item；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._rv.pop；self._mp.pop | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.BIDC.set_desc | (self, id: int, desc: Any) | v2sim/gui/mainbox/controls/network.py#L55 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.BIDC.set_desc 的实现与调用链确认。 | 输入：id, desc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.gui.mainbox.controls.network.NetworkPanel | class(Frame) | v2sim/gui/mainbox/controls/network.py#L75 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self._center；__init__；EventQueue；self._Q.register；Canvas；self._cv.pack；self._cv.bind；PropertyPanel；self._pr.pack；self.clear；self._r.hasGeoProj；endswith；RuntimeError；self._cv.scale；self._cv.move；self._cv.delete | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.Enabled | (self) -> bool；(self, v: bool)；(self, v: bool) | v2sim/gui/mainbox/controls/network.py#L204；v2sim/gui/mainbox/controls/network.py#L208；v2sim/gui/mainbox/controls/network.py#L208 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.Enabled 的实现与调用链确认。 | 输入：无显式输入；输出：bool；输入：v；输出：返回值未注解；输入：v；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.Grid | (self) -> Optional[fGrid] | v2sim/gui/mainbox/controls/network.py#L212 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.Grid 的实现与调用链确认。 | 输入：无显式输入；输出：Optional[fGrid] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.LocateEdge | (self, edge: str, color: str='red') | v2sim/gui/mainbox/controls/network.py#L723 | Locate an edge by highlighting it in given color, red by default | 输入：edge, color；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._cv.itemconfig；self._located_edges.add | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.LocateEdges | (self, edges: Iterable[str], color: str='red') | v2sim/gui/mainbox/controls/network.py#L730 | Locate a set of edges by highlighting them in given color, red by default | 输入：edges, color；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.LocateEdge | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.UnlocateAllEdges | (self) | v2sim/gui/mainbox/controls/network.py#L735 | Unlocate all edges that are located | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._located_edges.clear；self.UnlocateEdge | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.UnlocateEdge | (self, edge: str) | v2sim/gui/mainbox/controls/network.py#L741 | Unlocate an edge by restoring its color | 输入：edge；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__get_edge_prop；self._cv.itemconfig | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.__chk | (s: str) | v2sim/gui/mainbox/controls/network.py#L472 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.__chk 的实现与调用链确认。 | 输入：s；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | lower；s.strip | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.__finish_edit | (self) | v2sim/gui/mainbox/controls/network.py#L477 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.__finish_edit 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._pr.getAllData；isinstance；float；int；MB.showerror；self.UnlocateEdge；self._Redges.pop；self._r.rename_edge；self._items.set_desc；self.LocateEdge；set；self._r.update_edge；self.__move_edge；self._r.hasGeoProj；self.__move_node；self.convLL2PlotXY | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.__get_edge_prop | (self, edge: str) -> Tuple[Any, float] | v2sim/gui/mainbox/controls/network.py#L748 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.__get_edge_prop 的实现与调用链确认。 | 输入：edge；输出：Tuple[Any, float] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.__init__ | (self, master, roadnet: Optional[RoadNet]=None, grid: Optional[fGrid]=None, save_callback: Optional[Callable[[bool], None]]=None, **kwargs) | v2sim/gui/mainbox/controls/network.py#L80 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.__init__ 的实现与调用链确认。 | 输入：master, roadnet, grid, save_callback, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；EventQueue；self._Q.register；Canvas；self._cv.pack；self._cv.bind；PropertyPanel；self._pr.pack；self.clear；ConfigItemDict；self.setRoadNet；self.setGrid；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.__move_bus | (self, i: int, e: Bus, newLL: Tuple[float, float]=(-1, -1), newPlotXY: Tuple[float, float]=(0, 0), move_bus: bool=True) | v2sim/gui/mainbox/controls/network.py#L449 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.__move_bus 的实现与调用链确认。 | 输入：i, e, newLL, newPlotXY, move_bus；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.convRealXY2PlotXY；self._cv.move；self._g.GensAtBus；chain；self.convLL2PlotXY；self.convPlotXY2RealXY；self._items.queryID；self.__replot_genline；self.__move_line | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.__move_edge | (self, i: int, e: Edge) | v2sim/gui/mainbox/controls/network.py#L431 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.__move_edge 的实现与调用链确认。 | 输入：i, e；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._r.get_offset_shape；self.convRealXY2PlotXY；self._cv.coords | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.__move_gen | (self, i: int, e: GenLike, newLL: Tuple[float, float]=(-1, -1), newPlotXY: Tuple[float, float]=(0, 0), move_gen: bool=True) | v2sim/gui/mainbox/controls/network.py#L406 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.__move_gen 的实现与调用链确认。 | 输入：i, e, newLL, newPlotXY, move_gen；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.convRealXY2PlotXY；self.convPlotXY2RealXY；self._cv.move；self.__replot_genline；self.convLL2PlotXY；self._g.Bus | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.__move_line | (self, i: int, e: Line) | v2sim/gui/mainbox/controls/network.py#L423 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.__move_line 的实现与调用链确认。 | 输入：i, e；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.convRealXY2PlotXY；self._cv.coords；self._g.Bus | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.__move_node | (self, i: int, e: Node, newPlotX: float, newPlotY: float, move_node: bool=True) | v2sim/gui/mainbox/controls/network.py#L438 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.__move_node 的实现与调用链确认。 | 输入：i, e, newPlotX, newPlotY, move_node；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.convRealXY2PlotXY；self.convPlotXY2RealXY；chain；self._cv.move；self._items.queryID；self.__move_edge | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.__replot_genline | (self, i: int, e: GenLike, b: Bus) | v2sim/gui/mainbox/controls/network.py#L418 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.__replot_genline 的实现与调用链确认。 | 输入：i, e, b；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.convRealXY2PlotXY；self._cv.coords | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel._center | (self) | v2sim/gui/mainbox/controls/network.py#L710 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel._center 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._cv.bbox；self._cv.winfo_width；self._cv.winfo_height；self.move；min；self.scale；max | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel._draw | (self, scale: float=1.0, dx: float=0.0, dy: float=0.0, center: bool=True) | v2sim/gui/mainbox/controls/network.py#L789 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel._draw 的实现与调用链确认。 | 输入：scale, dx, dy, center；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._cv.delete；self._r.getBoundary；self._r.edges.keys；self._r.nodes.values；self._r.get_offset_shape；self.__get_edge_prop；self._Q.delegate；nd.get_coord；chain；self._Q.trigger；max；self.convRealXY2PlotXY；g.__class__.__name__.lower；self._g.Bus | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel._draw_async | (self, scale: float=1.0, dx: float=0.0, dy: float=0.0, center: bool=True, after: OAfter=None) | v2sim/gui/mainbox/controls/network.py#L766 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel._draw_async 的实现与调用链确认。 | 输入：scale, dx, dy, center, after；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._Q.submit；self._Q.setcallback | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel._draw_bus | (self, x, y, r, color, lw, name) | v2sim/gui/mainbox/controls/network.py#L785 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel._draw_bus 的实现与调用链确认。 | 输入：x, y, r, color, lw, name；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | itemdesc；self._cv.create_text；self._cv.create_rectangle | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel._draw_done_center | (self) | v2sim/gui/mainbox/controls/network.py#L76 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel._draw_done_center 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._center | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel._draw_edge | (self, shape: PointList, color: str, lw: float, ename: str) | v2sim/gui/mainbox/controls/network.py#L760 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel._draw_edge 的实现与调用链确认。 | 输入：shape, color, lw, ename；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._cv.create_line；itemdesc | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel._draw_gen | (self, x, y, r, color, lw, name, xb, yb, tp) | v2sim/gui/mainbox/controls/network.py#L774 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel._draw_gen 的实现与调用链确认。 | 输入：x, y, r, color, lw, name, xb, yb, tp；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | itemdesc；self._cv.create_text；self._cv.create_line；self._cv.create_oval；self._cv.create_polygon；self._cv.create_rectangle | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel._draw_line | (self, x1, y1, x2, y2, color, lw, name) | v2sim/gui/mainbox/controls/network.py#L771 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel._draw_line 的实现与调用链确认。 | 输入：x1, y1, x2, y2, color, lw, name；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | itemdesc；self._cv.create_line | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel._draw_node | (self, x: float, y: float, r, color, name: str) | v2sim/gui/mainbox/controls/network.py#L756 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel._draw_node 的实现与调用链确认。 | 输入：x, y, r, color, name；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._cv.create_oval；itemdesc | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel._float2func | (v: str) | v2sim/gui/mainbox/controls/network.py#L389 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel._float2func 的实现与调用链确认。 | 输入：v；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | eval；isinstance；ConstFunc；SegFunc | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel._onLClick | (self, event) | v2sim/gui/mainbox/controls/network.py#L227 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel._onLClick 的实现与调用链确认。 | 输入：event；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._cv.find_closest；self._cv.find_overlapping；typename.endswith；self.UnlocateAllEdges；self._pr.tree.show_title；self._pr.setData2；self.LocateEdge；self._r.hasGeoProj；ConfigItem；self._r.convertXY2LonLat；self.check_pos；_EMW；self._g.Bus；self._g.Line；self._pr.setData；_removesuffix | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel._onMotion | (self, event) | v2sim/gui/mainbox/controls/network.py#L646 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel._onMotion 的实现与调用链确认。 | 输入：event；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self.move | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel._onMouseWheel | (self, event) | v2sim/gui/mainbox/controls/network.py#L698 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel._onMouseWheel 的实现与调用链确认。 | 输入：event；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.scale | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel._onRClick | (self, event) | v2sim/gui/mainbox/controls/network.py#L640 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel._onRClick 的实现与调用链确认。 | 输入：event；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel._onRelease | (self, event) | v2sim/gui/mainbox/controls/network.py#L658 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel._onRelease 的实现与调用链确认。 | 输入：event；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self._cv.coords；self.convPlotXY2LL；self._onLClick；len；self._g.Bus；self.__move_bus；RuntimeError；self._g.Gen；self.__move_gen；self._g.PVWind；self._g.ESS；self.__move_node | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.check_pos | (self, b: PositionBase) | v2sim/gui/mainbox/controls/network.py#L116 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.check_pos 的实现与调用链确认。 | 输入：b；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._r.hasGeoProj | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.clear | (self) | v2sim/gui/mainbox/controls/network.py#L175 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.clear 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._cv.delete；BIDC；set | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.convLL2PlotXY | (self, lon: Optional[float], lat: Optional[float]) -> Tuple[float, float] | v2sim/gui/mainbox/controls/network.py#L154 | Convert longitude and latitude to canvas coordinates | 输入：lon, lat；输出：Tuple[float, float] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._r.hasGeoProj；self._r.convertLonLat2XY | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.convPlotXY2LL | (self, x: float, y: float) -> Tuple[float, float] | v2sim/gui/mainbox/controls/network.py#L165 | Convert canvas coordinates to longitude and latitude | 输入：x, y；输出：Tuple[float, float] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._r.convertXY2LonLat | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.convPlotXY2RealXY | (self, plotX: float, plotY: float) | v2sim/gui/mainbox/controls/network.py#L149 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.convPlotXY2RealXY 的实现与调用链确认。 | 输入：plotX, plotY；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.convRealXY2PlotXY | (self, realX: float, realY: float) | v2sim/gui/mainbox/controls/network.py#L146 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.convRealXY2PlotXY 的实现与调用链确认。 | 输入：realX, realY；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.move | (self, dx: float, dy: float, item='all') | v2sim/gui/mainbox/controls/network.py#L140 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.move 的实现与调用链确认。 | 输入：dx, dy, item；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._cv.move | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.save | (self, grid_path: str, net_path: str) | v2sim/gui/mainbox/controls/network.py#L830 | Save the current network to files | 输入：grid_path, net_path；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self._g.saveFileXML；self._r.save；self._r.is_from_sumo | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.saved | (self) -> bool；(self, v: bool)；(self, v: bool) | v2sim/gui/mainbox/controls/network.py#L399；v2sim/gui/mainbox/controls/network.py#L402；v2sim/gui/mainbox/controls/network.py#L402 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.saved 的实现与调用链确认。 | 输入：无显式输入；输出：bool；输入：v；输出：返回值未注解；输入：v；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核；存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核；存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | 静态扫描未发现直接函数调用；self.save_callback；self.save_callback | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.savefig | (self, save_to: str) | v2sim/gui/mainbox/controls/network.py#L128 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.savefig 的实现与调用链确认。 | 输入：save_to；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | endswith；RuntimeError；self._cv.postscript；save_to.lower | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.scale | (self, x: float, y: float, s: float, item='all') | v2sim/gui/mainbox/controls/network.py#L134 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.NetworkPanel.scale 的实现与调用链确认。 | 输入：x, y, s, item；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._cv.scale | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.setGrid | (self, grid: fGrid, repaint: bool=True) | v2sim/gui/mainbox/controls/network.py#L215 | Set the power grid to be displayed grid: ELGraph, the road network to be displayed repaint: bool, whether to repaint the network. This repaint operation will block the main thread! | 输入：grid, repaint；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self._draw_async | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.network.NetworkPanel.setRoadNet | (self, roadnet: RoadNet, repaint: bool=True, after: OAfter=None) | v2sim/gui/mainbox/controls/network.py#L188 | Set the road network to be displayed roadnet: ELGraph, the road network to be displayed repaint: bool, whether to repaint the network. after: Optional[Callable[[], None]], the function to be called after the network is repainted. If after is None, this repaint operation will block the main thread! If after is not None, this repaint operation will be done asynchronously. | 输入：roadnet, repaint, after；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._r.create_color_map；self._draw_async；self._r.is_from_sumo | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.gui.mainbox.controls.network._removesuffix | (s: str, suffix: str) -> str | v2sim/gui/mainbox/controls/network.py#L17 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network._removesuffix 的实现与调用链确认。 | 输入：s, suffix；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | s.endswith；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.gui.mainbox.controls.network.itemdesc | class | v2sim/gui/mainbox/controls/network.py#L23 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.network.itemdesc 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
