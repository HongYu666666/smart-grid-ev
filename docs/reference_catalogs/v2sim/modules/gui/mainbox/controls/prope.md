# `v2sim.gui.mainbox.controls.prope`

本模块共记录 11 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gui.mainbox.controls.prope.PropertyEditor | class(Toplevel) | v2sim/gui/mainbox/controls/prope.py#L62 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.prope.PropertyEditor 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；self.title；PropertyPanel；self.__panel.pack；Frame；self.__fr.pack；Button；self.__btn_save.grid；self.__panel.getAllData；self.getAllData；self.var.set；self.destroy；repr；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.prope.PropertyEditor.__init__ | (self, data: Dict[str, str], var: StringVar, edit_modes: ConfigItemDict) | v2sim/gui/mainbox/controls/prope.py#L63 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.prope.PropertyEditor.__init__ 的实现与调用链确认。 | 输入：data, var, edit_modes；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；self.title；PropertyPanel；self.__panel.pack；Frame；self.__fr.pack；Button；self.__btn_save.grid；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.prope.PropertyEditor.getAllData | (self) -> Dict[str, str] | v2sim/gui/mainbox/controls/prope.py#L74 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.prope.PropertyEditor.getAllData 的实现与调用链确认。 | 输入：无显式输入；输出：Dict[str, str] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.__panel.getAllData | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.prope.PropertyEditor.save | (self) | v2sim/gui/mainbox/controls/prope.py#L77 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.prope.PropertyEditor.save 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.getAllData；self.var.set；self.destroy；repr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.gui.mainbox.controls.prope.PropertyPanel | class(Frame) | v2sim/gui/mainbox/controls/prope.py#L9 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.prope.PropertyPanel 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__desc_var.set；self.tree.tree_item_edit_done；self.tree.clear；data.items；self.setData；ConfigItemDict；__init__；ScrollableTreeView；self.tree.column；self.tree.heading；self.tree.tree.bind；self.tree.pack；StringVar；Label；self.__desc.pack；self.tree.get_children | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.prope.PropertyPanel.__init__ | (self, master, data: Dict[str, str], edit_modes: ConfigItemDict, **kwargs) | v2sim/gui/mainbox/controls/prope.py#L39 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.prope.PropertyPanel.__init__ 的实现与调用链确认。 | 输入：master, data, edit_modes, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；ScrollableTreeView；self.tree.column；self.tree.heading；self.tree.tree.bind；self.tree.pack；StringVar；Label；self.__desc.pack；self.setData；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.prope.PropertyPanel.__onclick | (self, event) | v2sim/gui/mainbox/controls/prope.py#L10 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.prope.PropertyPanel.__onclick 的实现与调用链确认。 | 输入：event；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__desc_var.set；len；self.tree.selection；self.tree.item；self.__em.get_desc | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.prope.PropertyPanel.getAllData | (self) -> Dict[str, str] | v2sim/gui/mainbox/controls/prope.py#L55 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.prope.PropertyPanel.getAllData 的实现与调用链确认。 | 输入：无显式输入；输出：Dict[str, str] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.tree.get_children；self.tree.tree.item | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.prope.PropertyPanel.setData | (self, data: Dict[str, Any], edit_modes: ConfigItemDict) | v2sim/gui/mainbox/controls/prope.py#L18 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.prope.PropertyPanel.setData 的实现与调用链确认。 | 输入：data, edit_modes；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.tree.tree_item_edit_done；self.tree.clear；data.items；self.tree.insert；self.tree.setCellEditMode；edit_modes.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.prope.PropertyPanel.setData2 | (self, *val_and_modes: Tuple[Any, ConfigItem]) | v2sim/gui/mainbox/controls/prope.py#L31 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.prope.PropertyPanel.setData2 的实现与调用链确认。 | 输入：*val_and_modes；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | ConfigItemDict；self.setData | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.prope.PropertyPanel.setDataEmpty | (self) | v2sim/gui/mainbox/controls/prope.py#L28 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.prope.PropertyPanel.setDataEmpty 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.setData；ConfigItemDict | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
