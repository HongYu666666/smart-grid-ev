# `v2sim.gui.mainbox.controls.pdfe`

本模块共记录 6 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gui.mainbox.controls.pdfe.PDFuncEditor | class(Toplevel) | v2sim/gui/mainbox/controls/pdfe.py#L16 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.pdfe.PDFuncEditor 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.tree.clear；pdfunc.__dict__.items；__init__；self.title；eval；isinstance；StringVar；Frame；Label；self.mlabel.grid；Combobox；self.cb.bind；self.cb.grid；self.fr0.pack；ScrollableTreeView；self.tree.column | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.pdfe.PDFuncEditor.__init__ | (self, var: StringVar) | v2sim/gui/mainbox/controls/pdfe.py#L22 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.pdfe.PDFuncEditor.__init__ 的实现与调用链确认。 | 输入：var；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；self.title；eval；isinstance；StringVar；Frame；Label；self.mlabel.grid；Combobox；self.cb.bind；self.cb.grid；self.fr0.pack；ScrollableTreeView；self.tree.column；self.tree.heading；self.tree.pack | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.pdfe.PDFuncEditor.getAllData | (self) -> PDFunc | v2sim/gui/mainbox/controls/pdfe.py#L54 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.pdfe.PDFuncEditor.getAllData 的实现与调用链确认。 | 输入：无显式输入；输出：PDFunc | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.tree.get_children；CreatePDFunc；self.tree.tree.item；float；self.model.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.pdfe.PDFuncEditor.reset_tree | (self, pdfunc: PDFunc) | v2sim/gui/mainbox/controls/pdfe.py#L17 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.pdfe.PDFuncEditor.reset_tree 的实现与调用链确认。 | 输入：pdfunc；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.tree.clear；pdfunc.__dict__.items；self.tree.insert | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.pdfe.PDFuncEditor.save | (self) | v2sim/gui/mainbox/controls/pdfe.py#L61 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.pdfe.PDFuncEditor.save 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.getAllData；self.var.set；self.destroy；repr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.gui.mainbox.controls.pdfe._removeprefix | (s: str, prefix: str) -> str | v2sim/gui/mainbox/controls/pdfe.py#L11 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.pdfe._removeprefix 的实现与调用链确认。 | 输入：s, prefix；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | s.startswith；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
