# `v2sim.gui.mainbox.controls.sid`

本模块共记录 4 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gui.mainbox.controls.sid.SelectItemDialog | class(Toplevel) | v2sim/gui/mainbox/controls/sid.py#L5 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.sid.SelectItemDialog 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；self.title；self.geometry；Treeview；zip；tree.pack；tree.bind；Button；btn.pack；self.tree.selection；isinstance；tree.heading；tree.insert；self.destroy；MB.showwarning；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.sid.SelectItemDialog.__init__ | (self, items: List[List[Any]], title: str, columns: List[Union[str, Tuple[str, str]]]) | v2sim/gui/mainbox/controls/sid.py#L6 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.sid.SelectItemDialog.__init__ 的实现与调用链确认。 | 输入：items, title, columns；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；self.title；self.geometry；Treeview；zip；tree.pack；tree.bind；Button；btn.pack；isinstance；tree.heading；tree.insert；super；col_id.append；col_name.append；_L | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.sid.SelectItemDialog.confirm_selection | (self) | v2sim/gui/mainbox/controls/sid.py#L41 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.sid.SelectItemDialog.confirm_selection 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.destroy；MB.showwarning；_L | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.controls.sid.SelectItemDialog.on_select | (self, event) | v2sim/gui/mainbox/controls/sid.py#L36 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.controls.sid.SelectItemDialog.on_select 的实现与调用链确认。 | 输入：event；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.tree.selection | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
