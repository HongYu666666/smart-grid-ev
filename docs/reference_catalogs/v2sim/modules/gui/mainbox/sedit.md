# `v2sim.gui.mainbox.sedit`

本模块共记录 18 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gui.mainbox.sedit.StationEditor | class(Frame) | v2sim/gui/mainbox/sedit.py#L17 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.sedit.StationEditor 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；EventQueue；self._Q.register；ScrollableTreeView；self.tree.setheadings；self.tree.pack；Frame；Button；self.btn_find.pack；Entry；self.entry_find.pack；Label；self.lb_cnt.pack；self.panel2.pack；LabelFrame；self.gens.pack | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.sedit.StationEditor.FindCS | (self, edge: str) | v2sim/gui/mainbox/sedit.py#L208 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.sedit.StationEditor.FindCS 的实现与调用链确认。 | 输入：edge；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.tree.get_children；self.tree.tree.selection_set；self.tree.tree.focus；self.tree.tree.see；self.tree.item | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.sedit.StationEditor.__init__ | (self, master, generatorFunc, mode: StationEditorMode, file: str='', **kwargs) | v2sim/gui/mainbox/sedit.py#L18 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.sedit.StationEditor.__init__ 的实现与调用链确认。 | 输入：master, generatorFunc, mode, file, **kwargs；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；EventQueue；self._Q.register；ScrollableTreeView；self.tree.setheadings；self.tree.pack；Frame；Button；self.btn_find.pack；Entry；self.entry_find.pack；Label；self.lb_cnt.pack；self.panel2.pack；LabelFrame；self.gens.pack | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.sedit.StationEditor.__load | (self, file: str) | v2sim/gui/mainbox/sedit.py#L354 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.sedit.StationEditor.__load 的实现与调用链确认。 | 输入：file；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.tree.clear；self.lb_cnt.config；self._Q.delegate；LoadCSList；LoadGSList；showerr；format；str；isinstance；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.sedit.StationEditor._busModeChanged | (self) | v2sim/gui/mainbox/sedit.py#L261 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.sedit.StationEditor._busModeChanged 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.busMode.get；self.entry_bussel.config；self.entry_busrandN.config | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.sedit.StationEditor._on_btn_find_click | (self) | v2sim/gui/mainbox/sedit.py#L216 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.sedit.StationEditor._on_btn_find_click 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.FindCS；self.entry_find.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.sedit.StationEditor._pBuyChanged | (self) | v2sim/gui/mainbox/sedit.py#L235 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.sedit.StationEditor._pBuyChanged 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.pbuy.get；self.entry_pbuy.config | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.sedit.StationEditor._pSellChanged | (self) | v2sim/gui/mainbox/sedit.py#L242 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.sedit.StationEditor._pSellChanged 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.psell.get；self.entry_psell.config | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.sedit.StationEditor._useModeChanged | (self) | v2sim/gui/mainbox/sedit.py#L249 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.sedit.StationEditor._useModeChanged 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.useMode.get；self.entry_sel.config；self.entry_randN.config | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.sedit.StationEditor.clear | (self) | v2sim/gui/mainbox/sedit.py#L350 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.sedit.StationEditor.clear 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.tree.clear；self.lb_cnt.config；format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.sedit.StationEditor.generate | (self) | v2sim/gui/mainbox/sedit.py#L274 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.sedit.StationEditor.generate 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | try_int；self.allow_queue.get；self.btn_regen.config；self.entry_seed.get；self.entry_slots.get；self.useMode.get；self.pbuy.get；try_float；self.gf；try_split；self.busMode.get；self.entry_pbuy.get；self.psell.get；self.use_cscsv.get；self.entry_sel.get；self.entry_randN.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.sedit.StationEditor.load | (self, file: str) | v2sim/gui/mainbox/sedit.py#L347 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.sedit.StationEditor.load 的实现与调用链确认。 | 输入：file；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._Q.submit | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.sedit.StationEditor.mode | (self) | v2sim/gui/mainbox/sedit.py#L156 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.sedit.StationEditor.mode 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.sedit.StationEditor.save | (self) | v2sim/gui/mainbox/sedit.py#L163 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.sedit.StationEditor.save 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | Element；enumerate；write；ConstPriceGetter；len；float；RangeList；mkFunc；root.append；ToUPriceGetter；isinstance；eval；c.to_xml；ElementTree；SegFunc | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.sedit.StationEditor.saved | (self) | v2sim/gui/mainbox/sedit.py#L160 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.sedit.StationEditor.saved 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.sedit.StationEditor.setCSCSV | (self, val: bool) | v2sim/gui/mainbox/sedit.py#L227 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.sedit.StationEditor.setCSCSV 的实现与调用链确认。 | 输入：val；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.rb_cscsv.configure；self.use_cscsv.get；self.use_cscsv.set | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gui.mainbox.sedit.StationEditor.setPoly | (self, val: bool) | v2sim/gui/mainbox/sedit.py#L219 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.sedit.StationEditor.setPoly 的实现与调用链确认。 | 输入：val；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.rb_poly.configure；self.use_cscsv.get；self.use_cscsv.set | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.gui.mainbox.sedit.StationEditorMode | class(Enum) | v2sim/gui/mainbox/sedit.py#L12 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.mainbox.sedit.StationEditorMode 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
