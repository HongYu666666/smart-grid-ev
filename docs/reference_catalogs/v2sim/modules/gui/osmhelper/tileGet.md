# `v2sim.gui.osmhelper.tileGet`

本模块共记录 9 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | v2sim.gui.osmhelper.tileGet.fromLatLonToPoint | (lat, lon) | v2sim/gui/osmhelper/tileGet.py#L33 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.osmhelper.tileGet.fromLatLonToPoint 的实现与调用链确认。 | 输入：lat, lon；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | math.sin；math.radians；math.log | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.gui.osmhelper.tileGet.fromLatLonToTile | (lat, lon, zoom) | v2sim/gui/osmhelper/tileGet.py#L41 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.osmhelper.tileGet.fromLatLonToTile 的实现与调用链确认。 | 输入：lat, lon, zoom；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | int；math.asinh；math.tan；math.radians | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.gui.osmhelper.tileGet.fromTileToLatLon | (xtile, ytile, zoom) | v2sim/gui/osmhelper/tileGet.py#L49 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.osmhelper.tileGet.fromTileToLatLon 的实现与调用链确认。 | 输入：xtile, ytile, zoom；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | math.degrees；math.atan；math.sinh | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.gui.osmhelper.tileGet.get | (args=None) | v2sim/gui/osmhelper/tileGet.py#L211 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.osmhelper.tileGet.get 的实现与调用链确认。 | 输入：args；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | get_options；os.path.join；sumolib.output.parse_fast；sumolib.net.readNet；net.getBBoxXY；net.convertXY2LonLat；sumolib.openz；sumolib.xml.writeHeader；print；float；retrieveMapServerTiles；tuple；min；max；options.bbox.split；retrieveOpenStreetMapTiles | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.gui.osmhelper.tileGet.getZoomWidthHeight | (south, west, north, east, maxTileSize) | v2sim/gui/osmhelper/tileGet.py#L56 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.osmhelper.tileGet.getZoomWidthHeight 的实现与调用链确认。 | 输入：south, west, north, east, maxTileSize；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | fromLatLonToPoint | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.gui.osmhelper.tileGet.get_options | (args=None) | v2sim/gui/osmhelper/tileGet.py#L150 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.osmhelper.tileGet.get_options 的实现与调用链确认。 | 输入：args；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | sumolib.options.ArgumentParser；optParser.add_option；optParser.parse_args；URL_SHORTCUTS.get；optParser.error；options.url.lower；options.url.startswith；float；options.bbox.split | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.gui.osmhelper.tileGet.retrieveMapServerTiles | (options, west, south, east, north, decals, net, pattern) | v2sim/gui/osmhelper/tileGet.py#L110 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.osmhelper.tileGet.retrieveMapServerTiles 的实现与调用链确认。 | 输入：options, west, south, east, north, decals, net, pattern；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | range；fromLatLonToTile；signal.signal；Pool；future.get；os.path.join；pattern.format；pattern.endswith；worker；futures.append；fromTileToLatLon；net.convertLonLat2XY；print；pool.apply_async；os.path.basename | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.gui.osmhelper.tileGet.retrieveOpenStreetMapTiles | (options, west, south, east, north, decals, net, is_retina) | v2sim/gui/osmhelper/tileGet.py#L79 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.osmhelper.tileGet.retrieveOpenStreetMapTiles 的实现与调用链确认。 | 输入：options, west, south, east, north, decals, net, is_retina；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | range；fromLatLonToTile；urllib.build_opener；urllib.install_opener；os.path.join；worker；fromTileToLatLon；net.convertLonLat2XY；print；os.path.basename | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.gui.osmhelper.tileGet.worker | (options, request, filename) | v2sim/gui/osmhelper/tileGet.py#L70 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gui.osmhelper.tileGet.worker 的实现与调用链确认。 | 输入：options, request, filename；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | print；urllib.urlretrieve；ValueError；os.stat | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
