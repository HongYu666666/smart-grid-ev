# `v2sim.veh.vdict`

本模块共记录 16 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | v2sim.veh.vdict.LoadVehicles | (file_path: str, node_mode: bool=True) | v2sim/veh/vdict.py#L24 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.vdict.LoadVehicles 的实现与调用链确认。 | 输入：file_path, node_mode；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | getroot；RuntimeError；ReadXML；Lang.EV_LOAD_ERROR.format；split；_get；Trip；trips.append；VehType；_dget；attr.get；args.update；EV；trip.attrib.pop；int；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| class | v2sim.veh.vdict.VDict | class | v2sim/veh/vdict.py#L104 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.vdict.VDict 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | KeyError；isinstance；chain；LoadVehicles；VDict；Element；endswith；write；fh.close；len；self.evs.keys；self.gvs.keys；self.evs.values；self.gvs.values；self.evs.items；self.gvs.items | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.veh.vdict.VDict.__contains__ | (self, name: str) | v2sim/veh/vdict.py#L132 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.vdict.VDict.__contains__ 的实现与调用链确认。 | 输入：name；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.veh.vdict.VDict.__delitem__ | (self, name: str) | v2sim/veh/vdict.py#L124 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.vdict.VDict.__delitem__ 的实现与调用链确认。 | 输入：name；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | KeyError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.veh.vdict.VDict.__getitem__ | (self, name: str) | v2sim/veh/vdict.py#L109 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.vdict.VDict.__getitem__ 的实现与调用链确认。 | 输入：name；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | KeyError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.veh.vdict.VDict.__init__ | (self, evs: EVDict, gvs: GVDict) | v2sim/veh/vdict.py#L105 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.vdict.VDict.__init__ 的实现与调用链确认。 | 输入：evs, gvs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.veh.vdict.VDict.__len__ | (self) | v2sim/veh/vdict.py#L135 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.vdict.VDict.__len__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.veh.vdict.VDict.__setitem__ | (self, name: str, veh: Vehicle) | v2sim/veh/vdict.py#L116 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.vdict.VDict.__setitem__ 的实现与调用链确认。 | 输入：name, veh；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；TypeError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.veh.vdict.VDict.from_file | (file_path: str, node_mode: bool=True) | v2sim/veh/vdict.py#L148 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.vdict.VDict.from_file 的实现与调用链确认。 | 输入：file_path, node_mode；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | LoadVehicles；VDict | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.veh.vdict.VDict.items | (self) | v2sim/veh/vdict.py#L144 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.vdict.VDict.items 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | chain；self.evs.items；self.gvs.items | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.veh.vdict.VDict.keys | (self) | v2sim/veh/vdict.py#L138 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.vdict.VDict.keys 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | chain；self.evs.keys；self.gvs.keys | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.veh.vdict.VDict.reset | (self) | v2sim/veh/vdict.py#L164 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.vdict.VDict.reset 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | chain；self.evs.values；self.gvs.values；v.reset | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.veh.vdict.VDict.save | (self, fname: str) | v2sim/veh/vdict.py#L152 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.vdict.VDict.save 的实现与调用链确认。 | 输入：fname；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | Element；chain；endswith；write；fh.close；self.evs.values；self.gvs.values；rt.append；gzip.open；open；v.to_xml；fname.lower；ElementTree | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.veh.vdict.VDict.values | (self) | v2sim/veh/vdict.py#L141 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.vdict.VDict.values 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | chain；self.evs.values；self.gvs.values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | v2sim.veh.vdict._dget | (d: dict, names: tuple[str, ...], default: float) -> float | v2sim/veh/vdict.py#L19 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.vdict._dget 的实现与调用链确认。 | 输入：d, names, default；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | float | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | v2sim.veh.vdict._get | (d: dict, names: tuple[str, ...], default: Optional[str]=None) -> str | v2sim/veh/vdict.py#L13 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.vdict._get 的实现与调用链确认。 | 输入：d, names, default；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | KeyError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
