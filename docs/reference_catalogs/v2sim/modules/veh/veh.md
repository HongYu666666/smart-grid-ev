# `v2sim.veh.veh`

本模块共记录 44 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.veh.veh.GV | class(Vehicle) | v2sim/veh/veh.py#L360 | Gas Vehicle Class | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | to_xml；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.GV.__repr__ | (self) -> str | v2sim/veh/veh.py#L397 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.GV.__repr__ 的实现与调用链确认。 | 输入：无显式输入；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.GV.end_refueling | (self) -> float | v2sim/veh/veh.py#L389 | End refueling/recharging | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.GV.refuel | (self, amount: float, unit_cost: float) | v2sim/veh/veh.py#L371 | Add energy to the energy storage | 输入：amount, unit_cost；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.GV.start_refueling | (self, energy_target: Optional[float]=None) | v2sim/veh/veh.py#L362 | Start refueling | 输入：energy_target；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.GV.to_xml | (self) -> Element | v2sim/veh/veh.py#L400 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.GV.to_xml 的实现与调用链确认。 | 输入：无显式输入；输出：Element | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | to_xml；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.veh.veh.Trip | class | v2sim/veh/veh.py#L9 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.Trip 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | str；Element；join | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Trip.__repr__ | (self) | v2sim/veh/veh.py#L20 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.Trip.__repr__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Trip.__str__ | (self) | v2sim/veh/veh.py#L23 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.Trip.__str__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Trip.to_xml | (self) -> Element | v2sim/veh/veh.py#L26 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.Trip.to_xml 的实现与调用链确认。 | 输入：无显式输入；输出：Element | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | Element；join；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.veh.veh.VehStatus | class(enum.IntEnum) | v2sim/veh/veh.py#L44 | Vehicle status enumeration Driving: Driving to destination or charging station Pending: Already notified SUMO to start, but the vehicle has not yet started in SUMO Charging: Charging at charging station Parking: Parking (between two trips, or before the trip, or after the trip) Depleted: Battery depleted | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.veh.veh.VehType | class(enum.IntEnum) | v2sim/veh/veh.py#L60 | Vehicle type enumeration | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | s.lower；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.VehType.from_str | (s: str) -> 'VehType' | v2sim/veh/veh.py#L71 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.VehType.from_str 的实现与调用链确认。 | 输入：s；输出：'VehType' | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | s.lower；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.veh.veh.Vehicle | class | v2sim/veh/veh.py#L91 | Vehicle Base Class | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._trips.append；self._trips.clear；repr；Element；len；e.append；str；trip.to_xml；int | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.E | (self) -> float | v2sim/veh/veh.py#L175 | Get the current energy stored (kWh for EV, L for GV) | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.Emax | (self) -> float | v2sim/veh/veh.py#L180 | Get the energy capacity (kWh for EV, L for GV) | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.__init__ | (self, name: str, vtype: VehType, cap: float, pct: float, epm: float, omega: float, kr: float, kf: float, trips: List[Trip], trip_info: Dict[str, Any], base: Optional[str]=None) | v2sim/veh/veh.py#L93 | Initialize Vehicle | 输入：name, vtype, cap, pct, epm, omega, kr, kf, trips, trip_info, base；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.__repr__ | (self) -> str | v2sim/veh/veh.py#L333 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.Vehicle.__repr__ 的实现与调用链确认。 | 输入：无显式输入；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.__str__ | (self) | v2sim/veh/veh.py#L336 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.Vehicle.__str__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | repr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle._unsafe_drive | (self, new_dis: float) | v2sim/veh/veh.py#L261 | Update the distance traveled and energy consumption in driving state without checking | 输入：new_dis；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.add_trip | (self, trip: Trip, day: Optional[int]=None) | v2sim/veh/veh.py#L310 | Add a trip to the vehicle's trip list | 输入：trip, day；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._trips.append；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.brief | (self) | v2sim/veh/veh.py#L329 | Get a brief description of this vehicle | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.clear_odometer | (self) | v2sim/veh/veh.py#L229 | Set the distance traveled to 0 before the trip starts | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.clear_trips | (self) | v2sim/veh/veh.py#L324 | Clear all trips of the vehicle | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._trips.clear | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.cost | (self) -> float | v2sim/veh/veh.py#L215 | Total charging cost incurred by the vehicle ($) | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.drive | (self, new_dis: float) | v2sim/veh/veh.py#L245 | Update the distance traveled and energy consumption. The vehicle must not be in refueling/recharging state. | 输入：new_dis；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.is_energy_enough | (self, dist: float) -> bool | v2sim/veh/veh.py#L270 | Whether the current energy level is sufficient to travel a distance of dist. Note that this function uses the user's estimated distance, that is, range >= kr * dist | 输入：dist；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.kf | (self) -> float；(self, val: float)；(self, val: float) | v2sim/veh/veh.py#L205；v2sim/veh/veh.py#L210；v2sim/veh/veh.py#L210 | Energy percentage threshold for fast charging/refueling at departure；待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.Vehicle.kf 的实现与调用链确认。；待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.Vehicle.kf 的实现与调用链确认。 | 输入：无显式输入；输出：float；输入：val；输出：返回值未注解；输入：val；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.kr | (self) -> float；(self, val: float)；(self, val: float) | v2sim/veh/veh.py#L194；v2sim/veh/veh.py#L200；v2sim/veh/veh.py#L200 | User's estimation deviation of distance. For example, if kr=0.9, it means that the user thinks that the current energy can support 90% of the actual mileage.；待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.Vehicle.kr 的实现与调用链确认。；待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.Vehicle.kr 的实现与调用链确认。 | 输入：无显式输入；输出：float；输入：val；输出：返回值未注解；输入：val；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.name | (self) -> str | v2sim/veh/veh.py#L140 | Vehicle's name | 输入：无显式输入；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.next_trip | (self) | v2sim/veh/veh.py#L300 | Increment the trip ID by 1 and return the trip ID. If it is already the last trip, return -1, and the trip ID will not be changed. | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.odometer | (self) -> float | v2sim/veh/veh.py#L225 | Distance traveled by the vehicle in this trip (m), note that leaving a CS is considered a new trip | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.omega | (self) -> float；(self, val: float)；(self, val: float) | v2sim/veh/veh.py#L185；v2sim/veh/veh.py#L190；v2sim/veh/veh.py#L190 | Decision parameter for selecting fast charging station；待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.Vehicle.omega 的实现与调用链确认。；待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.Vehicle.omega 的实现与调用链确认。 | 输入：无显式输入；输出：float；输入：val；输出：返回值未注解；输入：val；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.range | (self) -> float | v2sim/veh/veh.py#L220 | Maximum distance under the current energy level (m) | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.reset | (self) | v2sim/veh/veh.py#L233 | Reset the vehicle to the initial state | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.soc | (self) -> float | v2sim/veh/veh.py#L168 | Get the current energy percentage (0.0~1.0) | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.status | (self) -> VehStatus；(self, val: VehStatus)；(self, val: VehStatus) | v2sim/veh/veh.py#L150；v2sim/veh/veh.py#L155；v2sim/veh/veh.py#L155 | Current vehicle status；待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.Vehicle.status 的实现与调用链确认。；待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.Vehicle.status 的实现与调用链确认。 | 输入：无显式输入；输出：VehStatus；输入：val；输出：返回值未注解；输入：val；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.target_CS | (self) -> Union[None, str]；(self, val)；(self, val) | v2sim/veh/veh.py#L159；v2sim/veh/veh.py#L164；v2sim/veh/veh.py#L164 | Name of the target fast charging station. When this item is None, it means that it isn't guided to a CS；待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.Vehicle.target_CS 的实现与调用链确认。；待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.veh.Vehicle.target_CS 的实现与调用链确认。 | 输入：无显式输入；输出：Union[None, str]；输入：val；输出：返回值未注解；输入：val；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.to_xml | (self) -> Element | v2sim/veh/veh.py#L339 | Get the XML representation of the vehicle for SUMO | 输入：无显式输入；输出：Element | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | Element；e.append；str；trip.to_xml；int | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.trip | (self) | v2sim/veh/veh.py#L286 | Current trip | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.trip_id | (self) | v2sim/veh/veh.py#L291 | Current trip ID (indexed from 0) | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.trip_info | (self) -> Dict[str, Any] | v2sim/veh/veh.py#L296 | Trip generation related information | 输入：无显式输入；输出：Dict[str, Any] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.trips | (self) | v2sim/veh/veh.py#L281 | Vehicle's trips | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.veh.Vehicle.vtype | (self) -> VehType | v2sim/veh/veh.py#L145 | Vehicle type | 输入：无显式输入；输出：VehType | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
