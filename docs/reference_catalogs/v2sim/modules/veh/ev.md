# `v2sim.veh.ev`

本模块共记录 44 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.veh.ev.ChargeRatePool | class | v2sim/veh/ev.py#L17 | Charging rate correction function pool | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.ChargeRatePool.add | (name: str, func: 'Callable[[float, EV], float]') | v2sim/veh/ev.py#L25 | Add charging rate correction function | 输入：name, func；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.ChargeRatePool.get | (name: str) -> 'Callable[[float, EV], float]' | v2sim/veh/ev.py#L30 | Get charging rate correction function | 输入：name；输出：'Callable[[float, EV], float]' | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.veh.ev.EV | class(Vehicle) | v2sim/veh/ev.py#L35 | 扩展车辆状态以表达电池、充电、放电、SOC 和 V2G 行为。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；ChargeRatePool.get；reset；min；self._bidirectional_charge；to_xml；e.attrib.update；self._sc_time.toXMLNode；len；self._v2g_time.toXMLNode；isinstance；RangeList；max；self._chrate_mod；self._sc_time.__contains__；e.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.veh.ev.EV.__init__ | (self, name: str, vtype: VehType, cap: float, pct: float, epm: float, ecf: float, ecs: float, ed: float, pcf: float, pcs: float, pdv: float, omega: float, kr: float, kf: float, ks: float, kv: float, trips: List[Trip], trip_info: Dict[str, Any], base: Optional[str]=None, rmod: str='Linear', sc_time: Union[None, RangeList]=None, max_sc_cost: float=100.0, v2g_time: Union[None, RangeList]=None, min_v2g_earn: float=0.0) | v2sim/veh/ev.py#L37 | Initialize Electric Vehicle | 输入：name, vtype, cap, pct, epm, ecf, ecs, ed, pcf, pcs, pdv, omega, kr, kf, ks, kv, trips, trip_info, base, rmod, sc_time, max_sc_cost, v2g_time, min_v2g_earn；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；ChargeRatePool.get；isinstance；RangeList；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.__repr__ | (self) | v2sim/veh/ev.py#L395 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.ev.EV.__repr__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV._bidirectional_charge | (self, sec: int, unit_cost: float, real_etar: float) -> Tuple[float, float] | v2sim/veh/ev.py#L330 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.ev.EV._bidirectional_charge 的实现与调用链确认。 | 输入：sec, unit_cost, real_etar；输出：Tuple[float, float] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | min；self._chrate_mod | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.bidirectional_charge | (self, sec: int, unit_cost: float, real_etar: Optional[float]=None) -> Tuple[float, float] | v2sim/veh/ev.py#L314 | 在双向模式下按约束为车辆充电。 | 输入：sec, unit_cost, real_etar；输出：Tuple[float, float] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._bidirectional_charge | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.veh.ev.EV.bidirectional_discharge | (self, sec: int, unit_earn: float) -> Tuple[float, float] | v2sim/veh/ev.py#L342 | 在双向模式下向站点或电网侧释放车辆电量。 | 输入：sec, unit_earn；输出：Tuple[float, float] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | min | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.veh.ev.EV.charge | (self, seconds: int, unit_cost: float) -> Tuple[float, float] | v2sim/veh/ev.py#L264 | 按时间步和功率约束更新车辆充电能量与 SOC。 | 输入：seconds, unit_cost；输出：Tuple[float, float] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | min；self._chrate_mod | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.veh.ev.EV.end_bidirectional | (self) -> Tuple[float, float, float] | v2sim/veh/ev.py#L364 | 结束双向充放电并清理临时 V2G 状态。 | 输入：无显式输入；输出：Tuple[float, float, float] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.veh.ev.EV.end_charging | (self) -> Tuple[float, float] | v2sim/veh/ev.py#L284 | 结束单向充电并清理临时充电状态。 | 输入：无显式输入；输出：Tuple[float, float] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.veh.ev.EV.estimated_charging_time | (self) -> float | v2sim/veh/ev.py#L151 | Time required to complete charging at the current charge level, target charge level and charging rate | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | max | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.eta_d | (self) -> float | v2sim/veh/ev.py#L201 | V2G discharging efficiency | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.eta_fc | (self) -> float | v2sim/veh/ev.py#L191 | Fast charging efficiency | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.eta_sc | (self) -> float | v2sim/veh/ev.py#L196 | Slow charging efficiency | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.kf | (self) -> float；(self, val: float)；(self, val: float) | v2sim/veh/ev.py#L161；v2sim/veh/ev.py#L166；v2sim/veh/ev.py#L166 | SoC threshold for fast charging at departure；待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.ev.EV.kf 的实现与调用链确认。；待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.ev.EV.kf 的实现与调用链确认。 | 输入：无显式输入；输出：float；输入：val；输出：返回值未注解；输入：val；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.ks | (self) -> float；(self, val: float)；(self, val: float) | v2sim/veh/ev.py#L171；v2sim/veh/ev.py#L176；v2sim/veh/ev.py#L176 | Select the SOC threshold for slow charging；待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.ev.EV.ks 的实现与调用链确认。；待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.ev.EV.ks 的实现与调用链确认。 | 输入：无显式输入；输出：float；输入：val；输出：返回值未注解；输入：val；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.kv2g | (self) -> float；(self, val: float)；(self, val: float) | v2sim/veh/ev.py#L181；v2sim/veh/ev.py#L186；v2sim/veh/ev.py#L186 | Select the SOC threshold for slow charging；待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.ev.EV.kv2g 的实现与调用链确认。；待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.ev.EV.kv2g 的实现与调用链确认。 | 输入：无显式输入；输出：float；输入：val；输出：返回值未注解；输入：val；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.maximum_slow_charge_cost | (self) -> float | v2sim/veh/ev.py#L236 | The maximum slow charging cost willing to join slow charge, $/kWh | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.minimum_v2g_earn | (self) -> float | v2sim/veh/ev.py#L231 | The minimum V2G earn user willing to join V2G, $/kWh | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.pc_actual | (self) -> float | v2sim/veh/ev.py#L206 | Vehicle's actual charging rate, kWh/s | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.pcf | (self) -> float | v2sim/veh/ev.py#L211 | Vehicle's maximum fast charging rate, kWh/s | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.pcs | (self) -> float | v2sim/veh/ev.py#L216 | Vehicle's maximum slow charging rate, kWh/s | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.pd_actual | (self) -> float | v2sim/veh/ev.py#L221 | Vehicle's actual V2G discharging rate, kWh/s | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.pdv | (self) -> float | v2sim/veh/ev.py#L226 | Vehicle's maximum V2G reverse power rate, kWh/s | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.reset | (self) | v2sim/veh/ev.py#L110 | Reset the vehicle to the initial state | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | reset；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.set_force_fast_charge | (self, cs: Optional[str]=None) | v2sim/veh/ev.py#L136 | Force the vehicle to fast charge at next departure. + If cs is not None, set the target fast charging station to cs. If cs is None, find a suitable FCS automatically. + After the next departure, this configuration will be cleared automatically, whether the vehicle departs successfully or not. | 输入：cs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.set_force_slow_charge | (self) | v2sim/veh/ev.py#L143 | Force the vehicle to enter slow charge station at next arrival. Note: + If there is no vacancy in the SCS, this configuration will be ignored. + After the next arrival, this configuration will be cleared automatically, whether the vehicle enters the SCS or not. | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.set_leave_at_etar | (self, val: bool) | v2sim/veh/ev.py#L121 | Set whether to leave immediately after reaching target energy. If set to True, the vehicle will not join V2G. | 输入：val；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.set_temp_max_pc | (self, pc: float) | v2sim/veh/ev.py#L126 | Set temporary maximum charging power kWh/s. This function must be called in MaxPCAllocator. | 输入：pc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.set_temp_pd | (self, pd: float) | v2sim/veh/ev.py#L131 | Set temporary discharging power kWh/s. This function must be called in V2GAllocator. | 输入：pd；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.slow_charge_time | (self) -> RangeList | v2sim/veh/ev.py#L246 | The time range that the user is willing to join slow charge. None means all day | 输入：无显式输入；输出：RangeList | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.start_bidirectional | (self, pc: float, pd: float, fast_charging: bool=True) | v2sim/veh/ev.py#L294 | 初始化车辆双向充放电状态。 | 输入：pc, pd, fast_charging；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | min | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.veh.ev.EV.start_charging | (self, pc: float, fast_charging: bool=True) | v2sim/veh/ev.py#L250 | 使车辆进入单向充电状态并初始化本次充电过程。 | 输入：pc, fast_charging；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | min | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.veh.ev.EV.to_xml | (self) -> Element | v2sim/veh/ev.py#L398 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.ev.EV.to_xml 的实现与调用链确认。 | 输入：无显式输入；输出：Element | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | to_xml；e.attrib.update；self._sc_time.toXMLNode；len；self._v2g_time.toXMLNode；e.append；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.v2g_time | (self) -> RangeList | v2sim/veh/ev.py#L241 | The time range that the user is willing to join V2G. None means all day | 输入：无显式输入；输出：RangeList | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.willing_to_slow_charge | (self, t: int, c: float) -> bool | v2sim/veh/ev.py#L386 | User determines whether the vehicle is willing to slow charge Parameters: t: current time c: current slow charge cost, $/kWh | 输入：t, c；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._sc_time.__contains__ | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.EV.willing_to_v2g | (self, t: int, e: float) -> bool | v2sim/veh/ev.py#L377 | User determines whether the vehicle is willing to v2g Parameters: t: current time e: current V2G earn, $/kWh | 输入：t, e；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._v2g_time.__contains__ | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.veh.ev.SV | class(EV) | v2sim/veh/ev.py#L422 | Swapping Battery EV | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | to_xml；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.SV.__repr__ | (self) | v2sim/veh/ev.py#L424 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.ev.SV.__repr__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.veh.ev.SV.to_xml | (self) -> Element | v2sim/veh/ev.py#L427 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.ev.SV.to_xml 的实现与调用链确认。 | 输入：无显式输入；输出：Element | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | to_xml；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.veh.ev._EqualChargeRate | (rate: float, ev: 'EV') -> float | v2sim/veh/ev.py#L8 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.ev._EqualChargeRate 的实现与调用链确认。 | 输入：rate, ev；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.veh.ev._LinearChargeRate | (rate: float, ev: 'EV') -> float | v2sim/veh/ev.py#L11 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.veh.ev._LinearChargeRate 的实现与调用链确认。 | 输入：rate, ev；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
