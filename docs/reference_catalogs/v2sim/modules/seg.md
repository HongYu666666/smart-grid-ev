# `v2sim.seg`

本模块共记录 10 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.seg.KDTreeSegmentSearch | class | v2sim/seg.py#L6 | 基于KDTree的线段快速搜索 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | segments.astype；np.sum；np.sqrt；self._sample_segment_points；KDTree；np.vstack；np.minimum；np.maximum；np.linspace；range；reshape；self.kdtree.query；np.unique；self.endpoint_tree.query；self.find_closest_candidates_kdtree；self.find_closest_candidates_endpoints | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.seg.KDTreeSegmentSearch.__init__ | (self, segments: np.ndarray, leaf_size: int=40, sample_points_per_segment: int=5) | v2sim/seg.py#L9 | 初始化线段搜索器 | 输入：segments, leaf_size, sample_points_per_segment；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | segments.astype；np.sum；np.sqrt；self._sample_segment_points；KDTree；np.vstack；np.minimum；np.maximum | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.seg.KDTreeSegmentSearch._compute_distances_to_candidates | (self, point: np.ndarray, candidate_indices: np.ndarray) -> Tuple[np.ndarray, np.ndarray] | v2sim/seg.py#L149 | 计算点到候选线段的距离 | 输入：point, candidate_indices；输出：Tuple[np.ndarray, np.ndarray] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；np.zeros；enumerate；self._distance_to_segment_numba | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.seg.KDTreeSegmentSearch._distance_to_segment_numba | (point: np.ndarray, segment: np.ndarray, vector: np.ndarray, length_sq: float) -> Tuple[float, float] | v2sim/seg.py#L121 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.seg.KDTreeSegmentSearch._distance_to_segment_numba 的实现与调用链确认。 | 输入：point, segment, vector, length_sq；输出：Tuple[float, float] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | max；np.sqrt；min | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.seg.KDTreeSegmentSearch._sample_segment_points | (self, n_samples: int) -> Tuple[np.ndarray, np.ndarray] | v2sim/seg.py#L41 | 为每条线段采样多个点 | 输入：n_samples；输出：Tuple[np.ndarray, np.ndarray] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.linspace；range；np.array；np.arange；all_points.append；all_indices.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.seg.KDTreeSegmentSearch.batch_find_closest_segments | (self, points: np.ndarray, n_candidates: int=100, batch_size: int=1000) -> Tuple[np.ndarray, np.ndarray, np.ndarray] | v2sim/seg.py#L195 | 批量处理多个点 | 输入：points, n_candidates, batch_size；输出：Tuple[np.ndarray, np.ndarray, np.ndarray] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.zeros；range；min；print；self.find_closest_segment | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.seg.KDTreeSegmentSearch.find_closest_candidates_combined | (self, point: np.ndarray, n_candidates: int=50) -> np.ndarray | v2sim/seg.py#L101 | 组合使用两种方法找到候选线段 | 输入：point, n_candidates；输出：np.ndarray | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.find_closest_candidates_kdtree；self.find_closest_candidates_endpoints；np.unique；np.concatenate；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.seg.KDTreeSegmentSearch.find_closest_candidates_endpoints | (self, point: np.ndarray, n_candidates: int=30) -> np.ndarray | v2sim/seg.py#L81 | 使用端点KDTree找到候选线段 | 输入：point, n_candidates；输出：np.ndarray | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | reshape；self.endpoint_tree.query；np.unique；np.array；min | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.seg.KDTreeSegmentSearch.find_closest_candidates_kdtree | (self, point: np.ndarray, n_candidates: int=50) -> np.ndarray | v2sim/seg.py#L61 | 使用KDTree找到候选线段 | 输入：point, n_candidates；输出：np.ndarray | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | reshape；self.kdtree.query；np.unique；np.array；min；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.seg.KDTreeSegmentSearch.find_closest_segment | (self, point: np.ndarray, n_candidates: int=100) -> Tuple[int, float, np.ndarray] | v2sim/seg.py#L167 | 找到距离给定点最近的线段 | 输入：point, n_candidates；输出：Tuple[int, float, np.ndarray] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.array；self.find_closest_candidates_combined；self._compute_distances_to_candidates；np.argmin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
