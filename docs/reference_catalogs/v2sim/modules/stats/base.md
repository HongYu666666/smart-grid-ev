# `v2sim.stats.base`

本模块共记录 12 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.stats.base.StaBase | class | v2sim/stats/base.py#L36 | Base class for statistics recorder | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | open；self._writer.write；NotImplementedError；self.GetData；len；enumerate；self._writer.close；self.close；str；self._writer.flush；to_base62；ValueError；round；Path；join；abs | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.stats.base.StaBase.GetData | (self, inst: TrafficInst, plugins: Dict[str, PluginBase]) -> Iterable[Any] | v2sim/stats/base.py#L72 | Get Data | 输入：inst, plugins；输出：Iterable[Any] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | NotImplementedError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.stats.base.StaBase.GetLocalizedName | () -> str | v2sim/stats/base.py#L59 | Get Localized Name | 输入：无显式输入；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.stats.base.StaBase.GetPluginDependency | () -> List[str] | v2sim/stats/base.py#L63 | Get Plugin Dependency | 输入：无显式输入；输出：List[str] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.stats.base.StaBase.LogOnce | (self) | v2sim/stats/base.py#L76 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.stats.base.StaBase.LogOnce 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.GetData；len；enumerate；self._writer.flush；ValueError；round；abs；self._pre.get；self._writer.write | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.stats.base.StaBase.Writer | (self) | v2sim/stats/base.py#L68 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.stats.base.StaBase.Writer 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.stats.base.StaBase.__exit__ | (self) | v2sim/stats/base.py#L101 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.stats.base.StaBase.__exit__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.close | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.stats.base.StaBase.__init__ | (self, name: str, path: str, items: List[str], tinst: TrafficInst, plugins: Dict[str, PluginBase], precision: Optional[Dict[str, int]]=None, compress: bool=True) | v2sim/stats/base.py#L39 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.stats.base.StaBase.__init__ 的实现与调用链确认。 | 输入：name, path, items, tinst, plugins, precision, compress；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | open；self._writer.write；len；str；to_base62；Path；enumerate；join | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.stats.base.StaBase.close | (self) | v2sim/stats/base.py#L98 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.stats.base.StaBase.close 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self._writer.close | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.stats.base.cross_list | (a: Iterable[str], b: Sequence[str]) -> List[str] | v2sim/stats/base.py#L7 | Generate cross table header | 输入：a, b；输出：List[str] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.stats.base.cross_list2 | (a: Iterable[str], b: Sequence[str]) -> List[str] | v2sim/stats/base.py#L17 | Generate cross table header | 输入：a, b；输出：List[str] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.stats.base.to_base62 | (num: int) | v2sim/stats/base.py#L28 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.stats.base.to_base62 的实现与调用链确认。 | 输入：num；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | divmod | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
