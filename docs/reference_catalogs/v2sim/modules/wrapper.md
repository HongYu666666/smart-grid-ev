# `v2sim.wrapper`

本模块共记录 12 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.wrapper.AltCommand | class | v2sim/wrapper.py#L79 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.wrapper.AltCommand 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.wrapper.AltCommand.apply | (self, sim_inst: V2SimInstance) | v2sim/wrapper.py#L87 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.wrapper.AltCommand.apply 的实现与调用链确认。 | 输入：sim_inst；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.wrapper.GenerationCommand | class | v2sim/wrapper.py#L24 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.wrapper.GenerationCommand 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | all；self.all_empty；TrafficGenerator；traff_gen.FCSFromArgs；traff_gen.SCSFromArgs；traff_gen.GSFromArgs；traff_gen.VTripsFromArgs；print | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.wrapper.GenerationCommand.all_empty | (self) | v2sim/wrapper.py#L30 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.wrapper.GenerationCommand.all_empty 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | all | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.wrapper.GenerationCommand.generate | (self, proj_dir: str, silent: bool=False) | v2sim/wrapper.py#L33 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.wrapper.GenerationCommand.generate 的实现与调用链确认。 | 输入：proj_dir, silent；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.all_empty；TrafficGenerator；traff_gen.FCSFromArgs；traff_gen.SCSFromArgs；traff_gen.GSFromArgs；traff_gen.VTripsFromArgs；print | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.wrapper.PlotCommand | class | v2sim/wrapper.py#L60 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.wrapper.PlotCommand 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | PlotCommand；AdvancedPlot；open；f.readlines；line.strip；plotter.configure；startswith | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.wrapper.PlotCommand.__init__ | (self, command: List[str]) | v2sim/wrapper.py#L61 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.wrapper.PlotCommand.__init__ 的实现与调用链确认。 | 输入：command；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.wrapper.PlotCommand.execute | (self) | v2sim/wrapper.py#L71 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.wrapper.PlotCommand.execute 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | AdvancedPlot；plotter.configure | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.wrapper.PlotCommand.from_file | (fname: str) | v2sim/wrapper.py#L65 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.wrapper.PlotCommand.from_file 的实现与调用链确认。 | 输入：fname；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | PlotCommand；open；f.readlines；line.strip；startswith | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.wrapper.get_internal_components | () | v2sim/wrapper.py#L12 | Get internal components Returns: A tuple containing two lists: - The first list contains plugins - The second list contains statistical items | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | GetInternalPlugins；GetInternalStatistics | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.wrapper.get_sim_params | (args: Union[str, ArgChecker], check_illegal: bool=True) -> Dict[str, Any] | v2sim/wrapper.py#L105 | Get simulation parameters used by the simulate function args: Command line parameters or ArgChecker instance check_illegal: Whether to check illegal parameters, default is True | 输入：args, check_illegal；输出：Dict[str, Any] | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | isinstance；GenerationCommand；gen_cmd.all_empty；args.pop_str；args.pop_int；TimeConfig；args.pop_bool；SaveStateOptions；split；args.pop_float；CommonConfig；ArgChecker；PlotCommand.from_file；UXsimConfig；SUMOConfig；args.keys | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.wrapper.simulate_single | (proj_dir: str, time: TimeConfig, break_at: Optional[int]=None, out_dir: Optional[str]=None, seed=0, silent: bool=False, vb=None, vscfg: Optional[CommonConfig]=None, config: Union[None, SUMOConfig, UXsimConfig]=None, disabled_plugins: Optional[List[str]]=None, logging_items: Optional[List[str]]=None, state_option: LoadStateOption=LoadStateOption.Skip, state_dir: Optional[str]=None, save_option: SaveStateOptions=SaveStateOptions.Skip, client_options: Optional[ClientOptions]=None, gen_cmds: Optional[GenerationCommand]=None, plot_cmd: Optional[PlotCommand]=None, copy_proj_to_out: bool=False, copy_state_to_proj: bool=False, alt_cmds: Optional[AltCommand]=None) | v2sim/wrapper.py#L226 | 面向命令行或脚本的单案例封装，读取项目配置、创建实例并运行到结束。 | 输入：proj_dir, time, break_at, out_dir, seed, silent, vb, vscfg, config, disabled_plugins, logging_items, state_option, state_dir, save_option, client_options, gen_cmds, plot_cmd, copy_proj_to_out, copy_state_to_proj, alt_cmds；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | V2SimInstance.from_project；gen_cmds.generate；alt_cmds.apply；inst.simulate；plot_cmd.execute；resolve；shutil.copytree；Path；pres_p.exists；pres_p.is_dir；print | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
