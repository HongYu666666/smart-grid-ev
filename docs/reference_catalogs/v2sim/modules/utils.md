# `v2sim.utils`

本模块共记录 21 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | v2sim.utils.AddRecentProject | (path: Union[str, Iterable[str]]) | v2sim/utils.py#L25 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.utils.AddRecentProject 的实现与调用链确认。 | 输入：path；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | isinstance；GetRecentProjects；reversed；str；projects.insert；open；list；resolve；projects.remove；f.write；absolute；Path | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.utils.AddtionalTypes | class | v2sim/utils.py#L131 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.utils.AddtionalTypes 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.utils.CheckAddtionalType | (file: PathLike) -> AddtionalTypes | v2sim/utils.py#L136 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.utils.CheckAddtionalType 的实现与调用链确认。 | 输入：file；输出：AddtionalTypes | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | getroot；AddtionalTypes；RuntimeError；root.find；ReadXML；Lang.ERROR_FILE_TYPE_NOT_SUPPORTED.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.utils.CheckFile | (file: PathLike) | v2sim/utils.py#L77 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.utils.CheckFile 的实现与调用链确认。 | 输入：file；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | str；Path；p.exists；rename | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.utils.CheckPyVersion | (ver: Tuple[int, int, int, bool]) -> bool | v2sim/utils.py#L290 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.utils.CheckPyVersion 的实现与调用链确认。 | 输入：ver；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | PyVersion | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.utils.ClearBakFiles | (dir: PathLike) | v2sim/utils.py#L88 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.utils.ClearBakFiles 的实现与调用链确认。 | 输入：dir；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | iterdir；Path；x.is_file；x.unlink | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.utils.ClearRecentProjects | () | v2sim/utils.py#L15 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.utils.ClearRecentProjects 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | RECENT_PROJECTS_FILE.exists；RECENT_PROJECTS_FILE.unlink | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.utils.DetectFiles | (dir: PathLike) -> FileDetectResult | v2sim/utils.py#L145 | Detect simulation-realted files (SUMO config, SCS, FCS, power grid, etc.) in the given directory. Args: dir (str): Directory path Returns: FileDetectResult: A dictionary containing the detected files | 输入：dir；输出：FileDetectResult | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；p.iterdir；exists；FileDetectResult；isinstance；Path；str；filename.lower；filenamel.endswith；CheckAddtionalType；add；FileExistsError；x.is_file；as_posix；Lang.ERROR_CONFIG_DIR_FILE_DUPLICATE.format；addtional.add | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.utils.FileDetectResult | class | v2sim/utils.py#L94 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.utils.FileDetectResult 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | getattr；hasattr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.utils.FileDetectResult.__contains__ | (self, key: str) -> bool | v2sim/utils.py#L127 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.utils.FileDetectResult.__contains__ 的实现与调用链确认。 | 输入：key；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | hasattr；getattr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.utils.FileDetectResult.__getitem__ | (self, key: str) | v2sim/utils.py#L118 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.utils.FileDetectResult.__getitem__ 的实现与调用链确认。 | 输入：key；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | getattr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.utils.FileDetectResult.get | (self, key: str) -> Optional[str] | v2sim/utils.py#L124 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.utils.FileDetectResult.get 的实现与调用链确认。 | 输入：key；输出：Optional[str] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | getattr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.utils.FileDetectResult.has | (self, key: str) -> bool | v2sim/utils.py#L121 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.utils.FileDetectResult.has 的实现与调用链确认。 | 输入：key；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | hasattr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.utils.GetRecentProjects | () -> List[str] | v2sim/utils.py#L19 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.utils.GetRecentProjects 的实现与调用链确认。 | 输入：无显式输入；输出：List[str] | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | RECENT_PROJECTS_FILE.exists；open；line.strip | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.utils.LoadFCS | (filename: PathLike) -> Set[str] | v2sim/utils.py#L55 | Load FCS file and return a set of node names | 输入：filename；输出：Set[str] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | getroot；set；RuntimeError；ReadXML；Lang.ERROR_FILE_TYPE_NOT_SUPPORTED.format；fcs_nodes.add | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.utils.LoadSCS | (filename: PathLike) -> Set[str] | v2sim/utils.py#L66 | Load SCS file and return a set of node names | 输入：filename；输出：Set[str] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | getroot；set；RuntimeError；ReadXML；Lang.ERROR_FILE_TYPE_NOT_SUPPORTED.format；scs_nodes.add | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.utils.PyVersion | () -> Tuple[int, int, int, bool] | v2sim/utils.py#L285 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.utils.PyVersion 的实现与调用链确认。 | 输入：无显式输入；输出：Tuple[int, int, int, bool] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | hasattr；sys._is_gil_enabled | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.utils.ReadXML | (file: PathLike, compressed: Optional[bool]=None) -> ElementTree | v2sim/utils.py#L40 | Read XML file, support compressed GZ file file: file path compressed: whether the file is compressed. If None, the function will detect it, but only .xml and .xml.gz are supported. | 输入：file, compressed；输出：ElementTree | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | lower；filel.endswith；str；gzip.open；ElementTree；RuntimeError；Lang.ERROR_FILE_TYPE_NOT_SUPPORTED.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.utils.V2SimConfig | class | v2sim/utils.py#L224 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.utils.V2SimConfig 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | _RENAME_FIELDS.items；V2SimConfig；isinstance；open；json.dump；json.load；data.pop | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.utils.V2SimConfig.load | (file: str) -> 'V2SimConfig' | v2sim/utils.py#L244 | Load V2Sim configuration from a file. Args: file (str): Path to the configuration file Returns: V2SimConfig: A V2SimConfig object with the loaded configuration | 输入：file；输出：'V2SimConfig' | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | _RENAME_FIELDS.items；V2SimConfig；isinstance；open；json.load；data.pop | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.utils.V2SimConfig.save | (self, file: str) | v2sim/utils.py#L274 | Save V2Sim configuration to a file. Args: file (str): Path to the configuration file | 输入：file；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | open；json.dump | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
