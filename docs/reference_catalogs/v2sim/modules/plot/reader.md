# `v2sim.plot.reader`

本模块共记录 51 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.plot.reader.ReadOnlyStatistics | class(StaReader) | v2sim/plot/reader.py#L31 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | list；__init__；self.GetColumn；self.FCS_attrib_of；self.SCS_attrib_of；self.GS_attrib_of；x.rfind；set；isinstance；self.__loadhead；self.__fcs_head.sort；self.__scs_head.sort；self.__gs_head.sort；self.__ev_head.sort；self.__gen_head.sort；self.__bus_head.sort | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.EV_attrib_of | (self, veh: str, attrib: str) -> SegFunc | v2sim/plot/reader.py#L255 | EV information | 输入：veh, attrib；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.GetColumn | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.EV_net_cost_of | (self, veh: str) -> SegFunc | v2sim/plot/reader.py#L260 | EV net cost | 输入：veh；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.GetColumn | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.FCS_attrib_of | (self, cs: str, attrib: str) -> SegFunc | v2sim/plot/reader.py#L168 | Charging station information | 输入：cs, attrib；输出：SegFunc | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.GetColumn；SegFunc.qs | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.FCS_count_of | (self, cs: str) -> SegFunc | v2sim/plot/reader.py#L199 | Number of vehicles in the CS | 输入：cs；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.FCS_attrib_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.FCS_head | (self) -> List[str] | v2sim/plot/reader.py#L82 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.FCS_head 的实现与调用链确认。 | 输入：无显式输入；输出：List[str] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self.__loadhead；self.__fcs_head.sort | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.FCS_load_all | (self, tl=-math.inf, tr=math.inf) -> List[SegFunc] | v2sim/plot/reader.py#L195 | Charging power of all CS | 输入：tl, tr；输出：List[SegFunc] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | slice；self.FCS_load_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.FCS_load_of | (self, cs: str) -> SegFunc | v2sim/plot/reader.py#L191 | Charging power | 输入：cs；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.FCS_attrib_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.FCS_pricebuy_of | (self, cs: str) -> SegFunc | v2sim/plot/reader.py#L203 | Buy price | 输入：cs；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.FCS_attrib_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.GS_attrib_of | (self, gs: str, attrib: str) -> SegFunc | v2sim/plot/reader.py#L184 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.GS_attrib_of 的实现与调用链确认。 | 输入：gs, attrib；输出：SegFunc | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.GetColumn；SegFunc.qs | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.GS_count_of | (self, gs: str) -> SegFunc | v2sim/plot/reader.py#L251 | Gas station vehicle count | 输入：gs；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.GS_attrib_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.GS_head | (self) -> List[str] | v2sim/plot/reader.py#L98 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.GS_head 的实现与调用链确认。 | 输入：无显式输入；输出：List[str] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self.__loadhead；self.__gs_head.sort | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.G_attrib_of | (self, g: str, attrib: str) -> SegFunc | v2sim/plot/reader.py#L266 | Generator information | 输入：g, attrib；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.GetColumn | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.G_total | (self, attrib: str) -> SegFunc | v2sim/plot/reader.py#L271 | Total generation data | 输入：attrib；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.GetColumn | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.SCS_attrib_of | (self, cs: str, attrib: str) -> SegFunc | v2sim/plot/reader.py#L176 | Charging station information | 输入：cs, attrib；输出：SegFunc | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.GetColumn；SegFunc.qs | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.SCS_charge_load_all | (self, tl=-math.inf, tr=math.inf) -> List[SegFunc] | v2sim/plot/reader.py#L211 | Charging power of all CS | 输入：tl, tr；输出：List[SegFunc] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | slice；self.SCS_charge_load_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.SCS_charge_load_of | (self, cs: str) -> SegFunc | v2sim/plot/reader.py#L207 | Charging power | 输入：cs；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.SCS_attrib_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.SCS_count_of | (self, cs: str) -> SegFunc | v2sim/plot/reader.py#L239 | Number of vehicles in the CS | 输入：cs；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.SCS_attrib_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.SCS_head | (self) -> List[str] | v2sim/plot/reader.py#L90 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.SCS_head 的实现与调用链确认。 | 输入：无显式输入；输出：List[str] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self.__loadhead；self.__scs_head.sort | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.SCS_net_load_all | (self, tl=-math.inf, tr=math.inf) -> List[SegFunc] | v2sim/plot/reader.py#L235 | Net charging power of all CS | 输入：tl, tr；输出：List[SegFunc] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | slice；self.SCS_net_load_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.SCS_net_load_of | (self, cs: str) -> SegFunc | v2sim/plot/reader.py#L231 | Net charging power | 输入：cs；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.SCS_charge_load_of；self.SCS_v2g_load_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.SCS_pricebuy_of | (self, cs: str) -> SegFunc | v2sim/plot/reader.py#L243 | Buy price | 输入：cs；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.SCS_attrib_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.SCS_pricesell_of | (self, cs: str) -> SegFunc | v2sim/plot/reader.py#L247 | Sell price | 输入：cs；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.SCS_attrib_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.SCS_v2g_cap_all | (self, tl=-math.inf, tr=math.inf) -> List[SegFunc] | v2sim/plot/reader.py#L227 | V2G capacity of all CS | 输入：tl, tr；输出：List[SegFunc] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | slice；self.SCS_v2g_cap_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.SCS_v2g_cap_of | (self, cs: str) -> SegFunc | v2sim/plot/reader.py#L223 | V2G capacity | 输入：cs；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.SCS_attrib_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.SCS_v2g_load_all | (self, tl=-math.inf, tr=math.inf) -> List[SegFunc] | v2sim/plot/reader.py#L219 | Discharging power (V2G) of all CS | 输入：tl, tr；输出：List[SegFunc] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | slice；self.SCS_v2g_load_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.SCS_v2g_load_of | (self, cs: str) -> SegFunc | v2sim/plot/reader.py#L215 | Discharging power (V2G) | 输入：cs；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.SCS_attrib_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.__init__ | (self, path: str) | v2sim/plot/reader.py#L68 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.__init__ 的实现与调用链确认。 | 输入：path；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.__loadhead | (self, name: str) -> List[str] | v2sim/plot/reader.py#L59 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.__loadhead 的实现与调用链确认。 | 输入：name；输出：List[str] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | list；x.rfind；set；__trans；keys；self.GetTable | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.bus_attrib_of | (self, b: str, attrib: str) -> SegFunc | v2sim/plot/reader.py#L276 | Bus information | 输入：b, attrib；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.GetColumn | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.bus_head | (self) -> List[str] | v2sim/plot/reader.py#L125 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.bus_head 的实现与调用链确认。 | 输入：无显式输入；输出：List[str] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self.__loadhead；self.__bus_head.sort；self.__bus_head.remove | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.bus_total | (self, attrib: str) -> SegFunc | v2sim/plot/reader.py#L281 | Total bus data | 输入：attrib；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.GetColumn | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.ess_attrib_of | (self, e: str, attrib: str) -> SegFunc | v2sim/plot/reader.py#L296 | ESS information | 输入：e, attrib；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.GetColumn | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.ess_head | (self) -> List[str] | v2sim/plot/reader.py#L158 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.ess_head 的实现与调用链确认。 | 输入：无显式输入；输出：List[str] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self.__loadhead；self.__ess_head.sort；self.__ess_head.remove | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.gen_head | (self) -> List[str] | v2sim/plot/reader.py#L114 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.gen_head 的实现与调用链确认。 | 输入：无显式输入；输出：List[str] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self.__loadhead；self.__gen_head.sort；self.__gen_head.remove | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.has_BUS | (self) -> bool | v2sim/plot/reader.py#L47 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.has_BUS 的实现与调用链确认。 | 输入：无显式输入；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.has_ESS | (self) -> bool | v2sim/plot/reader.py#L56 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.has_ESS 的实现与调用链确认。 | 输入：无显式输入；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.has_EV | (self) -> bool | v2sim/plot/reader.py#L41 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.has_EV 的实现与调用链确认。 | 输入：无显式输入；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.has_FCS | (self) -> bool | v2sim/plot/reader.py#L32 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.has_FCS 的实现与调用链确认。 | 输入：无显式输入；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.has_GEN | (self) -> bool | v2sim/plot/reader.py#L44 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.has_GEN 的实现与调用链确认。 | 输入：无显式输入；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.has_GS | (self) -> bool | v2sim/plot/reader.py#L38 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.has_GS 的实现与调用链确认。 | 输入：无显式输入；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.has_LINE | (self) -> bool | v2sim/plot/reader.py#L50 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.has_LINE 的实现与调用链确认。 | 输入：无显式输入；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.has_PVW | (self) -> bool | v2sim/plot/reader.py#L53 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.has_PVW 的实现与调用链确认。 | 输入：无显式输入；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.has_SCS | (self) -> bool | v2sim/plot/reader.py#L35 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.has_SCS 的实现与调用链确认。 | 输入：无显式输入；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.line_attrib_of | (self, l: str, attrib: str) -> SegFunc | v2sim/plot/reader.py#L286 | Line information | 输入：l, attrib；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.GetColumn | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.line_head | (self) -> List[str] | v2sim/plot/reader.py#L136 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.line_head 的实现与调用链确认。 | 输入：无显式输入；输出：List[str] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self.__loadhead；self.__line_head.sort；self.__line_head.remove | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.pvw_attrib_of | (self, p: str, attrib: str) -> SegFunc | v2sim/plot/reader.py#L291 | PV and wind information | 输入：p, attrib；输出：SegFunc | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.GetColumn | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.pvw_head | (self) -> List[str] | v2sim/plot/reader.py#L147 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.pvw_head 的实现与调用链确认。 | 输入：无显式输入；输出：List[str] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self.__loadhead；self.__pvw_head.sort；self.__pvw_head.remove | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.plot.reader.ReadOnlyStatistics.veh_head | (self) -> List[str] | v2sim/plot/reader.py#L106 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.ReadOnlyStatistics.veh_head 的实现与调用链确认。 | 输入：无显式输入；输出：List[str] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self.__loadhead；self.__ev_head.sort | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| class | v2sim.plot.reader.StatisticsNotSupportedError | class(Exception) | v2sim/plot/reader.py#L8 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader.StatisticsNotSupportedError 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | v2sim.plot.reader._parse_val | (x: str) -> List[Any] | v2sim/plot/reader.py#L12 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.reader._parse_val 的实现与调用链确认。 | 输入：x；输出：List[Any] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | c.isdigit；v.append；int；ord | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
