# `v2sim.plot.plot`

本模块共记录 38 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.plot.plot.AdvancedPlot | class | v2sim/plot/plot.py#L41 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.new_fig；plt.subplots；expr.find；eval；series.split；SegFunc.cross_interpolate；isinstance；interpolate；self.calc_expr；self.__setticks；self.ax.set_xticks；plt.setp；self.get_accum_series；self.ax.stackplot；self.ax.set_ylabel；self.ax.set_ylim | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.__init__ | (self, tl: int=0, tr: int=-1, w: int=12, h: int=3, remove_edge: bool=True, double_side: bool=False, pic_ext: str='png', dpi: int=128, quick_plot_title: bool=True) | v2sim/plot/plot.py#L42 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.__init__ 的实现与调用链确认。 | 输入：tl, tr, w, h, remove_edge, double_side, pic_ext, dpi, quick_plot_title；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.new_fig | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.__setticks | (self) | v2sim/plot/plot.py#L276 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.__setticks 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.ax.set_xticks；plt.setp；bisect.bisect_right；list；map；self.ax.get_xticklabels；range | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.add_accum | (self, name: str, plot_max: bool) | v2sim/plot/plot.py#L295 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.add_accum 的实现与调用链确认。 | 输入：name, plot_max；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.get_accum_series；self.ax.stackplot；self.__setticks；max；print；self.ax.plot；self.ax.text；sum；range；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.add_data | (self, expr: str, label: Optional[str]=None, color: Optional[str]=None, linestyle: Optional[str]='-', linewidth: Optional[float]=1, side: Literal['left', 'right']='left') | v2sim/plot/plot.py#L249 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.add_data 的实现与调用链确认。 | 输入：expr, label, color, linestyle, linewidth, side；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.calc_expr；isinstance；self.__setticks；self.ax.plot；self.check_right；self.ax2.plot | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.calc_expr | (self, expr: str) | v2sim/plot/plot.py#L70 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.calc_expr 的实现与调用链确认。 | 输入：expr；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | expr.find；eval；self.get_series；ValueError；len；ret_expr.replace | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.check_right | (self) | v2sim/plot/plot.py#L66 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.check_right 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.ax.twinx | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.configure | (self, commands: Union[str, List[str]]) | v2sim/plot/plot.py#L363 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.configure 的实现与调用链确认。 | 输入：commands；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | isinstance；split_string_except_quotes；new_cmds.extend；cmd.strip；len；self.add_data；ValueError；self.add_accum；float；plt.subplots；self.title；self.x_label；int；self.yleft_label；self.yright_label；self.yticks | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.get_accum_series | (self, series: str) -> Tuple[List[int], List[List[Any]], List[str]] | v2sim/plot/plot.py#L96 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.get_accum_series 的实现与调用链确认。 | 输入：series；输出：Tuple[List[int], List[List[Any]], List[str]] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | series.split；SegFunc.cross_interpolate；len；se.FCS_load_all；int；ValueError；ReadOnlyStatistics；max；se.SCS_net_load_all；domain.startswith；RuntimeError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.get_series | (self, series: str) -> SegFunc | v2sim/plot/plot.py#L137 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.get_series 的实现与调用链确认。 | 输入：series；输出：SegFunc | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | series.split；interpolate；len；se.G_total；ReadOnlyStatistics；max；d.slice；int；RuntimeError；ValueError；se.bus_total；se.FCS_load_of；se.FCS_count_of；se.FCS_pricebuy_of；se.SCS_charge_load_of；se.SCS_v2g_load_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.hline | (self, y: float, color: str='black', linestyle: str='--', linewidth: float=1.5) | v2sim/plot/plot.py#L344 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.hline 的实现与调用链确认。 | 输入：y, color, linestyle, linewidth；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.ax.axhline | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.legend | (self, loc: str='upper right', ncol: int=1) | v2sim/plot/plot.py#L336 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.legend 的实现与调用链确认。 | 输入：loc, ncol；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len；warn；self.fig.legend；self.ax.get_legend_handles_labels | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.load_series | (self, path: Union[str, ReadOnlyStatistics]) | v2sim/plot/plot.py#L125 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.load_series 的实现与调用链确认。 | 输入：path；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；max；ReadOnlyStatistics；RuntimeError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.new_fig | (self, tl: int=0, tr: int=-1, w: int=12, h: int=3, remove_edge: bool=True, double_side: bool=False) | v2sim/plot/plot.py#L52 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.new_fig 的实现与调用链确认。 | 输入：tl, tr, w, h, remove_edge, double_side；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | plt.subplots；plt.close；set_visible；self.ax.twinx | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.quick_bus | (self, tl: int, tr: int, bus_name: str, activel: bool, reactivel: bool, volt: bool, activeg: bool, reactiveg: bool, save_to: str='', res_path: str='results') | v2sim/plot/plot.py#L561 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.quick_bus 的实现与调用链确认。 | 输入：tl, tr, bus_name, activel, reactivel, volt, activeg, reactiveg, save_to, res_path；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.new_fig；self.legend；self.x_label；p.mkdir；self.save；self.add_data；self.yleft_label；self.y_label；self.title；Path；str；Lang.PLOT_BUS.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.quick_bus_tot | (self, tl: int, tr: int, active: bool, reactive: bool, active_gen: bool, reactive_gen: bool, save_to: str='', res_path: str='results') | v2sim/plot/plot.py#L667 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.quick_bus_tot 的实现与调用链确认。 | 输入：tl, tr, active, reactive, active_gen, reactive_gen, save_to, res_path；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.new_fig；self.legend；self.x_label；p.mkdir；self.save；self.add_data；self.y_label；self.title；Path；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.quick_ess | (self, tl: int, tr: int, ess_name: str, P: bool, soc: bool, save_to: str='', res_path: str='results') | v2sim/plot/plot.py#L728 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.quick_ess 的实现与调用链确认。 | 输入：tl, tr, ess_name, P, soc, save_to, res_path；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.new_fig；self.legend；self.x_label；p.mkdir；self.save；self.add_data；self.yleft_label；self.y_label；self.title；Path；str；Lang.PLOT_ESS.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.quick_ev | (self, tl: int, tr: int, ev_name: str, soc: bool, status: bool, cost: bool, earn: bool, cpure: bool, save_to: str='', res_path: str='results') | v2sim/plot/plot.py#L613 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.quick_ev 的实现与调用链确认。 | 输入：tl, tr, ev_name, soc, status, cost, earn, cpure, save_to, res_path；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.new_fig；self.x_label；p.mkdir；self.save；self.add_data；self.yleft_label；self.y_label；self.legend；self.title；Path；str；RuntimeError；Lang.PLOT_EV.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.quick_fcs | (self, tl: int, tr: int, cs_name: str, load: bool, price: bool, wcnt: bool, save_to: str='', res_path: str='results') | v2sim/plot/plot.py#L483 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.quick_fcs 的实现与调用链确认。 | 输入：tl, tr, cs_name, load, price, wcnt, save_to, res_path；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.new_fig；self.legend；self.x_label；p.mkdir；self.save；ValueError；self.add_data；self.yleft_label；self.y_label；self.title；Path；str；RuntimeError；Lang.PLOT_FCS_TITLE.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.quick_fcs_accum | (self, tl: int, tr: int, plot_max: bool, save_to: str='', res_path: str='results') | v2sim/plot/plot.py#L459 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.quick_fcs_accum 的实现与调用链确认。 | 输入：tl, tr, plot_max, save_to, res_path；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.new_fig；self.add_accum；self.yleft_label；self.legend；self.x_label；p.mkdir；self.save；self.title；Path；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.quick_gen | (self, tl: int, tr: int, gen_name: str, active: bool, reactive: bool, costp: bool, save_to: str='', res_path: str='results') | v2sim/plot/plot.py#L592 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.quick_gen 的实现与调用链确认。 | 输入：tl, tr, gen_name, active, reactive, costp, save_to, res_path；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.new_fig；self.legend；self.x_label；p.mkdir；self.save；self.add_data；self.yleft_label；self.y_label；self.title；Path；str；Lang.PLOT_GEN.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.quick_gen_tot | (self, tl: int, tr: int, active: bool, reactive: bool, costp: bool, save_to: str='', res_path: str='results') | v2sim/plot/plot.py#L647 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.quick_gen_tot 的实现与调用链确认。 | 输入：tl, tr, active, reactive, costp, save_to, res_path；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.new_fig；self.legend；self.x_label；p.mkdir；self.save；self.add_data；self.yleft_label；self.y_label；self.title；Path；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.quick_gs | (self, tl: int, tr: int, gs_name: str, cnt: bool, save_to: str='', res_path: str='results') | v2sim/plot/plot.py#L547 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.quick_gs 的实现与调用链确认。 | 输入：tl, tr, gs_name, cnt, save_to, res_path；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.new_fig；self.add_data；self.yleft_label；self.legend；self.x_label；p.mkdir；self.save；self.title；Path；str；Lang.PLOT_GS.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.quick_line | (self, tl: int, tr: int, line_name: str, active: bool, reactive: bool, current: bool, save_to: str='', res_path: str='results') | v2sim/plot/plot.py#L689 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.quick_line 的实现与调用链确认。 | 输入：tl, tr, line_name, active, reactive, current, save_to, res_path；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.new_fig；self.legend；self.x_label；p.mkdir；self.save；self.add_data；self.yleft_label；self.y_label；self.title；Path；str；Lang.PLOT_LINE.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.quick_pvw | (self, tl: int, tr: int, pvw_name: str, P: bool, cr: bool, save_to: str='', res_path: str='results') | v2sim/plot/plot.py#L710 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.quick_pvw 的实现与调用链确认。 | 输入：tl, tr, pvw_name, P, cr, save_to, res_path；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.new_fig；self.legend；self.x_label；p.mkdir；self.save；self.add_data；self.yleft_label；self.y_label；self.title；Path；str；Lang.PLOT_PVW.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.quick_scs | (self, tl: int, tr: int, cs_name: str, cload: bool, dload: bool, netload: bool, v2gcap: bool, wcnt: bool, pricebuy: bool, pricesell: bool, save_to: str='', res_path: str='results') | v2sim/plot/plot.py#L509 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.quick_scs 的实现与调用链确认。 | 输入：tl, tr, cs_name, cload, dload, netload, v2gcap, wcnt, pricebuy, pricesell, save_to, res_path；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.new_fig；self.legend；self.x_label；p.mkdir；self.save；self.yleft_label；self.y_label；self.add_data；self.title；Path；str；RuntimeError；Lang.PLOT_SCS_TITLE.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.quick_scs_accum | (self, tl: int, tr: int, plot_max: bool, save_to: str='', res_path: str='results') | v2sim/plot/plot.py#L471 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.quick_scs_accum 的实现与调用链确认。 | 输入：tl, tr, plot_max, save_to, res_path；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.new_fig；self.add_accum；self.yleft_label；self.legend；self.x_label；p.mkdir；self.save；self.title；Path；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.save | (self, save_to: str, mloc: bool=True, zeroxmargin: bool=True) | v2sim/plot/plot.py#L350 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.save 的实现与调用链确认。 | 输入：save_to, mloc, zeroxmargin；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | ScalarFormatter；self.ax.yaxis.set_major_formatter；self.ax.set_xlim；self.fig.savefig；plt.close；MultipleLocator；self.ax.xaxis.set_major_locator；self.ax.margins | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.title | (self, title: str) | v2sim/plot/plot.py#L330 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.title 的实现与调用链确认。 | 输入：title；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.ax.set_title | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.vline | (self, x: float, color: str='black', linestyle: str='--', linewidth: float=1.5) | v2sim/plot/plot.py#L347 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.vline 的实现与调用链确认。 | 输入：x, color, linestyle, linewidth；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.ax.axvline | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.x_label | (self, label: str) | v2sim/plot/plot.py#L327 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.x_label 的实现与调用链确认。 | 输入：label；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.ax.set_xlabel | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.y_label | (self, label: str, side: Literal['left', 'right']='left') | v2sim/plot/plot.py#L305 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.y_label 的实现与调用链确认。 | 输入：label, side；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.yleft_label；self.yright_label | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.yleft_label | (self, label: str) | v2sim/plot/plot.py#L311 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.yleft_label 的实现与调用链确认。 | 输入：label；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.ax.set_ylabel | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.yleft_range | (self, low: float, high: float) | v2sim/plot/plot.py#L314 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.yleft_range 的实现与调用链确认。 | 输入：low, high；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.ax.set_ylim | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.yright_label | (self, label: str) | v2sim/plot/plot.py#L317 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.yright_label 的实现与调用链确认。 | 输入：label；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.check_right；isinstance；self.ax2.set_ylabel | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.yright_range | (self, low: float, high: float) | v2sim/plot/plot.py#L322 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.yright_range 的实现与调用链确认。 | 输入：low, high；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.check_right；isinstance；self.ax2.set_ylim | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plot.plot.AdvancedPlot.yticks | (self, ticks, labels: Optional[Iterable[str]]=None) | v2sim/plot/plot.py#L333 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.AdvancedPlot.yticks 的实现与调用链确认。 | 输入：ticks, labels；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.ax.set_yticks | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.plot.plot.split_string_except_quotes | (string: str, delim: str, trim_quotes: bool=True) -> List[str] | v2sim/plot/plot.py#L20 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plot.plot.split_string_except_quotes 的实现与调用链确认。 | 输入：string, delim, trim_quotes；输出：List[str] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | result.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
