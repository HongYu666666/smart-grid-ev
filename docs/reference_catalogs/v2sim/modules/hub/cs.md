# `v2sim.hub.cs`

本模块共记录 73 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.hub.cs.AllocEnv | class | v2sim/hub/cs.py#L13 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.AllocEnv 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.hub.cs.BiCS | class(CS) | v2sim/hub/cs.py#L570 | Charging Station supporting V2G. | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；reset；self._c_evs.clear；self._d_evs.clear；to_xml；veh.start_bidirectional；veh.end_bidirectional；self.is_offline；sum；len；self._pc_alloc；self._pd_alloc；self.pop_veh；super；ev.willing_to_v2g；list | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.BiCS.__init__ | (self, *args, **kwargs) | v2sim/hub/cs.py#L572 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.BiCS.__init__ 的实现与调用链确认。 | 输入：*args, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.BiCS.__str__ | (self) | v2sim/hub/cs.py#L699 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.BiCS.__str__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.BiCS._ev_enter_chi | (self, veh: EV) | v2sim/hub/cs.py#L590 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.BiCS._ev_enter_chi 的实现与调用链确认。 | 输入：veh；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | veh.start_bidirectional | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.BiCS._ev_leave_chi | (self, veh: EV) | v2sim/hub/cs.py#L593 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.BiCS._ev_leave_chi 的实现与调用链确认。 | 输入：veh；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | veh.end_bidirectional | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.BiCS.get_V2G_cap | (self, _t: int, /) -> float | v2sim/hub/cs.py#L596 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.BiCS.get_V2G_cap 的实现与调用链确认。 | 输入：_t；输出：float | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.is_offline；sum；ev.willing_to_v2g；self._psell | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.BiCS.reset | (self) | v2sim/hub/cs.py#L578 | Reset the charging station to its initial state. | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | reset；self._c_evs.clear；self._d_evs.clear；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.BiCS.to_xml | (self) | v2sim/hub/cs.py#L585 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.BiCS.to_xml 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | to_xml；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.BiCS.update | (self, sec: int, cur_time: int, v2g_demand: float, pb_e: float, ps_e: float) -> List[EV] | v2sim/hub/cs.py#L605 | Charge and discharge the EV with the current parameters for sec seconds. Ensure get_V2G_cap() is called before update() in each time step to get the latest V2G capacity. | 输入：sec, cur_time, v2g_demand, pb_e, ps_e；输出：List[EV] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；self.is_offline；self._d_evs.clear；self._pc_alloc；self._pd_alloc；self.pop_veh；self._c_evs.clear；list；AllocEnv；ev.bidirectional_discharge；self.real_pbuy；ev._bidirectional_charge；self.real_psell；ev.willing_to_slow_charge；ret.append；self._c_evs.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.hub.cs.CS | class(BaseStation[EV], ABC) | v2sim/hub/cs.py#L135 | 表示单个充电站的车辆队列、充放电功率、价格和 V2G 能力。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；float；MaxPCAllocPool.get；V2GAllocPool.get；self._owners.members.add；reset；Element；ret.append；self._psell；len；chain；self.__contains__；_pget_from_like；OwnerGroup；str；self._pbuy.to_xml | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.hub.cs.CS.Pc | (self) -> float | v2sim/hub/cs.py#L348 | Current charging power, kWh/s | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.Pc_MW | (self) -> float | v2sim/hub/cs.py#L358 | Current charging power, MW, 3.6MW = 1kWh/s | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.Pc_kW | (self) -> float | v2sim/hub/cs.py#L353 | Current charging power, kW, 3600kW = 1kWh/s | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.Pd | (self) -> float | v2sim/hub/cs.py#L363 | Current V2G discharge power, kWh/s | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.Pd_MW | (self) -> float | v2sim/hub/cs.py#L373 | Current V2G discharge power, MW, 3.6MW = 1kWh/s | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.Pd_kW | (self) -> float | v2sim/hub/cs.py#L368 | Current V2G discharge power, kW, 3600kW = 1kWh/s | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.Pv2g | (self) -> float | v2sim/hub/cs.py#L378 | Current maximum V2G discharge power, kWh/s | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.Pv2g_MW | (self) -> float | v2sim/hub/cs.py#L388 | Current maximum V2G discharge power, MW, 3.6MW = 1kWh/s | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.Pv2g_kW | (self) -> float | v2sim/hub/cs.py#L383 | Current maximum V2G discharge power, kW, 3600kW = 1kWh/s | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.__contains__ | (self, veh: EV) -> bool | v2sim/hub/cs.py#L401 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.CS.__contains__ 的实现与调用链确认。 | 输入：veh；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.__init__ | (self, name: str, bind: str, slots: int, bus: str, x: float, y: float, cs_type: CSType, max_pc: float, max_pd: float, price_buy: PriceGetterLike, price_buy_is_service_fee: bool=False, price_sell: Optional[PriceGetterLike]=None, price_sell_is_service_fee: bool=False, offline: Optional[RangeList]=None, owners: Optional[OwnerGroup]=None, pc_alloc: str='Average', pd_alloc: str='Average', allow_queuing: bool=True) | v2sim/hub/cs.py#L137 | Initialize the CS | 输入：name, bind, slots, bus, x, y, cs_type, max_pc, max_pd, price_buy, price_buy_is_service_fee, price_sell, price_sell_is_service_fee, offline, owners, pc_alloc, pd_alloc, allow_queuing；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；float；MaxPCAllocPool.get；V2GAllocPool.get；_pget_from_like；super；isinstance；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.__len__ | (self) -> int | v2sim/hub/cs.py#L404 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.CS.__len__ 的实现与调用链确认。 | 输入：无显式输入；输出：int | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.__repr__ | (self) | v2sim/hub/cs.py#L221 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.CS.__repr__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.__str__ | (self) | v2sim/hub/cs.py#L224 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.CS.__str__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS._ev_enter_chi | (self, veh: EV) | v2sim/hub/cs.py#L430 | Handle vehicle entering the charging station | 输入：veh；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS._ev_leave_chi | (self, veh: EV) | v2sim/hub/cs.py#L467 | Handle vehicle entering the charging station | 输入：veh；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS._unsafe_add_veh | (self, veh: EV) | v2sim/hub/cs.py#L454 | Add a vehicle to the charging queue without any check. For internal use only. | 输入：veh；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len；self._ev_enter_chi；self._chi.add；self._buf.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.add_single_owner | (self, owner: str) | v2sim/hub/cs.py#L201 | Add a single owner to the private charging station. | 输入：owner；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._owners.members.add；OwnerGroup | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.add_veh | (self, veh: EV) -> bool | v2sim/hub/cs.py#L434 | 将车辆加入站点或等待队列并更新站内关联状态。 | 输入：veh；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len；self._ev_enter_chi；self._chi.add；self._buf.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.hub.cs.CS.averageSOC | (self, include_waiting: bool=True) -> float | v2sim/hub/cs.py#L414 | Average SOC of all vehicles in the charging station. When include_waiting is True, the average SOC of all vehicles (including those waiting) is returned. When include_waiting is False, only the average SOC of vehicles being charged is returned. | 输入：include_waiting；输出：float | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；sum；self.vehicles | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.bus | (self) -> str | v2sim/hub/cs.py#L278 | The distribution network bus to which the charging station connects | 输入：无显式输入；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.get_V2G_cap | (self, t: int) -> float | v2sim/hub/cs.py#L331 | 汇总站内车辆在当前状态下可提供的 V2G 容量。 | 输入：t；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.hub.cs.CS.has_veh | (self, veh: EV) -> bool | v2sim/hub/cs.py#L492 | Check if there is a vehicle with the specified ID. | 输入：veh；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.__contains__ | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.is_charging | (self, veh: EV) -> bool | v2sim/hub/cs.py#L392 | Get the charging status of the vehicle. If the vehicle does not exist, a ValueError will be raised. | 输入：veh；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.is_owned_by | (self, veh_name: str) -> bool | v2sim/hub/cs.py#L239 | Check if this charging station is owned by the specified owner. | 输入：veh_name；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.is_pc_constrained | (self) -> bool | v2sim/hub/cs.py#L227 | Check if the charging power is constrained by the PDN | 输入：无显式输入；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.is_private | (self) -> bool | v2sim/hub/cs.py#L235 | Check if this is a private charging station | 输入：无显式输入；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.is_public | (self) -> bool | v2sim/hub/cs.py#L231 | Check if this is a public charging station | 输入：无显式输入；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.pop_veh | (self, ev: EV) -> bool | v2sim/hub/cs.py#L471 | 从站点移除车辆并返回被移除对象。 | 输入：ev；输出：bool | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._ev_leave_chi；self._chi.remove；self._buf.popleft；self._ev_enter_chi；self._chi.add；self._buf.remove；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.hub.cs.CS.psell | (self, t: int, veh: EV) -> float | v2sim/hub/cs.py#L282 | Electricity selling price, $/kWh | 输入：t, veh；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._psell；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.psell_is_service_fee | (self) -> bool | v2sim/hub/cs.py#L287 | Whether the price_sell is a service fee rather than the actual price of energy. | 输入：无显式输入；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.real_psell | (self, t: int, veh: EV, elec_price: float) -> float | v2sim/hub/cs.py#L292 | The actual unit revenue for user, $/kWh | 输入：t, veh, elec_price；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | ValueError；self._psell | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.reset | (self) | v2sim/hub/cs.py#L211 | Reset the charging station to its initial state. | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | reset；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.set_Pc_lim | (self, value: float) | v2sim/hub/cs.py#L337 | Set the maximum charging power of the charging station | 输入：value；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.supports_V2G | (self) -> bool | v2sim/hub/cs.py#L303 | Check if this charging station supports V2G | 输入：无显式输入；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.to_xml | (self, v2g: bool=True) -> Element | v2sim/hub/cs.py#L249 | Get the XML Element of the charging station | 输入：v2g；输出：Element | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | Element；ret.append；str；self._pbuy.to_xml；len；self._psell.to_xml；self._offline.toXMLNode；self._owners.to_xml | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.update | (self, sec: int, cur_time: int, v2g_demand: float, pb_e: float, ps_e: float) -> List[EV] | v2sim/hub/cs.py#L308 | 按当前时间更新站内车辆的充电、放电和功率聚合状态。 | 输入：sec, cur_time, v2g_demand, pb_e, ps_e；输出：List[EV] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.hub.cs.CS.veh_count | (self, only_charging: bool=False) -> int | v2sim/hub/cs.py#L322 | Return the number of vehicles in the charging station. When only_charging is True, only the number of vehicles being charged is returned. | 输入：only_charging；输出：int | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.vehicles | (self) | v2sim/hub/cs.py#L411 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.CS.vehicles 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | chain | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.CS.wait_count | (self) -> int | v2sim/hub/cs.py#L407 | Number of vehicles waiting for charging | 输入：无显式输入；输出：int | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.hub.cs.CSType | class(Enum) | v2sim/hub/cs.py#L101 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.CSType 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.hub.cs.MaxPCAllocPool | class | v2sim/hub/cs.py#L82 | Charging rate correction function pool | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.MaxPCAllocPool.add | (name: str, func: MaxPCAllocator) | v2sim/hub/cs.py#L91 | Add charging rate correction function | 输入：name, func；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.MaxPCAllocPool.get | (name: str) -> MaxPCAllocator | v2sim/hub/cs.py#L96 | Get charging rate correction function | 输入：name；输出：MaxPCAllocator | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.hub.cs.OwnerGroup | class | v2sim/hub/cs.py#L105 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.OwnerGroup 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；Element；ret.append；self.members.add；grp.to_xml；self.members.update；join；split；self.subgroups.add；ValueError；OwnerGroup | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.OwnerGroup.__contains__ | (self, owner: str) -> bool | v2sim/hub/cs.py#L120 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.OwnerGroup.__contains__ 的实现与调用链确认。 | 输入：owner；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.OwnerGroup.__init__ | (self, e: Optional[Element]=None) | v2sim/hub/cs.py#L106 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.OwnerGroup.__init__ 的实现与调用链确认。 | 输入：e；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；self.members.add；self.members.update；split；self.subgroups.add；ValueError；OwnerGroup | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.OwnerGroup.to_xml | (self, tag='owners') -> Element | v2sim/hub/cs.py#L126 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.OwnerGroup.to_xml 的实现与调用链确认。 | 输入：tag；输出：Element | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | Element；ret.append；grp.to_xml；join | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.hub.cs.UniCS | class(CS) | v2sim/hub/cs.py#L502 | Charging station not supporting V2G. | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | to_xml；veh.start_charging；ev.end_charging；self.is_offline；list；self._buf.clear；self._chi.clear；len；self._pc_alloc；super；chain；self._ev_leave_chi；AllocEnv；self.pop_veh；ev.charge；self.real_pbuy | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.UniCS.__str__ | (self) | v2sim/hub/cs.py#L566 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.UniCS.__str__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.UniCS._ev_enter_chi | (self, veh: EV) | v2sim/hub/cs.py#L509 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.UniCS._ev_enter_chi 的实现与调用链确认。 | 输入：veh；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | veh.start_charging | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.UniCS._ev_leave_chi | (self, ev: EV) | v2sim/hub/cs.py#L512 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.UniCS._ev_leave_chi 的实现与调用链确认。 | 输入：ev；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | ev.end_charging | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.UniCS.get_V2G_cap | (self, _t: int, /) -> float | v2sim/hub/cs.py#L515 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.UniCS.get_V2G_cap 的实现与调用链确认。 | 输入：_t；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.UniCS.to_xml | (self) | v2sim/hub/cs.py#L504 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs.UniCS.to_xml 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | to_xml；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.UniCS.update | (self, sec: int, cur_time: int, v2g_demand: float, pb_e: float, ps_e: float) -> List[EV] | v2sim/hub/cs.py#L518 | Charge the EV with the current parameters for sec seconds. | 输入：sec, cur_time, v2g_demand, pb_e, ps_e；输出：List[EV] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.is_offline；list；self._buf.clear；self._chi.clear；len；self._pc_alloc；chain；self._ev_leave_chi；AllocEnv；self.pop_veh；ev.charge；self.real_pbuy；ret.append；ev.willing_to_slow_charge | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.hub.cs.V2GAllocPool | class | v2sim/hub/cs.py#L26 | Charging rate correction function pool | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.V2GAllocPool.add | (name: str, func: V2GAllocator) | v2sim/hub/cs.py#L33 | Add charging rate correction function | 输入：name, func；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.cs.V2GAllocPool.get | (name: str) -> V2GAllocator | v2sim/hub/cs.py#L38 | Get charging rate correction function | 输入：name；输出：V2GAllocator | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.hub.cs._AverageMaxPCAllocator | (env: AllocEnv, vcnt: int, max_pc0: float, max_pc_tot: float) | v2sim/hub/cs.py#L44 | Average maximum charging power allocator env: Allocation environment vcnt: Number of vehicles being charged max_pc0: Maximum charging power of a single pile, kWh/s max_pc_tot: Maximum charging power of the entire CS given by the PDN, kWh/s | 输入：env, vcnt, max_pc0, max_pc_tot；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | min；ev.set_temp_max_pc | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.hub.cs._AverageV2GAllocator | (env: AllocEnv, veh_cnt: int, v2g_demand: float, v2g_cap: float) | v2sim/hub/cs.py#L20 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs._AverageV2GAllocator 的实现与调用链确认。 | 输入：env, veh_cnt, v2g_demand, v2g_cap；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | ev.set_temp_pd | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.hub.cs._PrioritizedMaxPCAllocator | (env: AllocEnv, vcnt: int, max_pc0: float, max_pc_tot: float) | v2sim/hub/cs.py#L57 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs._PrioritizedMaxPCAllocator 的实现与调用链确认。 | 输入：env, vcnt, max_pc0, max_pc_tot；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | ev.set_temp_max_pc | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.hub.cs._TimeBasedMaxPCAllocator | (env: AllocEnv, vcnt: int, max_pc0: float, max_pc_tot: float) | v2sim/hub/cs.py#L66 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.cs._TimeBasedMaxPCAllocator 的实现与调用链确认。 | 输入：env, vcnt, max_pc0, max_pc_tot；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | loban.sort；loban.append；ev.set_temp_max_pc；max | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
