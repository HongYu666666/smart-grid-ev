# `v2sim.hub.hub`

本模块共记录 81 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.hub.hub.CSHub | class(StationHub[CS, EV]) | v2sim/hub/hub.py#L300 | 聚合快充站和慢充站并提供站点级车辆、功率和 V2G 操作。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；reset；veh.set_leave_at_etar；add_veh；_append；self.__pd_cap.append；self._append；self.create_kdtree；is_charging；zip；repr；len；self.__pd_dem.append；cs.veh_count；cs.get_V2G_cap；repeat | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.hub.hub.CSHub.__init__ | (self, par: List[CS], auto_leave_at_etar: bool) | v2sim/hub/hub.py#L302 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.CSHub.__init__ 的实现与调用链确认。 | 输入：par, auto_leave_at_etar；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；len；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.CSHub.__repr__ | (self) | v2sim/hub/hub.py#L440 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.CSHub.__repr__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | join；map | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.CSHub.__str__ | (self) | v2sim/hub/hub.py#L443 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.CSHub.__str__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | repr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.CSHub._append | (self, cs: CS) | v2sim/hub/hub.py#L321 | Append a charging station. If you want to add multiple charging stations, use extend() instead. | 输入：cs；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | _append；self.__pd_cap.append；len；self.__pd_dem.append；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.CSHub.add_veh | (self, veh: EV, cs: Union[int, str]) -> bool | v2sim/hub/hub.py#L316 | Add vehicles to the specified station | 输入：veh, cs；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | veh.set_leave_at_etar；add_veh；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.CSHub.append | (self, cs: CS) | v2sim/hub/hub.py#L329 | Append a charging station. If you want to add multiple charging stations, use extend() instead. | 输入：cs；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._append；self.create_kdtree | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.CSHub.clear_V2G_demand | (self) | v2sim/hub/hub.py#L410 | Clear V2G demand | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.CSHub.extend | (self, cs_list: Iterable[CS]) | v2sim/hub/hub.py#L334 | Extend the charging station list. | 输入：cs_list；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.create_kdtree；self._append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.CSHub.get_Pc | (self, k: float=1) -> List[float] | v2sim/hub/hub.py#L369 | List of discharge power of all charging stations | 输入：k；输出：List[float] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.CSHub.get_Pd | (self, k: float=1) -> List[float] | v2sim/hub/hub.py#L351 | List of charging power of all charging stations, default unit kWh/s | 输入：k；输出：List[float] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.CSHub.get_V2G_cap | (self, t: int) -> List[float] | v2sim/hub/hub.py#L395 | Get the maximum V2G power (considering losses) of each CS at the current time t, in kWh/s | 输入：t；输出：List[float] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | cs.get_V2G_cap | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.CSHub.get_online_Pc | (self, t: int, k: float=1) -> List[float] | v2sim/hub/hub.py#L378 | List of discharge power of all available charging stations | 输入：t, k；输出：List[float] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | cs.is_online | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.CSHub.get_online_Pd | (self, t: int, k: float=1) -> List[float] | v2sim/hub/hub.py#L360 | List of charging power of all available charging stations t: Determination time k: Unit conversion coefficient, default is 1, indicating no conversion; 3600 indicates conversion to kW; 3600/Sb_kVA indicates conversion to per unit value | 输入：t, k；输出：List[float] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | cs.is_online | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.CSHub.get_online_veh_count | (self, t: int, only_charging=False) -> List[int] | v2sim/hub/hub.py#L345 | List of the number of vehicles at all available charging stations at t seconds. When only_charging is True, only the number of vehicles being charged is returned. | 输入：t, only_charging；输出：List[int] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | cs.veh_count；cs.is_online | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.CSHub.get_veh_count | (self, only_charging: bool=False) -> List[int] | v2sim/hub/hub.py#L339 | List of the number of vehicles at all charging stations. When only_charging is True, only the number of vehicles being charged is returned. | 输入：only_charging；输出：List[int] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | cs.veh_count | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.CSHub.is_charging | (self, veh: EV) -> bool | v2sim/hub/hub.py#L388 | Check if the given EV is charging | 输入：veh；输出：bool | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | is_charging | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.CSHub.reset | (self) | v2sim/hub/hub.py#L309 | Reset station hub to initial state. | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | reset；len；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.CSHub.set_V2G_demand | (self, v2g_demand: List[float]) | v2sim/hub/hub.py#L405 | Set V2G demand | 输入：v2g_demand；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.CSHub.update | (self, sec: int, cur_time: int, pb_e: float, ps_e: float) -> List[EV] | v2sim/hub/hub.py#L414 | 统一推进所属充电站状态并汇总站群结果。 | 输入：sec, cur_time, pb_e, ps_e；输出：List[EV] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | zip；len；repeat；cs.update；ret.extend | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| class | v2sim.hub.hub.FCSHub | class(CSHub) | v2sim/hub/hub.py#L481 | List of FCS(UniCS/BiCS). Index starts from 0. | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | isinstance；__init__；LoadFCSList；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.FCSHub.__init__ | (self, par: Union[str, List[CS]]) | v2sim/hub/hub.py#L483 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.FCSHub.__init__ 的实现与调用链确认。 | 输入：par；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | isinstance；__init__；LoadFCSList；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.hub.hub.GSHub | class(StationHub[GS, GV]) | v2sim/hub/hub.py#L447 | List of GS. Index starts from 0. | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；__init__；repr；LoadGSList；ret.extend；len；super；gs.update；s.is_online；join；map | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.GSHub.__init__ | (self, par: Union[str, List[GS]]) | v2sim/hub/hub.py#L449 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.GSHub.__init__ 的实现与调用链确认。 | 输入：par；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | isinstance；__init__；LoadGSList；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.GSHub.__repr__ | (self) | v2sim/hub/hub.py#L474 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.GSHub.__repr__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | join；map | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.GSHub.__str__ | (self) | v2sim/hub/hub.py#L477 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.GSHub.__str__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | repr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.GSHub.get_online_veh_count | (self, t: int) -> List[int] | v2sim/hub/hub.py#L470 | List of the number of vehicles at all available gas stations at t seconds. | 输入：t；输出：List[int] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len；s.is_online | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.GSHub.get_veh_count | (self) -> List[int] | v2sim/hub/hub.py#L466 | List of the number of vehicles at all gas stations at the current time. | 输入：无显式输入；输出：List[int] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.GSHub.update | (self, sec: int, cur_time: int, pb_g: float) -> List[GV] | v2sim/hub/hub.py#L453 | Refuel the GV with the current parameters. | 输入：sec, cur_time, pb_g；输出：List[GV] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | ret.extend；gs.update | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.hub.hub.LoadCSList | (filePath: PathLike) | v2sim/hub/hub.py#L108 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.LoadCSList 的实现与调用链确认。 | 输入：filePath；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | _LoadCSList | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.hub.hub.LoadFCSList | (filePath: PathLike) | v2sim/hub/hub.py#L110 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.LoadFCSList 的实现与调用链确认。 | 输入：filePath；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | _LoadCSList | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.hub.hub.LoadGSList | (filePath: PathLike) -> List[GS] | v2sim/hub/hub.py#L113 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.LoadGSList 的实现与调用链确认。 | 输入：filePath；输出：List[GS] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | getroot；ValueError；Lang.STATIONHUB_INVALID_TYPE.format；_parse_station_params；_gs.append；ReadXML；GS | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.hub.hub.LoadSCSList | (filePath: PathLike) | v2sim/hub/hub.py#L109 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.LoadSCSList 的实现与调用链确认。 | 输入：filePath；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | _LoadCSList | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.hub.hub.LoadStationList | (filePath: str) | v2sim/hub/hub.py#L79 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.LoadStationList 的实现与调用链确认。 | 输入：filePath；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | getroot；ValueError；ReadXML；_parse_station_params；_fcs.append；_scs.append；BiCS；UniCS；_gs.append；GS；Lang.STATIONHUB_INVALID_TYPE.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.hub.hub.MixedHub | class | v2sim/hub/hub.py#L509 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.MixedHub 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | FCSHub；SCSHub；GSHub；defaultdict；self.fcs.reset；self.scs.reset；self.gs.reset；isinstance；MixedHub；fcs.append；scs.append；gs.append；LoadStationList；fcs.extend；scs.extend；gs.extend | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.MixedHub.__contains__ | (self, key: str) -> bool | v2sim/hub/hub.py#L548 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.MixedHub.__contains__ 的实现与调用链确认。 | 输入：key；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.MixedHub.__getitem__ | (self, key: str) | v2sim/hub/hub.py#L559 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.MixedHub.__getitem__ 的实现与调用链确认。 | 输入：key；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | KeyError；Lang.HUB_CS_NOT_EXIST.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.MixedHub.__init__ | (self, fcs: Union[str, List[CS]], scs: Union[str, List[CS]], gs: Union[str, List[GS]]) | v2sim/hub/hub.py#L510 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.MixedHub.__init__ 的实现与调用链确认。 | 输入：fcs, scs, gs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | FCSHub；SCSHub；GSHub；defaultdict；fcs.append；scs.append；gs.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.MixedHub.__iter__ | (self) | v2sim/hub/hub.py#L569 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.MixedHub.__iter__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.MixedHub.__len__ | (self) | v2sim/hub/hub.py#L551 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.MixedHub.__len__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.MixedHub.__repr__ | (self) | v2sim/hub/hub.py#L620 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.MixedHub.__repr__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.MixedHub.add_station | (self, station: Union[CS, GS]) | v2sim/hub/hub.py#L603 | Add a station to the hub. | 输入：station；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | isinstance；self.gs.append；gs.append；ValueError；self.fcs.append；fcs.append；self.scs.append；scs.append；type | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.MixedHub.check_kdtree | (self) | v2sim/hub/hub.py#L595 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.MixedHub.check_kdtree 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.fcs.create_kdtree；self.scs.create_kdtree；self.gs.create_kdtree | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.MixedHub.counts | (self) -> Tuple[int, int, int] | v2sim/hub/hub.py#L555 | Number of FCS, SCS and GS | 输入：无显式输入；输出：Tuple[int, int, int] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.MixedHub.from_file | (filepath: Union[str, Iterable[str]]) | v2sim/hub/hub.py#L537 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.MixedHub.from_file 的实现与调用链确认。 | 输入：filepath；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；MixedHub；LoadStationList；fcs.extend；scs.extend；gs.extend；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.MixedHub.get_bind_of | (self, name: str) -> str | v2sim/hub/hub.py#L526 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.MixedHub.get_bind_of 的实现与调用链确认。 | 输入：name；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | KeyError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.MixedHub.items | (self) | v2sim/hub/hub.py#L586 | List of all station name and object pairs. | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.MixedHub.keys | (self) | v2sim/hub/hub.py#L574 | List of all station names. | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.fcs._remap.keys；self.scs._remap.keys；self.gs._remap.keys | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.MixedHub.reset | (self) | v2sim/hub/hub.py#L520 | Reset mixed hub to initial state. | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.fcs.reset；self.scs.reset；self.gs.reset | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.MixedHub.values | (self) | v2sim/hub/hub.py#L580 | List of all station objects. | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.hub.hub.SCSHub | class(CSHub) | v2sim/hub/hub.py#L488 | List of SCS(UniCS/BiCS). Index starts from 0. | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | isinstance；__init__；LoadSCSList；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.SCSHub.__init__ | (self, par: Union[str, List[CS]]) | v2sim/hub/hub.py#L490 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.SCSHub.__init__ 的实现与调用链确认。 | 输入：par；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | isinstance；__init__；LoadSCSList；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.hub.hub.StationHub | class(Generic[T_Station, T_Vehicle], ABC) | v2sim/hub/hub.py#L137 | List of Stations(GS/CS). Index starts from 0. | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.create_kdtree；Element；write；self._veh.clear；self._s.__iter__；len；self._s.append；self._append；_create_kdt；self._kdtree.query；idx.reshape；self._remap.get；isinstance；add_veh；pop_veh；root.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.__contains__ | (self, cs_name: str) -> bool | v2sim/hub/hub.py#L242 | Check if a charging station with the specified name exists | 输入：cs_name；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.__getitem__ | (self, index: Union[str, int]) -> T_Station | v2sim/hub/hub.py#L235 | Get the index of given station. It can be either indices or a charging station name. | 输入：index；输出：T_Station | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.__init__ | (self, par: List[T_Station]) | v2sim/hub/hub.py#L139 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.StationHub.__init__ 的实现与调用链确认。 | 输入：par；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.create_kdtree；enumerate | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.__iter__ | (self) | v2sim/hub/hub.py#L159 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.StationHub.__iter__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._s.__iter__ | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.__len__ | (self) | v2sim/hub/hub.py#L162 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.StationHub.__len__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub._append | (self, s: T_Station) | v2sim/hub/hub.py#L165 | Append a charging station without updating the KDTree. Internal use only. | 输入：s；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._s.append；ValueError；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.add_veh | (self, veh: T_Vehicle, cs: Union[int, str]) -> bool | v2sim/hub/hub.py#L262 | Add vehicles to the specified station | 输入：veh, cs；输出：bool | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | add_veh；isinstance | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.append | (self, cs: T_Station) | v2sim/hub/hub.py#L172 | Append a charging station. If you want to add multiple charging stations, use extend() instead. | 输入：cs；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._append；self.create_kdtree | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.create_kdtree | (self) | v2sim/hub/hub.py#L182 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.StationHub.create_kdtree 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _create_kdt | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.extend | (self, cs_list: Iterable[T_Station]) | v2sim/hub/hub.py#L177 | Extend the charging station list. | 输入：cs_list；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.create_kdtree；self._append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.get_bind_of | (self, name: str) -> str | v2sim/hub/hub.py#L224 | Get the bind node/edge of the specified station | 输入：name；输出：str | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._remap.get；KeyError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.get_names | (self) -> List[str] | v2sim/hub/hub.py#L208 | List of names of all stations | 输入：无显式输入；输出：List[str] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.get_online_names | (self, t: int) -> List[str] | v2sim/hub/hub.py#L212 | List of names of all available stations at t second | 输入：t；输出：List[str] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | s.is_online | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.get_online_slots_of | (self, t: int) -> List[int] | v2sim/hub/hub.py#L220 | List of the number of chargers/oil pumps at all available stations at t second | 输入：t；输出：List[int] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | s.is_online | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.get_slots_of | (self) -> List[int] | v2sim/hub/hub.py#L216 | List of the number of chargers/oil pumps at all stations | 输入：无显式输入；输出：List[int] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.has_veh | (self, veh_id: str) -> bool | v2sim/hub/hub.py#L290 | Check if there is a vehicle with the specified ID | 输入：veh_id；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.index | (self, cs_name: str) -> int | v2sim/hub/hub.py#L199 | Get the index (starting from 0) of the CS by its name | 输入：cs_name；输出：int | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._remap.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.pop_veh | (self, veh: T_Vehicle) -> bool | v2sim/hub/hub.py#L275 | Remove vehicles from the charging station | 输入：veh；输出：bool | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pop_veh | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.reset | (self) | v2sim/hub/hub.py#L154 | Reset station hub to initial state. | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._veh.clear；s.reset | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.save | (self, filePath: Union[str, Path]) | v2sim/hub/hub.py#L147 | Save station hub to XML file. | 输入：filePath；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | Element；write；root.append；s.to_xml；ElementTree | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.select_near | (self, pos: Tuple[float, float], n: int=1) -> Iterable[int] | v2sim/hub/hub.py#L186 | Select the n nearest charging station numbers to (x, y). | 输入：pos, n；输出：Iterable[int] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._kdtree.query；idx.reshape；range；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub.StationHub.update | (self, sec: int, cur_time: int, *args, **kwargs) -> List[T_Vehicle] | v2sim/hub/hub.py#L252 | Update the station state for sec seconds. | 输入：sec, cur_time, *args, **kwargs；输出：List[T_Vehicle] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.hub.hub._LoadCSList | (filePath: PathLike, tag: Literal['fcs', 'scs', '']) -> List[CS] | v2sim/hub/hub.py#L98 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub._LoadCSList 的实现与调用链确认。 | 输入：filePath, tag；输出：List[CS] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | getroot；ValueError；_parse_station_params；_cs.append；ReadXML；Lang.STATIONHUB_INVALID_TYPE.format；BiCS；UniCS；tag.upper | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.hub.hub._MixedSList | class | v2sim/hub/hub.py#L496 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub._MixedSList 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.hub.hub._MixedSList.__iter__ | (self) | v2sim/hub/hub.py#L501 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub._MixedSList.__iter__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.hub.hub._create_kdt | (s: Sequence[BaseStation]) | v2sim/hub/hub.py#L127 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub._create_kdt 的实现与调用链确认。 | 输入：s；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | pts.append；KDTree；float | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.hub.hub._parse_station_params | (cs_node: Element) -> Dict | v2sim/hub/hub.py#L16 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub._parse_station_params 的实现与调用链确认。 | 输入：cs_node；输出：Dict | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | ValueError；int；float；args.update；PriceGetterPool.from_elem；cs_node.attrib.get；lower；RangeList；Lang.STATIONHUB_INVALID_TYPE.format；OwnerGroup；Lang.STATIONHUB_INVALID_TAG.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.hub.hub.create_empty_mixed_slist | () -> _MixedSList | v2sim/hub/hub.py#L506 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.hub.hub.create_empty_mixed_slist 的实现与调用链确认。 | 输入：无显式输入；输出：_MixedSList | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | _MixedSList | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
