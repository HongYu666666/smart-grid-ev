# `v2sim.net`

本模块共记录 72 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | v2sim.net.ConvertCase | (input_dir: str, output_dir: str, part_cnt: int, auto_partition: bool, non_passenger_links: bool, non_scc_links: bool) | v2sim/net.py#L885 | Convert case files in input directory and save to output directory. | 输入：input_dir, output_dir, part_cnt, auto_partition, non_passenger_links, non_scc_links；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | DetectFiles；Path；out_dir.mkdir；print；RoadNet.load_sumo；r.save；shutil.copy；r.remove_items_outside_max_scc；min；r.partition_roadnet；str；os.cpu_count | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| class | v2sim.net.Edge | class | v2sim/net.py#L45 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.Edge 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.Edge.__getitem__ | (self, key: str) -> str | v2sim/net.py#L61 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.Edge.__getitem__ 的实现与调用链确认。 | 输入：key；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.Edge.__init__ | (self, name: str, from_node: Node, to_node: Node, length: float, lanes: int, speed_limit: float=13.89, world_id: int=-1, nickname: Optional[str]=None, extras: Optional[Dict[str, str]]=None) | v2sim/net.py#L46 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.Edge.__init__ 的实现与调用链确认。 | 输入：name, from_node, to_node, length, lanes, speed_limit, world_id, nickname, extras；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.Edge.__setitem__ | (self, key: str, value: str) | v2sim/net.py#L64 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.Edge.__setitem__ 的实现与调用链确认。 | 输入：key, value；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.Edge.instant_travel_time | (self, t: int) -> float | v2sim/net.py#L57 | Get the travel time of free flowing condition. The parameter t is ignored, but kept for compatibility. | 输入：t；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| class | v2sim.net.Node | class | v2sim/net.py#L25 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.Node 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.Node.__getitem__ | (self, key: str) -> str | v2sim/net.py#L38 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.Node.__getitem__ 的实现与调用链确认。 | 输入：key；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.Node.__init__ | (self, node_id: str, x: float, y: float, extras: Optional[Dict[str, str]]=None) | v2sim/net.py#L26 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.Node.__init__ 的实现与调用链确认。 | 输入：node_id, x, y, extras；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.Node.__setitem__ | (self, key: str, value: str) | v2sim/net.py#L41 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.Node.__setitem__ 的实现与调用链确认。 | 输入：key, value；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.Node.get_coord | (self) -> Tuple[float, float] | v2sim/net.py#L34 | Get the coordinates of the node. | 输入：无显式输入；输出：Tuple[float, float] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| class | v2sim.net.RoadNet | class | v2sim/net.py#L128 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | KDTreeSegmentSearch；list；np.array；KDTree；self.__kdst.find_closest_segment；self.kdtree.query；self.sumo.getEdge；e.getShape；len；isinstance；allows；_TarjanSCC；_largeStackExec；enumerate；self.edges.values；Node | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.__init__ | (self) | v2sim/net.py#L130 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.__init__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet._geographic_clustering | (self, edge_groups: List[Set[str]], num_partitions: int) -> Dict[str, int] | v2sim/net.py#L711 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet._geographic_clustering 的实现与调用链确认。 | 输入：edge_groups, num_partitions；输出：Dict[str, int] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.mean；group_features.append；group_edges.append；len；enumerate；kmeans；vq；coords.append；list；zip | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet._group_reverse_edges | (self) -> List[Set[str]] | v2sim/net.py#L685 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet._group_reverse_edges 的实现与调用链确认。 | 输入：无显式输入；输出：List[Set[str]] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.edges.items；set；visited.add；edge_groups.append；current_group.add | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.add_edge | (self, edge_id: str, from_node: Union[str, Node], to_node: Union[str, Node], length_m: float, lanes: int, speed_limit: float, world_id: int=-1, nickname: Optional[str]=None) -> Edge | v2sim/net.py#L340 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.add_edge 的实现与调用链确认。 | 输入：edge_id, from_node, to_node, length_m, lanes, speed_limit, world_id, nickname；输出：Edge | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；Edge；from_node.outgoing_edges.append；to_node.incoming_edges.append；ValueError；self.get_node；Lang.EDGE_EXISTS.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.add_node | (self, node_id: str, x: int, y: int, extras: Optional[Dict[str, str]]=None) -> Node | v2sim/net.py#L288 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.add_node 的实现与调用链确认。 | 输入：node_id, x, y, extras；输出：Node | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | Node；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.allows_passengers | (self, edge_id: str) -> bool | v2sim/net.py#L248 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.allows_passengers 的实现与调用链确认。 | 输入：edge_id；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | allows；self.sumo.getEdge | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.calc_kdsegtree | (self) | v2sim/net.py#L145 | Calculate the KDTree of the road network nodes for fast nearest neighbor search. | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | KDTreeSegmentSearch；self.sumo.getEdge；e.getShape；range；np.array；len；segs.append；self.__edgeL.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.calc_kdtree | (self) | v2sim/net.py#L162 | Calculate the KDTree of the road network nodes for fast nearest neighbor search. | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | list；np.array；KDTree；self.nodes.values；node.get_coord | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.calc_max_scc | (self) | v2sim/net.py#L251 | Calculate the largest strongly connected component (SCC) of the road network. Returns a set of node IDs in the largest SCC. | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | list；_TarjanSCC；_largeStackExec；enumerate；self.edges.values；self.nodes.values；SubNet；scc_tmp.values；edges.add；range；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.check_scc_size | (self, display: bool=True) | v2sim/net.py#L185 | Check if the size of the largest strongly connected component is large enough | 输入：display；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len；print；Lang.WARN_SCC_TOO_SMALL.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.convertLonLat2XY | (self, lon, lat, rawUTM=False) | v2sim/net.py#L662 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.convertLonLat2XY 的实现与调用链确认。 | 输入：lon, lat, rawUTM；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.getGeoProj；self.getLocationOffset | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.convertXY2LonLat | (self, x, y, rawUTM=False) | v2sim/net.py#L670 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.convertXY2LonLat 的实现与调用链确认。 | 输入：x, y, rawUTM；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.getLocationOffset；self.getGeoProj | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.create_color_map | (self) | v2sim/net.py#L754 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.create_color_map 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；len；plt.cm.get_cmap；mcolors.to_hex；list；cmap；enumerate；self.edges.values；mcolors.TABLEAU_COLORS.values；range；sorted | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.create_paraworlds | (self, **kwargs) | v2sim/net.py#L584 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.create_paraworlds 的实现与调用链确认。 | 输入：**kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | kwargs.pop；all；any；self.edges.values；ParaWorlds；RuntimeError；W.addLink；append；World；W.addNode；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.create_singleworld | (self, **kwargs) | v2sim/net.py#L560 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.create_singleworld 的实现与调用链确认。 | 输入：**kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | kwargs.pop；World；self.edges.values；SingleWorld；world.addLink；append；world.addNode | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.create_world | (self, **kwargs) | v2sim/net.py#L539 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.create_world 的实现与调用链确认。 | 输入：**kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | kwargs.pop；self.edges.values；all；self.create_singleworld；self.create_paraworlds；sys._is_gil_enabled；hasattr；print | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.edge_count | (self) | v2sim/net.py#L422 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.edge_count 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.edge_ids | (self) | v2sim/net.py#L418 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.edge_ids 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | list；self.edges.keys | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.find_nearest_edge_id | (self, x: float, y: float) -> Tuple[float, str] | v2sim/net.py#L208 | Find the nearest edge to the given coordinates in max scc which allows passengers | 输入：x, y；输出：Tuple[float, str] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__kdst.find_closest_segment；self.calc_kdsegtree；np.array | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.find_nearest_node | (self, x: float, y: float) -> Node | v2sim/net.py#L218 | Find the nearest node to the given coordinates. | 输入：x, y；输出：Node | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.kdtree.query；idx.item | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.find_nearest_node_with_distance | (self, x: float, y: float) -> Tuple[float, Node] | v2sim/net.py#L225 | Find the nearest node to the given coordinates. | 输入：x, y；输出：Tuple[float, Node] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.kdtree.query；dist.item；idx.item | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.getBoundary | (self) | v2sim/net.py#L652 | return xmin,ymin,xmax,ymax network coordinates | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | max；min；self.nodes.values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.getGeoProj | (self) | v2sim/net.py#L624 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.getGeoProj 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.getGeoProjOrNone；RuntimeError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.getGeoProjOrNone | (self) | v2sim/net.py#L630 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.getGeoProjOrNone 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.hasGeoProj；pyproj.Proj；hasattr；pyproj.datadir.set_data_dir | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.getLocationOffset | (self) | v2sim/net.py#L648 | offset to be added after converting from geo-coordinates to UTM | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.getProjectorOrNone | (self) | v2sim/net.py#L643 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.getProjectorOrNone 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | Projector；self.hasGeoProj | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.get_edge | (self, edge_id: str) -> Edge | v2sim/net.py#L353 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.get_edge 的实现与调用链确认。 | 输入：edge_id；输出：Edge | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.get_edge_pos | (self, edge: str) | v2sim/net.py#L232 | Get the position of the edge in the road network. The position is the average of the shape of the edge. | 输入：edge；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.sumo.getEdge；e.getShape；len；isinstance | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.get_gl | (self) -> Dict[str, List[Tuple[str, Edge]]] | v2sim/net.py#L552 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.get_gl 的实现与调用链确认。 | 输入：无显式输入；输出：Dict[str, List[Tuple[str, Edge]]] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.edges.values；append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.get_node | (self, node_id: str) -> Node | v2sim/net.py#L297 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.get_node 的实现与调用链确认。 | 输入：node_id；输出：Node | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.get_offset_shape | (self, edge_id: str) | v2sim/net.py#L388 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.get_offset_shape 的实现与调用链确认。 | 输入：edge_id；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.get_edge；e.from_node.get_coord；e.to_node.get_coord；math.hypot；self.getBoundary | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.hasGeoProj | (self) | v2sim/net.py#L621 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.hasGeoProj 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.is_edge_in_largest_scc | (self, edge_id: str) -> bool | v2sim/net.py#L200 | Check if a edge is in the largest strongly connected component. | 输入：edge_id；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len；self.calc_max_scc | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.is_from_sumo | (self) | v2sim/net.py#L182 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.is_from_sumo 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.is_node_in_largest_scc | (self, node_id: str) -> bool | v2sim/net.py#L192 | Check if a node is in the largest strongly connected component. | 输入：node_id；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len；self.calc_max_scc | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.kdtree | (self) -> KDTree | v2sim/net.py#L171 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.kdtree 的实现与调用链确认。 | 输入：无显式输入；输出：KDTree | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.calc_kdtree | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.load | (fname: str, fmt: str='auto') | v2sim/net.py#L493 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.load 的实现与调用链确认。 | 输入：fname, fmt；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | RoadNet.load_raw；RoadNet.load_sumo；ValueError；Lang.UNKNOWN_NET_FORMAT.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.load_raw | (fname: str) | v2sim/net.py#L426 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.load_raw 的实现与调用链确认。 | 输入：fname；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | RoadNet；ReadXML；root.findall；root.find；RuntimeError；ret.add_node；ret.add_edge；tuple；location.attrib.get；map；node.attrib.pop；int；float；edge.attrib.get；split；node.attrib.copy | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.load_sumo | (fname: str, only_passenger: bool=True) | v2sim/net.py#L459 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.load_sumo 的实现与调用链确认。 | 输入：fname, only_passenger；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | RoadNet；isinstance；Lang.INVALID_SUMO_NETWORK.format；r.getNodes；r.getEdges；readNet；ret.add_node；ret.add_edge；len；tuple；RuntimeError；r.getLocationOffset；r.getBoundary；map；node.getID；int | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.node_count | (self) | v2sim/net.py#L337 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.node_count 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.node_ids | (self) | v2sim/net.py#L333 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.node_ids 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | list；self.nodes.keys | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.partition_roadnet | (self, num_partitions: int) -> None | v2sim/net.py#L677 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.partition_roadnet 的实现与调用链确认。 | 输入：num_partitions；输出：None | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._group_reverse_edges；self._geographic_clustering；partition_assignment.items | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.plot_roadnet | (self, figsize: Tuple[int, int]=(12, 10), node_size: int=50, show_nodes: bool=True, show_labels: bool=False, title: str='Road Network Partitioning') -> None | v2sim/net.py#L771 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.plot_roadnet 的实现与调用链确认。 | 输入：figsize, node_size, show_nodes, show_labels, title；输出：None | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | plt.subplots；self.getBoundary；self.create_color_map；self.edges.values；ax.set_xlabel；ax.set_ylabel；ax.set_title；ax.grid；color_map.items；ax.legend；plt.tight_layout；plt.savefig；np.sqrt；color_map.get；min；ax.plot | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.remove_edge | (self, edge_id: str) | v2sim/net.py#L363 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.remove_edge 的实现与调用链确认。 | 输入：edge_id；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.edges.pop；ValueError；Lang.EDGE_NOT_FOUND.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.remove_items_outside_max_scc | (self) | v2sim/net.py#L868 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.remove_items_outside_max_scc 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.remove_node；self.remove_edge；to_remove.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.remove_node | (self, node_id: str) | v2sim/net.py#L311 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.remove_node 的实现与调用链确认。 | 输入：node_id；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.nodes.pop；list；ValueError；self.edges.keys；Lang.NODE_NOT_FOUND.format；self.remove_edge | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.rename_edge | (self, old_id: str, new_id: str) | v2sim/net.py#L356 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.rename_edge 的实现与调用链确认。 | 输入：old_id, new_id；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.edges.pop；ValueError；Lang.EDGE_EXISTS.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.rename_node | (self, old_id: str, new_id: str) | v2sim/net.py#L300 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.rename_node 的实现与调用链确认。 | 输入：old_id, new_id；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.nodes.pop；ValueError；Lang.NODE_EXISTS.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.save | (self, fname: str) | v2sim/net.py#L506 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.save 的实现与调用链确认。 | 输入：fname；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | Element；self.hasGeoProj；self.nodes.values；self.edges.values；endswith；write；join；root.append；str；map；e.set；fname.lower；ElementTree；_w | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.scc | (self) -> List[SubNet] | v2sim/net.py#L283 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.scc 的实现与调用链确认。 | 输入：无显式输入；输出：List[SubNet] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len；self.calc_max_scc | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.sumo | (self) | v2sim/net.py#L178 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.sumo 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.update_edge | (self, edge_id: str, new_from_node_id: str, new_to_node_id: str) | v2sim/net.py#L369 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.update_edge 的实现与调用链确认。 | 输入：edge_id, new_from_node_id, new_to_node_id；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | ValueError；e.from_node.outgoing_edges.remove；new_from_node.outgoing_edges.append；e.to_node.incoming_edges.remove；new_to_node.incoming_edges.append；Lang.EDGE_NOT_FOUND.format；Lang.NODE_NOT_FOUND.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.update_node | (self, old_node_id: str, new_node_id: str) | v2sim/net.py#L323 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.update_node 的实现与调用链确认。 | 输入：old_node_id, new_node_id；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.nodes.pop；ValueError；Lang.NODE_NOT_FOUND.format；Lang.NODE_EXISTS.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net.RoadNet.world_count | (self) | v2sim/net.py#L750 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.RoadNet.world_count 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；len；self.edges.values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| class | v2sim.net.SubNet | class | v2sim/net.py#L69 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net.SubNet 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | field | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| class | v2sim.net._TarjanSCC | class | v2sim/net.py#L73 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net._TarjanSCC 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；self.__stk.append；self.__vis.add；len；range；defaultdict；enumerate；list；ret.sort；append；scc_dict.values；self.__tarjan；min；self.__stk.pop；self.__vis.remove | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net._TarjanSCC.__init__ | (self, n: int, gl: Dict[int, List[int]]) | v2sim/net.py#L74 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net._TarjanSCC.__init__ 的实现与调用链确认。 | 输入：n, gl；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net._TarjanSCC.__tarjan | (self, u: int) | v2sim/net.py#L88 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net._TarjanSCC.__tarjan 的实现与调用链确认。 | 输入：u；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__stk.append；self.__vis.add；self.__tarjan；min；self.__stk.pop；self.__vis.remove | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.net._TarjanSCC.calc_scc | (self) | v2sim/net.py#L108 | Calculate the strongly connected components (SCC) of the graph. Returns a list of SCC in a decreasing order by size, each element indicates the nodes in this SCC | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；range；defaultdict；enumerate；list；ret.sort；append；scc_dict.values；self.__tarjan | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | v2sim.net._largeStackExec | (func, *args) | v2sim/net.py#L16 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.net._largeStackExec 的实现与调用链确认。 | 输入：func, *args；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | threading.stack_size；sys.setrecursionlimit；threading.Thread；th.start；th.join | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
