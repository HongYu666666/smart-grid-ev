# `v2sim.sim.uxworld`

本模块共记录 68 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.sim.uxworld.ParaWorlds | class(WorldSpec) | v2sim/sim/uxworld.py#L213 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | defaultdict；worlds.items；deque；self.__create_pool；ThreadPoolExecutor；self.__aQ.clear；time.time；enumerate；get_link；W.addVehicle；self.__add_veh；len；sum；self.__pool.shutdown；sys.setrecursionlimit；threading.stack_size | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.__add_veh | (self, world_id: int, veh_id: str, from_node: str, to_node: str, trip_segment: int) | v2sim/sim/uxworld.py#L321 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.__add_veh 的实现与调用链确认。 | 输入：world_id, veh_id, from_node, to_node, trip_segment；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | W.addVehicle；append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.__create_pool | (self) | v2sim/sim/uxworld.py#L250 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.__create_pool 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | ThreadPoolExecutor；os.cpu_count | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.__getitem__ | (self, wid: int) -> World | v2sim/sim/uxworld.py#L256 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.__getitem__ 的实现与调用链确认。 | 输入：wid；输出：World | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.__init__ | (self, worlds: Dict[int, World], gl: Graph) | v2sim/sim/uxworld.py#L214 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.__init__ 的实现与调用链确认。 | 输入：worlds, gl；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | defaultdict；worlds.items；deque；self.__create_pool；add；range；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.__lstack_save | (self, filepath: str) | v2sim/sim/uxworld.py#L386 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.__lstack_save 的实现与调用链确认。 | 输入：filepath；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | sys.setrecursionlimit；gzip.open；pickle.dump；PyVersion | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds._save_obj | (self) | v2sim/sim/uxworld.py#L404 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds._save_obj 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.__pool.shutdown；sys.setrecursionlimit；pickle.dumps；self.__create_pool；PyVersion | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.add_vehicle | (self, veh_id: str, from_node: str, to_node: str, route: Union[None, Stage, List[str]]=None, algo: RoutingAlgorithm=RoutingAlgorithm.AstarFastest) | v2sim/sim/uxworld.py#L334 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.add_vehicle 的实现与调用链确认。 | 输入：veh_id, from_node, to_node, route, algo；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__add_veh；len；range；splitting_nodes.append；algo.run；isinstance；self.get_link；nodes.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.exec_simulation | (self, until_s: int) | v2sim/sim/uxworld.py#L271 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.exec_simulation 的实现与调用链确认。 | 输入：until_s；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__aQ.clear；time.time；enumerate；self.worlds.values；as_completed；W.exec_simulation；len；aQ.popleft；VEHICLES.pop；futures.append；self.__add_veh；self.__aQ.append；self.__veh_itineraies.pop；self.__pool.submit | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.get_arrived_vehicles | (self) | v2sim/sim/uxworld.py#L262 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.get_arrived_vehicles 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；self.__aQ.popleft | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.get_average_speed | (self) -> float | v2sim/sim/uxworld.py#L379 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.get_average_speed 的实现与调用链确认。 | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | sum；len；self.worlds.values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.get_coords | (self) -> CoordsDict | v2sim/sim/uxworld.py#L253 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.get_coords 的实现与调用链确认。 | 输入：无显式输入；输出：CoordsDict | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.get_gl | (self) -> Graph | v2sim/sim/uxworld.py#L259 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.get_gl 的实现与调用链确认。 | 输入：无显式输入；输出：Graph | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.get_link | (self, link_id: str) -> Optional[Link] | v2sim/sim/uxworld.py#L309 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.get_link 的实现与调用链确认。 | 输入：link_id；输出：Optional[Link] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | get_link | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.get_neighbor_links | (self, node_id: str) -> Iterable[Link] | v2sim/sim/uxworld.py#L312 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.get_neighbor_links 的实现与调用链确认。 | 输入：node_id；输出：Iterable[Link] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | get_node；chain；node.inlinks.values；node.outlinks.values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.get_running_vehicle_count | (self) -> int | v2sim/sim/uxworld.py#L376 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.get_running_vehicle_count 的实现与调用链确认。 | 输入：无显式输入；输出：int | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | sum；len；self.worlds.values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.get_time | (self) -> int | v2sim/sim/uxworld.py#L268 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.get_time 的实现与调用链确认。 | 输入：无显式输入；输出：int | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.get_vehicle | (self, veh_id: str) -> Vehicle | v2sim/sim/uxworld.py#L370 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.get_vehicle 的实现与调用链确认。 | 输入：veh_id；输出：Vehicle | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.get_vehicle_count | (self) -> int | v2sim/sim/uxworld.py#L373 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.get_vehicle_count 的实现与调用链确认。 | 输入：无显式输入；输出：int | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.has_vehicle | (self, veh_id: str) -> bool | v2sim/sim/uxworld.py#L367 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.has_vehicle 的实现与调用链确认。 | 输入：veh_id；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.links | (self) -> Iterable[Link] | v2sim/sim/uxworld.py#L318 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.links 的实现与调用链确认。 | 输入：无显式输入；输出：Iterable[Link] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.worlds.values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.load | (filepath: str) | v2sim/sim/uxworld.py#L417 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.load 的实现与调用链确认。 | 输入：filepath；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | _load_world；isinstance | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.save | (self, filepath: str) | v2sim/sim/uxworld.py#L395 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.save 的实现与调用链确认。 | 输入：filepath；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__pool.shutdown；threading.stack_size；threading.Thread；t.start；t.join；self.__create_pool | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.ParaWorlds.shutdown | (self) | v2sim/sim/uxworld.py#L382 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.ParaWorlds.shutdown 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.__pool.shutdown | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.sim.uxworld.RoutingAlgorithm | class(enum.Enum) | v2sim/sim/uxworld.py#L13 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.RoutingAlgorithm 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | astarF；astarS；dijF；dijS；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.RoutingAlgorithm.run | (self, gl: Graph, coords: CoordsDict, start_time: int, from_node: str, to_node: str) | v2sim/sim/uxworld.py#L19 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.RoutingAlgorithm.run 的实现与调用链确认。 | 输入：gl, coords, start_time, from_node, to_node；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | astarF；astarS；dijF；dijS；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.sim.uxworld.SingleWorld | class(WorldSpec) | v2sim/sim/uxworld.py#L110 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | deque；self.__aQ.clear；self.world.exec_simulation；self.world.addVehicle；len；self.world.get_link；self.world.get_node；sys.setrecursionlimit；threading.stack_size；threading.Thread；t.start；t.join；pickle.dumps；_load_world；isinstance；self.__aQ.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.__init__ | (self, world: World, gl: Graph) | v2sim/sim/uxworld.py#L111 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.__init__ 的实现与调用链确认。 | 输入：world, gl；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | deque | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.__lstack_save | (self, filepath: str) | v2sim/sim/uxworld.py#L184 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.__lstack_save 的实现与调用链确认。 | 输入：filepath；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | sys.setrecursionlimit；gzip.open；pickle.dump；PyVersion | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld._save_obj | (self) | v2sim/sim/uxworld.py#L199 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld._save_obj 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | sys.setrecursionlimit；pickle.dumps；PyVersion | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.add_vehicle | (self, veh_id: str, from_node: str, to_node: str, route: Union[None, Stage, List[str]]=None) | v2sim/sim/uxworld.py#L128 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.add_vehicle 的实现与调用链确认。 | 输入：veh_id, from_node, to_node, route；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.world.addVehicle；self.__aQ.append；isinstance | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.exec_simulation | (self, until_s: int) | v2sim/sim/uxworld.py#L122 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.exec_simulation 的实现与调用链确认。 | 输入：until_s；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__aQ.clear；self.world.exec_simulation | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.get_arrived_vehicles | (self) | v2sim/sim/uxworld.py#L160 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.get_arrived_vehicles 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；self.__aQ.popleft；self.world.VEHICLES.pop | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.get_average_speed | (self) -> float | v2sim/sim/uxworld.py#L176 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.get_average_speed 的实现与调用链确认。 | 输入：无显式输入；输出：float | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；sum；self.world.VEHICLES_RUNNING.values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.get_coords | (self) -> CoordsDict | v2sim/sim/uxworld.py#L173 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.get_coords 的实现与调用链确认。 | 输入：无显式输入；输出：CoordsDict | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.get_gl | (self) -> Graph | v2sim/sim/uxworld.py#L170 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.get_gl 的实现与调用链确认。 | 输入：无显式输入；输出：Graph | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.get_link | (self, link_id: str) -> Optional[Link] | v2sim/sim/uxworld.py#L148 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.get_link 的实现与调用链确认。 | 输入：link_id；输出：Optional[Link] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.world.get_link | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.get_neighbor_links | (self, node_id: str) -> Iterable[Link] | v2sim/sim/uxworld.py#L151 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.get_neighbor_links 的实现与调用链确认。 | 输入：node_id；输出：Iterable[Link] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.world.get_node；chain；node.inlinks.values；node.outlinks.values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.get_running_vehicle_count | (self) -> int | v2sim/sim/uxworld.py#L145 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.get_running_vehicle_count 的实现与调用链确认。 | 输入：无显式输入；输出：int | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.get_time | (self) -> int | v2sim/sim/uxworld.py#L167 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.get_time 的实现与调用链确认。 | 输入：无显式输入；输出：int | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.get_vehicle | (self, veh_id: str) -> Vehicle | v2sim/sim/uxworld.py#L139 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.get_vehicle 的实现与调用链确认。 | 输入：veh_id；输出：Vehicle | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.get_vehicle_count | (self) -> int | v2sim/sim/uxworld.py#L142 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.get_vehicle_count 的实现与调用链确认。 | 输入：无显式输入；输出：int | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.has_vehicle | (self, veh_id: str) -> bool | v2sim/sim/uxworld.py#L136 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.has_vehicle 的实现与调用链确认。 | 输入：veh_id；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.links | (self) -> List[Link] | v2sim/sim/uxworld.py#L157 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.links 的实现与调用链确认。 | 输入：无显式输入；输出：List[Link] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.load | (filepath: str) | v2sim/sim/uxworld.py#L208 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.load 的实现与调用链确认。 | 输入：filepath；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | _load_world；isinstance | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.save | (self, filepath: str) | v2sim/sim/uxworld.py#L193 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.save 的实现与调用链确认。 | 输入：filepath；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | threading.stack_size；threading.Thread；t.start；t.join | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.SingleWorld.shutdown | (self) | v2sim/sim/uxworld.py#L181 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.SingleWorld.shutdown 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.sim.uxworld.WorldSpec | class(ABC) | v2sim/sim/uxworld.py#L31 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.WorldSpec 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.WorldSpec._save_obj | (self) -> dict | v2sim/sim/uxworld.py#L83 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.WorldSpec._save_obj 的实现与调用链确认。 | 输入：无显式输入；输出：dict | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.WorldSpec.add_vehicle | (self, veh_id: str, from_node: str, to_node: str, route: Union[None, Stage, List[str]]=None) | v2sim/sim/uxworld.py#L36 | Add a vehicle into simulation | 输入：veh_id, from_node, to_node, route；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.WorldSpec.exec_simulation | (self, until_s: int) | v2sim/sim/uxworld.py#L33 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.WorldSpec.exec_simulation 的实现与调用链确认。 | 输入：until_s；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.WorldSpec.get_arrived_vehicles | (self) -> Generator[Tuple[str, Vehicle], None, None] | v2sim/sim/uxworld.py#L47 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.WorldSpec.get_arrived_vehicles 的实现与调用链确认。 | 输入：无显式输入；输出：Generator[Tuple[str, Vehicle], None, None] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.WorldSpec.get_average_speed | (self) -> float | v2sim/sim/uxworld.py#L68 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.WorldSpec.get_average_speed 的实现与调用链确认。 | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.WorldSpec.get_coords | (self) -> CoordsDict | v2sim/sim/uxworld.py#L65 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.WorldSpec.get_coords 的实现与调用链确认。 | 输入：无显式输入；输出：CoordsDict | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.WorldSpec.get_gl | (self) -> Graph | v2sim/sim/uxworld.py#L53 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.WorldSpec.get_gl 的实现与调用链确认。 | 输入：无显式输入；输出：Graph | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.WorldSpec.get_link | (self, link_id: str) -> Optional[Link] | v2sim/sim/uxworld.py#L71 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.WorldSpec.get_link 的实现与调用链确认。 | 输入：link_id；输出：Optional[Link] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.WorldSpec.get_neighbor_links | (self, node_id: str) -> Iterable[Link] | v2sim/sim/uxworld.py#L74 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.WorldSpec.get_neighbor_links 的实现与调用链确认。 | 输入：node_id；输出：Iterable[Link] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.WorldSpec.get_time | (self) -> int | v2sim/sim/uxworld.py#L50 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.WorldSpec.get_time 的实现与调用链确认。 | 输入：无显式输入；输出：int | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.WorldSpec.get_vehicle | (self, veh_id: str) -> Vehicle | v2sim/sim/uxworld.py#L59 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.WorldSpec.get_vehicle 的实现与调用链确认。 | 输入：veh_id；输出：Vehicle | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.WorldSpec.get_vehicle_count | (self) -> int | v2sim/sim/uxworld.py#L62 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.WorldSpec.get_vehicle_count 的实现与调用链确认。 | 输入：无显式输入；输出：int | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.WorldSpec.has_vehicle | (self, veh_id: str) -> bool | v2sim/sim/uxworld.py#L56 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.WorldSpec.has_vehicle 的实现与调用链确认。 | 输入：veh_id；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.WorldSpec.links | (self) -> Iterable[Link] | v2sim/sim/uxworld.py#L77 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.WorldSpec.links 的实现与调用链确认。 | 输入：无显式输入；输出：Iterable[Link] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.WorldSpec.load | (filepath: str) | v2sim/sim/uxworld.py#L90 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.WorldSpec.load 的实现与调用链确认。 | 输入：filepath；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.WorldSpec.save | (self, filepath: str) | v2sim/sim/uxworld.py#L86 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.WorldSpec.save 的实现与调用链确认。 | 输入：filepath；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxworld.WorldSpec.shutdown | (self) | v2sim/sim/uxworld.py#L80 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.WorldSpec.shutdown 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.uxworld._load_world | (filepath: str) -> WorldSpec | v2sim/sim/uxworld.py#L102 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld._load_world 的实现与调用链确认。 | 输入：filepath；输出：WorldSpec | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | _load_world_unsafe；os.path.isfile；FileNotFoundError；gzip.open；pickle.load | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.uxworld._load_world_unsafe | (data: dict) -> WorldSpec | v2sim/sim/uxworld.py#L93 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld._load_world_unsafe 的实现与调用链确认。 | 输入：data；输出：WorldSpec | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；CheckPyVersion；format；PyVersion | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.uxworld.load_world | (filepath: str) -> WorldSpec | v2sim/sim/uxworld.py#L422 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.uxworld.load_world 的实现与调用链确认。 | 输入：filepath；输出：WorldSpec | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | _load_world；isinstance | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
