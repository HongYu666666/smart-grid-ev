# `v2sim.sim.sumo`

本模块共记录 26 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.sim.sumo.TrafficSUMO | class(TrafficInst) | v2sim/sim/sumo.py#L74 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.sumo.TrafficSUMO 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；self.__snet.getEdges；_prepare_trips_and_scs；traci.vehicle.getPosition；traci.simulation.findRoute；self._prepare_stations；dijMC；clear_odometer；traci.vehicle.add；traci.vehicle.setRoute；isinstance；self.find_best_station；self.__snet.getEdge；e.getShape；self._hubs.fcs.get_online_names；traci.start | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.__batch_depart | (self) | v2sim/sim/sumo.py#L299 | Sent out all vehicles that reaching the departure time | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._que.pop；self.__start_trip；self._que.empty；isinstance；max；self._log.depart；self.__get_nearest_CS；self._hubs.gs.get_online_names；self._hubs.fcs.get_online_names；len；RuntimeError；self._hubs.scs.has_veh；int；self._log.depart_delay；self._que.push；self._fQ.push | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.__get_edge_pos | (self, eid: str) -> Tuple[float, float] | v2sim/sim/sumo.py#L223 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.sumo.TrafficSUMO.__get_edge_pos 的实现与调用链确认。 | 输入：eid；输出：Tuple[float, float] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__snet.getEdge；e.getShape；isinstance；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.__get_nearest_CS | (self, cur_edge: str) -> Tuple[Optional[str], float, Optional[Stage]] | v2sim/sim/sumo.py#L281 | Find the nearest charging station cur_edge: Current road | 输入：cur_edge；输出：Tuple[Optional[str], float, Optional[Stage]] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._hubs.fcs.get_online_names；self.find_route；self._hubs.get_bind_of | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.__init__ | (self, start_time: int, step_len: int, end_time: int, roadnet: RoadNet, trip_logger: TripLogger, vehs: VDict, hubs: MixedHub, pdn: Grid, gasoline_price: TimeFunc, seed: int=0, silent: bool=False, *, road_net_file: str, initial_state_folder: str='', routing_algo: str='CH', ignore_driving: bool=False, suppress_route_not_found: bool=True, gui: bool=False, sumocfg_file: str='') | v2sim/sim/sumo.py#L75 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.sumo.TrafficSUMO.__init__ 的实现与调用链确认。 | 输入：start_time, step_len, end_time, roadnet, trip_logger, vehs, hubs, pdn, gasoline_price, seed, silent, road_net_file, initial_state_folder, routing_algo, ignore_driving, suppress_route_not_found, gui, sumocfg_file；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；self.__snet.getEdges；_prepare_trips_and_scs；e.getID；self.__load_v2sim_state；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.__load_sumo_state | (self, folder: str) | v2sim/sim/sumo.py#L493 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.sumo.TrafficSUMO.__load_sumo_state 的实现与调用链确认。 | 输入：folder；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | traci.simulation.loadState；Path；traffic.exists；FileNotFoundError；str；Lang.ERROR_STATE_FILE_NOT_FOUND.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.__load_v2sim_state | (self, folder: str) | v2sim/sim/sumo.py#L475 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.sumo.TrafficSUMO.__load_v2sim_state 的实现与调用链确认。 | 输入：folder；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | Path；inst.exists；FileNotFoundError；gzip.open；pickle.load；isinstance；CheckPyVersion；RuntimeError；Lang.ERROR_STATE_FILE_NOT_FOUND.format；str；PyVersion | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.__sel_best_station | (self, veh: Vehicle, cur_edge: Optional[str]=None, cur_pos: Optional[Tuple[float, float]]=None) -> Tuple[str, Stage] | v2sim/sim/sumo.py#L200 | Select the nearest available charging station based on the edge where the car is currently located, and return the path and average weight | 输入：veh, cur_edge, cur_pos；输出：Tuple[str, Stage] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self.find_best_station；traci.vehicle.getRoadID；traci.vehicle.getPosition；hub.get_online_names；RuntimeError；Lang.VEH_TYPE_NOT_SUPPORTED.format；type | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.__start_trip | (self, veh: Vehicle) -> bool | v2sim/sim/sumo.py#L229 | Start the current trip of a vehicle veh_id: Vehicle ID Return: Departure succeeded: True, Fast charging station selection weight (if fast charging is required) Departure failed: False, None | 输入：veh；输出：bool | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self.find_route；self._hubs.scs.pop_veh；veh.is_energy_enough；self._add_veh；self._add_veh2；self.__get_edge_pos；self.__sel_best_station；self._log.leave_SCS；self._hubs.get_bind_of；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO._add_veh | (self, veh_id: str, route: List[str]) | v2sim/sim/sumo.py#L175 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.sumo.TrafficSUMO._add_veh 的实现与调用链确认。 | 输入：veh_id, route；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | clear_odometer；traci.vehicle.add；traci.vehicle.setRoute | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO._add_veh2 | (self, veh_id: str, st_edge: str, ed_edge: str, agg_routing: bool=False) | v2sim/sim/sumo.py#L180 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.sumo.TrafficSUMO._add_veh2 的实现与调用链确认。 | 输入：veh_id, st_edge, ed_edge, agg_routing；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | clear_odometer；traci.vehicle.add；traci.vehicle.setRoute；traci.vehicle.changeTarget；traci.vehicle.setRoutingMode；RuntimeError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.create | (case: CaseData, tlogger: TripLogger, vscfg: CommonConfig, config: SUMOConfig, seed: int=0, silent: bool=False) | v2sim/sim/sumo.py#L508 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.sumo.TrafficSUMO.create 的实现与调用链确认。 | 输入：case, tlogger, vscfg, config, seed, silent；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | TrafficSUMO；asdict | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.edges | (self) | v2sim/sim/sumo.py#L192 | Get all roads | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.find_best_station | (self, veh: Vehicle, O: str, to_stations: List[str], omega: float, to_charge: float, max_dist: float, hub: StationHub) -> Tuple[str, Stage] | v2sim/sim/sumo.py#L149 | Find the best station to go from O. | 输入：veh, O, to_stations, omega, to_charge, max_dist, hub；输出：Tuple[str, Stage] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._prepare_stations；dijMC；Ds.keys；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.find_route | (self, O: str, D: str, use_cache: bool=False) -> Stage | v2sim/sim/sumo.py#L126 | Fin the best route from O to D | 输入：O, D, use_cache；输出：Stage | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | traci.simulation.findRoute；len；Stage；print；RuntimeError；Lang.ERROR_ROUTE_NOT_FOUND.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.get_average_vcr | (self) -> float | v2sim/sim/sumo.py#L117 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.sumo.TrafficSUMO.get_average_vcr 的实现与调用链确认。 | 输入：无显式输入；输出：float | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | traci.edge.getLastStepMeanSpeed；edge.getSpeed；edge.getID | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.get_edge_names | (self) -> List[str] | v2sim/sim/sumo.py#L196 | Get the names of all roads | 输入：无显式输入；输出：List[str] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.get_veh_pos | (self, veh_id: str) -> Tuple[float, float] | v2sim/sim/sumo.py#L114 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.sumo.TrafficSUMO.get_veh_pos 的实现与调用链确认。 | 输入：veh_id；输出：Tuple[float, float] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | traci.vehicle.getPosition | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.load | (case: CaseData, folder: str, tlogger: TripLogger, vscfg: CommonConfig, config: SUMOConfig, seed: int=0, silent: bool=False) | v2sim/sim/sumo.py#L533 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.sumo.TrafficSUMO.load 的实现与调用链确认。 | 输入：case, folder, tlogger, vscfg, config, seed, silent；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | TrafficSUMO；asdict | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.load_state | (self, folder: str) | v2sim/sim/sumo.py#L499 | Load the state of the simulation folder: Folder path | 输入：folder；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.__load_v2sim_state；self.__load_sumo_state | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.routing_algo | (self) -> str | v2sim/sim/sumo.py#L171 | Get the current routing algorithm | 输入：无显式输入；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.save | (self, folder: Union[str, Path]) | v2sim/sim/sumo.py#L454 | Save the current state of the simulation to a folder | 输入：folder；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | f.mkdir；traci.simulation.saveState；isinstance；Path；str；gzip.open；pickle.dump；PyVersion | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.simulation_start | (self) | v2sim/sim/sumo.py#L338 | Start simulation sumocfg_file: SUMO configuration file path start_time: Start time (seconds), if not specified, provided by the SUMO configuration file gui: Whether to display the graphical interface | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | traci.start；int；self.__batch_depart；chain；str；self.__load_sumo_state；traci.simulation.getTime；KDTree；self.__get_edge_pos；float | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.simulation_step | (self, step_len: int) | v2sim/sim/sumo.py#L378 | Simulation step. step_len: Step length (seconds) v2g_demand: V2G demand list (kWh/s) | 输入：step_len；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | traci.simulationStep；int；self.__batch_depart；traci.simulation.getArrivedIDList；post_simulation_step；float；traci.simulation.getTime；traci.vehicle.getDistance；veh.drive；traci.simulation.getDepartedIDList；traci.vehicle.getIDList；self._end_trip；self._start_restore；super；traci.vehicle.getRoadID；self.__get_nearest_CS | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.sumo.TrafficSUMO.simulation_stop | (self) | v2sim/sim/sumo.py#L450 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.sumo.TrafficSUMO.simulation_stop 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | traci.close；self._log.close | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.sumo.dijMC | (gl: Net, from_edge: str, omega: float, to_edges: Iterable[str], node_scores: Dict[str, float], max_length: float=float('inf')) -> Stage | v2sim/sim/sumo.py#L34 | Find the BEST route based on score = omega * (time + waiting) + charging_cost. Uses time as primary key, length as secondary key. | 输入：gl, from_edge, omega, to_edges, node_scores, max_length；输出：Stage | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | float；set；Stage；heapq.heappop；visited.add；gl.getEdge；keys；edge_obj.getID；node_scores.get；e.getAllowedOutgoing；traci.edge.getTraveltime；edge_obj.getLength；heapq.heappush | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
