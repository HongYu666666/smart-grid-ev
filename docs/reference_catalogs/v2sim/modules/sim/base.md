# `v2sim.sim.base`

本模块共记录 45 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.sim.base.CommonConfig | class | v2sim/sim/base.py#L417 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.base.CommonConfig 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | ConstFunc | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.sim.base.SUMOConfig | class | v2sim/sim/base.py#L423 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.base.SUMOConfig 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.sim.base.StageLike | class(Protocol) | v2sim/sim/base.py#L22 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.base.StageLike 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.sim.base.TrafficInst | class(ABC) | v2sim/sim/base.py#L28 | 定义交通仿真后端必须提供的车辆、站点、时间推进与生命周期接口。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | random.seed；PQueue；self._vehs.values；self._vehs.evs.values；chain；self._gp；self._hubs.gs.update；self._hubs.fcs.update；self._hubs.scs.update；isinstance；self.scs.add_veh；self._log.arrive；veh.next_trip；sum；Lang.DEPART_TIME_PASSED.format；self._que.push | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.sim.base.TrafficInst.__init__ | (self, start_time: int, step_len: int, end_time: int, roadnet: RoadNet, trip_logger: TripLogger, vehs: VDict, hubs: MixedHub, pdn: Grid, gasoline_price: TimeFunc, seed: int=0, silent: bool=False, dist_based_restoration: bool=False, step_callback: Optional[StepCallback]=None) | v2sim/sim/base.py#L29 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.base.TrafficInst.__init__ 的实现与调用链确认。 | 输入：start_time, step_len, end_time, roadnet, trip_logger, vehs, hubs, pdn, gasoline_price, seed, silent, dist_based_restoration, step_callback；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | random.seed | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst._add_veh2 | (self, veh_id: str, O: str, D: str) | v2sim/sim/base.py#L203 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.base.TrafficInst._add_veh2 的实现与调用链确认。 | 输入：veh_id, O, D；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst._end_restore | (self, veh: Vehicle) | v2sim/sim/base.py#L226 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.base.TrafficInst._end_restore 的实现与调用链确认。 | 输入：veh；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；RuntimeError；self._log.depart_FCS；self._add_veh2；self._hubs.fcs.get_bind_of；self._log.depart_GS；self._hubs.gs.get_bind_of；Lang.VEH_TYPE_NOT_SUPPORTED.format；veh.brief；type | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst._end_trip | (self, veh: Vehicle, dist: float) | v2sim/sim/base.py#L275 | End the current trip of a vehicle and add its next trip to the departure queue. If the destination of the trip meets the charging conditions, try to charge. veh: Vehicle object dist: Distance traveled in this trip | 输入：veh, dist；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._log.arrive；veh.next_trip；isinstance；self._start_charging_SCS；self._que.push | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst._prepare_stations | (self, veh: Vehicle, to_stations: List[str], omega: float, to_charge: float, hub: StationHub) | v2sim/sim/base.py#L299 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.base.TrafficInst._prepare_stations 的实现与调用链确认。 | 输入：veh, to_stations, omega, to_charge, hub；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；RuntimeError；isinstance；s.pbuy；Lang.VEH_TYPE_NOT_SUPPORTED.format；type；s.wait_count；max；min | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst._prepare_trips_and_scs | (self) | v2sim/sim/base.py#L50 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.base.TrafficInst._prepare_trips_and_scs 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | PQueue；self._vehs.values；self._vehs.evs.values；self._que.push；random.choice；self.scs.add_veh；random.random；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst._start_charging_SCS | (self, veh: EV, bind: str) -> bool | v2sim/sim/base.py#L243 | Make a vehicle enter the charging state (slow charging station) veh: Vehicle instance bind: Node/edge where the vehicle is located | 输入：veh, bind；输出：bool | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.scs.add_veh；float；self._log.join_SCS；isinstance；scs_candidates.append；scs.wait_count | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst._start_restore | (self, veh: Vehicle, dist: float=-1) | v2sim/sim/base.py#L205 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.base.TrafficInst._start_restore 的实现与调用链确认。 | 输入：veh, dist；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self._hubs.fcs.add_veh；self._log.arrive_FCS；min；self._hubs.gs.add_veh；self._log.arrive_GS；RuntimeError；self._log.warn_smallcap；max；Lang.VEH_TYPE_NOT_SUPPORTED.format；self.find_route；type | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.add_station | (self, station: Union[CS, GS]) | v2sim/sim/base.py#L408 | Add a new station to the simulation. station: Station instance | 输入：station；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._hubs.add_station | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.add_trip | (self, veh_id: str, trip: Trip, force_sc: bool=False, force_fc: bool=False, force_fcs: Optional[str]=None) | v2sim/sim/base.py#L376 | Add a new trip for a vehicle. The trip will be added to the departure queue, but not added to the vehicle's trip list. veh_id: Vehicle ID trip: Trip instance force_sc: Whether to force slow charging at the destination if needed force_fc: Whether to force fast charging on the way if needed force_fcs: If not None and force_fc is True, force fast charging at the specified fast charging station on the way Note: The force will not be set immediately. They will be set when the vehicle departs. | 输入：veh_id, trip, force_sc, force_fc, force_fcs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | Lang.DEPART_TIME_PASSED.format；self._que.push；RuntimeError；Lang.VEH_NOT_FOUND.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.add_veh | (self, veh: Vehicle) | v2sim/sim/base.py#L392 | Add a new vehicle to the simulation. The vehicle will be added to the departure queue according to its first trip's departure time. veh: Vehicle instance | 输入：veh；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | RuntimeError；len；Lang.VEH_ALREADY_EXISTS.format；veh.next_trip；self._que.push | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.clear_step_callback | (self) | v2sim/sim/base.py#L192 | Clear the step callback function. | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.cur_time | (self) | v2sim/sim/base.py#L92 | Current time | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.edges | (self) | v2sim/sim/base.py#L141 | Get all road instances | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.end_time | (self) | v2sim/sim/base.py#L82 | Simulation end time | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.fcs | (self) -> FCSHub | v2sim/sim/base.py#L107 | Fast charging station list | 输入：无显式输入；输出：FCSHub | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.find_best_station | (self, veh: Vehicle, O: str, to_stations: List[str], omega: float, to_charge: float, max_dist: float, hub: StationHub) -> Tuple[str, StageLike] | v2sim/sim/base.py#L361 | 根据车辆状态与站点条件选择候选充电站；策略语义需由场景适配层再次约束。 | 输入：veh, O, to_stations, omega, to_charge, max_dist, hub；输出：Tuple[str, StageLike] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.sim.base.TrafficInst.find_route | (self, O: str, D: str) -> StageLike | v2sim/sim/base.py#L350 | Find a route from O to D. | 输入：O, D；输出：StageLike | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.get_average_vcr | (self) -> float | v2sim/sim/base.py#L345 | Calculate the smoothness of the traffic network. | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.get_edge_names | (self) -> List[str] | v2sim/sim/base.py#L145 | Get all road names | 输入：无显式输入；输出：List[str] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.get_queue_rate | (self) -> float | v2sim/sim/base.py#L329 | Calculate the average queue rate of all stations. | 输入：无显式输入；输出：float | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | s.wait_count | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.get_sta_head | (self) -> List[str] | v2sim/sim/base.py#L131 | Get the names of all stations | 输入：无显式输入；输出：List[str] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._hubs.gs.get_names；self._hubs.fcs.get_names；self._hubs.scs.get_names | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.get_total_queue_count | (self) -> int | v2sim/sim/base.py#L340 | Calculate the total number of vehicles in the queues of all stations. | 输入：无显式输入；输出：int | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | sum；s.wait_count | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.get_veh_count | (self) -> List[int] | v2sim/sim/base.py#L135 | Get the number of vehicles in all stations | 输入：无显式输入；输出：List[int] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._hubs.gs.get_veh_count；self._hubs.fcs.get_veh_count；self._hubs.scs.get_veh_count | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.get_veh_pos | (self, veh_id: str) -> Tuple[float, float] | v2sim/sim/base.py#L69 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.base.TrafficInst.get_veh_pos 的实现与调用链确认。 | 输入：veh_id；输出：Tuple[float, float] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.gs | (self) -> GSHub | v2sim/sim/base.py#L102 | Gas station list | 输入：无显式输入；输出：GSHub | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.itertrips | (self) | v2sim/sim/base.py#L127 | Get an iterator for all trips | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | chain；self._vehs.values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.pdn | (self) -> Grid | v2sim/sim/base.py#L72 | Power distribution network | 输入：无显式输入；输出：Grid | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.post_simulation_step | (self, deltaT: int) | v2sim/sim/base.py#L163 | 在单步交通推进后处理统计、回调或状态同步。 | 输入：deltaT；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._gp；self._hubs.gs.update；self._hubs.fcs.update；self._hubs.scs.update；self._pdn._cp；self._pdn._dp；self._end_restore；len；self._fQ.pop；self._start_restore；self._step_cb；self._fQ.empty | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.sim.base.TrafficInst.save | (self, path: Union[str, Path]) | v2sim/sim/base.py#L200 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.base.TrafficInst.save 的实现与调用链确认。 | 输入：path；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.scs | (self) -> SCSHub | v2sim/sim/base.py#L112 | Slow charging station list | 输入：无显式输入；输出：SCSHub | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.set_step_callback | (self, callback: StepCallback) | v2sim/sim/base.py#L185 | Set a callback function to be called at each simulation step. callback: A function that takes the TrafficInst instance as an argument. | 输入：callback；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.simulation_start | (self) | v2sim/sim/base.py#L158 | 启动交通仿真并准备车辆行程与充电站状态。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.sim.base.TrafficInst.simulation_step | (self, step_len: int) | v2sim/sim/base.py#L161 | 推进交通状态、车辆事件和充电过程的一个时间步。 | 输入：step_len；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.sim.base.TrafficInst.simulation_stop | (self) | v2sim/sim/base.py#L197 | 停止交通后端并释放其运行资源。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.sim.base.TrafficInst.start_time | (self) | v2sim/sim/base.py#L77 | Simulation start time | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.stations | (self) -> MixedHub | v2sim/sim/base.py#L117 | All stations | 输入：无显式输入；输出：MixedHub | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.step_len | (self) | v2sim/sim/base.py#L87 | Simulation step length | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.trip_logger | (self) -> TripLogger | v2sim/sim/base.py#L97 | Trip logger | 输入：无显式输入；输出：TripLogger | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.base.TrafficInst.vehs | (self) -> VDict | v2sim/sim/base.py#L122 | Vehicle dictionary, key is vehicle ID, value is EV instance | 输入：无显式输入；输出：VDict | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.sim.base.UXsimConfig | class | v2sim/sim/base.py#L430 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.base.UXsimConfig 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
