# `v2sim.sim.uxsim.utils`

本模块共记录 8 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.sim.uxsim.utils.LoggingWarning | class(UserWarning) | v2sim/sim/uxsim/utils.py#L138 | This warns that when vehicle_logging_timestep_interval is not 1 but called vehicle logging-related functions. | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.uxsim.utils.catch_exceptions_and_warn | (warning_msg='') | v2sim/sim/uxsim/utils.py#L14 | A decorator that catches exceptions in the decorated function and raises a warning with the specified message. | 输入：warning_msg；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | warnings.simplefilter；functools.wraps；func；sys._getframe；warnings.warn；traceback.extract_stack；str；type | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.uxsim.utils.equal_tolerance | (val, check, rel_tol=0.1, abs_tol=0.0) | v2sim/sim/uxsim/utils.py#L176 | Function for tests. | 输入：val, check, rel_tol, abs_tol；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | abs | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.uxsim.utils.get_font_for_matplotlib | (fname=None) | v2sim/sim/uxsim/utils.py#L92 | Get a multi-language (currently Japanese only) font for matplotlib. TODO: check if it works on different OS. It is only checked on Japanese Windows. TODO: explore if it can be extended for other languages. | 输入：fname；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | font_manager.findSystemFonts；any；font.lower | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.uxsim.utils.lange | (l) | v2sim/sim/uxsim/utils.py#L57 | Super lazy function for abbreviating range(len(l)). | 输入：l；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | range；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.uxsim.utils.plot_multiple_y | (x=None, ys: Optional[Iterable]=None, labels=None, **kwarg) | v2sim/sim/uxsim/utils.py#L144 | Plot multiple y values on the same plot, normalizing each value to [0, 1]. | 输入：x, ys, labels, **kwarg；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | enumerate；plt.ylabel；plt.legend；np.min；np.max；plt.plot；lange | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.uxsim.utils.print_columns | (*lists) | v2sim/sim/uxsim/utils.py#L118 | Convinient function to check contents of 1d lists. For debug. | 输入：*lists；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | max；range；print；len；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.uxsim.utils.printtry | (*args, **kwargs) | v2sim/sim/uxsim/utils.py#L67 | Print messages safely, evaluating any callable arguments and catching exceptions. Mainly written by GPT-4. | 输入：*args, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | print；callable；arg | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
