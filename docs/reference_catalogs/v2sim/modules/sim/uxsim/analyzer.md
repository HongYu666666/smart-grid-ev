# `v2sim.sim.uxsim.analyzer`

本模块共记录 8 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.sim.uxsim.analyzer.Analyzer | class | v2sim/sim/uxsim/analyzer.py#L11 | Class for analyzing and visualizing a simulation result. | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | catch_exceptions_and_warn；ddict；np.zeros；floyd_warshall；s.W.VEHICLES.values；s.od_tt.keys；s.compute_accurate_traj；s.compute_edie_state；frozenset；range；item；np.sum；warnings.warn；lange；np.nan_to_num；sum | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxsim.analyzer.Analyzer.__init__ | (s, W, font_pillow=None, font_matplotlib=None) | v2sim/sim/uxsim/analyzer.py#L16 | Create a result analysis object. | 输入：s, W, font_pillow, font_matplotlib；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxsim.analyzer.Analyzer.compute_accurate_traj | (s) | v2sim/sim/uxsim/analyzer.py#L131 | Generate more complete vehicle trajectories for each link by extrapolating recorded trajectories. It is assumed that vehicles are in free-flow travel at the end of the link. | 输入：s；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | s.W.VEHICLES.values；warnings.warn；lange；len；s.W.get_link；append；l.tss.append；l.xss.append；l.ls.append；l.cs.append；l.names.append；insert | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxsim.analyzer.Analyzer.compute_edie_state | (s) | v2sim/sim/uxsim/analyzer.py#L174 | Compute Edie's traffic state for each link. | 输入：s；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | s.compute_accurate_traj；np.zeros；lange；np.nan_to_num；np.errstate；ddict；range；int；sum；list；values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxsim.analyzer.Analyzer.compute_mfd | (s, links=None) | v2sim/sim/uxsim/analyzer.py#L277 | Compute network average flow and density for MFD. | 输入：s, links；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | s.compute_edie_state；frozenset；range；s.W.get_link；len；sum | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxsim.analyzer.Analyzer.link_analysis_coarse | (s) | v2sim/sim/uxsim/analyzer.py#L113 | Analyze link-level coarse stats: traffic volume, remaining vehicles, free-flow travel time, average travel time, its std. | 输入：s；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | ddict；item；np.average；np.std | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxsim.analyzer.Analyzer.od_analysis | (s) | v2sim/sim/uxsim/analyzer.py#L47 | Analyze OD-specific stats: number of trips, number of completed trips, free-flow travel time, average travel time, its std, total distance traveled. | 输入：s；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | ddict；np.zeros；floyd_warshall；s.W.VEHICLES.values；s.od_tt.keys；item；np.sum；len；sum；append；np.average；np.std；hasattr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.uxsim.analyzer.Analyzer.show_simulation_progress | (s) | v2sim/sim/uxsim/analyzer.py#L260 | Print simulation progress. | 输入：s；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | catch_exceptions_and_warn；sum；print；time.time | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
