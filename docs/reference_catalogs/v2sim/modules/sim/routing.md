# `v2sim.sim.routing`

本模块共记录 16 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.sim.routing.LinkLike | class(Protocol) | v2sim/sim/routing.py#L16 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.routing.LinkLike 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.routing.LinkLike.instant_travel_time | (self, t: int) -> float | v2sim/sim/routing.py#L19 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.routing.LinkLike.instant_travel_time 的实现与调用链确认。 | 输入：t；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.sim.routing.Stage | class | v2sim/sim/routing.py#L8 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.routing.Stage 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.routing.astarF | (gl: Graph, node_coords: CoordsDict, ctime: int, from_node: str, to_node: str) -> Stage | v2sim/sim/routing.py#L146 | A* algorithm for FASTEST path using time as cost. Uses f_score (time + heuristic) as primary key, time as secondary key, length as tertiary key. | 输入：gl, node_coords, ctime, from_node, to_node；输出：Stage | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | heuristic；heapq.heappush；set；Stage；math.hypot；heapq.heappop；visited.add；gl.get；float；link.instant_travel_time | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.routing.astarMC | (gl: Graph, node_coords: CoordsDict, ctime: int, from_node: str, omega: float, to_nodes: Iterable[str], node_scores: Dict[str, float], max_length: float=float('inf'), avg_speed: float=20.0) -> Stage | v2sim/sim/routing.py#L384 | A* version of dijMC - Find the BEST route using kDTree heuristic. | 输入：gl, node_coords, ctime, from_node, omega, to_nodes, node_scores, max_length, avg_speed；输出：Stage | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | float；build_target_kdtree；create_heuristic_with_kdtree；score_heuristic；set；Stage；distance_heuristic；node_scores.get；heapq.heappop；visited.add；gl.get；link.instant_travel_time；heapq.heappush；path.copy；path_edges.copy | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.routing.astarMF | (gl: Graph, node_coords: CoordsDict, ctime: int, from_node: str, to_nodes: Iterable[str], avg_speed: float) -> Stage | v2sim/sim/routing.py#L290 | A* version of dijMF - Find the FASTEST route to any node in to_nodes using kDTree heuristic. | 输入：gl, node_coords, ctime, from_node, to_nodes, avg_speed；输出：Stage | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | build_target_kdtree；create_time_heuristic_with_kdtree；heuristic；set；Stage；float；heapq.heappop；visited.add；gl.get；link.instant_travel_time；heapq.heappush；path.copy；path_edges.copy | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.routing.astarMS | (gl: Graph, node_coords: CoordsDict, ctime: int, from_node: str, to_nodes: Iterable[str]) -> Stage | v2sim/sim/routing.py#L337 | A* version of dijMS - Find the SHORTEST route to any node in to_nodes using kDTree heuristic. | 输入：gl, node_coords, ctime, from_node, to_nodes；输出：Stage | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | build_target_kdtree；create_heuristic_with_kdtree；heuristic；set；Stage；float；heapq.heappop；visited.add；gl.get；link.instant_travel_time；heapq.heappush；path.copy；path_edges.copy | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.routing.astarS | (gl: Graph, node_coords: CoordsDict, ctime: int, from_node: str, to_node: str) -> Stage | v2sim/sim/routing.py#L193 | A* algorithm for SHORTEST path using length as cost. Uses f_score (length + heuristic) as primary key, length as secondary key, time as tertiary key. | 输入：gl, node_coords, ctime, from_node, to_node；输出：Stage | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | heuristic；heapq.heappush；set；Stage；math.hypot；heapq.heappop；visited.add；gl.get；float；link.instant_travel_time | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.routing.build_target_kdtree | (node_coords: CoordsDict, to_nodes: Iterable[str]) -> Tuple[KDTree, Dict[int, str]] | v2sim/sim/routing.py#L241 | 为目标节点构建kDTree并建立索引到节点ID的映射 Returns: (kdtree, index_to_node_id) | 输入：node_coords, to_nodes；输出：Tuple[KDTree, Dict[int, str]] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | enumerate；KDTree；target_coords.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.routing.create_heuristic_with_kdtree | (node_coords: CoordsDict, kdtree: KDTree) -> Callable | v2sim/sim/routing.py#L259 | 创建使用kDTree的启发式函数 | 输入：node_coords, kdtree；输出：Callable | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | kdtree.query；float | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.routing.create_time_heuristic_with_kdtree | (node_coords: CoordsDict, kdtree: KDTree, avg_speed: float=20.0) -> Callable | v2sim/sim/routing.py#L274 | 创建使用kDTree的时间启发式函数 | 输入：node_coords, kdtree, avg_speed；输出：Callable | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | kdtree.query；float | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.routing.dijF | (gl: Graph, ctime: int, from_node: str, to_node: str) -> Stage | v2sim/sim/routing.py#L138 | Fastest path between two specific nodes. | 输入：gl, ctime, from_node, to_node；输出：Stage | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | dijMF | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.routing.dijMC | (gl: Graph, ctime: int, from_node: str, omega: float, to_nodes: Iterable[str], node_scores: Dict[str, float], max_length: float=float('inf')) -> Stage | v2sim/sim/routing.py#L23 | Find the BEST route based on score = omega * (time + waiting) + charging_cost. Uses time as primary key, length as secondary key. | 输入：gl, ctime, from_node, omega, to_nodes, node_scores, max_length；输出：Stage | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | float；set；Stage；heapq.heappop；visited.add；gl.get；link.instant_travel_time；heapq.heappush；path.copy；path_edges.copy | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.routing.dijMF | (gl: Graph, ctime: int, from_node: str, to_nodes: Iterable[str]) -> Stage | v2sim/sim/routing.py#L68 | Find the FASTEST route to any node in to_nodes. Uses time as primary key, length as secondary key. | 输入：gl, ctime, from_node, to_nodes；输出：Stage | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；Stage；float；heapq.heappop；visited.add；gl.get；link.instant_travel_time；heapq.heappush；path.copy；path_edges.copy | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.routing.dijMS | (gl: Graph, ctime: int, from_node: str, to_nodes: Iterable[str]) -> Stage | v2sim/sim/routing.py#L103 | Find the SHORTEST route to any node in to_nodes. Uses length as primary key, time as secondary key. | 输入：gl, ctime, from_node, to_nodes；输出：Stage | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；Stage；float；heapq.heappop；visited.add；gl.get；link.instant_travel_time；heapq.heappush；path.copy；path_edges.copy | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.sim.routing.dijS | (gl: Graph, ctime: int, from_node: str, to_node: str) -> Stage | v2sim/sim/routing.py#L142 | Shortest path between two specific nodes. | 输入：gl, ctime, from_node, to_node；输出：Stage | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | dijMS | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
