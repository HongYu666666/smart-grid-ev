# `v2sim.sim.ux`

本模块共记录 24 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.sim.ux.TrafficUX | class(TrafficInst) | v2sim/sim/ux.py#L24 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.ux.TrafficUX 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；list；set；create_func；_prepare_trips_and_scs；get_xy_coords；self.W.links；self._prepare_stations；clear_odometer；self.W.add_vehicle；isinstance；self.find_best_station；self.__batch_depart；chain；self._hubs.check_kdtree；self.W.exec_simulation | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.__batch_depart | (self) | v2sim/sim/ux.py#L258 | Sent out all vehicles that reaching the departure time | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._que.pop；self.__start_trip；self._que.empty；isinstance；max；self._log.depart；self.find_best_route；self._hubs.gs.get_online_names；self._hubs.fcs.get_online_names；len；RuntimeError；self._hubs.get_bind_of；nodes.keys；self._fQ.push；self._log.depart_failed；self._hubs.scs.has_veh | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.__init__ | (self, start_time: int, step_len: int, end_time: int, roadnet: RoadNet, trip_logger: TripLogger, vehs: VDict, hubs: MixedHub, pdn: Grid, gasoline_price: TimeFunc, seed: int=0, silent: bool=False, *, routing_algo: str='dijkstra', show_uxsim_info: bool=False, randomize_uxsim: bool=True, no_parallel: bool=False) | v2sim/sim/ux.py#L25 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.ux.TrafficUX.__init__ 的实现与调用链确认。 | 输入：start_time, step_len, end_time, roadnet, trip_logger, vehs, hubs, pdn, gasoline_price, seed, silent, routing_algo, show_uxsim_info, randomize_uxsim, no_parallel；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；list；set；create_func；_prepare_trips_and_scs；self._rnet.edges.keys；warn；isinstance；super；len；Lang.WARN_CS_NOT_IN_SCC.format；print；join；Lang.PARA_WORLDS.format；self._rnet.is_node_in_largest_scc | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.__sel_best_station | (self, veh: Vehicle, cur_node: Optional[str]=None, cur_edge: Optional[str]=None) -> Tuple[str, Stage] | v2sim/sim/ux.py#L175 | Select the best available station (FCS/GS) based on the edge where the car is currently located, and return the path and average weight | 输入：veh, cur_node, cur_edge；输出：Tuple[str, Stage] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self.find_best_station；hub.get_online_names；self.W.has_vehicle；RuntimeError；self.W.get_link；Lang.VEH_NOT_FOUND.format；self.W.get_vehicle；Lang.VEH_HAS_NO_LINK.format；Lang.VEH_TYPE_NOT_SUPPORTED.format；type | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.__start_trip | (self, veh: Vehicle) -> bool | v2sim/sim/ux.py#L207 | Start the current trip of a vehicle | 输入：veh；输出：bool | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；self.find_route；self._hubs.scs.pop_veh；veh.is_energy_enough；self._add_veh；self._add_veh2；self.__sel_best_station；self._log.leave_SCS；self._hubs.get_bind_of；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX._add_veh | (self, veh_id: str, from_node: str, to_node: str, route: Union[Stage, List[str]]) | v2sim/sim/ux.py#L158 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.ux.TrafficUX._add_veh 的实现与调用链确认。 | 输入：veh_id, from_node, to_node, route；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | clear_odometer；self.W.add_vehicle | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX._add_veh2 | (self, veh_id: str, O: str, D: str) | v2sim/sim/ux.py#L162 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.ux.TrafficUX._add_veh2 的实现与调用链确认。 | 输入：veh_id, O, D；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | clear_odometer；self.W.add_vehicle | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX._partial_load_unsafe | (d: dict, tlogger: TripLogger) -> 'TrafficUX' | v2sim/sim/ux.py#L409 | Load a TrafficUX from a saved_state object (unsafe, for advanced users only, at your own risk!) object: Saved_state object triplogger_save_path: If not None, change the trip logger save path to this path Return: TrafficUX instance, without world loaded! | 输入：d, tlogger；输出：'TrafficUX' | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；CheckPyVersion；RuntimeError；Lang.PY_VERSION_MISMATCH_TI.format；Lang.PICKLER_MISMATCH_TI.format；PyVersion | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX._save_obj | (self) | v2sim/sim/ux.py#L394 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.ux.TrafficUX._save_obj 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | pickle.dumps；PyVersion | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.create | (case: CaseData, tlogger: TripLogger, vscfg: CommonConfig, config: UXsimConfig, seed: int=0, silent: bool=False) | v2sim/sim/ux.py#L451 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.ux.TrafficUX.create 的实现与调用链确认。 | 输入：case, tlogger, vscfg, config, seed, silent；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | TrafficUX；asdict | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.edges | (self) | v2sim/sim/ux.py#L167 | Get all roads | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | list；self._rnet.edges.values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.find_best_route | (self, O: str, Ds: Iterable[str], fastest: bool=True) | v2sim/sim/ux.py#L103 | Find the best route from O to one of Ds. | 输入：O, Ds, fastest；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | astarMF；astarMS；dijMF；dijMS；self.W.get_gl；self.W.get_coords；max；self.W.get_average_speed | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.find_best_station | (self, veh: Vehicle, O: str, to_stations: List[str], omega: float, to_charge: float, max_dist: float, hub: StationHub) -> Tuple[str, Stage] | v2sim/sim/ux.py#L123 | Find the best station to go from O. | 输入：veh, O, to_stations, omega, to_charge, max_dist, hub；输出：Tuple[str, Stage] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._prepare_stations；astarMC；dijMC；len；self.W.get_gl；self.W.get_coords；Ds.keys；max；self.W.get_average_speed | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.find_route | (self, O: str, D: str, fastest: bool=True) -> Stage | v2sim/sim/ux.py#L86 | Find the best route from node O to node D. | 输入：O, D, fastest；输出：Stage | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | astarF；astarS；dijMF；dijMS；self.W.get_gl；self.W.get_coords | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.get_average_vcr | (self) -> float | v2sim/sim/ux.py#L77 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.ux.TrafficUX.get_average_vcr 的实现与调用链确认。 | 输入：无显式输入；输出：float | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.W.links；float | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.get_edge_names | (self) -> List[str] | v2sim/sim/ux.py#L171 | Get the names of all roads | 输入：无显式输入；输出：List[str] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.get_veh_pos | (self, veh_id: str) -> Tuple[float, float] | v2sim/sim/ux.py#L74 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.ux.TrafficUX.get_veh_pos 的实现与调用链确认。 | 输入：veh_id；输出：Tuple[float, float] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | get_xy_coords；self.W.get_vehicle | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.load | (folder: Union[str, Path], tlogger: TripLogger) -> 'TrafficUX' | v2sim/sim/ux.py#L429 | Load a TrafficUX from a saved_state folder folder: Folder path tlogger: TripLogger instance to use Return: TrafficUX instance | 输入：folder, tlogger；输出：'TrafficUX' | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | TrafficUX._partial_load_unsafe；load_world；isinstance；Path；inst.exists；FileNotFoundError；gzip.open；pickle.load；str；Lang.ERROR_STATE_FILE_NOT_FOUND.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.routing_algo | (self) -> str | v2sim/sim/ux.py#L149 | Routing algorithm, can be "dijkstra" or "astar" | 输入：无显式输入；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.save | (self, folder: Union[str, Path]) | v2sim/sim/ux.py#L373 | Save the current state of the simulation folder: Folder path | 输入：folder；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | f.mkdir；self.W.save；isinstance；Path；str；gzip.open；pickle.dump；PyVersion | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.show_uxsim_info | (self) -> bool | v2sim/sim/ux.py#L154 | Whether to display uxsim information | 输入：无显式输入；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.simulation_start | (self) | v2sim/sim/ux.py#L314 | Start simulation | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__batch_depart；chain；self._hubs.check_kdtree；get_coord；float；self._rnet.get_node | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.simulation_step | (self, step_len: int) | v2sim/sim/ux.py#L325 | Simulation step. step_len: Step length (seconds) v2g_demand: V2G demand list (kWh/s) | 输入：step_len；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.W.exec_simulation；self.__batch_depart；self.W.get_arrived_vehicles；post_simulation_step；v0.traveled_route；sum；veh.drive；self._end_trip；self._start_restore；super；self.W.get_running_vehicle_count；self.W.get_average_speed；warn；Warning；Lang.SIMULATION_MAY_STALL.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.sim.ux.TrafficUX.simulation_stop | (self) | v2sim/sim/ux.py#L368 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.ux.TrafficUX.simulation_stop 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._log.close；print；self.W.shutdown | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
