# `v2sim.sim.utils`

本模块共记录 6 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.sim.utils.CaseData | class | v2sim/sim/utils.py#L54 | 聚合案例目录中的交通、车辆、站点、插件与仿真配置。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.vehicles.reset；self.mixed_hub.reset；Path；DetectFiles；__print；CaseData；FileNotFoundError；RuntimeError；RoadNet.load_raw；RoadNet.load_sumo；Lang.INFO_NET.format；VDict.from_file；LoadFCSList；LoadSCSList；LoadGSList；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.sim.utils.CaseData.parse | (case_dir: Union[str, Path], tc: TimeConfig, silent: bool=False, vehicles: Optional[VDict]=None) | v2sim/sim/utils.py#L75 | 从案例目录解析配置文件并构造 CaseData。 | 输入：case_dir, tc, silent, vehicles；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | Path；DetectFiles；__print；CaseData；FileNotFoundError；RuntimeError；RoadNet.load_raw；RoadNet.load_sumo；Lang.INFO_NET.format；VDict.from_file；LoadFCSList；LoadSCSList；LoadGSList；str；MixedHub；Grid.fromFile | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.sim.utils.CaseData.reset | (self) | v2sim/sim/utils.py#L64 | Reset case data to initial state. Note: 1. V2G plugin will alter the grid by adding generators, it will not be reset here. 2. Road network is assumed to be static and will not be reset. | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.vehicles.reset；self.mixed_hub.reset | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.sim.utils.CaseType | class(Enum) | v2sim/sim/utils.py#L13 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.sim.utils.CaseType 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.sim.utils.TimeConfig | class | v2sim/sim/utils.py#L19 | 保存仿真开始时间、步长和结束时间，并向交通后端配置传递时间参数。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | getroot；root.find；isinstance；str；RuntimeError；tnode.find；inode.find；ReadXML；Lang.ERROR_FILE_TYPE_NOT_SUPPORTED.format；int；nfnode.get；bnode.get；enode.get；Path | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.sim.utils.TimeConfig.sumocfg_update | (self, sumocfg_file: str) | v2sim/sim/utils.py#L24 | Load from SUMO configuration file and update time settings. Return network file in the configuration. | 输入：sumocfg_file；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | getroot；root.find；isinstance；str；RuntimeError；tnode.find；inode.find；ReadXML；Lang.ERROR_FILE_TYPE_NOT_SUPPORTED.format；int；nfnode.get；bnode.get；enode.get；Path | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
