# `v2sim.gen.veh`

本模块共记录 46 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gen.veh.ProgressBar | class | v2sim/gen/veh.py#L19 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh.ProgressBar 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | time.time；self.update；print；Lang.INFO_DONE_WITH_SECOND.format；round | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.ProgressBar.__init__ | (self, total: int, silent: bool=False) | v2sim/gen/veh.py#L20 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh.ProgressBar.__init__ 的实现与调用链确认。 | 输入：total, silent；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | time.time | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.ProgressBar.increment | (self) | v2sim/gen/veh.py#L36 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh.ProgressBar.increment 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.update | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.ProgressBar.update | (self, current: int) | v2sim/gen/veh.py#L27 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh.ProgressBar.update 的实现与调用链确认。 | 输入：current；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | print；time.time；Lang.INFO_DONE_WITH_SECOND.format；round | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| class | v2sim.gen.veh.RoutingCacheMode | class(Enum) | v2sim/gen/veh.py#L39 | Routing cache mode | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.RoutingCacheMode.__repr__ | (self) | v2sim/gen/veh.py#L48 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh.RoutingCacheMode.__repr__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.RoutingCacheMode.__str__ | (self) | v2sim/gen/veh.py#L45 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh.RoutingCacheMode.__str__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| class | v2sim.gen.veh.SUMOVehGenerator | class(VehGenerator) | v2sim/gen/veh.py#L497 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh.SUMOVehGenerator 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；sumolib.net.readNet；self.taz_of_edge.get；self._getDest1；self._getNextTAZandPlace；Trip；self._getDestA；self._genFirstTrip1；self._genTripA；self._genTripF；v.add_trip；self._genFirstTripA；getroot；root.findall；self._findTAZbyEdge；random.choice | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.SUMOVehGenerator.__init__ | (self, CROOT: str, PNAME: str, mode: TripsGenMode=TripsGenMode.AUTO) | v2sim/gen/veh.py#L498 | Initialization | 输入：CROOT, PNAME, mode；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；sumolib.net.readNet；getroot；root.findall；super；open；fp.readlines；RuntimeError；RoadNet.load；PolygonMan；ln.split；ReadXML；split；poly.getConvertedType；poly.center；name.strip | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.SUMOVehGenerator._findTAZbyEdge | (self, edge_id: str) -> Optional[str] | v2sim/gen/veh.py#L555 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh.SUMOVehGenerator._findTAZbyEdge 的实现与调用链确认。 | 输入：edge_id；输出：Optional[str] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.taz_of_edge.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.SUMOVehGenerator._genFirstTrip1 | (self, v: Vehicle, trip_id, weekday: bool=True) | v2sim/gen/veh.py#L575 | Generate the first trip of the first day | 输入：v, trip_id, weekday；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._getDest1；self._getNextTAZandPlace；Trip；self._findTAZbyEdge；random.choice；RuntimeError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.SUMOVehGenerator._genFirstTripA | (self, trip_id, v: Vehicle, weekday: bool=True) | v2sim/gen/veh.py#L679 | Generate the first trip of a non-first day | 输入：trip_id, v, weekday；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._getDest1；self._getNextTAZandPlace；Trip；len；self._findTAZbyEdge；RuntimeError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.SUMOVehGenerator._genTripA | (self, trip_id: str, from_TAZ: str, from_type: str, from_edge: str, start_time: int, weekday: bool=True) | v2sim/gen/veh.py#L600 | Generate the second trip | 输入：trip_id, from_TAZ, from_type, from_edge, start_time, weekday；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._getDestA；self._getNextTAZandPlace；Trip；self._genStopTimeIdx | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.SUMOVehGenerator._genTripF | (self, trip_id: str, from_TAZ: str, from_type, from_EDGE: str, start_time: int, first_TAZ: str, first_EDGE: str, weekday: bool=True) | v2sim/gen/veh.py#L626 | Generate the third trip | 输入：trip_id, from_TAZ, from_type, from_EDGE, start_time, first_TAZ, first_EDGE, weekday；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | Trip；self._genStopTimeIdx | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.SUMOVehGenerator._genTripsChain1 | (self, v: Vehicle) | v2sim/gen/veh.py#L654 | Generate a full day of trips on the first day | 输入：v；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._genFirstTrip1；self._genTripA；self._genTripF；v.add_trip | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.SUMOVehGenerator._genTripsChainA | (self, v: Vehicle, daynum: int=1) | v2sim/gen/veh.py#L705 | Generate a full day of trips on a non-first day | 输入：v, daynum；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._genFirstTripA；self._genTripA；self._genTripF；v.add_trip | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.SUMOVehGenerator._getNextTAZandPlace | (self, from_TAZ: str, from_EDGE: str, next_place_type: str) -> Tuple[str, str] | v2sim/gen/veh.py#L558 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh.SUMOVehGenerator._getNextTAZandPlace 的实现与调用链确认。 | 输入：from_TAZ, from_EDGE, next_place_type；输出：Tuple[str, str] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | random.choice；random_diff；RuntimeError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| class | v2sim.gen.veh.TripsGenMode | class(Enum) | v2sim/gen/veh.py#L52 | Generation mode | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.TripsGenMode.__repr__ | (self) | v2sim/gen/veh.py#L62 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh.TripsGenMode.__repr__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.TripsGenMode.__str__ | (self) | v2sim/gen/veh.py#L59 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh.TripsGenMode.__str__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| class | v2sim.gen.veh.UXVehGenerator | class(VehGenerator) | v2sim/gen/veh.py#L335 | Class to generate trips for UXsim | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；RoadNet.load；random_diff2；self._getDest1；self._getNextNode；Trip；self._getDestA；self._genFirstTrip1；self._genTripA；self._genTripF；v.add_trip；self._genFirstTripA；_readNodeTypes；self._genStopTimeIdx；len；super | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.UXVehGenerator.__init__ | (self, CROOT: str, PNAME: str, mode: TripsGenMode=TripsGenMode.AUTO) | v2sim/gen/veh.py#L337 | Initialization | 输入：CROOT, PNAME, mode；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；RoadNet.load；_readNodeTypes；super；PolygonMan；RuntimeError；_TypeNodes；poly.getConvertedType；self.net.find_nearest_node_with_distance；self.net.is_node_in_largest_scc；append；poly.center | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.UXVehGenerator._genFirstTrip1 | (self, v: Vehicle, trip_id, weekday: bool=True) | v2sim/gen/veh.py#L375 | Generate the first trip of the first day | 输入：v, trip_id, weekday；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._getDest1；self._getNextNode；Trip；random.choices | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.UXVehGenerator._genFirstTripA | (self, trip_id: str, v: Vehicle, weekday: bool=True) | v2sim/gen/veh.py#L459 | Generate the first trip of a non-first day | 输入：trip_id, v, weekday；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._getDest1；self._getNextNode；Trip；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.UXVehGenerator._genTripA | (self, trip_id: str, from_type: str, from_node: str, start_time: int, weekday: bool=True) -> Trip | v2sim/gen/veh.py#L398 | Generate the second trip | 输入：trip_id, from_type, from_node, start_time, weekday；输出：Trip | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._getDestA；self._getNextNode；Trip；self._genStopTimeIdx；min | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.UXVehGenerator._genTripF | (self, trip_id: str, from_type: str, from_node: str, start_time: int, first_node: str, weekday: bool=True) | v2sim/gen/veh.py#L423 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh.UXVehGenerator._genTripF 的实现与调用链确认。 | 输入：trip_id, from_type, from_node, start_time, first_node, weekday；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | Trip；self._genStopTimeIdx | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.UXVehGenerator._genTripsChain1 | (self, v: Vehicle) | v2sim/gen/veh.py#L439 | Generate a full day of trips on the first day ev: vehicle instance | 输入：v；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._genFirstTrip1；self._genTripA；self._genTripF；v.add_trip | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.UXVehGenerator._genTripsChainA | (self, v: Vehicle, daynum: int=1) | v2sim/gen/veh.py#L481 | Generate a full day of trips on a non-first day | 输入：v, daynum；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._genFirstTripA；self._genTripA；self._genTripF；v.add_trip | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.UXVehGenerator._getNextNode | (self, from_node: str, next_place_type: str) -> str | v2sim/gen/veh.py#L371 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh.UXVehGenerator._getNextNode 的实现与调用链确认。 | 输入：from_node, next_place_type；输出：str | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | random_diff2 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| class | v2sim.gen.veh.VehGenerator | class(ABC) | v2sim/gen/veh.py#L66 | Class to generate trips | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | DetectFiles；VehicleTypePool；PDGamma；PDDiscrete.fromCSVFileI；self._getPs；int；NotImplementedError；range；self.vTypes.sample；isinstance；self.gen_trip_for_veh；self.vTypes.sample_evtype；create_veh；self.vTypes.sample_gvtype；VDict；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.VehGenerator.__init__ | (self, CROOT: str, PNAME: str) | v2sim/gen/veh.py#L68 | Initialization | 输入：CROOT, PNAME；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | DetectFiles；VehicleTypePool；PDGamma；PDDiscrete.fromCSVFileI；os.path.join；ReadOnlyTable；list；range；read_trans_pdfs；CDDiscrete；int；map；tbwd.col；len；PDDiscrete | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.VehGenerator._genStopTimeIdx | (self, from_type: str, weekday: bool) | v2sim/gen/veh.py#L147 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh.VehGenerator._genStopTimeIdx 的实现与调用链确认。 | 输入：from_type, weekday；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | int；cdf.sample | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.VehGenerator._genTripsChain1 | (self, v: Vehicle) | v2sim/gen/veh.py#L152 | Generate a full day of trips on the first day | 输入：v；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | NotImplementedError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.VehGenerator._genTripsChainA | (self, v: Vehicle, daynum: int=1) | v2sim/gen/veh.py#L157 | Generate a full day of trips on a non-first day | 输入：v, daynum；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | NotImplementedError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.VehGenerator._getDest1 | (self, pfr: str, weekday: bool=True) | v2sim/gen/veh.py#L115 | Get the destination of the trip secondary to the first trip | 输入：pfr, weekday；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | int；self._getPs；pdf.sample；self.pdf_start_weekday.sample；self.pdf_start_weekend.sample | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.VehGenerator._getDestA | (self, from_type: str, init_time_i: int, weekday: bool) | v2sim/gen/veh.py#L133 | Get the destination of the next trip for non-first trips | 输入：from_type, init_time_i, weekday；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self._getPs；cdf.sample | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.VehGenerator._getPs | (self, is_weekday: bool, dtype: str, time_index: int) | v2sim/gen/veh.py#L112 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh.VehGenerator._getPs 的实现与调用链确认。 | 输入：is_weekday, dtype, time_index；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.VehGenerator.gen_ev | (self, day_count: int, veh_id: str, omega: PDFuncLike=None, krel: PDFuncLike=None, kfc: PDFuncLike=None, v2g_prop: float=1.0 + 0.0001, ksc: PDFuncLike=None, kv2g: PDFuncLike=None) -> EV | v2sim/gen/veh.py#L209 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh.VehGenerator.gen_ev 的实现与调用链确认。 | 输入：day_count, veh_id, omega, krel, kfc, v2g_prop, ksc, kv2g；输出：EV | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.vTypes.sample_evtype；create_veh；self.gen_trip_for_veh；self.soc_pdf.sample | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.VehGenerator.gen_gv | (self, day_count: int, veh_id: str, omega: PDFuncLike=None, krel: PDFuncLike=None, kfc: PDFuncLike=None) -> GV | v2sim/gen/veh.py#L217 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh.VehGenerator.gen_gv 的实现与调用链确认。 | 输入：day_count, veh_id, omega, krel, kfc；输出：GV | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.vTypes.sample_gvtype；create_veh；self.gen_trip_for_veh；self.soc_pdf.sample | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.VehGenerator.gen_trip_for_veh | (self, day_count: int, veh: Vehicle, use_buffer_day: bool=True, clear_existing: bool=False) | v2sim/gen/veh.py#L161 | Generate trips for an existing vehicle. | 输入：day_count, veh, use_buffer_day, clear_existing；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | range；veh.clear_trips；len；self._genTripsChainA；self._genTripsChain1 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.VehGenerator.gen_trip_for_vehs | (self, day_count: int, vehs: VDict, use_buffer_day: bool=True, clear_existing: bool=False, silent: bool=False, fname: Optional[str]=None, seed=None) | v2sim/gen/veh.py#L273 | Generate trips for existing vehicles. If seed is not None, the random seed will be set for reproducibility. Note that the random seed is only set for the generation of trips, and will be reset to the original state after generation. | 输入：day_count, vehs, use_buffer_day, clear_existing, silent, fname, seed；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | len；ProgressBar；chain；random.getstate；random.seed；vehs.evs.values；vehs.gvs.values；self.gen_trip_for_veh；pb.increment；vehs.save；random.setstate | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.VehGenerator.gen_veh | (self, day_count: int, veh_id: str, omega: PDFuncLike=None, krel: PDFuncLike=None, kfc: PDFuncLike=None, v2g_prop: float=1.0 + 0.0001, ksc: PDFuncLike=None, kv2g: PDFuncLike=None) -> Vehicle | v2sim/gen/veh.py#L184 | Generate trips for a vehicle. The generated vehicle is returned as a vehicle instance. | 输入：day_count, veh_id, omega, krel, kfc, v2g_prop, ksc, kv2g；输出：Vehicle | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.vTypes.sample；isinstance；self.gen_trip_for_veh；self.soc_pdf.sample；create_veh | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh.VehGenerator.gen_vehs | (self, N: Union[int, Tuple[int, int]], fname: Optional[str]=None, day_count: int=7, silent: bool=False, omega: PDFuncLike=None, krel: PDFuncLike=None, kfc: PDFuncLike=None, v2g_prop: float=1.0 + 0.0001, ksc: PDFuncLike=None, kv2g: PDFuncLike=None, seed=None) -> VDict | v2sim/gen/veh.py#L224 | Generate EV and trips of N vehicles. The generated vehicles are returned as an EVDict instance, and will be saved to the file if fname is provided. If seed is not None, the random seed will be set for reproducibility. Note that the random seed is only set for the generation of vehicles and trips, and will be reset to the original state after generation. | 输入：N, fname, day_count, silent, omega, krel, kfc, v2g_prop, ksc, kv2g, seed；输出：VDict | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | isinstance；VDict；random.getstate；random.seed；ProgressBar；range；ret.save；random.setstate；self.gen_ev；pb.increment；self.gen_gv；self.gen_veh；RuntimeError；type | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| class | v2sim.gen.veh._TypeNodes | class | v2sim/gen/veh.py#L302 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh._TypeNodes 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.names.append；self.weights.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | v2sim.gen.veh._TypeNodes.append | (self, name: str, weight: float) | v2sim/gen/veh.py#L306 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh._TypeNodes.append 的实现与调用链确认。 | 输入：name, weight；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.names.append；self.weights.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | v2sim.gen.veh._readNodeTypes | (fname: str) | v2sim/gen/veh.py#L310 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.veh._readNodeTypes 的实现与调用链确认。 | 输入：fname；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | open；fp.readlines；ln.split；name.strip；split；_TypeNodes；item_str.strip；node_names.append；node_weights.append；lst.strip；item_str.split；node.strip；float；weight.strip | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
