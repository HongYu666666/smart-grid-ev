# `v2sim.gen.misc`

本模块共记录 17 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gen.misc.EVType | class | v2sim/gen/misc.py#L10 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.misc.EVType 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.gen.misc.GVType | class | v2sim/gen/misc.py#L19 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.misc.GVType 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.gen.misc.VehicleTypePool | class | v2sim/gen/misc.py#L29 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.misc.VehicleTypePool 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | getroot；Lang.ERROR_FILE_TYPE_NOT_SUPPORTED.format；sum；self.__evtypes.append；self.__evt_weights.append；self.__gvtypes.append；self.__gvt_weights.append；enumerate；ValueError；random.choices；random.uniform；self.sample_evtype；self.sample_gvtype；ReadXML；EVType；float | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.misc.VehicleTypePool.__init__ | (self, desc_file: str) | v2sim/gen/misc.py#L30 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.misc.VehicleTypePool.__init__ 的实现与调用链确认。 | 输入：desc_file；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | getroot；Lang.ERROR_FILE_TYPE_NOT_SUPPORTED.format；sum；ReadXML；self.__evtypes.append；self.__evt_weights.append；EVType；float；self.__gvtypes.append；self.__gvt_weights.append；ValueError；GVType；VehType.from_str；parse_val | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.misc.VehicleTypePool.add_evtype | (self, evtype: EVType, weight: float) | v2sim/gen/misc.py#L74 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.misc.VehicleTypePool.add_evtype 的实现与调用链确认。 | 输入：evtype, weight；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__evtypes.append；self.__evt_weights.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.misc.VehicleTypePool.add_gvtype | (self, gvtype: GVType, weight: float) | v2sim/gen/misc.py#L79 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.misc.VehicleTypePool.add_gvtype 的实现与调用链确认。 | 输入：gvtype, weight；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__gvtypes.append；self.__gvt_weights.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.misc.VehicleTypePool.remove_evtype | (self, evtype: EVType) | v2sim/gen/misc.py#L84 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.misc.VehicleTypePool.remove_evtype 的实现与调用链确认。 | 输入：evtype；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | enumerate；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.misc.VehicleTypePool.remove_gvtype | (self, gvtype: GVType) | v2sim/gen/misc.py#L93 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.misc.VehicleTypePool.remove_gvtype 的实现与调用链确认。 | 输入：gvtype；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | enumerate；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.misc.VehicleTypePool.sample | (self) -> Union[EVType, GVType] | v2sim/gen/misc.py#L68 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.misc.VehicleTypePool.sample 的实现与调用链确认。 | 输入：无显式输入；输出：Union[EVType, GVType] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | random.uniform；self.sample_evtype；self.sample_gvtype | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.misc.VehicleTypePool.sample_evtype | (self) -> EVType | v2sim/gen/misc.py#L62 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.misc.VehicleTypePool.sample_evtype 的实现与调用链确认。 | 输入：无显式输入；输出：EVType | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | random.choices | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.misc.VehicleTypePool.sample_gvtype | (self) -> GVType | v2sim/gen/misc.py#L65 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.misc.VehicleTypePool.sample_gvtype 的实现与调用链确认。 | 输入：无显式输入；输出：GVType | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | random.choices | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.gen.misc._impl_PDFuncLike | (x: PDFuncLike, default: PDFunc) -> float | v2sim/gen/misc.py#L121 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.misc._impl_PDFuncLike 的实现与调用链确认。 | 输入：x, default；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | TypeError；default.sample；isinstance；x.sample | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.gen.misc.add_trip_to_veh | (veh: Vehicle, trip: Trip, day: int=-1) | v2sim/gen/misc.py#L184 | Add a trip to the vehicle. veh: Vehicle object trip_id: Unique trip ID depart_time: Departure time in seconds since midnight of the day. from_bind: Origin node/edge ID from_type: The type of place where the trip starts. to_bind: Destination node/edge ID to_type: The type of place where the trip will end. day: The day of the trip. -1 means the depart_time is absolute time in seconds. otherwise, depart_time is in seconds since midnight of the day. | 输入：veh, trip, day；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | veh.trips.append；hasattr；TypeError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.gen.misc.create_veh | (veh_id: str, vT: EVType, soc: float, omega: PDFuncLike=None, krel: PDFuncLike=None, kfc: PDFuncLike=None, v2g_prop: float=1.0 + 0.0001, ksc: PDFuncLike=None, kv2g: PDFuncLike=None) -> EV；(veh_id: str, vT: GVType, soc: float, omega: PDFuncLike=None, krel: PDFuncLike=None, kfc: PDFuncLike=None) -> GV；(veh_id: str, vT: Union[GVType, EVType], soc: float, omega: PDFuncLike=None, krel: PDFuncLike=None, kfc: PDFuncLike=None, v2g_prop: float=1.0 + 0.0001, ksc: PDFuncLike=None, kv2g: PDFuncLike=None)；(veh_id: str, vT: GVType, soc: float, omega: PDFuncLike=None, krel: PDFuncLike=None, kfc: PDFuncLike=None) -> GV；(veh_id: str, vT: Union[GVType, EVType], soc: float, omega: PDFuncLike=None, krel: PDFuncLike=None, kfc: PDFuncLike=None, v2g_prop: float=1.0 + 0.0001, ksc: PDFuncLike=None, kv2g: PDFuncLike=None) | v2sim/gen/misc.py#L131；v2sim/gen/misc.py#L136；v2sim/gen/misc.py#L139；v2sim/gen/misc.py#L136；v2sim/gen/misc.py#L139 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.misc.create_veh 的实现与调用链确认。；Initialize EV object；待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.misc.create_veh 的实现与调用链确认。；Initialize EV object | 输入：veh_id, vT, soc, omega, krel, kfc, v2g_prop, ksc, kv2g；输出：EV；输入：veh_id, vT, soc, omega, krel, kfc；输出：GV；输入：veh_id, vT, soc, omega, krel, kfc, v2g_prop, ksc, kv2g；输出：返回值未注解；输入：veh_id, vT, soc, omega, krel, kfc；输出：GV；输入：veh_id, vT, soc, omega, krel, kfc, v2g_prop, ksc, kv2g；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验；静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用；isinstance；EV；_impl_PDFuncLike；GV；TypeError；random.random；PDUniform；静态扫描未发现直接函数调用；isinstance；EV；_impl_PDFuncLike；GV；TypeError；random.random；PDUniform | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.gen.misc.parse_val | (s: str, unit: str) -> float | v2sim/gen/misc.py#L24 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.misc.parse_val 的实现与调用链确认。 | 输入：s, unit；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | float；endswith；ValueError；unit.lower；s.lower；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.gen.misc.random_diff | (seq: Sequence[Any], exclude: Any) | v2sim/gen/misc.py#L102 | Choose a random element from `seq` that is not equal to `exclude` | 输入：seq, exclude；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | RuntimeError；random.choice；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.gen.misc.random_diff2 | (seq: Sequence[Any], weights: List[float], exclude: Any) | v2sim/gen/misc.py#L111 | Choose a random element from `seq` that is not equal to `exclude` | 输入：seq, weights, exclude；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | RuntimeError；len；random.choices | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
