# `v2sim.gen.core`

本模块共记录 23 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.gen.core.ListSelection | class(IntEnum) | v2sim/gen/core.py#L49 | List selection method | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | random.sample；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.core.ListSelection.select | (self, lst: List[T], n: int=-1, given: List[T]=[]) -> List[T] | v2sim/gen/core.py#L55 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.core.ListSelection.select 的实现与调用链确认。 | 输入：lst, n, given；输出：List[T] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | random.sample；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.gen.core.PricingMethod | class(IntEnum) | v2sim/gen/core.py#L66 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.core.PricingMethod 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.gen.core.ProcExisting | class(IntEnum) | v2sim/gen/core.py#L22 | How to handle existing files | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | exists；unlink；self.do；Path；rename；FileExistsError；Lang.ERROR_FILE_EXISTS.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.core.ProcExisting.check | (self, path: str) | v2sim/gen/core.py#L42 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.core.ProcExisting.check 的实现与调用链确认。 | 输入：path；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | exists；self.do；Path | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.core.ProcExisting.do | (self, path: str) | v2sim/gen/core.py#L29 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.core.ProcExisting.do 的实现与调用链确认。 | 输入：path；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | unlink；Path；exists；rename；FileExistsError；Lang.ERROR_FILE_EXISTS.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.gen.core.StationFilterConfig | class | v2sim/gen/core.py#L71 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.core.StationFilterConfig 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | name.lower；name.startswith；name.endswith；lname.startswith；lname.endswith；self.not_start_with.lower；self.not_end_with.lower；self.start_with.lower；self.end_with.lower | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.core.StationFilterConfig.check | (self, name: str) -> bool | v2sim/gen/core.py#L78 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.core.StationFilterConfig.check 的实现与调用链确认。 | 输入：name；输出：bool | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | name.lower；name.startswith；name.endswith；lname.startswith；lname.endswith；self.not_start_with.lower；self.not_end_with.lower；self.start_with.lower；self.end_with.lower | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.gen.core.TrafficGenerator | class | v2sim/gen/core.py#L117 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.core.TrafficGenerator 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | DetectFiles；RoadNet.load；isinstance；args.pop_int；args.pop_str；args.pop_float；lower；self.VTrips；gen_vehs；random.seed；Element；zip；write；self._Station；params.pop_int；params.pop_float | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.core.TrafficGenerator.FCS | (self, seed: int, slots: int, *, file: str='', bus: ListSelection=ListSelection.ALL, busCount: int=-1, grid_file: str='', givenBus: List[str]=[], cs: ListSelection=ListSelection.ALL, csCount: int=-1, givenCS: List[str]=[], priceBuyMethod: PricingMethod=PricingMethod.FIXED, priceBuy: float=1.5, priceBuyIsServiceFee: bool=False) | v2sim/gen/core.py#L435 | Generate fast charging station file seed: Randomization seed slots: Number of charging piles per fast charging station bus: Bus selection method busCount: Number of buses selected, valid when the bus selection method is random givenBus: Specified bus, valid when the bus selection method is specified cs: Charging station selection method csCount: Number of charging stations selected, valid when the charging station selection method is random givenCS: Specified charging station, valid when the charging station selection method is specified priceBuyMethod: Pricing method priceBuy: Specified price (list) | 输入：seed, slots, file, bus, busCount, grid_file, givenBus, cs, csCount, givenCS, priceBuyMethod, priceBuy, priceBuyIsServiceFee；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._Station | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.core.TrafficGenerator.FCSFromArgs | (self, params: Union[str, ArgChecker]) | v2sim/gen/core.py#L642 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.core.TrafficGenerator.FCSFromArgs 的实现与调用链确认。 | 输入：params；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | isinstance；self.__StationFromArgs；ArgChecker | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.core.TrafficGenerator.GS | (self, seed: int, slots: int, *, file: str='', gs: ListSelection=ListSelection.ALL, gsCount: int=-1, givenGS: List[str]=[], priceBuyMethod: PricingMethod=PricingMethod.FIXED, priceBuyIsServiceFee: bool=False, priceBuy: float=7.0) | v2sim/gen/core.py#L541 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.core.TrafficGenerator.GS 的实现与调用链确认。 | 输入：seed, slots, file, gs, gsCount, givenGS, priceBuyMethod, priceBuyIsServiceFee, priceBuy；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._Station | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.core.TrafficGenerator.GSFromArgs | (self, params: Union[str, ArgChecker]) | v2sim/gen/core.py#L652 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.core.TrafficGenerator.GSFromArgs 的实现与调用链确认。 | 输入：params；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | isinstance；self.__StationFromArgs；ArgChecker | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.core.TrafficGenerator.SCS | (self, seed: int, slots: int, *, file: str='', bus: ListSelection=ListSelection.ALL, busCount: int=-1, grid_file: str='', givenBus: List[str]=[], cs: ListSelection=ListSelection.ALL, csCount: int=-1, givenCS: List[str]=[], priceBuyMethod: PricingMethod=PricingMethod.FIXED, priceBuyIsServiceFee: bool=False, priceBuy: float=1.5, priceSellMethod: PricingMethod=PricingMethod.FIXED, priceSellIsServiceFee: bool=False, priceSell: float=1.5) | v2sim/gen/core.py#L483 | Generate slow charging station file seed: Randomization seed slots: Number of charging piles per fast charging station bus: Bus selection method busCount: Number of buses selected, valid when the bus selection method is random givenBus: Specified bus, valid when the bus selection method is specified cs: Charging station selection method csCount: Number of charging stations selected, valid when the charging station selection method is random givenCS: Specified charging station, valid when the charging station selection method is specified priceBuyMethod: User purchase price pricing method priceBuy: Specified price (list) hasSell: Whether to sell electricity priceSellMethod: User selling price pricing method priceSell: Specified price (list) | 输入：seed, slots, file, bus, busCount, grid_file, givenBus, cs, csCount, givenCS, priceBuyMethod, priceBuyIsServiceFee, priceBuy, priceSellMethod, priceSellIsServiceFee, priceSell；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._Station | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.core.TrafficGenerator.SCSFromArgs | (self, params: Union[str, ArgChecker]) | v2sim/gen/core.py#L647 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.core.TrafficGenerator.SCSFromArgs 的实现与调用链确认。 | 输入：params；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | isinstance；self.__StationFromArgs；ArgChecker | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.core.TrafficGenerator.StationFromArgs | (self, params: Union[str, ArgChecker]) | v2sim/gen/core.py#L634 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.core.TrafficGenerator.StationFromArgs 的实现与调用链确认。 | 输入：params；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；params.pop_str；self.__StationFromArgs；ArgChecker；Exception；Lang.ERROR_UNKNOWN_CS_TYPE.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.core.TrafficGenerator.VTrips | (self, n: Union[int, Tuple[int, int]], seed: int, day_count: int=7, save: bool=True, cname: str=DEFAULT_CNAME, mode: TripsGenMode=TripsGenMode.AUTO, omega: PDFuncLike=None, krel: PDFuncLike=None, kfc: PDFuncLike=None, v2g_prop: float=1.0, ksc: PDFuncLike=None, kv2g: PDFuncLike=None) | v2sim/gen/core.py#L233 | Generate trips n: Number of vehicles seed: Randomization seed day_count: Number of days cname: Trip parameter folder mode: Generation mode, "Auto" for automatic, "TAZ" for TAZ-based, "Poly" for polygon-based routing_cache: Routing cache mode omega: PDFunc \| None = None krel: PDFunc \| None = None kfc: PDFunc \| None = None v2g_prop: Proportion of users willing to participate in V2G, for EV only ksc: PDFunc \| None = None, for EV only kv2g: PDFunc \| None = None, for EV only | 输入：n, seed, day_count, save, cname, mode, omega, krel, kfc, v2g_prop, ksc, kv2g；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | gen_vehs；self.__existing.do；gtype | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.core.TrafficGenerator.VTripsFromArgs | (self, args: Union[str, ArgChecker]) | v2sim/gen/core.py#L202 | Generate trips from command line arguments args: ArgChecker or command line | 输入：args；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；args.pop_int；args.pop_str；args.pop_float；lower；self.VTrips；ArgChecker；time.time_ns；args.empty；KeyError；Lang.ERROR_ILLEGAL_CMD.format；join；ValueError；keys；Lang.ERROR_INVALID_TRIP_GEN_MODE.format；args.to_dict | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.core.TrafficGenerator._Station | (self, seed: int, *, csv_file: str='', poly_file: str='', slots: int=10, mode: Literal['fcs', 'scs', 'gs']='fcs', bus: ListSelection=ListSelection.ALL, busCount: int=-1, grid_file: str='', givenBus: List[str]=[], station: ListSelection=ListSelection.ALL, stationCount: int=-1, givenStations: List[str]=[], priceBuyMethod: PricingMethod=PricingMethod.FIXED, priceBuyIsServiceFee: bool=False, priceBuy: float=1.0, hasSell: bool=False, priceSellMethod: PricingMethod=PricingMethod.FIXED, priceSellIsServiceFee: bool=False, priceSell: float=1.5, allowQueue: bool=True) | v2sim/gen/core.py#L260 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.core.TrafficGenerator._Station 的实现与调用链确认。 | 输入：seed, csv_file, poly_file, slots, mode, bus, busCount, grid_file, givenBus, station, stationCount, givenStations, priceBuyMethod, priceBuyIsServiceFee, priceBuy, hasSell, priceSellMethod, priceSellIsServiceFee, priceSell, allowQueue；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | random.seed；Element；zip；write；self.__existing.do；station.select；repeat；Grid.fromFile；KDTree；bus.select；root.append；open；f.readlines；split；range；sorted | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.core.TrafficGenerator.__StationFromArgs | (self, cs_type: str, params: ArgChecker) | v2sim/gen/core.py#L564 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.core.TrafficGenerator.__StationFromArgs 的实现与调用链确认。 | 输入：cs_type, params；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | params.pop_int；params.pop_float；params.pop_bool；split；time.time_ns；params.pop_str；Exception；print；len；Lang.ERROR_CANNOT_USE_TOGETHER.format；self.FCS；self.SCS；self.GS；Lang.ERROR_UNKNOWN_CS_TYPE.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.core.TrafficGenerator.__get_available | (self, mode: str) -> List[str] | v2sim/gen/core.py#L192 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.core.TrafficGenerator.__get_available 的实现与调用链确认。 | 输入：mode；输出：List[str] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.gen.core.TrafficGenerator.__init__ | (self, root: str, silent: bool=False, existing: ProcExisting=ProcExisting.BACKUP, fcs_filter_config: Optional[StationFilterConfig]=None, scs_filter_config: Optional[StationFilterConfig]=None, gs_filter_config: Optional[StationFilterConfig]=None) | v2sim/gen/core.py#L118 | Generator initialization root: Root directory silent: Whether to be silent existing: How to handle existing files | 输入：root, silent, existing, fcs_filter_config, scs_filter_config, gs_filter_config；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | DetectFiles；RoadNet.load；V2SimConfig.load；FileNotFoundError；StationFilterConfig；Grid.fromFile；print；fcs_filter_config.check；scs_filter_config.check；gs_filter_config.check；self.__rnet.getProjectorOrNone | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.gen.core.gen_5seg_price | (end_time: int, price: float) | v2sim/gen/core.py#L101 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.gen.core.gen_5seg_price 的实现与调用链确认。 | 输入：end_time, price；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | range；SegFunc；t.append；p.append；random.uniform；random.choice | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
