# `v2sim.plugins.pdn`

本模块共记录 19 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.plugins.pdn.PluginPDN | class(PluginBase[float], IGridPlugin) | v2sim/plugins/pdn.py#L57 | 把充电站聚合负荷映射到 FPowerKit 配网模型并执行约束与减载计算。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | ConfigDict；self.SetPreStep；open；self.SetPostSimulation；DetectFiles；strip；elem.get；Calculator；str；CombinedSolver；self.__sol.SetErrorSaveTo；defaultdict；chain；self.__pds.items；isinstance；self.__max_reduce_prop.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.plugins.pdn.PluginPDN.BestCost | (self) -> float | v2sim/plugins/pdn.py#L251 | Get the cost of the best generation plan, requires the last solve to be successful | 输入：无显式输入；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pdn.PluginPDN.Description | (self) -> str | v2sim/plugins/pdn.py#L59 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.pdn.PluginPDN.Description 的实现与调用链确认。 | 输入：无显式输入；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pdn.PluginPDN.ElemShouldHave | () -> ConfigDict | v2sim/plugins/pdn.py#L76 | Get the plugin configuration item list | 输入：无显式输入；输出：ConfigDict | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | ConfigDict；ConfigItem；_locale | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pdn.PluginPDN.GeneratorPlan | (self) -> Dict[str, float] | v2sim/plugins/pdn.py#L257 | Get the best generation plan, requires the last solve to be successful | 输入：无显式输入；输出：Dict[str, float] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | _sv | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pdn.PluginPDN.GetAverageMaxLoadReductionProportion | (self) -> float | v2sim/plugins/pdn.py#L229 | Get the average maximum load reduction proportion among all charging stations | 输入：无显式输入；输出：float | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__max_reduce_prop.items；sum；len；self.__pds.values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pdn.PluginPDN.GetMaxLoadReductionProportion | (self, bus: str) -> float | v2sim/plugins/pdn.py#L219 | Get the maximum load reduction proportion at the specified bus | 输入：bus；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.__max_reduce_prop.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pdn.PluginPDN.GetMaxLoadReductionProportionOfCS | (self, cs_name: str) -> float | v2sim/plugins/pdn.py#L225 | Get the maximum load reduction proportion of the specified charging station | 输入：cs_name；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.__max_reduce_prop_cs.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pdn.PluginPDN.Grid | (self) -> Grid | v2sim/plugins/pdn.py#L173 | Get the grid instance | 输入：无显式输入；输出：Grid | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pdn.PluginPDN.HasEverFailed | (self) -> bool | v2sim/plugins/pdn.py#L164 | Check if the PDN has ever failed to solve | 输入：无显式输入；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pdn.PluginPDN.Init | (self, elem: Element, inst: TrafficInst, work_dir: Path, res_dir: Path, plg_deps: 'List[PluginBase]') -> float | v2sim/plugins/pdn.py#L88 | 读取配网配置、建立求解器并绑定充电站与母线。 | 输入：elem, inst, work_dir, res_dir, plg_deps；输出：float | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.SetPreStep；open；self.SetPostSimulation；DetectFiles；strip；elem.get；Calculator；str；CombinedSolver；self.__sol.SetErrorSaveTo；defaultdict；chain；self.__pds.items；set；decs.clear；warn | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.plugins.pdn.PluginPDN.PostSimulation | (self) | v2sim/plugins/pdn.py#L239 | 在仿真结束时关闭配网插件输出并保存最终状态。 | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__fh.close；len；self.__max_reduce_prop.items；sum；print；self.__pds.values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.plugins.pdn.PluginPDN.PreStep | (self, _t: int, /, sta: PluginStatus) -> Tuple[bool, float] | v2sim/plugins/pdn.py#L177 | 在时间步前求解配网状态并在需要时计算充电负荷削减。 | 输入：_t, sta；输出：Tuple[bool, float] | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.__sol.solve；print；self.__gr.savePQofBus；self.__gr.ApplyAllESS；self.isSmartChargeEnabled；os.path.join；isinstance；chain；self.__sol.est.DecBuses.items；format；c.set_Pc_lim；x.Limit；max；self.__max_reduce_prop_cs.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.plugins.pdn.PluginPDN.Solver | (self) | v2sim/plugins/pdn.py#L169 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.pdn.PluginPDN.Solver 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pdn.PluginPDN.__create_closure | (cslist: List[CS], Sb_MVA: float) | v2sim/plugins/pdn.py#L63 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.pdn.PluginPDN.__create_closure 的实现与调用链确认。 | 输入：cslist, Sb_MVA；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | sum | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pdn.PluginPDN._load_state | (self, state: object) -> None | v2sim/plugins/pdn.py#L72 | Load the plugin state | 输入：state；输出：None | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pdn.PluginPDN._save_state | (self) -> object | v2sim/plugins/pdn.py#L68 | Save the plugin state | 输入：无显式输入；输出：object | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pdn.PluginPDN.isSmartChargeEnabled | (self) -> bool | v2sim/plugins/pdn.py#L157 | Check if smart charging is enabled | 输入：无显式输入；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | isinstance；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.plugins.pdn._sv | (x) -> float | v2sim/plugins/pdn.py#L53 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.pdn._sv 的实现与调用链确认。 | 输入：x；输出：float | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
