# `v2sim.plugins.v2g`

本模块共记录 9 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.plugins.v2g.PluginV2G | class(PluginBase[V2GRes]) | v2sim/plugins/v2g.py#L22 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.v2g.PluginV2G 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | ConfigDict；self.SetPreStep；self.SetPostStep；enumerate；isinstance；self.__pdn.isSmartChargeEnabled；RuntimeError；chain；self.__pdn.Grid.AddGen；self.__pdn.Solver.est.UpdateGrid；len；Generator；self.IsOnline；self.__inst.fcs.clear_V2G_demand；self.__inst.scs.clear_V2G_demand；ComFunc | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.v2g.PluginV2G.Description | (self) -> str | v2sim/plugins/v2g.py#L24 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.v2g.PluginV2G.Description 的实现与调用链确认。 | 输入：无显式输入；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.v2g.PluginV2G.ElemShouldHave | () -> ConfigDict | v2sim/plugins/v2g.py#L35 | Get the plugin configuration item list | 输入：无显式输入；输出：ConfigDict | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | ConfigDict | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.v2g.PluginV2G.Init | (self, elem: Element, inst: TrafficInst, work_dir: Path, res_dir: Path, plg_deps: 'List[PluginBase]') -> V2GRes | v2sim/plugins/v2g.py#L39 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.v2g.PluginV2G.Init 的实现与调用链确认。 | 输入：elem, inst, work_dir, res_dir, plg_deps；输出：V2GRes | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.SetPreStep；self.SetPostStep；enumerate；isinstance；self.__pdn.isSmartChargeEnabled；RuntimeError；chain；self.__pdn.Grid.AddGen；self.__pdn.Solver.est.UpdateGrid；len；Generator；ComFunc；self.__get_cap | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.v2g.PluginV2G.__get_cap | (self, i: int) | v2sim/plugins/v2g.py#L77 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.v2g.PluginV2G.__get_cap 的实现与调用链确认。 | 输入：i；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.IsOnline | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.v2g.PluginV2G._load_state | (self, state: object) -> None | v2sim/plugins/v2g.py#L31 | Load the plugin state | 输入：state；输出：None | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.v2g.PluginV2G._save_state | (self) -> object | v2sim/plugins/v2g.py#L27 | Save the plugin state | 输入：无显式输入；输出：object | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.v2g.PluginV2G._work | (self, _t: int, /, sta: PluginStatus) -> Tuple[bool, List[float]] | v2sim/plugins/v2g.py#L93 | Get the V2G demand power of all bus with slow charging stations at time _t, unit kWh/s, 3.6MW=3600kW=1kWh/s | 输入：_t, sta；输出：Tuple[bool, List[float]] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__inst.fcs.clear_V2G_demand；self.__inst.scs.clear_V2G_demand；self.__inst.fcs.set_V2G_demand；self.__inst.scs.set_V2G_demand；f；sum；self.__pdn.Grid.Gen | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.v2g.PluginV2G._work_post | (self, _t: int, /, sta: PluginStatus) -> Tuple[bool, List[float]] | v2sim/plugins/v2g.py#L83 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.v2g.PluginV2G._work_post 的实现与调用链确认。 | 输入：_t, sta；输出：Tuple[bool, List[float]] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.IsOnline；chain；self.__inst.fcs.get_V2G_cap；self.__inst.scs.get_V2G_cap | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
