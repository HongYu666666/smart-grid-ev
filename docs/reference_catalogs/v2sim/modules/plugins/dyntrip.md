# `v2sim.plugins.dyntrip`

本模块共记录 8 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | v2sim.plugins.dyntrip.PluginDynamicTrip | class(PluginBase[None]) | v2sim/plugins/dyntrip.py#L12 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.dyntrip.PluginDynamicTrip 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | ConfigDict；open；self.__file.write；self.SetPreStep；self.SetPostSimulation；self.__file.close；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.dyntrip.PluginDynamicTrip.Description | (self) -> str | v2sim/plugins/dyntrip.py#L14 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.dyntrip.PluginDynamicTrip.Description 的实现与调用链确认。 | 输入：无显式输入；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.dyntrip.PluginDynamicTrip.ElemShouldHave | () -> ConfigDict | v2sim/plugins/dyntrip.py#L25 | Get the plugin configuration item list | 输入：无显式输入；输出：ConfigDict | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | ConfigDict | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.dyntrip.PluginDynamicTrip.Init | (self, elem: Element, inst: TrafficInst, work_dir: Path, res_dir: Path, plg_deps: 'List[PluginBase]') -> None | v2sim/plugins/dyntrip.py#L29 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.dyntrip.PluginDynamicTrip.Init 的实现与调用链确认。 | 输入：elem, inst, work_dir, res_dir, plg_deps；输出：None | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | open；self.__file.write；self.SetPreStep；self.SetPostSimulation；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.dyntrip.PluginDynamicTrip._close | (self) | v2sim/plugins/dyntrip.py#L37 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.dyntrip.PluginDynamicTrip._close 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.__file.close | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.dyntrip.PluginDynamicTrip._load_state | (self, state: object) -> None | v2sim/plugins/dyntrip.py#L21 | Load the plugin state | 输入：state；输出：None | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.dyntrip.PluginDynamicTrip._save_state | (self) -> object | v2sim/plugins/dyntrip.py#L17 | Save the plugin state | 输入：无显式输入；输出：object | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.dyntrip.PluginDynamicTrip._work | (self, _t: int, /, sta: PluginStatus) -> Tuple[bool, None] | v2sim/plugins/dyntrip.py#L40 | Get the V2G demand power of all bus with slow charging stations at time _t, unit kWh/s, 3.6MW=3600kW=1kWh/s | 输入：_t, sta；输出：Tuple[bool, None] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
