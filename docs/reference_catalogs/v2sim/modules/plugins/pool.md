# `v2sim.plugins.pool`

本模块共记录 31 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | v2sim.plugins.pool.GetInternalPlugins | () | v2sim/plugins/pool.py#L18 | Get internal plugin list Returns: A list containing PluginExports of internal plugins | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _internal_plugins.items；plg_ret.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.plugins.pool.PluginError | class(Exception) | v2sim/plugins/pool.py#L42 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.pool.PluginError 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.plugins.pool.PluginHelper | class | v2sim/plugins/pool.py#L46 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.pool.PluginHelper 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | is_dir；dest_dir.mkdir；absolute；PluginHelper.has_plugin；dest_path_link.exists；Path；is_file；dest_path_py.exists；PluginHelper.get_lang_file；shutil.copy2；dest_path_link.unlink；open；f.write；src_lang.unlink；dest_path_py.unlink；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginHelper.add_plugin | (file: Union[str, Path], as_link: bool=False) -> bool | v2sim/plugins/pool.py#L67 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.pool.PluginHelper.add_plugin 的实现与调用链确认。 | 输入：file, as_link；输出：bool | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | dest_dir.mkdir；absolute；PluginHelper.has_plugin；PluginHelper.get_lang_file；shutil.copy2；Path；open；f.write；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginHelper.del_plugin | (name: str) -> bool | v2sim/plugins/pool.py#L90 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.pool.PluginHelper.del_plugin 的实现与调用链确认。 | 输入：name；输出：bool | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | dest_path_link.exists；dest_path_link.unlink；PluginHelper.get_lang_file；dest_path_py.exists；src_lang.unlink；dest_path_py.unlink | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginHelper.get_lang_file | (src_path: Union[str, Path]) -> Union[Path, None] | v2sim/plugins/pool.py#L48 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.pool.PluginHelper.get_lang_file 的实现与调用链确认。 | 输入：src_path；输出：Union[Path, None] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | is_dir；Path；is_file | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginHelper.has_plugin | (name: str) -> bool | v2sim/plugins/pool.py#L59 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.pool.PluginHelper.has_plugin 的实现与调用链确认。 | 输入：name；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | dest_path_link.exists；dest_path_py.exists | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.plugins.pool.PluginMan | class | v2sim/plugins/pool.py#L170 | 装配并调度当前案例启用的全部插件。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | getroot；self.__curPlugins.values；self.__curPlugins.items；states.items；CheckPyVersion；Lang.PY_VERSION_MISMATCH_PLG.format；Lang.PICKLER_MISMATCH_PLG.format；isinstance；self.LoadStates；self._unsafe_load_states；exists；print；Path；PluginError；self.Add；p._presim | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.plugins.pool.PluginMan.Add | (self, plugin: PluginBase) | v2sim/plugins/pool.py#L236 | Add new plugin to current plugin list | 输入：plugin；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | PluginError；Lang.PLG_ALREADY_EXISTS.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginMan.GetPluginByName | (self, name: str) -> PluginBase | v2sim/plugins/pool.py#L242 | Get plugin by name | 输入：name；输出：PluginBase | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginMan.GetPlugins | (self) -> Dict[str, PluginBase] | v2sim/plugins/pool.py#L246 | Get all plugins | 输入：无显式输入；输出：Dict[str, PluginBase] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginMan.LoadStates | (self, states: Dict[str, object]) | v2sim/plugins/pool.py#L257 | Load all plugin states | 输入：states；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | states.items；_load_state；PluginError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginMan.LoadStatesFromFile | (self, state_file: Path) | v2sim/plugins/pool.py#L276 | Load plugin states from file | 输入：state_file；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self._unsafe_load_states；gzip.open；pickle.load | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginMan.PostSimulationAll | (self) | v2sim/plugins/pool.py#L217 | 在仿真结束后调用全部插件的收尾钩子。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.__curPlugins.values；p._postsim | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.plugins.pool.PluginMan.PostStepAll | (self, _t: int) -> Dict[str, object] | v2sim/plugins/pool.py#L229 | 在每个时间步后调用全部在线插件并汇总结果。 | 输入：_t；输出：Dict[str, object] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__curPlugins.items；p._postcall | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.plugins.pool.PluginMan.PreSimulationAll | (self) | v2sim/plugins/pool.py#L212 | 在仿真开始前依序调用全部在线插件的准备钩子。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.__curPlugins.values；p._presim | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.plugins.pool.PluginMan.PreStepAll | (self, _t: int) -> Dict[str, object] | v2sim/plugins/pool.py#L222 | 在每个时间步前调用全部在线插件并汇总结果。 | 输入：_t；输出：Dict[str, object] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__curPlugins.items；p._precall | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| method | v2sim.plugins.pool.PluginMan.SaveStates | (self) -> Dict[str, object] | v2sim/plugins/pool.py#L250 | Save all plugin states | 输入：无显式输入；输出：Dict[str, object] | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.__curPlugins.items；p._save_state | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginMan.__init__ | (self, plg_xml: Optional[str], res_dir: Path, inst: TrafficInst, plugin_pool: PluginPool, no_plg: Optional[List[str]]=None, initial_state: Optional[Dict[str, object]]=None) | v2sim/plugins/pool.py#L171 | Load plugins from file plg_xml: Plugin configuration file path, None means not load res_dir: Result directory inst: Traffic simulation instance no_plg: Plugins not to load plugin_pool: Available plugin pool initial_state: Initial plugin states, if specified, load plugin states from it | 输入：plg_xml, res_dir, inst, plugin_pool, no_plg, initial_state；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | getroot；exists；print；Path；PluginError；self.Add；Lang.PLG_NOT_EXIST.format；ElementTree；Lang.PLG_NOT_EXIST_OR_BAD.format；deps.append；plugin_type；len；itm.attrib.get；Lang.PLG_INVALID_PLUGIN.format；join；Lang.PLG_DEPS_NOT_LOADED.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginMan._unsafe_load_states | (self, d: Dict[str, Any]) | v2sim/plugins/pool.py#L265 | Internal method, do not call by users! | 输入：d；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | CheckPyVersion；Lang.PY_VERSION_MISMATCH_PLG.format；Lang.PICKLER_MISMATCH_PLG.format；isinstance；self.LoadStates；PyVersion | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| class | v2sim.plugins.pool.PluginPool | class | v2sim/plugins/pool.py#L110 | Plugin pool | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__curPlugins.items；list；self.__curPlugins.__contains__；self._Register；_internal_plugins.items；self.__curPlugins.keys；PluginError；issubclass；Lang.PLG_REGISTERED.format；isinstance；Lang.PLG_NOT_SUBCLASS.format；print；Lang.PLG_DEPS_NOT_REGISTERED.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginPool.GetAllPluginNames | (self) -> List[str] | v2sim/plugins/pool.py#L144 | Get all plugin names | 输入：无显式输入；输出：List[str] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | list；self.__curPlugins.keys | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginPool.GetAllPlugins | (self) -> Dict[str, Tuple[type, List[str]]] | v2sim/plugins/pool.py#L140 | Get all plugins | 输入：无显式输入；输出：Dict[str, Tuple[type, List[str]]] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginPool.GetPluginDependencies | (self, name: str) -> List[str] | v2sim/plugins/pool.py#L136 | Get plugin dependencies by name | 输入：name；输出：List[str] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginPool.GetPluginType | (self, name: str) -> type | v2sim/plugins/pool.py#L132 | Get plugin type by name | 输入：name；输出：type | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginPool.Register | (self, name: str, plugin: type, deps: List[str]) | v2sim/plugins/pool.py#L161 | Register new plugin to plugin pool | 输入：name, plugin, deps；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self._Register；issubclass；PluginError；Lang.PLG_NOT_SUBCLASS.format；Lang.PLG_DEPS_NOT_REGISTERED.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginPool._Register | (self, name: str, plugin: type, deps: List[str]) | v2sim/plugins/pool.py#L152 | Register new plugin to plugin pool without checking dependencies | 输入：name, plugin, deps；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | PluginError；Lang.PLG_REGISTERED.format；isinstance | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginPool.__contains__ | (self, name: str) -> bool | v2sim/plugins/pool.py#L148 | Check if plugin exists | 输入：name；输出：bool | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | self.__curPlugins.__contains__ | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginPool.__getitem__ | (self, name: str) -> Tuple[type, List[str]] | v2sim/plugins/pool.py#L128 | Get plugin by name | 输入：name；输出：Tuple[type, List[str]] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | v2sim.plugins.pool.PluginPool.__init__ | (self, use_internal_plugins: bool=True) | v2sim/plugins/pool.py#L112 | Initialize use_internal_plugins: Whether to load internal plugins | 输入：use_internal_plugins；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.__curPlugins.items；_internal_plugins.items；self._Register；print；PluginError；Lang.PLG_DEPS_NOT_REGISTERED.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | v2sim.plugins.pool.RegPlugin | (name: str, dependencies: Optional[List[str]]=None) | v2sim/plugins/pool.py#L30 | 待人工补充：源码未提供可直接确认的功能说明；需结合 v2sim.plugins.pool.RegPlugin 的实现与调用链确认。 | 输入：name, dependencies；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | issubclass；PluginError；Lang.PLG_NOT_SUBCLASS.format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
