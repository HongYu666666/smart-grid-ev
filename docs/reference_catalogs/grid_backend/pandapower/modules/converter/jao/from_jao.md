# `pandapower.converter.jao.from_jao`

本模块共记录 20 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.converter.jao.from_jao._add_bus_geo | (net: pandapowerNet, line_geo_data: pd.DataFrame) -> None | pandapower/converter/jao/from_jao.py#L562 | Adds geodata to the buses. The function needs to handle cases where line_geo_data does not include no or multiple geodata per bus. Primarly, the geodata are allocate via EIC Code names, if ambigous, names are considered. | 输入：net, line_geo_data；输出：None | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | line_geo_data.pivot_table；pd.MultiIndex.from_frame；pd.concat；net.line.EIC_Code.duplicated；net.line.name.duplicated；len；set_axis；pd.Series；pd.DataFrame；np.all；_add_bus_geo_inner；lgd_EIC_bus.set_axis；lgd_name_bus.set_axis；logger.error；any；is_tieline.all | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao._allocate_trafos_to_buses_and_create_buses | (net: pandapowerNet, data: dict[str, pd.DataFrame], bus_idx: pd.Series, vn_hv_kv: np.ndarray, vn_lv_kv: np.ndarray, rel_deviation_threshold_for_trafo_bus_creation: float=0.2, log_rel_vn_deviation: float=0.12, **kwargs) -> pd.DataFrame | pandapower/converter/jao/from_jao.py#L695 | Provides a DataFrame of data to allocate transformers to the buses according to their location names. If locations of transformers do not exist due to the data of the lines and tielines sheets, additional buses are created. If locations exist but have a far different voltage level than the transformer, either a warning is logged or additional buses are created according to rel_deviation_threshold_for_trafo_bus_creation and log_rel_vn_deviation. | 输入：net, data, bus_idx, vn_hv_kv, vn_lv_kv, rel_deviation_threshold_for_trafo_bus_creation, log_rel_vn_deviation, **kwargs；输出：pd.DataFrame | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；_find_trafo_locations；pd.DataFrame；astype；copy；list；pd.concat；logger.warning；np.ones；pd.Series；name_vn_series.isin；np.array；_drop_duplicates_and_join_TSO；create_buses；range；len | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao._create_buses_from_line_data | (net: pandapowerNet, data: dict[str, pd.DataFrame]) -> None | pandapower/converter/jao/from_jao.py#L277 | Creates buses to the pandapower net using information from the lines and tielines sheets (excel file). | 输入：net, data；输出：None | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pd.DataFrame；deepcopy；_drop_duplicates_and_join_TSO；create_buses；any；len；AssertionError；str；float；set_axis；pd.concat | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao._create_lines | (net: pandapowerNet, data: dict[str, pd.DataFrame], max_i_ka_fillna: Union[float, int]) -> None | pandapower/converter/jao/from_jao.py#L301 | Creates lines to the pandapower net using information from the lines and tielines sheets (excel file). | 输入：net, data, max_i_ka_fillna；输出：None | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _get_bus_idx；np.isclose；np.isnan；create_lines_from_parameters；sum；logger.warning；list；fillna；key.lower；tuple；xs；zip | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao._create_transformers_and_buses | (net: pandapowerNet, data: dict[str, pd.DataFrame], **kwargs) -> None | pandapower/converter/jao/from_jao.py#L347 | Creates transformers to the pandapower net using information from the transformers sheet (excel file). | 输入：net, data, **kwargs；输出：None | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _get_bus_idx；_get_transformer_voltages；_allocate_trafos_to_buses_and_create_buses；np.sqrt；set_axis；_get_float_column；create_transformers_from_parameters；max_i_a.isnull；np.isclose；np.sign；astype；xs；str.strip；str.split；replace | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao._data_correction | (data: dict[str, pd.DataFrame], html_str: Optional[str], max_i_ka_fillna: Union[float, int]) -> Optional[str] | pandapower/converter/jao/from_jao.py#L144 | Corrects input data in particular with regard to obvious weaknesses in the data provided, such as inconsistent spellings and missing necessary information | 输入：data, html_str, max_i_ka_fillna；输出：Optional[str] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _multi_str_repl；apply；str.replace；astype；reset_index；pd.MultiIndex.from_arrays；replace；_float_col_comma_correction；nonnull_taps.str.startswith；nonnull_taps.str.endswith；nonnull_taps.str.contains；is_object_dtype；str.strip；taps.apply；columns.to_frame；str.startswith | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao._drop_duplicates_and_join_TSO | (bus_df: pd.DataFrame) -> pd.DataFrame | pandapower/converter/jao/from_jao.py#L885 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.converter.jao.from_jao._drop_duplicates_and_join_TSO 的实现与调用链确认。 | 输入：bus_df；输出：pd.DataFrame | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | bus_df.drop_duplicates；agg；any；AssertionError；bus_df.groupby；bus_df.duplicated；join | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao._fill_geo_at_one_sided_branches_without_geo_extent | (net: pandapowerNet) | pandapower/converter/jao/from_jao.py#L925 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.converter.jao.from_jao._fill_geo_at_one_sided_branches_without_geo_extent 的实现与调用链确认。 | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _check_geo_availablitiy；set_line_geodata_from_bus_geodata；zip；len；net.bus.geo.isnull；net.line.from_bus.isin；net.line.to_bus.isin；net.trafo.hv_bus.isin；net.trafo.lv_bus.isin | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao._find_trafo_locations | (trafo_bus_names, bus_location_names) | pandapower/converter/jao/from_jao.py#L841 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.converter.jao.from_jao._find_trafo_locations 的实现与调用链确认。 | 输入：trafo_bus_names, bus_location_names；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | replace；trafo_bus_names_expended.map；str.strip；trafo_bus_names_expended.apply；trafo_bus_names_joined.isin；trafo_bus_names_longest_part.isin；pd.concat；ValueError；fillna；any；max；len；sum；agg；trafo_bus_names.str.split；str.split | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao._float_col_comma_correction | (data: dict[str, pd.DataFrame], key: str, col_names: list) | pandapower/converter/jao/from_jao.py#L674 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.converter.jao.from_jao._float_col_comma_correction 的实现与调用链确认。 | 输入：data, key, col_names；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pd.to_numeric；str.replace；astype | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao._get_bus_idx | (net: pandapowerNet) -> pd.Series | pandapower/converter/jao/from_jao.py#L901 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.converter.jao.from_jao._get_bus_idx 的实现与调用链确认。 | 输入：net；输出：pd.Series | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | set_index；reset_index；rename_axis | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao._get_float_column | (df, col_tuple, fill=0) | pandapower/converter/jao/from_jao.py#L895 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.converter.jao.from_jao._get_float_column 的实现与调用链确认。 | 输入：df, col_tuple, fill；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | fillna；series.astype | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao._get_transformer_voltages | (data: dict[str, pd.DataFrame], bus_idx: pd.Series) -> tuple[np.ndarray, np.ndarray] | pandapower/converter/jao/from_jao.py#L680 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.converter.jao.from_jao._get_transformer_voltages 的实现与调用链确认。 | 输入：data, bus_idx；输出：tuple[np.ndarray, np.ndarray] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.max；np.min；is_integer_dtype；vn_hv_kv.astype；vn_lv_kv.astype；list | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao._invent_connections_between_grid_groups | (net: pandapowerNet, minimal_trafo_invention: bool=False, **kwargs) -> None | pandapower/converter/jao/from_jao.py#L412 | Adds connections between islanded grid groups via: | 输入：net, minimal_trafo_invention, **kwargs；输出：None | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | get_grid_groups；_get_bus_idx；sort_index；pd.Series；location_names.str.replace；pd.MultiIndex.from_arrays；net.bus.name.str.split；buses_with_single_base.items；bus_idx.index.get_level_values；location_names.duplicated；grid_groups_at_location.drop_duplicates；sort_values；values.flatten；copy；pd.concat；name.str.contains | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao._lng_lat_to_df | (dict_: dict, line_EIC: str, line_name: str) -> pd.DataFrame | pandapower/converter/jao/from_jao.py#L916 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.converter.jao.from_jao._lng_lat_to_df 的实现与调用链确认。 | 输入：dict_, line_EIC, line_name；输出：pd.DataFrame | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | pd.DataFrame | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao._multi_str_repl | (st: str, repl: list[tuple]) -> str | pandapower/converter/jao/from_jao.py#L957 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.converter.jao.from_jao._multi_str_repl 的实现与调用链确认。 | 输入：st, repl；输出：str | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | st.replace | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao._parse_html_str | (html_str: str) -> pd.DataFrame | pandapower/converter/jao/from_jao.py#L232 | Converts ths geodata from the html file (information hidden in the string), from Lines in particular, to a DataFrame that can be used later in _add_bus_geo() | 输入：html_str；输出：pd.DataFrame | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | find；json.loads；pd.Series；pd.concat；st.find；html_str.find；len；AssertionError；_filter_name；str.strip；range；_lng_lat_to_df；enumerate | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao.drop_islanded_grid_groups | (net: pandapowerNet, min_bus_number: Union[int, str], **kwargs) -> None | pandapower/converter/jao/from_jao.py#L521 | Drops grid groups that are islanded and include a number of buses below min_bus_number. | 输入：net, min_bus_number, **kwargs；输出：None | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | get_grid_groups；reduce；drop_buses；logger.info；set；max；_grid_groups_to_drop_by_min_bus_number；isinstance；grid_groups.buses.apply；NotImplementedError；len；grid_groups_to_drop.n_buses.sum；x.isdisjoint | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao.from_jao | (excel_file_path: str, html_file_path: Optional[str], extend_data_for_grid_group_connections: bool, drop_grid_groups_islands: bool=False, apply_data_correction: bool=True, max_i_ka_fillna: Union[float, int]=999, **kwargs) -> pandapowerNet | pandapower/converter/jao/from_jao.py#L24 | Converts European (Core) EHV grid data provided by JAO (Joint Allocation Office), the "Single Allocation Platform (SAP) for all European Transmission System Operators (TSOs) that operate in accordance to EU legislation". | 输入：excel_file_path, html_file_path, extend_data_for_grid_group_connections, drop_grid_groups_islands, apply_data_correction, max_i_ka_fillna, **kwargs；输出：pandapowerNet | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | pd.read_excel；create_empty_network；_create_buses_from_line_data；_create_lines；_create_transformers_and_buses；_data_correction；_invent_connections_between_grid_groups；drop_islanded_grid_groups；_add_bus_geo；set_line_geodata_from_bus_geodata；open；f.read；_parse_html_str；kwargs.get；logger.error；os.path.splitext | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.jao.from_jao.get_grid_groups | (net: pandapowerNet, **kwargs) -> pd.DataFrame | pandapower/converter/jao/from_jao.py#L906 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.converter.jao.from_jao.get_grid_groups 的实现与调用链确认。 | 输入：net, **kwargs；输出：pd.DataFrame | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pd.DataFrame；apply；kwargs.keys；kwargs.pop；connected_components；create_nxgraph | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
