# `pandapower.converter.cim.pp_tools`

本模块共记录 4 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.converter.cim.pp_tools._set_column_to_type | (input_df: pd.DataFrame, column: str, data_type: str) -> None | pandapower/converter/cim/pp_tools.py#L20 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.converter.cim.pp_tools._set_column_to_type 的实现与调用链确认。 | 输入：input_df, column, data_type；输出：None | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | astype；logger.error；logger.exception | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.cim.pp_tools.add_slack_and_lines_to_boundary_nodes | (net: pandapowerNet, voltage_levels: List[int]=None) | pandapower/converter/cim/pp_tools.py#L79 | Add lines with low impedance and a slack to the boundary nodes with the highest voltage. :param net: The pandapower network :param voltage_levels: The voltage levels to add lines and slacks. For each given voltage level, lines and one slack will be connected to the corresponding boundary nodes. Optional, default: Highest voltage level from boundary nodes. :return: | 输入：net, voltage_levels；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | cim_tools.get_pp_net_special_columns_dict；create_std_type；max；logger.info；create_bus；create_ext_grid；create_lines；len；str | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.cim.pp_tools.get_not_existing_column | (df: pd.DataFrame) -> str | pandapower/converter/cim/pp_tools.py#L112 | Get a not existing column name in a DataFrame :param df: The DataFrame where to get a not existing column :return: The name of a not existing column, e.g. temp_col_0 or temp_col_1 | 输入：df；输出：str | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | str | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.converter.cim.pp_tools.set_pp_col_types | (net: Union[pandapowerNet, Dict], ignore_errors: bool=False) -> pandapowerNet | pandapower/converter/cim/pp_tools.py#L28 | Set the data types for some columns from pandapower assets. This mainly effects bus columns (to int, e.g. sgen.bus or line.from_bus) and in_service and other boolean columns (to bool, e.g. line.in_service or gen.slack). :param net: The pandapower network to update the data types. :param ignore_errors: Ignore problems if set to True (no warnings displayed). Optional, default: False. :return: The pandapower network with updated data types. | 输入：net, ignore_errors；输出：pandapowerNet | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | time.time；get_structure_dict；logger.info；hasattr；_set_column_to_type；logger.warning | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
