# `pandapower.converter.cim.pp_classes`

本模块共记录 5 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | pandapower.converter.cim.pp_classes.PandapowerDiagnostic | class | pandapower/converter/cim/pp_classes.py#L14 | Create a pandapower diagnostic dictionary with CIM IDs instead of pandapower IDs. :param net: The pandapower network. :param diagnostic_: The pandapower diagnostic. If None a pp.diagnostic(net) will be run. Optional, default: None. :return: The pandapower diagnostic with CIM IDs. | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | logging.getLogger；cim_tools.get_pp_net_special_columns_dict；isinstance；self.diagnostic.items；hasattr；self.logger.info；diagnostic；self._rec_replace_pp_diagnostic_with_cim_ids；items；open；json.dump；input_obj.items；list；return_obj.append；one_prf_item.keys；tuple | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.converter.cim.pp_classes.PandapowerDiagnostic.__init__ | (self, net: pandapowerNet, diagnostic_: Dict=None) | pandapower/converter/cim/pp_classes.py#L21 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.converter.cim.pp_classes.PandapowerDiagnostic.__init__ 的实现与调用链确认。 | 输入：net, diagnostic_；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | logging.getLogger | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.converter.cim.pp_classes.PandapowerDiagnostic._rec_replace_pp_diagnostic_with_cim_ids | (self, input_obj, element_type: str=None) | pandapower/converter/cim/pp_classes.py#L26 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.converter.cim.pp_classes.PandapowerDiagnostic._rec_replace_pp_diagnostic_with_cim_ids 的实现与调用链确认。 | 输入：input_obj, element_type；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | cim_tools.get_pp_net_special_columns_dict；isinstance；input_obj.items；return_obj.append；self._rec_replace_pp_diagnostic_with_cim_ids；list；tuple | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.converter.cim.pp_classes.PandapowerDiagnostic.replace_pp_diagnostic_with_cim_ids | (self) -> Dict | pandapower/converter/cim/pp_classes.py#L69 | Create a pandapower diagnostic dictionary with CIM IDs instead of pandapower IDs. :param net: The pandapower network. :param diagnostic: The pandapower diagnostic. If None a pp.diagnostic(net) will be run. Optional, default: None. :return: The pandapower diagnostic with CIM IDs. | 输入：无显式输入；输出：Dict | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.diagnostic.items；hasattr；diagnostic；self._rec_replace_pp_diagnostic_with_cim_ids；items；list；one_prf_item.keys | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.converter.cim.pp_classes.PandapowerDiagnostic.serialize | (self, diagnostic: Dict, path_to_store: str) | pandapower/converter/cim/pp_classes.py#L88 | Serialize a pandapower diagnostic as json to disk. :param diagnostic: The pandapower diagnostic dictionary. :param path_to_store: The path to store the json on the disk. :return: | 输入：diagnostic, path_to_store；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.logger.info；open；json.dump | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
