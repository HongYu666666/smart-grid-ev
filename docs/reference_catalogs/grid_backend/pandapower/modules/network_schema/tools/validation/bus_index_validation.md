# `pandapower.network_schema.tools.validation.bus_index_validation`

本模块共记录 3 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.network_schema.tools.validation.bus_index_validation._bus_index_validation | (element: str, net: pandapowerNet) | pandapower/network_schema/tools/validation/bus_index_validation.py#L127 | Validates that all bus references in a network element exist in the corresponding bus tables. | 输入：element, net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | bus_columns.append；validate；col.lower；_create_multi_column_reference_schema | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.network_schema.tools.validation.bus_index_validation._create_index_validation_check | (reference_df: pd.DataFrame, column_name: str, element_name: str) -> pa.Check | pandapower/network_schema/tools/validation/bus_index_validation.py#L7 | Creates a pandera check function that validates if values exist in the index of a reference dataframe. | 输入：reference_df, column_name, element_name；输出：pa.Check | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；pa.Check；series.isin；mask.all；unique；values.tolist；ValueError；failing_values.tolist | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.network_schema.tools.validation.bus_index_validation._create_multi_column_reference_schema | (reference_df: pd.DataFrame, columns_to_validate: list[str], element_name: str) -> pa.DataFrameSchema | pandapower/network_schema/tools/validation/bus_index_validation.py#L69 | Creates a pandera schema that validates multiple columns against the index of a reference dataframe. | 输入：reference_df, columns_to_validate, element_name；输出：pa.DataFrameSchema | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pa.DataFrameSchema；pa.Column；_create_index_validation_check | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
