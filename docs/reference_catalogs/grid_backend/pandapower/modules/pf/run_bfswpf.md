# `pandapower.pf.run_bfswpf`

本模块共记录 6 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.pf.run_bfswpf._bfswpf | (DLF, bus, gen, branch, baseMVA, Ybus, Sbus, V0, ref, pv, pq, buses_ordered_bfs_nets, options, **kwargs) | pandapower/pf/run_bfswpf.py#L204 | distribution power flow solution according to [1] :param DLF: direct-Load-Flow matrix which relates bus current injections to voltage drops from the root bus | 输入：DLF, bus, gen, branch, baseMVA, Ybus, Sbus, V0, ref, pv, pq, buses_ordered_bfs_nets, options, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；_makeYsh_bfsw；np.zeros；np.ones；enumerate；V0.copy；np.isin；np.conj；print；_evaluate_Fx；_check_for_convergence；flatten；np.all；makeSbus；qg_min_lim.any；qg_max_lim.any | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pf.run_bfswpf._get_bibc_bcbv | (ppci, options, bus, branch, graph) | pandapower/pf/run_bfswpf.py#L159 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.pf.run_bfswpf._get_bibc_bcbv 的实现与调用链确认。 | 输入：ppci, options, bus, branch, graph；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；_make_bibc_bcbv | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pf.run_bfswpf._get_options | (options) | pandapower/pf/run_bfswpf.py#L347 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.pf.run_bfswpf._get_options 的实现与调用链确认。 | 输入：options；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pf.run_bfswpf._makeYsh_bfsw | (bus, branch, baseMVA) | pandapower/pf/run_bfswpf.py#L175 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.pf.run_bfswpf._makeYsh_bfsw 的实现与调用链确认。 | 输入：bus, branch, baseMVA；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.bincount；real.astype；np.conj | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pf.run_bfswpf._make_bibc_bcbv | (bus, branch, graph) | pandapower/pf/run_bfswpf.py#L27 | performs depth-first-search bus ordering and creates Direct Load Flow (DLF) matrix which establishes direct relation between bus current injections and voltage drops from each bus to the root bus | 输入：bus, branch, graph；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；graph.copy；list；real.astype；dict；branches_ind_dict.update；csr_matrix；transpose；zip；csgraph.breadth_first_order；buses_ordered_bfs_nets.append；csgraph.breadth_first_tree；enumerate；toarray；range；np.sort | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pf.run_bfswpf._run_bfswpf | (ppci, options, **kwargs) | pandapower/pf/run_bfswpf.py#L357 | SPARSE version of distribution power flow solution according to [1] :References: [1] Jen-Hao Teng, "A Direct Approach for Distribution System Load Flow Solutions", IEEE Transactions on Power Delivery, vol. 18, no. 3, pp. 882-887, July 2003. | 输入：ppci, options, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | perf_counter；_get_pf_variables_from_ppci；_get_options；_import_numba_extensions_if_flag_is_true；makeSbus；_get_Y_bus；real.astype；csr_matrix；any；_bfswpf；pfsoln；_store_results_from_pf_in_ppci；G_trees.append；_get_bibc_bcbv；branch.copy；makeYbus | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
