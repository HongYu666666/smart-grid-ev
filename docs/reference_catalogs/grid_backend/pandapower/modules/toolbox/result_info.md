# `pandapower.toolbox.result_info`

本模块共记录 16 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.toolbox.result_info._check_overlapping_constraints | (opf_task_overview) | pandapower/toolbox/result_info.py#L282 | Logs variables where the minimum constraint is bigger than the maximum constraint. | 输入：opf_task_overview；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；items；logger.error；min；range；any；str；replace；AssertionError；overlap.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.result_info._cluster_same_floats | (df: pd.DataFrame, subset: Collection[str] \| None=None, **kwargs) -> pd.DataFrame | pandapower/toolbox/result_info.py#L240 | Clusters indices with close values. The values of df[subset] must be numericals. | 输入：df, subset, **kwargs；输出：pd.DataFrame | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | any；pd.DataFrame；astype；enumerate；logger.error；columns.tolist；np.empty；sum；df.index.duplicated；df.duplicated；list；df.select_dtypes；len；all；np.isclose | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.result_info._determine_costs_dict | (net: pandapowerNet, opf_task_overview: dict) | pandapower/toolbox/result_info.py#L196 | Determines which flexibilities do not have costs in the net. Each element is considered as one, i.e. if ext_grid 0, for instance, is flexible in both, P and Q, and has one cost entry for P, it is not considered as 'flexibilities_without_costs'. | 输入：net, opf_task_overview；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；set；list；chain；astype；logger.warning；str；sorted | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.result_info._determine_flexibilities_dict | (net: pandapowerNet, data: dict, delta_pq: float, **kwargs) | pandapower/toolbox/result_info.py#L68 | Determines which flexibilities exists in the net. | 输入：net, data, delta_pq, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | tuple；zip；_cluster_same_floats；len；all；_find_idx_without_numerical_difference；set；isin；pd.Series | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.result_info._determine_network_constraints_dict | (net: pandapowerNet, data: dict, **kwargs) | pandapower/toolbox/result_info.py#L165 | Determines which flexibilities exists in the net. | 输入：net, data, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；_cluster_same_floats；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.result_info._find_idx_without_numerical_difference | (df, column1, column2, delta, idx=None, equal_nan=False) | pandapower/toolbox/result_info.py#L128 | Returns indices where comlumn1 and column2 have a numerical difference bigger than delta. | 输入：df, column1, column2, delta, idx, equal_nan；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | idx.difference；any；idx_without_null.difference；isnull；abs | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.result_info._get_keys_and_elements_from_opf_task_dict | (dict_) | pandapower/toolbox/result_info.py#L353 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.toolbox.result_info._get_keys_and_elements_from_opf_task_dict 的实现与调用链确认。 | 输入：dict_；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | list；sorted；join；np.array；np.argsort；c.isupper | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.result_info._log_opf_task_overview | (opf_task_overview) | pandapower/toolbox/result_info.py#L303 | Logs OPF task information. | 输入：opf_task_overview；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | opf_task_overview.items；logger.info；isinstance；_get_keys_and_elements_from_opf_task_dict；zip；AssertionError；len；list；NotImplementedError；chain；symmetric_difference；set | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.result_info.clear_result_tables | (net) | pandapower/toolbox/result_info.py#L406 | Clears all ``res_`` DataFrames in net. | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；key.startswith；drop | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.result_info.compute_switch_flows | (net) | pandapower/toolbox/result_info.py#L415 | Compute power flow through zero-impedance bus-bus switches via nodal balance. | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | get；defaultdict；enumerate；fused_groups.items；set；multi_groups.items；UserWarning；len；add；net.get；np.zeros；int；bus_to_group.get；sw_by_group.get；next；reversed | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.result_info.lf_info | (net, numv=1, numi=2) | pandapower/toolbox/result_info.py#L19 | Prints some basic information of the results in a net (max/min voltage, max trafo load, max line load). | 输入：net, numv, numi；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | logger.info；iterrows；net.res_bus.sort_values；net.res_line.sort_values；net.res_trafo.sort_values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.result_info.opf_task | (net, delta_pq=0.001, keep=False, log=True) | pandapower/toolbox/result_info.py#L46 | Collects some basic inforamtion of the optimal powerflow task und prints them. | 输入：net, delta_pq, keep, log；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _check_necessary_opf_parameters；_determine_flexibilities_dict；_determine_network_constraints_dict；_determine_costs_dict；_check_overlapping_constraints；copy.deepcopy；_log_opf_task_overview | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.result_info.overloaded_lines | (net, max_load=100) | pandapower/toolbox/result_info.py#L383 | Returns the results for all lines with loading_percent > max_load or None, if there are none. | 输入：net, max_load；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | UserWarning | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.result_info.res_power_columns | (element_type, side=0) | pandapower/toolbox/result_info.py#L654 | Returns columns names of result tables for active and reactive power | 输入：element_type, side；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pp_elements；isinstance；ValueError；res_power_columns | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.result_info.switch_info | (net, sidx) | pandapower/toolbox/result_info.py#L361 | Prints what buses and elements are connected by a certain switch. | 输入：net, sidx；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | logger.info | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.result_info.violated_buses | (net, min_vm_pu, max_vm_pu) | pandapower/toolbox/result_info.py#L394 | Returns all bus indices where vm_pu is not within min_vm_pu and max_vm_pu or returns None, if there are none of those buses. | 输入：net, min_vm_pu, max_vm_pu；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | UserWarning | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
