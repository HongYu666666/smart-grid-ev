# `pandapower.toolbox.grid_modification`

本模块共记录 47 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.toolbox.grid_modification.__close_switches_for_oos | (closed_switches, switches, et_label, elements_oos_idx) | pandapower/toolbox/grid_modification.py#L310 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.toolbox.grid_modification.__close_switches_for_oos 的实现与调用链确认。 | 输入：closed_switches, switches, et_label, elements_oos_idx；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；isin；closed_switches.update；newly_closed.tolist | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.__drop_inactive_elements_other | (net) | pandapower/toolbox/grid_modification.py#L961 | Drops inactive elements other than branches and buses | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pp_elements；len；drop_elements_simple；logger.info | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.__drop_inactive_other_branches | (net) | pandapower/toolbox/grid_modification.py#L987 | Cleans up a pandapower network by removing all out-of-service “other branch elements”. | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pp_elements；len；drop_elements_simple | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification._adapt_profiles_in_replace_functions | (net, element_type_old, element_index_old, element_type_new, element_index_new) | pandapower/toolbox/grid_modification.py#L1963 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.toolbox.grid_modification._adapt_profiles_in_replace_functions 的实现与调用链确认。 | 输入：net, element_type_old, element_index_old, element_type_new, element_index_new；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pd.Index；key_old.replace；drop；isinstance；isin；pd.concat；key.startswith；pd.Series | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification._adapt_result_tables_in_replace_functions | (net, element_type_old, element_index_old, element_type_new, element_index_new) | pandapower/toolbox/grid_modification.py#L1951 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.toolbox.grid_modification._adapt_result_tables_in_replace_functions 的实现与调用链确认。 | 输入：net, element_type_old, element_index_old, element_type_new, element_index_new；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pd.Index；pd.concat；drop；isin；pd.Series | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification._drop_controller_at_elements | (net, element_type, idx, ctrl_idx, to_drop) | pandapower/toolbox/grid_modification.py#L851 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.toolbox.grid_modification._drop_controller_at_elements 的实现与调用链确认。 | 输入：net, element_type, idx, ctrl_idx, to_drop；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | ctrl_dict.get；np.array；hasattr；ensure_iterability；np.any；to_drop.append；isin；list；_update_further_controller_parameters；pd.Series | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification._inner_branches | (net, buses, task, branch_elements=None) | pandapower/toolbox/grid_modification.py#L913 | Drops or finds branches that connects buses within 'buses' at all branch sides (e.g. 'from_bus' and 'to_bus'). | 输入：net, buses, task, branch_elements；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | branch_element_bus_dict；branch_dict.items；pd.Series；any；isin；drop_lines；NotImplementedError；drop_trafos；drop；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification._merge_nets | (net1, net2, validate=True, merge_results=True, tol=1e-09, std_prio_on_net1=True, return_net2_reindex_lookup=False, net2_reindex_log_level='info', **runpp_kwargs) | pandapower/toolbox/grid_modification.py#L213 | Function to concatenate two nets into one data structure. The elements keep their indices unless both nets have the same indices. In that case, net2 elements get reindex. The reindex lookup of net2 elements can be retrieved by passing return_net2_reindex_lookup=True. | 输入：net1, net2, validate, merge_results, tol, std_prio_on_net1, return_net2_reindex_lookup, net2_reindex_log_level, **runpp_kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | copy.deepcopy；len；runpp；isin；any；log_to_level；max；net2.items；range；dict；reindex_elements；pd.concat；_preserve_dtypes；copy；update；abs | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification._replace_group_member_element_type | (net, old_elements, old_element_type, new_elements, new_element_type, detach_from_gr=True) | pandapower/toolbox/grid_modification.py#L1932 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.toolbox.grid_modification._replace_group_member_element_type 的实现与调用链确认。 | 输入：net, old_elements, old_element_type, new_elements, new_element_type, detach_from_gr；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pd.Series；check_unique_group_rows；isinstance；AssertionError；old_elements.isin；any；detach_from_groups；attach_to_group；tolist | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification._select_cost_df | (net, p2, cost_type) | pandapower/toolbox/grid_modification.py#L43 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.toolbox.grid_modification._select_cost_df 的实现与调用链确认。 | 输入：net, p2, cost_type；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.array；et.unique；element.isin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification._update_further_controller_parameters | (net, ctrl_idx, elm_staying) | pandapower/toolbox/grid_modification.py#L868 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.toolbox.grid_modification._update_further_controller_parameters 的实现与调用链确认。 | 输入：net, ctrl_idx, elm_staying；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | list；np.array | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.close_switch_at_line_with_two_open_switches | (net) | pandapower/toolbox/grid_modification.py#L613 | Finds lines that have opened switches at both ends and closes one of them. Function is usually used when optimizing section points to prevent the algorithm from ignoring isolated lines. | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；nl.groupby；len；logger.info；closed_switches.add | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.create_replacement_switch_for_branch | (net, element_type, element_index) | pandapower/toolbox/grid_modification.py#L1055 | Creates a switch parallel to a branch, connecting the same buses as the branch. The switch is closed if the branch is in service and open if the branch is out of service. The in_service status of the original branch is not affected and should be set separately, if needed. | 输入：net, element_type, element_index；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | create_switch；logger.debug；all | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.drop_buses | (net, buses, drop_elements=True) | pandapower/toolbox/grid_modification.py#L709 | Drops specified buses, their bus_geodata and by default drops all elements connected to them as well. | 输入：net, buses, drop_elements；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | detach_from_groups；drop；net.res_bus.index.intersection；len；drop_elements_at_buses；drop_measurements_at_elements；drop_controllers_at_buses | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.drop_controllers_at_buses | (net, buses) | pandapower/toolbox/grid_modification.py#L888 | Drop all the controllers for the elements connected to the given buses. | 输入：net, buses；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | get_connected_elements_dict；drop_controllers_at_elements | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.drop_controllers_at_elements | (net, element_type, idx=None) | pandapower/toolbox/grid_modification.py#L840 | Drop all the controllers for the given elements (idx). | 输入：net, element_type, idx；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | net.controller.drop；ensure_iterability；_drop_controller_at_elements | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.drop_duplicated_measurements | (net, buses=None, keep='first') | pandapower/toolbox/grid_modification.py#L897 | Drops duplicated measurements at given set of buses. If buses is None, all buses are considered. | 输入：net, buses, keep；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | fillna；net.measurement.drop；analyzed_meas.duplicated；net.measurement.element.isin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.drop_elements | (net, element_type, element_index, **kwargs) | pandapower/toolbox/grid_modification.py#L667 | Drops element, result and group entries, as well as, associated elements from the pandapower net. | 输入：net, element_type, element_index, **kwargs；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | drop_buses；drop_trafos；drop_lines；drop_elements_simple | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.drop_elements_at_buses | (net, buses, bus_elements=True, branch_elements=True, drop_measurements=True) | pandapower/toolbox/grid_modification.py#L782 | drop elements connected to given buses | 输入：net, buses, bus_elements, branch_elements, drop_measurements；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | element_bus_tuples；drop_measurements_at_elements；drop_switches_at_buses；any；isin；drop_lines；drop_trafos；detach_from_groups；drop；isinstance；index.intersection；logger.debug；element.isin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.drop_elements_simple | (net, element_type, element_index) | pandapower/toolbox/grid_modification.py#L682 | Drops element, result and group entries from the pandapower net. | 输入：net, element_type, element_index；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | ensure_iterability；detach_from_groups；drop；logger.warning；isinstance；index.intersection；logger.debug；len；plural_s | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.drop_inactive_elements | (net, respect_switches=True) | pandapower/toolbox/grid_modification.py#L1046 | Drops any elements not in service AND any elements connected to inactive buses. | 输入：net, respect_switches；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | set_isolated_areas_out_of_service；drop_out_of_service_elements | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.drop_inner_branches | (net, buses, branch_elements=None) | pandapower/toolbox/grid_modification.py#L954 | Drops branches that connects buses within 'buses' at all branch sides (e.g. 'from_bus' and 'to_bus'). | 输入：net, buses, branch_elements；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | _inner_branches | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.drop_lines | (net, lines) | pandapower/toolbox/grid_modification.py#L756 | Deletes all lines and their geodata in the given list of indices and removes any switches connected to it. | 输入：net, lines；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | detach_from_groups；drop；drop_measurements_at_elements；net.res_line.index.intersection；logger.debug；len；isin；set；plural_s | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.drop_measurements_at_elements | (net, element_type, idx=None, side=None) | pandapower/toolbox/grid_modification.py#L827 | Drop measurements of given element_type and (if given) given elements (idx) and side. | 输入：net, element_type, idx, side；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | net.measurement.element.isin；net.measurement.drop；ensure_iterability；np.full | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.drop_out_of_service_elements | (net) | pandapower/toolbox/grid_modification.py#L1003 | Drop all elements (including corresponding dataframes such as switches, measurements, result tables, geodata) with "in_service" is False. Buses that are connected to in-service branches are not deleted. | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；__drop_inactive_other_branches；pd.Index；__drop_inactive_elements_other；drop_lines；drop_trafos；inactive_buses.difference；drop_buses；element_bus_tuples；unique；pd.concat | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.drop_switches_at_buses | (net, buses) | pandapower/toolbox/grid_modification.py#L820 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.toolbox.grid_modification.drop_switches_at_buses 的实现与调用链确认。 | 输入：net, buses；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | drop；logger.debug；len；isin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.drop_trafos | (net, trafos, table='trafo') | pandapower/toolbox/grid_modification.py#L727 | Deletes all trafos and in the given list of indices and removes any switches connected to it. | 输入：net, trafos, table；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | detach_from_groups；drop；len；drop_measurements_at_elements；index.intersection；logger.debug；UserWarning；isin；plural_s | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.fuse_buses | (net, b1, b2, drop=True, fuse_bus_measurements=True) | pandapower/toolbox/grid_modification.py#L631 | Reroutes any connections to buses in b2 to the given bus b1. Additionally drops the buses b2, if drop=True (default). | 输入：net, b1, b2, drop, fuse_bus_measurements；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | element_bus_tuples；isinstance；drop_buses；drop_inner_branches；set；bus_meas.element.isin；drop_duplicated_measurements；isin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.get_inner_branches | (net, buses, branch_elements=None) | pandapower/toolbox/grid_modification.py#L946 | Returns indices of branches that connects buses within 'buses' at all branch sides (e.g. 'from_bus' and 'to_bus'). | 输入：net, buses, branch_elements；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | _inner_branches | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.merge_nets | (net1, net2, validate=True, merge_results=True, tol=1e-09, **kwargs) | pandapower/toolbox/grid_modification.py#L158 | Function to concatenate two nets into one data structure. The elements keep their indices unless both nets have the same indices. In that case, net2 elements get reindexed. The reindex lookup of net2 elements can be retrieved by passing return_net2_reindex_lookup=True. | 输入：net1, net2, validate, merge_results, tol, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；_merge_nets；intersection；FutureWarning；warnings.warn；set | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.merge_parallel_line | (net, idx) | pandapower/toolbox/grid_modification.py#L473 | Changes the impedances of the parallel line so that it equals a single line. | 输入：net, idx；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | complex | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.merge_same_bus_generation_plants | (net: pandapowerNet, add_info: bool=True, error: bool=True, gen_elms: tuple[str, ...]=('ext_grid', 'gen', 'sgen')) | pandapower/toolbox/grid_modification.py#L527 | Merge generation plants connected to the same buses so that a maximum of one generation plant per node remains. | 输入：net, add_info, error, gen_elms；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pd.Index；pd.concat；np.repeat；gen_df.reset_index；unique；cols.intersection；sum；drop；duplicated；len；ValueError；logger.error；all；dropna；isnull；gen_df.index.isin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.repl_to_line | (net, idx, std_type, name=None, in_service=False, **kwargs) | pandapower/toolbox/grid_modification.py#L389 | creates a power line in parallel to the existing power line based on the values of the new std_type. The new parallel line has an impedance value, which is chosen so that the resulting impedance of the new line and the already existing line is equal to the impedance of the replaced line. Or for electrical engineers: | 输入：net, idx, std_type, name, in_service, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | change_std_type；create_line_from_parameters；complex；str；create_switch | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.replace_ext_grid_by_gen | (net: pandapowerNet, ext_grids: Collection \| None=None, gen_indices: Collection \| None=None, slack: bool=False, cols_to_keep: list \| None=None, add_cols_to_keep: list \| None=None) | pandapower/toolbox/grid_modification.py#L1297 | Replaces external grids by generators. | 输入：net, ext_grids, gen_indices, slack, cols_to_keep, add_cols_to_keep；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | list；columns.intersection；existing_cols_to_keep.difference；zip；astype；_replace_group_member_element_type；net.ext_grid.drop；_adapt_result_tables_in_replace_functions；_adapt_profiles_in_replace_functions；ensure_iterability；isinstance；len；itertuples；create_gen；new_idx.append；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.replace_gen_by_ext_grid | (net: pandapowerNet, gens: Collection[int] \| None=None, ext_grid_indices: Collection \| None=None, cols_to_keep: list \| None=None, add_cols_to_keep: list \| None=None) | pandapower/toolbox/grid_modification.py#L1387 | Replaces generators by external grids. | 输入：net, gens, ext_grid_indices, cols_to_keep, add_cols_to_keep；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | list；columns.intersection；existing_cols_to_keep.difference；zip；_replace_group_member_element_type；net.gen.drop；_adapt_result_tables_in_replace_functions；_adapt_profiles_in_replace_functions；ensure_iterability；isinstance；len；itertuples；create_ext_grid；new_idx.append；ValueError；set | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.replace_gen_by_sgen | (net: pandapowerNet, gens: Collection[int] \| None=None, sgen_indices: Collection[int] \| Collection[None] \| None=None, cols_to_keep: list \| None=None, add_cols_to_keep: list \| None=None) | pandapower/toolbox/grid_modification.py#L1463 | Replaces generators by static generators. | 输入：net, gens, sgen_indices, cols_to_keep, add_cols_to_keep；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | list；columns.intersection；existing_cols_to_keep.difference；zip；_replace_group_member_element_type；net.gen.drop；_adapt_result_tables_in_replace_functions；_adapt_profiles_in_replace_functions；ensure_iterability；isinstance；len；itertuples；create_sgen；new_idx.append；ValueError；set | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.replace_impedance_by_line | (net: pandapowerNet, index: pd.Index \| None=None, only_valid_replace: bool=True, max_i_ka: float=float('nan')) | pandapower/toolbox/grid_modification.py#L1165 | Creates lines by given impedances data, while the impedances are dropped. | 输入：net, index, only_valid_replace, max_i_ka；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | float；ensure_iterability；zip；_replace_group_member_element_type；drop_elements_simple；_adapt_result_tables_in_replace_functions；_adapt_profiles_in_replace_functions；list；len；iterrows；new_index.append；logger.error；create_line_from_parameters；np.isclose；index.remove；np.sqrt | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.replace_line_by_impedance | (net: pandapowerNet, index: pd.Index \| None=None, sn_mva: list[float] \| Literal['max_i_ka'] \| None=None, only_valid_replace: bool=True) | pandapower/toolbox/grid_modification.py#L1223 | Creates impedances by given lines data, while the lines are dropped. | 输入：net, index, sn_mva, only_valid_replace；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | list；iterrows；_replace_group_member_element_type；drop_lines；_adapt_result_tables_in_replace_functions；_adapt_profiles_in_replace_functions；ensure_iterability；new_index.append；len；tolist；logger.error；create_impedance；index.remove | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.replace_pq_elmtype | (net: pandapowerNet, old_element_type: Literal['sgen', 'load', 'storage'], new_element_type: Literal['sgen', 'load', 'storage'], old_indices: Collection[int] \| int \| None=None, new_indices: Collection[int] \| None=None, cols_to_keep: list \| None=None, add_cols_to_keep: list \| None=None) -> Collection[int] | pandapower/toolbox/grid_modification.py#L1643 | Replaces e.g. static generators by loads or loads by storages and so forth. | 输入：net, old_element_type, new_element_type, old_indices, new_indices, cols_to_keep, add_cols_to_keep；输出：Collection[int] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | list；columns.intersection；existing_cols_to_keep.difference；set；zip；_replace_group_member_element_type；drop；_adapt_result_tables_in_replace_functions；_adapt_profiles_in_replace_functions；ensure_iterability；logger.warning；len；isinstance；itertuples；fct；new_idx.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.replace_sgen_by_gen | (net: pandapowerNet, sgens: Collection[int] \| None=None, gen_indices: Collection \| None=None, cols_to_keep: list \| None=None, add_cols_to_keep: list \| None=None) | pandapower/toolbox/grid_modification.py#L1539 | Replaces static generators by generators. | 输入：net, sgens, gen_indices, cols_to_keep, add_cols_to_keep；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | list；columns.intersection；existing_cols_to_keep.difference；zip；np.array；_replace_group_member_element_type；net.sgen.drop；_adapt_result_tables_in_replace_functions；_adapt_profiles_in_replace_functions；ensure_iterability；isinstance；len；pd.Series；itertuples；create_gen；new_idx.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.replace_ward_by_internal_elements | (net: pandapowerNet, wards: Collection[int] \| None=None) | pandapower/toolbox/grid_modification.py#L1766 | Replaces wards by loads and shunts. | 输入：net, wards；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | list；element_associated_groups；itertuples；drop_elements_simple；ensure_iterability；create_load；create_shunt；new_load_idx.append；new_shunt_idx.append；attach_to_groups；np.multiply；pd.concat；to_numpy | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.replace_xward_by_internal_elements | (net, xwards=None, set_xward_bus_limits=False) | pandapower/toolbox/grid_modification.py#L1824 | Replaces xward by loads, shunts, impedance and generators | 输入：net, xwards, set_xward_bus_limits；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | element_associated_groups；itertuples；drop_elements_simple；ensure_iterability；create_bus；create_load；create_shunt；create_gen；create_impedance；attach_to_groups；log_to_level；net.res_xward.drop；tolist | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.replace_xward_by_ward | (net, index=None, drop=True) | pandapower/toolbox/grid_modification.py#L1880 | Replace xward elements by ward elements in the given grid model. The series impedance component of xward is ignored and lost after the replacement. | 输入：net, index, drop；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _replace_group_member_element_type；list；create_ward；new_index.append；drop_elements_simple；ensure_iterability | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.replace_zero_branches_with_switches | (net, elements=('line', 'impedance'), zero_length=True, zero_impedance=True, in_service_only=True, min_length_km=0, min_r_ohm_per_km=0, min_x_ohm_per_km=0, min_c_nf_per_km=0, min_rft_pu=0, min_xft_pu=0, min_rtf_pu=0, min_xtf_pu=0, drop_affected=False) | pandapower/toolbox/grid_modification.py#L1093 | Creates a replacement switch for branches with zero impedance (line, impedance) and sets them out of service. | 输入：net, elements, zero_length, zero_impedance, in_service_only, min_length_km, min_r_ohm_per_km, min_x_ohm_per_km, min_c_nf_per_km, min_rft_pu, min_xft_pu, min_rtf_pu, min_xtf_pu, drop_affected；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；TypeError；set；branch_zero.update；create_replacement_switch_for_branch；affected_elements.add；list；logger.info；index.tolist；drop_lines；drop；len；np.abs | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.select_subnet | (net, buses, include_switch_buses=False, include_results=False, keep_everything_else=False) | pandapower/toolbox/grid_modification.py#L52 | Selects a subnet by a list of bus indices and returns a net with all elements connected to them. | 输入：net, buses, include_switch_buses, include_results, keep_everything_else；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；pp_elements；_select_cost_df；pandapowerNet；itertuples；copy.deepcopy；create_empty_network；list；clear_result_tables；bus.isin；net.line.from_bus.isin；net.line.to_bus.isin；net.dcline.from_bus.isin；net.dcline.to_bus.isin；net.trafo.hv_bus.isin；net.trafo.lv_bus.isin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.set_element_status | (net, buses, in_service) | pandapower/toolbox/grid_modification.py#L295 | Sets buses and all elements connected to them in or out of service. | 输入：net, buses, in_service；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；get_connected_elements；list | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.toolbox.grid_modification.set_isolated_areas_out_of_service | (net, respect_switches=True) | pandapower/toolbox/grid_modification.py#L319 | Set all isolated buses and all elements connected to isolated buses out of service. | 输入：net, respect_switches；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | unsupplied_buses；len；set；sum；logger.info；set_element_status；apply；size；t3_oos.any；__close_switches_for_oos；merge；np.where；unique；list；status.all；eq | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
