# `pandapower.toolbox.comparison`

本模块共记录 4 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.toolbox.comparison.compare_arrays | (x, y) | pandapower/toolbox/comparison.py#L99 | Returns an array of bools whether array x is equal to array y. Strings are allowed in x or y. NaN values are assumed as equal. | 输入：x, y；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | ValueError；np.equal；pd.isna | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.comparison.dataframes_equal | (df1, df2, ignore_index_order=True, assume_geojson_strings=True, **kwargs) | pandapower/toolbox/comparison.py#L21 | Returns a boolean whether the given two dataframes are equal or not. | 输入：df1, df2, ignore_index_order, assume_geojson_strings, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | logger.warning；kwargs.pop；sort_index；df1.columns.difference；df2.columns.difference；pdt.assert_frame_equal；len；pd.concat；ValueError；astype；notnull1.symmetric_difference；df1_geo.columns.symmetric_difference；df1.sort_index；df2.sort_index；df1.geo.isnull；df2.geo.isnull | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.comparison.nets_equal | (net1, net2, check_only_results=False, check_without_results=False, exclude_elms=None, name_selection=None, assume_geojson_strings=True, **kwargs) | pandapower/toolbox/comparison.py#L110 | Returns a boolean whether the two given pandapower networks are equal. | 输入：net1, net2, check_only_results, check_without_results, exclude_elms, name_selection, assume_geojson_strings, **kwargs；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | nets_equal_keys；logger.warning；len；isinstance；join | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.comparison.nets_equal_keys | (net1, net2, check_only_results, check_without_results, exclude_elms, name_selection, assume_geojson_strings, **kwargs) | pandapower/toolbox/comparison.py#L148 | Returns a lists of keys which are 1) not equal and 2) not checked. Used within :func:`nets_equal`. | 输入：net1, net2, check_only_results, check_without_results, exclude_elms, name_selection, assume_geojson_strings, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | UserWarning；list；set；len；logger.warning；isinstance；not_equal.append；dataframes_equal；key.startswith；np.array_equal；np.isclose；DeepDiff；graphs_equal；not_checked_keys.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
