# `pandapower.toolbox.data_modification`

本模块共记录 9 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.toolbox.data_modification.add_column_from_element_to_elements | (net, column, replace, elements=None, continue_on_missing_column=True) | pandapower/toolbox/data_modification.py#L72 | Adds column data to elements, inferring them from the column data of the elements linked by the columns "element" and "element_type" or "et". | 输入：net, column, replace, elements, continue_on_missing_column；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pp_elements；len；set；str；KeyError；logger.debug；all；isnull | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.data_modification.add_column_from_node_to_elements | (net, column, replace, elements=None, branch_bus=None, verbose=True) | pandapower/toolbox/data_modification.py#L22 | Adds column data to elements, inferring them from the column data of buses they are connected to. | 输入：net, column, replace, elements, branch_bus, verbose；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | element_bus_tuples；ValueError；pp_elements；sum；all；compare_arrays；logger.warning；logger.debug；isnull | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.data_modification.add_zones_to_elements | (net, replace=True, elements=None, **kwargs) | pandapower/toolbox/data_modification.py#L128 | Adds zones to elements, inferring them from the zones of buses they are connected to. | 输入：net, replace, elements, **kwargs；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | add_column_from_node_to_elements；pp_elements | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.data_modification.create_continuous_bus_index | (net, start=0, store_old_index=False) | pandapower/toolbox/data_modification.py#L198 | Creates a continuous bus index starting at 'start' and replaces all references of old indices by the new ones. | 输入：net, start, store_old_index；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | net.bus.sort_index；list；dict；reindex_buses；np.arange；zip；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.data_modification.create_continuous_elements_index | (net, start=0, add_df_to_reindex=set()) | pandapower/toolbox/data_modification.py#L338 | Creating a continuous index for all the elements, starting at zero and replaces all references of old indices by the new ones. | 输入：net, start, add_df_to_reindex；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；pp_elements；create_continuous_bus_index；sort_index；list；np.arange；unique；reindex_elements；isinstance；logger.debug；len；id_characteristic.dropna；dict；logger.info；zip；sorted | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.data_modification.reindex_buses | (net, bus_lookup, allow_duplicate_index=False) | pandapower/toolbox/data_modification.py#L136 | Changes the index of net.bus and considers the new bus indices in all other pandapower element tables. | 输入：net, bus_lookup, allow_duplicate_index；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；sorted；get_indices；element_bus_tuples；notnull；set；logger.error；bus_lookup.update；set_index；bus_lookup.keys；ValueError；np.arange；list；pd.to_numeric；str；bus_lookup.values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.data_modification.reindex_elements | (net, element_type, new_indices=None, old_indices=None, lookup=None) | pandapower/toolbox/data_modification.py#L220 | Changes the index of the DataFrame net[element_type]. | 输入：net, element_type, new_indices, old_indices, lookup；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pd.Series；set_index；ValueError；dict；reindex_buses；get_indices；pd.Index；len；uuid.uuid4；sum；zip；index.intersection；np.arange；list；old_indices.intersection；element.isin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.data_modification.set_data_type_of_columns_to_default | (net) | pandapower/toolbox/data_modification.py#L407 | Overwrites dtype of DataFrame columns of PandapowerNet elements to default dtypes defined in pandapower. The function "convert_format" does that automatically for nets saved with pandapower versions below 1.6. If this is required for versions starting with 1.6, it should be done manually with this function. | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | create_empty_network；net.items；isinstance；astype；set；reindex | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.data_modification.set_scaling_by_type | (net, scalings, scale_load=True, scale_sgen=True) | pandapower/toolbox/data_modification.py#L379 | Sets scaling of loads and/or sgens according to a dictionary mapping type to a scaling factor. Note that the type-string is case sensitive. E.g. scaling = {"pv": 0.8, "bhkw": 0.6} | 输入：net, scalings, scale_load, scale_sgen；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | defaultdict；isinstance；UserWarning；scaleit；zip | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
