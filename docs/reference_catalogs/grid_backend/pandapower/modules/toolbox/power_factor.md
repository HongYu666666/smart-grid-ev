# `pandapower.toolbox.power_factor`

本模块共记录 10 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.toolbox.power_factor._cosphi_from_pq | (p, q) | pandapower/toolbox/power_factor.py#L148 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.toolbox.power_factor._cosphi_from_pq 的实现与调用链确认。 | 输入：p, q；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | logger.warning；np.cos；int；np.arctan；np.sign | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.power_factor._cosphi_from_pq_bulk | (p, q, len_=None) | pandapower/toolbox/power_factor.py#L160 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.toolbox.power_factor._cosphi_from_pq_bulk 的实现与调用链确认。 | 输入：p, q, len_；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.array；np.empty；sum；np.cos；len；ensure_iterability；logger.warning；np.arctan；astype；np.sign | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.power_factor._pq_from_cosphi | (s, cosphi, qmode, pmode) | pandapower/toolbox/power_factor.py#L65 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.toolbox.power_factor._pq_from_cosphi 的实现与调用链确认。 | 输入：s, cosphi, qmode, pmode；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | logger.warning；np.sqrt；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.power_factor._pq_from_cosphi_bulk | (s, cosphi, qmode, pmode, len_=None) | pandapower/toolbox/power_factor.py#L91 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.toolbox.power_factor._pq_from_cosphi_bulk 的实现与调用链确认。 | 输入：s, cosphi, qmode, pmode, len_；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.array；len；np.ones；ensure_iterability；any；logger.warning；set；ValueError；np.sqrt；str；list | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.power_factor.cosphi_from_pos | (cosphi) | pandapower/toolbox/power_factor.py#L257 | All positive cosphi values are converted back to signed cosphi values | 输入：cosphi；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.array；ensure_iterability；isinstance | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.power_factor.cosphi_from_pq | (p, q) | pandapower/toolbox/power_factor.py#L133 | Analog to pq_from_cosphi, but the other way around. In consumer viewpoint (pandapower): "underexcited" (Q absorption, decreases voltage) and "overexcited" (Q injection, increases voltage) | 输入：p, q；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | hasattr；_cosphi_from_pq_bulk；len；_cosphi_from_pq | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.power_factor.cosphi_pos_neg_from_pq | (p, q) | pandapower/toolbox/power_factor.py#L187 | Returns the cosphi value(s) for given active and reactive power(s). Positive q values lead to positive cosphi values, negative to negative. If p, q come from generator view point, positive cosphi correspond to overexcited behaviour while in load view point positive cosphi correspond to underexcited behaviour. | 输入：p, q；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.copysign；cosphi_from_pq | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.power_factor.cosphi_to_pos | (cosphi) | pandapower/toolbox/power_factor.py#L229 | Signed cosphi values are converted into positive cosphi values from 0 to 1 and 1 to 2. | 输入：cosphi；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.array；ensure_iterability；isinstance | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.power_factor.pq_from_cosphi | (s, cosphi, qmode, pmode) | pandapower/toolbox/power_factor.py#L37 | Calculates P/Q values from rated apparent power and cosine(phi) values. | 输入：s, cosphi, qmode, pmode；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | hasattr；_pq_from_cosphi_bulk；len；isinstance；_pq_from_cosphi | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.toolbox.power_factor.signing_system_value | (element_type) | pandapower/toolbox/power_factor.py#L22 | Returns a 1 for all bus elements using the consumver viewpoint and a -1 for all bus elements using the generator viewpoint. | 输入：element_type；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | pp_elements；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
