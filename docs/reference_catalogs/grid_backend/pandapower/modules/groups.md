# `pandapower.groups`

本模块共记录 31 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.groups._get_lists_from_df | (df: pd.DataFrame, cols: list[str]) -> list[list] | pandapower/groups.py#L209 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.groups._get_lists_from_df 的实现与调用链确认。 | 输入：df, cols；输出：list[list] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | tolist | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups._sum_powers | (net, index, formula_character, unit) | pandapower/groups.py#L662 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.groups._sum_powers 的实现与调用链确认。 | 输入：net, index, formula_character, unit；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | tolist；len；group_element_index；index.intersection；logger.warning；net.keys；no_res_table_found.append；missing_res_idx.append；sum；no_power_column_found.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.attach_to_group | (net: pandapowerNet, index: int, element_types: str \| list[str], element_indices: list[list[int]] \| list[int], reference_columns: str \| list[str] \| None=None, take_existing_reference_columns: bool=True) -> None | pandapower/groups.py#L88 | Appends the group by the elements given. | 输入：net, index, element_types, element_indices, reference_columns, take_existing_reference_columns；输出：None | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _group_parameter_list；group_name；np.isin；zip；len；ValueError；np.sum；_check_elements_existence；_set_multiple_entries；net.group.sort_index；append；tolist；list；create_group；set_group_reference_column；net.group.drop | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.attach_to_groups | (net: pandapowerNet, index: list[int], element_types: str \| list[str], element_indices: list[list[int]] \| list[int], reference_columns: str \| list[str] \| None=None) -> None | pandapower/groups.py#L62 | Appends the groups by the elements given. | 输入：net, index, element_types, element_indices, reference_columns；输出：None | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | attach_to_group | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.check_unique_group_rows | (net: pandapowerNet, raise_error: bool=True, log_level: Literal['error', 'warning', 'info', 'debug', 'UserWarning']='warning') | pandapower/groups.py#L474 | Checks whether all groups have unique names. raise_error decides whether duplicated names lead to error or log message. | 输入：net, raise_error, log_level；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | reset_index；any；ValueError；df.duplicated；UserWarning；log_to_level；len；agg；groupby；net.group.reset_index | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.compare_group_elements | (net: pandapowerNet, index1: int, index2: int) -> bool | pandapower/groups.py#L439 | allow_cross_reference_column_comparison | 输入：net, index1, index2；输出：bool | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | ensure_lists_in_group_element_column；tolist；set_index；len；ValueError；set；symmetric_difference；group_element_index；pd.Index | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.count_group_elements | (net: pandapowerNet, index: int) -> pd.Series | pandapower/groups.py#L393 | Returns a Series concluding the number of included elements in self.elements_dict | 输入：net, index；输出：pd.Series | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | pd.Series；len；zip；hasattr；isinstance；_get_lists_from_df | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.detach_from_group | (net, index, element_type, element_index) | pandapower/groups.py#L169 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.groups.detach_from_group 的实现与调用链确认。 | 输入：net, index, element_type, element_index；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | deprecated；detach_from_groups | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.detach_from_groups | (net: pandapowerNet, element_type: str, element_index: int \| list[int], index: int \| None=None) -> None | pandapower/groups.py#L173 | Detaches elements from the group with the given group index 'index'. No errors are raised if elements are passed to be dropped from groups which already don't have these elements as members. A reverse function is available -> :func:`group.attach_to_group` | 输入：net, element_type, element_index, index；输出：None | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pd.Index；np.isin；np.ones；net.group.index.tolist；ensure_iterability；np.arange；len；pd.isnull；tolist；difference；element_index.intersection | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.drop_group | (net: pandapowerNet, index: int) -> None | pandapower/groups.py#L32 | Drops the group of given index. | 输入：net, index；输出：None | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | net.group.drop | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.drop_group_and_elements | (net: pandapowerNet, index: int) -> None | pandapower/groups.py#L43 | Drops all elements of the group and in net.group the group itself. | 输入：net, index；输出：None | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | tolist；net.group.drop；group_element_index；drop；idx.intersection；net.keys；index.intersection | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.element_associated_groups | (net: pandapowerNet, element_type: str, element_index: Iterable[int] \| int, return_empties: bool=True, drop_empty_lines: bool=True) -> dict[int, list[int]] | pandapower/groups.py#L357 | Returns to which groups given elements belong to. | 输入：net, element_type, element_index, return_empties, drop_empty_lines；输出：dict[int, list[int]] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.isscalar；tolist；list；ensure_lists_in_group_element_column；pd.Series；associated.to_dict；set；dict.fromkeys；intersection；apply；np.atleast_1d；group_element_index；astype；pd.Index；associated.apply | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.elements_connected_to_group | (net: pandapowerNet, index: int, element_types: list[str], find_buses_only_from_buses: bool=False, respect_switches: bool=True, respect_in_service: bool=False, include_empty_lists: bool=False) -> dict[str, Collection[int]] | pandapower/groups.py#L904 | Returns a dict of indices of elements that are connected to the group. | 输入：net, index, element_types, find_buses_only_from_buses, respect_switches, respect_in_service, include_empty_lists；输出：dict[str, Collection[int]] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | group_element_index；get_connected_elements_dict；set；branch_element_bus_dict；append；list；bed.update；itertuples；sorted；connected.get；bed.keys；pd.Index；difference；connected.items；element_type_for_switch_et；ensure_iterability | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.ensure_lists_in_group_element_column | (net: pandapowerNet, drop_empty_lines: bool=True) | pandapower/groups.py#L546 | Ensure that all entries in net.group.element are of type list. | 输入：net, drop_empty_lines；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.ones；range；hasattr；list；isinstance；len；pd.isnull | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.group_element_index | (net: pandapowerNet, index: int, element_type: str) -> pd.Index | pandapower/groups.py#L263 | Returns the indices of the elements of the group in the element table net[element_type]. This function considers net.group.reference_column. | 输入：net, index, element_type；输出：pd.Index | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | group_row；pd.Index；pd.isnull；isin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.group_element_lists | (net: pandapowerNet, index: int) | pandapower/groups.py#L213 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.groups.group_element_lists 的实现与调用链确认。 | 输入：net, index；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | tuple；_get_lists_from_df | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.group_entries_exist_in_element_table | (net: pandapowerNet, index: int, element_type: str) -> npt.NDArray[bool] | pandapower/groups.py#L573 | Returns an array of booleans whether the entries in net.group.element exist in net[element_type], also considering reference_column | 输入：net, index, element_type；输出：npt.NDArray[bool] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | group_row；isinstance；ValueError；pd.isnull；np.isin；hasattr；np.array | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.group_index | (net: pandapowerNet, name: str) -> int | pandapower/groups.py#L240 | Returns the index of the group named as given | 输入：net, name；输出：int | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；ValueError；set | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.group_name | (net: pandapowerNet, index: int) -> str | pandapower/groups.py#L217 | Returns the name of the group and checks that all group rows include the same name | 输入：net, index；输出：str | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | ValueError；len；all；set；pd.isnull | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.group_res_p_mw | (net: pandapowerNet, index: int) | pandapower/groups.py#L755 | Sums all result table values `p_mw` of group members. | 输入：net, index；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | _sum_powers | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.group_res_power_per_bus | (net: pandapowerNet, index: int) -> pd.DataFrame | pandapower/groups.py#L699 | Returns active and reactive power consumptions of given group per bus | 输入：net, index；输出：pd.DataFrame | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pd.DataFrame；pp_elements；branch_element_bus_dict；tolist；len；group_element_index；logger.warning；float；index.intersection；res_power_columns；pq_sums.add；missing_res_idx.append；sum；signing_system_value；pq_sums.columns.difference；pq_sum.rename | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.group_res_q_mvar | (net: pandapowerNet, index: int) | pandapower/groups.py#L777 | Sums all result table values `q_mvar` of group members. | 输入：net, index；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | _sum_powers | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.group_row | (net: pandapowerNet, index: int, element_type: str) -> pd.Series | pandapower/groups.py#L289 | Returns the row which consists the data of the requested group index and element type. | 输入：net, index, element_type；输出：pd.Series | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set_index；isinstance；KeyError；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.groups_equal | (net: pandapowerNet, index1: int, index2: int, **kwargs) | pandapower/groups.py#L413 | Returns a boolean whether both group are equal. | 输入：net, index1, index2, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set_index；pdt.assert_frame_equal | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.isin_group | (net: pandapowerNet, element_type: str, element_index: int \| list[int], index: int \| list[int] \| None=None, drop_empty_lines: bool=True) -> bool \| npt.NDArray[bool] | pandapower/groups.py#L318 | Returns whether elements are in group(s). | 输入：net, element_type, element_index, index, drop_empty_lines；输出：bool \| npt.NDArray[bool] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；np.isscalar；tolist；ensure_lists_in_group_element_column；pd.Index；np.isin；ValueError；list；ensure_iterability；member_idx.union；np.atleast_1d；set；group_element_index | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.remove_not_existing_group_members | (net: pandapowerNet, verbose: bool=True) | pandapower/groups.py#L501 | Remove group members from net.group that do not exist in the elements tables. | 输入：net, verbose；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；np.ones；range；len；logger.info；isinstance；np.all；group_entries_exist_in_element_table；np.any；list；np.array；np.sum | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.return_group_as_net | (net: pandapowerNet, index: int, keep_everything_else: bool=False, verbose: bool=True, **kwargs) -> pandapowerNet | pandapower/groups.py#L863 | Returns a pandapower net consisting of the members of this group. | 输入：net, index, keep_everything_else, verbose, **kwargs；输出：pandapowerNet | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | copy.deepcopy；pp_elements；create_empty_network；tolist；len；remove_not_existing_group_members；group_element_index；drop；set；logger.warning；group_name；kwargs.get；index.difference | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.set_group_in_service | (net: pandapowerNet, index: int) | pandapower/groups.py#L604 | Sets all elements of the group in service. | 输入：net, index；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | set_value_to_group | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.set_group_out_of_service | (net: pandapowerNet, index: int) | pandapower/groups.py#L615 | Sets all elements of the group out of service. | 输入：net, index；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | set_value_to_group | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.set_group_reference_column | (net: pandapowerNet, index: int, reference_column: str, element_type: str \| None=None) | pandapower/groups.py#L799 | Set a reference_column to the group of given index. The values in net.group.element get updated. | 输入：net, index, reference_column, element_type；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；tolist；ensure_iterability；ValueError；pd.api.types.is_object_dtype；any；dupl_elements.append；pd.Series；np.sum；set；isnull；np.arange；net.group.columns.get_loc；duplicated；group_element_index；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.groups.set_value_to_group | (net: pandapowerNet, index: int, value: Any, column: str, replace: bool=True, append_column: bool=True) | pandapower/groups.py#L626 | Sets the same value to the column of the element tables of all elements/members of the group. | 输入：net, index, value, column, replace, append_column；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | zip；group_element_lists；pd.isnull；isnull；isin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
