# `pandapower.pypower.opf_model`

本模块共记录 13 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | pandapower.pypower.opf_model.opf_model | class(object) | pandapower/pypower/opf_model.py#L16 | This class implements the OPF model object used to encapsulate a given OPF problem formulation. It allows for access to optimization variables, constraints and costs in named blocks, keeping track of the ordering and indexing of the blocks as variables, constraints and costs are added to the problem. | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | len；str；append；zeros；ones；array；range；stderr.write；full；lil_matrix；A.tocsr；getattr；todense | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.pypower.opf_model.opf_model.__init__ | (self, ppc) | pandapower/pypower/opf_model.py#L26 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.pypower.opf_model.opf_model.__init__ 的实现与调用链确认。 | 输入：ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.pypower.opf_model.opf_model.__repr__ | (self) | pandapower/pypower/opf_model.py#L105 | String representation of the object. | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；str；range | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.pypower.opf_model.opf_model.add_constraints | (self, name, AorN, l, u=None, varsets=None) | pandapower/pypower/opf_model.py#L176 | Adds a set of constraints to the model. | 输入：name, AorN, l, u, varsets；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | append；range；stderr.write；len；full | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.pypower.opf_model.opf_model.add_vars | (self, name, N, v0=None, vl=None, vu=None) | pandapower/pypower/opf_model.py#L257 | Adds a set of variables to the model. | 输入：name, N, v0, vl, vu；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | append；stderr.write；zeros；full；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.pypower.opf_model.opf_model.build_cost_params | (self) | pandapower/pypower/opf_model.py#L298 | Builds and saves the full generalized cost parameters. | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | zeros；ones | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.pypower.opf_model.opf_model.getN | (self, selector, name=None) | pandapower/pypower/opf_model.py#L406 | Returns the number of variables, constraints or cost rows. | 输入：selector, name；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | getattr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.pypower.opf_model.opf_model.get_cost_params | (self) | pandapower/pypower/opf_model.py#L334 | Returns the cost parameter struct for user-defined costs. | 输入：无显式输入；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | stderr.write | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.pypower.opf_model.opf_model.get_idx | (self) | pandapower/pypower/opf_model.py#L359 | Returns the idx struct for vars, lin/nln constraints, costs. | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.pypower.opf_model.opf_model.get_ppc | (self) | pandapower/pypower/opf_model.py#L400 | Returns the PYPOWER case dict. | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.pypower.opf_model.opf_model.getv | (self) | pandapower/pypower/opf_model.py#L432 | Returns initial value, lower bound and upper bound for opt variables. | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | array；range | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.pypower.opf_model.opf_model.linear_constraints | (self) | pandapower/pypower/opf_model.py#L452 | Builds and returns the full set of linear constraints. | 输入：无显式输入；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | range；lil_matrix；full；array；A.tocsr；zeros；todense | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.pypower.opf_model.opf_model.userdata | (self, name, val=None) | pandapower/pypower/opf_model.py#L504 | Used to save or retrieve values of user data. | 输入：name, val；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | array | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
