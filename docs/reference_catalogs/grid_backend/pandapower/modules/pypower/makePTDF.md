# `pandapower.pypower.makePTDF`

本模块共记录 1 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.pypower.makePTDF.makePTDF | (baseMVA, bus, branch, slack=None, result_side=0, using_sparse_solver=False, branch_id=None, reduced=False) | pandapower/pypower/makePTDF.py#L24 | Builds the DC PTDF matrix for a given choice of slack. Returns the DC PTDF matrix for a given choice of slack. The matrix is C{nbr x nb}, where C{nbr} is the number of branches and C{nb} is the number of buses. The C{slack} can be a scalar (single slack bus) or an C{nb x 1} column vector of weights specifying the proportion of the slack taken up at each bus. If the C{slack} is not specified the reference bus is used by default. For convenience, C{slack} can also be an C{nb x nb} matrix, where each column specifies how the slack should be handled for injections at that bus. To restrict the PTDF computation to a subset of branches, supply a list of ppci branch indices in C{branch_id}. If C{reduced==True}, the output is reduced to the branches given in C{branch_id}, otherwise the complement rows are set to NaN. @see: L{makeLODF} @author: Ray Zimmerman (PSERC Cornell) | 输入：baseMVA, bus, branch, slack, result_side, using_sparse_solver, branch_id, reduced；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | isscalar；arange；find；any；ValueError；stderr.write；zeros；makeBdc；Bf.real.toarray；np.real；len；dot；range；factorized；Bbus.toarray；Bf.toarray | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
