# `pandapower.topology.create_graph`

本模块共记录 6 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.topology.create_graph.add_edges | (mg, indices, parameter, in_service, net, element, calc_branch_impedances=False, branch_impedance_unit='ohm') | pandapower/topology/create_graph.py#L319 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.topology.create_graph.add_edges 的实现与调用链确认。 | 输入：mg, indices, parameter, in_service, net, element, calc_branch_impedances, branch_impedance_unit；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.sqrt；zip；mg.add_edge | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.topology.create_graph.create_nxgraph | (net, respect_switches=True, include_lines=True, include_impedances=True, include_dclines=True, include_trafos=True, include_trafo3ws=True, include_tcsc=True, include_vsc=True, include_line_dc=True, nogobuses=None, notravbuses=None, multi=True, calc_branch_impedances=False, branch_impedance_unit='ohm', library='networkx', include_out_of_service=False, include_switches=True, trafo_length_km=None, switch_length_km=None) | pandapower/topology/create_graph.py#L41 | Converts a pandapower network into a NetworkX graph, which is a is a simplified representation of a network's topology, reduced to nodes and edges. Busses are being represented by nodes (Note: only buses with in_service = 1 appear in the graph), edges represent physical connections between buses (typically lines or trafos). | 输入：net, respect_switches, include_lines, include_impedances, include_dclines, include_trafos, include_trafo3ws, include_tcsc, include_vsc, include_line_dc, nogobuses, notravbuses, multi, calc_branch_impedances, branch_impedance_unit, library, include_out_of_service, include_switches, trafo_length_km, switch_length_km；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | get_edge_table；nx.Graph；ValueError；get_nx_ppc；init_par；add_edges；combinations；len；GraphToolInterface；net.switch.closed.values.astype；mask.any；_calc_impedance_parameters_from_dataframe；get_baseR；_calc_branch_values_from_trafo_df；_trafo_df_from_trafo3w；flatten | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.topology.create_graph.get_baseR | (net, ppc, buses) | pandapower/topology/create_graph.py#L340 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.topology.create_graph.get_baseR 的实现与调用链确认。 | 输入：net, ppc, buses；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.square | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.topology.create_graph.get_edge_table | (net, table_name, include_edges) | pandapower/topology/create_graph.py#L295 | Returns the table for the specified name according to parameter include_edges. If it is True, the whole table is returnedif False, None is returned. If it is an iterable, only the included indices are chosen. | 输入：net, table_name, include_edges；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.topology.create_graph.get_nx_ppc | (net) | pandapower/topology/create_graph.py#L361 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.topology.create_graph.get_nx_ppc 的实现与调用链确认。 | 输入：net；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | _init_nx_options；_init_ppc；_build_bus_ppc | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.topology.create_graph.init_par | (tab, calc_branch_impedances=False, include_out_of_service=False) | pandapower/topology/create_graph.py#L346 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.topology.create_graph.init_par 的实现与调用链确认。 | 输入：tab, calc_branch_impedances, include_out_of_service；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.zeros；np.ones；tab.in_service.values.copy | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
