# `pandapower.topology.graph_searches`

本模块共记录 11 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.topology.graph_searches.calc_distance_to_bus | (net, bus, respect_switches=True, nogobuses=None, notravbuses=None, weight='weight', g=None) | pandapower/topology/graph_searches.py#L78 | Calculates the shortest distance between a source bus and all buses connected to it. | 输入：net, bus, respect_switches, nogobuses, notravbuses, weight, g；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | pd.Series；create_nxgraph；nx.single_source_dijkstra_path_length | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.topology.graph_searches.connected_component | (mg, bus, notravbuses=[]) | pandapower/topology/graph_searches.py#L16 | Finds all buses in a NetworkX graph that are connected to a certain bus. | 输入：mg, bus, notravbuses；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | deque；stack.pop；iter；visited.add；stack.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.topology.graph_searches.connected_components | (mg, notravbuses=set()) | pandapower/topology/graph_searches.py#L47 | Clusters all buses in a NetworkX graph that are connected to each other. | 输入：mg, notravbuses；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；len；mg.edges；mg.nodes；connected_component；nodes.pop | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.topology.graph_searches.determine_stubs | (net, roots=None, mg=None, respect_switches=False) | pandapower/topology/graph_searches.py#L329 | Finds stubs in a network. Open switches are being ignored. Results are being written in a new column in the bus table ("on_stub") and line table ("is_stub") as True/False value. | 输入：net, roots, mg, respect_switches；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | get_2connected_buses；create_nxgraph；set；list；net.line.from_bus.isin；net.line.to_bus.isin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.topology.graph_searches.elements_on_path | (mg, path, element='line') | pandapower/topology/graph_searches.py#L386 | Finds all elements that connect a given path of buses. | 输入：mg, path, element；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | isinstance；ValueError；zip；keys；mg.get_edge_data | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.topology.graph_searches.find_basic_graph_characteristics | (g, roots, characteristics) | pandapower/topology/graph_searches.py#L145 | Determines basic characteristics of the given graph like connected buses, stubs, bridges, and articulation points. | 输入：g, roots, characteristics；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | dict.fromkeys；set；iter；next；path.append；len；visited.add；stack.append；stack.pop；min；add；path.pop | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.topology.graph_searches.find_graph_characteristics | (g, roots, characteristics) | pandapower/topology/graph_searches.py#L213 | Finds and returns different characteristics of the given graph which can be specified. | 输入：g, roots, characteristics；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | find_basic_graph_characteristics；char_dict.update；set；iter；next；visited.add；stack.append；stack.pop；visited_bridges.append；curr_notn1_area.append；len；visited_bridges.pop；update | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.topology.graph_searches.get_2connected_buses | (g, roots) | pandapower/topology/graph_searches.py#L315 | Get all buses which have at least two connections to the roots | 输入：g, roots；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | find_graph_characteristics | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.topology.graph_searches.get_end_points_of_continuously_connected_lines | (net, lines) | pandapower/topology/graph_searches.py#L420 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.topology.graph_searches.get_end_points_of_continuously_connected_lines 的实现与调用链确认。 | 输入：net, lines；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | nx.MultiGraph；mg.add_edges_from；set；combinations；line_buses.flatten；UserWarning；nx.shortest_path；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.topology.graph_searches.lines_on_path | (mg, path) | pandapower/topology/graph_searches.py#L363 | Finds all lines that connect a given path of buses. | 输入：mg, path；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | elements_on_path | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.topology.graph_searches.unsupplied_buses | (net, mg=None, slacks=None, respect_switches=True) | pandapower/topology/graph_searches.py#L111 | Finds buses, that are not connected electrically (no lines, trafos etc or if respect_switches is True only connected via open switches) to an external grid and that are in service. | 输入：net, mg, slacks, respect_switches；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；nx.connected_components；create_nxgraph；not_supplied.update | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
