# `pandapower.build_bus`

本模块共记录 29 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | pandapower.build_bus.DisjointSet | class(dict) | pandapower/build_bus.py#L133 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus.DisjointSet 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.find | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | pandapower.build_bus.DisjointSet.add | (self, item) | pandapower/build_bus.py#L134 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus.DisjointSet.add 的实现与调用链确认。 | 输入：item；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | pandapower.build_bus.DisjointSet.find | (self, item) | pandapower/build_bus.py#L137 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus.DisjointSet.find 的实现与调用链确认。 | 输入：item；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.find | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| method | pandapower.build_bus.DisjointSet.union | (self, item1, item2) | pandapower/build_bus.py#L144 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus.DisjointSet.union 的实现与调用链确认。 | 输入：item1, item2；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.find | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus._add_c_to_ppc | (net, ppc) | pandapower/build_bus.py#L1108 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus._add_c_to_ppc 的实现与调用链确认。 | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.nonzero；len；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus._add_ext_grid_sc_impedance | (net, ppc) | pandapower/build_bus.py#L996 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus._add_ext_grid_sc_impedance 的实现与调用链确认。 | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _sum_by_group；len；ValueError；np.sqrt | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus._add_load_sc_impedances_ppc | (net, ppc) | pandapower/build_bus.py#L1075 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus._add_load_sc_impedances_ppc 的实现与调用链确认。 | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.conj；_sum_by_group；np.exp；np.deg2rad | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus._add_motor_impedances_ppc | (net, ppc) | pandapower/build_bus.py#L1041 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus._add_motor_impedances_ppc 的实现与调用链确认。 | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _sum_by_group；any；np.sqrt；pd.isnull；UserWarning | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus._build_bus_dc_ppc | (net, ppc) | pandapower/build_bus.py#L446 | Generates the ppc["bus_dc"] array and the lookup pandapower indices -> ppc indices | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | dict；len；create_consecutive_bus_lookup；np.zeros；np.array；np.arange；np.concatenate；_fill_auxiliary_buses；set_reference_buses_dc；max | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus._build_bus_ppc | (net, ppc, sequence=None) | pandapower/build_bus.py#L327 | Generates the ppc["bus"] array and the lookup pandapower indices -> ppc indices | 输入：net, ppc, sequence；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；dict；np.zeros；np.array；np.arange；get_voltage_init_vector；np.concatenate；create_consecutive_bus_lookup；create_bus_lookup；np.empty；bus_sc.fill；np.hstack；set_reference_buses；_add_c_to_ppc；_fill_auxiliary_buses；max | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus._build_ssc_ppc | (net, ppc, mode) | pandapower/build_bus.py#L875 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus._build_ssc_ppc 的实现与调用链确认。 | 输入：net, ppc, mode；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；np.zeros；values.astype；np.deg2rad；astype | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus._build_svc_ppc | (net, ppc, mode) | pandapower/build_bus.py#L842 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus._build_svc_ppc 的实现与调用链确认。 | 输入：net, ppc, mode；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；np.zeros；np.deg2rad；values.astype | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus._build_vsc_ppc | (net, ppc, mode) | pandapower/build_bus.py#L911 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus._build_vsc_ppc 的实现与调用链确认。 | 输入：net, ppc, mode；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；np.zeros；values.astype；np.any；np.where；np.select；astype；NotImplementedError；np.isin；fillna | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus._calc_pq_elements_and_add_on_ppc | (net, ppc, sequence=None) | pandapower/build_bus.py#L601 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus._calc_pq_elements_and_add_on_ppc 的实现与调用链确认。 | 输入：net, ppc, sequence；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.array；astype；np.hstack；_sum_by_group；len；set；_get_motor_pq；element.endswith；_get_symmetric_pq_of_unsymetric_element；any；ValueError；sum；clip；pd.Series；copy；_calculate_qmin_qmax_from_q_capability_characteristics | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus._calc_shunts_and_add_on_ppc | (net, ppc) | pandapower/build_bus.py#L756 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus._calc_shunts_and_add_on_ppc 的实现与调用链确认。 | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | lower；np.array；len；np.hstack；np.sqrt；_sum_by_group；np.any；s.step_dependency_table.fillna；s.step_dependency_table.any；np.isnan；UserWarning；s.merge；pd.isna；warnings.warn；to_numpy；fillna | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus._fill_auxiliary_buses | (net, ppc, bus_lookup, element, bus_column, aux, bus_table='bus') | pandapower/build_bus.py#L540 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus._fill_auxiliary_buses 的实现与调用链确认。 | 输入：net, ppc, bus_lookup, element, bus_column, aux, bus_table；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | NotImplementedError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus._get_motor_pq | (net) | pandapower/build_bus.py#L741 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus._get_motor_pq 的实现与调用链确认。 | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.sqrt | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus._get_symmetric_pq_of_unsymetric_element | (net, element) | pandapower/build_bus.py#L734 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus._get_symmetric_pq_of_unsymetric_element 的实现与调用链确认。 | 输入：net, element；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.sum | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus.create_bus_lookup | (net, bus_index, bus_is_idx, numba) | pandapower/build_bus.py#L265 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus.create_bus_lookup 的实现与调用链确认。 | 输入：net, bus_index, bus_is_idx, numba；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | switches_with_pos_z_ohm.any；np.zeros；create_bus_lookup_numba；create_bus_lookup_numpy；np.isin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus.create_bus_lookup_numba | (net, bus_index, bus_is_idx) | pandapower/build_bus.py#L86 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus.create_bus_lookup_numba 的实现与调用链确认。 | 输入：net, bus_index, bus_is_idx；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.max；np.zeros；np.arange；ds_create；fill_bus_lookup；len；np.ones | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus.create_bus_lookup_numpy | (net, bus_index, closed_bb_switch_mask) | pandapower/build_bus.py#L169 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus.create_bus_lookup_numpy 的实现与调用链确认。 | 输入：net, bus_index, closed_bb_switch_mask；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | create_consecutive_bus_lookup；np.zeros；net._fused_bb_switches.any；len；np.unique；DisjointSet；zip；defaultdict；any；np.hstack；ds.union；add；v.values；list；chain；set | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus.create_consecutive_bus_lookup | (bus_index: np.ndarray) | pandapower/build_bus.py#L150 | create a mapping from arbitrary pp-index to a consecutive index starting at zero (ppc-index) Args: bus_index: the original bus index to create a mapping from. Returns: | 输入：bus_index；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.arange；len；np.array；np.ones；max | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus.ds_create | (ar, switch_bus, switch_elm, switch_et_bus, switch_closed, switch_z_ohm, bus_is_pv, bus_is_active, bus_in_service, merged_bus) | pandapower/build_bus.py#L66 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus.ds_create 的实现与调用链确认。 | 输入：ar, switch_bus, switch_elm, switch_et_bus, switch_closed, switch_z_ohm, bus_is_pv, bus_is_active, bus_in_service, merged_bus；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | jit；range；len；ds_union | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus.ds_find | (ar, bus) | pandapower/build_bus.py#L42 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus.ds_find 的实现与调用链确认。 | 输入：ar, bus；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | jit | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus.ds_union | (ar, bus1, bus2, bus_is_pv, bus_is_active, merged_bus) | pandapower/build_bus.py#L52 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus.ds_union 的实现与调用链确认。 | 输入：ar, bus1, bus2, bus_is_pv, bus_is_active, merged_bus；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | jit；ds_find | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus.fill_bus_lookup | (ar, bus_lookup, bus_index) | pandapower/build_bus.py#L78 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus.fill_bus_lookup 的实现与调用链确认。 | 输入：ar, bus_lookup, bus_index；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | jit；range；len；ds_find | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus.get_voltage_init_vector | (net, init_v, mode, sequence=None) | pandapower/build_bus.py#L285 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus.get_voltage_init_vector 的实现与调用链确认。 | 输入：net, init_v, mode, sequence；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；UserWarning；init_v.index.equals；index.equals；phase_to_sequence；np.abs；values.copy；np.exp；np.angle；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus.set_reference_buses | (net, ppc, bus_lookup, mode) | pandapower/build_bus.py#L566 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus.set_reference_buses 的实现与调用链确认。 | 输入：net, ppc, bus_lookup, mode；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；gen_slacks.any；list | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_bus.set_reference_buses_dc | (net, ppc, bus_lookup) | pandapower/build_bus.py#L583 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_bus.set_reference_buses_dc 的实现与调用链确认。 | 输入：net, ppc, bus_lookup；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.intersect1d；get；np.array | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
