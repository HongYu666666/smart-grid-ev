# `pandapower.contingency.contingency`

本模块共记录 10 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.contingency.contingency._convert_trafo_phase_shifter | (net, trafotable='trafo', tap_param='tap_changer_type') | pandapower/contingency/contingency.py#L348 | Moves tap_step_degree values for ideal phase shifters to the shift_degree values. Works both for old implementation (tap_phase_shifter) and for new implementation (tap_changer_type). | 输入：net, trafotable, tap_param；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | values.copy；shift_degree.values.copy；tap_step_degree.values.copy；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.contingency.contingency._get_iloc_index | (index, sub_index) | pandapower/contingency/contingency.py#L514 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.contingency.contingency._get_iloc_index 的实现与调用链确认。 | 输入：index, sub_index；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.arange；len；_get_sub_index | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.contingency.contingency._get_sub_index | (index, sub_index) | pandapower/contingency/contingency.py#L508 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.contingency.contingency._get_sub_index 的实现与调用链确认。 | 输入：index, sub_index；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.zeros；np.arange；len；index.max | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.contingency.contingency._log_violation | (element, var, val, limit_index, mask) | pandapower/contingency/contingency.py#L522 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.contingency.contingency._log_violation 的实现与调用链确认。 | 输入：element, var, val, limit_index, mask；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | np.any；np.printoptions；logger.info | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.contingency.contingency._update_contingency_results | (net, contingency_results, result_variables, nminus1, cause_element=None, cause_index=None) | pandapower/contingency/contingency.py#L376 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.contingency.contingency._update_contingency_results 的实现与调用链确认。 | 输入：net, contingency_results, result_variables, nminus1, cause_element, cause_index；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | result_variables.items；np.any；func；get；setdefault；np.full_like；np.isnan | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.contingency.contingency.check_elements_within_limits | (element_limits, contingency_results, nminus1=False, branch_tol=0.001, bus_tol=1e-06) | pandapower/contingency/contingency.py#L470 | Check if elements are within limits | 输入：element_limits, contingency_results, nminus1, branch_tol, bus_tol；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | contingency_results.items；np.any | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.contingency.contingency.get_element_limits | (net) | pandapower/contingency/contingency.py#L406 | Construct the dictionary of element limits | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.full_like；element_limits.update；len；update；pd.isnull；net.bus.get；get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.contingency.contingency.report_contingency_results | (element_limits, contingency_results, branch_tol=0.001, bus_tol=1e-06) | pandapower/contingency/contingency.py#L529 | Print log messages for elements with violations of limits | 输入：element_limits, contingency_results, branch_tol, bus_tol；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | contingency_results.items；_get_sub_index；results.items；_log_violation | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.contingency.contingency.run_contingency | (net: pandapowerNet, nminus1_cases: dict, pf_options: dict \| None=None, pf_options_nminus1: dict \| None=None, write_to_net: bool=True, contingency_evaluation_function: Callable=runpp, **kwargs) -> dict | pandapower/contingency/contingency.py#L50 | Obtain either loading (N-0) or max. loading (N-0 and all N-1 cases), and min/max bus voltage magnitude. The variable `temperature_degree_celsius` can be used in addition to `loading_percent` to obtain max. temperature. In the returned dictionary, the variable `loading_percent` represents the loading in N-0 case, `max_loading_percent` and `min_loading_percent` represent highest and lowest observed `loading_percent` among all calculated N-1 cases. The same convention applies to `temperature_degree_celsius` when applicable. This function can be passed through to :func:`pandapower.timeseries.run_timeseries` as the `run_control_fct` argument. | 输入：net, nminus1_cases, pf_options, pf_options_nminus1, write_to_net, contingency_evaluation_function, **kwargs；输出：dict | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | kwargs.get；contingency_results.keys；nminus1_cases.items；contingency_evaluation_function；_update_contingency_results；net.user_pf_options.get；update；append；contingency_results.items；len；get；pf_options.get；pf_options_nminus1.get；element_results.items；pf_options.items；pf_options_nminus1.items | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.contingency.contingency.run_contingency_ls2g | (net: pandapowerNet, nminus1_cases: dict, contingency_evaluation_function: Callable=runpp, **kwargs) | pandapower/contingency/contingency.py#L152 | Execute contingency analysis using the lightsim2grid library. This works much faster than using pandapower. This function can be passed through to pandapower.timeseries.run_timeseries as the run_control_fct argument. | 输入：net, nminus1_cases, contingency_evaluation_function, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；contingency_evaluation_function；net._options.get；ContingencyAnalysisCPP；s.change_solver；nminus1_cases.items；s.compute；s.get_voltages；s.compute_flows；s.get_flows；np.abs；np.nanmax；np.nanmin；np.nan_to_num；UserWarning；np.any | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
