# `pandapower.networks.dickert_lv_networks`

本模块共记录 5 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.networks.dickert_lv_networks._change_to_ohl | (net, idx_busbar, new_lines, n_cable) | pandapower/networks/dickert_lv_networks.py#L12 | This function changes line types from cable to ohl beginning at the end of the feeders in a way, that the tapped line has the most portions of overhead-lines. | 输入：net, idx_busbar, new_lines, n_cable；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | list；get_connected_elements；len；sorted；deepcopy；change_std_type；cable_lines.add；con_lines.pop | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.networks.dickert_lv_networks._create_feeder | (net, net_data, branching, idx_busbar, linetype, lv_vn_kv) | pandapower/networks/dickert_lv_networks.py#L54 | This function creates the dickert lv network feeders by creating the buses and lines. In case of branching the right position of branching must be found with respect to sum of feeder nodes 'n_DP' and the number of 'branching'. | 输入：net, net_data, branching, idx_busbar, linetype, lv_vn_kv；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | create_buses；append；set；enumerate；_create_loads_with_coincidence；int；new_lines.add；_change_to_ohl；ceil；create_line；round；str；ValueError；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.networks.dickert_lv_networks._create_loads_with_coincidence | (net, buses) | pandapower/networks/dickert_lv_networks.py#L31 | This function creates loads to lv feeders with respect to coincidence factor c. c is calculated by the number of feeder buses as described in Dickert, Schegner - 'Residential Load Models for Network Planning Purposes', Modern Electric Power Systems 2010, Wroclaw, Poland. | 输入：net, buses；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len；pq_from_cosphi；create_load | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.networks.dickert_lv_networks.create_dickert_lv_feeders | (net, busbar_index, feeders_range='short', linetype='cable', customer='single', case='good') | pandapower/networks/dickert_lv_networks.py#L126 | This function creates LV feeders from J. Dickert, M. Domagk and P. Schegner. "Benchmark low voltage distribution networks based on cluster analysis of actual grid properties". PowerTech, 2013 IEEE Grenoble. The number of this LV feeders will be in range of one to three, with respect to the optional given parameters 'feeders_range', 'linetype', 'customer' and 'case'. The given 'preferred lines for feeders' are used, knowing that there are some other standard types mentioned as well. | 输入：net, busbar_index, feeders_range, linetype, customer, case；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _create_feeder；keys；create_std_type；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.networks.dickert_lv_networks.create_dickert_lv_network | (feeders_range='short', linetype='cable', customer='single', case='good', trafo_type_name='0.4 MVA 20/0.4 kV', trafo_type_data=None) | pandapower/networks/dickert_lv_networks.py#L204 | This function creates a LV network from J. Dickert, M. Domagk and P. Schegner. "Benchmark low voltage distribution networks based on cluster analysis of actual grid properties". PowerTech, 2013 IEEE Grenoble. This LV network will have one to three feeders connected to MV-LV-Trafo. To connect more feeders with respect to the optional given parameters 'feeders_range', 'linetype', 'customer' and 'case', the 'create_dickert_lv_feeders' function can be executed. The given 'preferred lines for feeders' are used, knowing that there are some other standard types mentioned as well. | 输入：feeders_range, linetype, customer, case, trafo_type_name, trafo_type_data；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | create_empty_network；create_bus；create_ext_grid；create_transformer；create_dickert_lv_feeders；keys；create_std_type | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
