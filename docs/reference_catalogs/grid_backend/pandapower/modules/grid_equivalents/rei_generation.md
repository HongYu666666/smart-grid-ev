# `pandapower.grid_equivalents.rei_generation`

本模块共记录 8 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.grid_equivalents.rei_generation._calclate_equivalent_element_params | (net_zpbn, Ybus_eq, bus_lookups, show_computing_time=False, max_allowed_impedance=100000000.0, **kwargs) | pandapower/grid_equivalents/rei_generation.py#L558 | This function calculates the equivalent parameters | 输入：net_zpbn, Ybus_eq, bus_lookups, show_computing_time, max_allowed_impedance, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | time.perf_counter；len；pd.DataFrame；np.triu；list；Ybus_eq.sum；np.abs；astype；np.array；logger.info；range；round；reshape；np.ones；np.arange | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.rei_generation._calculate_equivalent_Ybus | (net_zpbn, bus_lookups, eq_type, show_computing_time=False, **kwargs) | pandapower/grid_equivalents/rei_generation.py#L28 | The function orders the admittance matrix of the original network into new format firstly, which is convenient for rei equivalent calculation.d Then it calculates the equivalent admittance matrix of the given network | 输入：net_zpbn, bus_lookups, eq_type, show_computing_time, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | time.perf_counter；todense；bus_lookup_ppc.keys；list；reduce；np.copy；bus_lookup_ppc.values；np.linspace；np.linalg.inv；logger.info；len；np.isin；str；logger.debug；np.linalg.pinv；round | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.rei_generation._create_bus_lookups | (net_zpbn, boundary_buses, all_internal_buses, all_external_buses, boundary_buses_inclusive_bswitch, show_computing_time=False) | pandapower/grid_equivalents/rei_generation.py#L425 | The function creates a bus lookup table according to the given zpbn network and the bus groups | 输入：net_zpbn, boundary_buses, all_internal_buses, all_external_buses, boundary_buses_inclusive_bswitch, show_computing_time；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | time.perf_counter；build_ppc_and_Ybus；bus_lookup_pd.copy；list；reduce；tolist；copy；sorted；bus_lookup_ppc.values；set；range；logger.info；np.arange；remove；len；round | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.rei_generation._create_net_zpbn | (net, boundary_buses, all_internal_buses, all_external_buses, load_separate=False, sgen_separate=True, gen_separate=True, show_computing_time=False, calc_volt_angles=True, runpp_fct=_runpp_except_voltage_angles, **kwargs) | pandapower/grid_equivalents/rei_generation.py#L123 | The function builds the zero power balance network with calculated impedance and voltage | 输入：net, boundary_buses, all_internal_buses, all_external_buses, load_separate, sgen_separate, gen_separate, show_computing_time, calc_volt_angles, runpp_fct, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _get_internal_and_external_nets；drop_buses；calc_zpbn_parameters；max；drop_and_edit_cost_functions；runpp_fct；drop；int；len；net_zpbn.res_bus.vm_pu.isnull；all；Z.drop；split；create_load；set；other_cols.copy | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.rei_generation._get_internal_and_external_nets | (net, boundary_buses, all_internal_buses, all_external_buses, show_computing_time=False, calc_volt_angles=True, runpp_fct=_runpp_except_voltage_angles, **kwargs) | pandapower/grid_equivalents/rei_generation.py#L522 | This function identifies the internal area and the external area | 输入：net, boundary_buses, all_internal_buses, all_external_buses, show_computing_time, calc_volt_angles, runpp_fct, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | time.perf_counter；deepcopy；drop_and_edit_cost_functions；drop_measurements_and_controllers；drop_buses；replace_motor_by_load；_integrate_power_elements_connected_with_switch_buses；runpp_fct；net_external.bus.index.tolist；logger.info | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.rei_generation._integrate_power_elements_connected_with_switch_buses | (net, net_external, all_external_buses) | pandapower/grid_equivalents/rei_generation.py#L700 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.grid_equivalents.rei_generation._integrate_power_elements_connected_with_switch_buses 的实现与调用链确认。 | 输入：net, net_external, all_external_buses；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | get_connected_switch_buses_groups；len；active_sgen.bus.isin；active_load.bus.isin；ValueError；active_gen.bus.isin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.rei_generation._replace_ext_area_by_impedances_and_shunts | (net_eq, bus_lookups, impedance_params, shunt_params, net_internal, return_internal, show_computing_time=False, calc_volt_angles=True, imp_threshold=1e-08, runpp_fct=_runpp_except_voltage_angles, **kwargs) | pandapower/grid_equivalents/rei_generation.py#L614 | This function implements the parameters of the equivalent shunts and equivalent impedance | 输入：net_eq, bus_lookups, impedance_params, shunt_params, net_internal, return_internal, show_computing_time, calc_volt_angles, imp_threshold, runpp_fct, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | drop_buses；drop_internal_branch_elements；net_eq.shunt.drop；range；pd.concat；astype；new_sws.rename；shunt_params.bus_pd.values.astype；pd.DataFrame；new_shunts.bus.isin；runpp_fct；net_eq.shunt.bus.isin；net_eq.impedance.index.max；net_eq.switch.index.max；not_very_low_imp.all；values.tolist | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.rei_generation.adapt_impedance_params | (Z, sign=1, adaption=1e-15) | pandapower/grid_equivalents/rei_generation.py#L112 | In some extreme cases, the created admittance matrix of the zpbn network is singular. The routine is unsolvable with it. In response, an impedance adaption is created and added. | 输入：Z, sign, adaption；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
