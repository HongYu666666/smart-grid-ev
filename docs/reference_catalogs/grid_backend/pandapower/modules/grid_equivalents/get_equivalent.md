# `pandapower.grid_equivalents.get_equivalent`

本模块共记录 6 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.grid_equivalents.get_equivalent._check_bus_groups | (all_internal_buses, all_external_buses, internal_buses, boundary_buses) | pandapower/grid_equivalents/get_equivalent.py#L520 | Checks the plausibility of the bus groups. | 输入：all_internal_buses, all_external_buses, internal_buses, boundary_buses；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len；ValueError；str；sorted | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.get_equivalent._determine_bus_groups | (net, boundary_buses, internal_buses, show_computing_time=False) | pandapower/grid_equivalents/get_equivalent.py#L412 | Defines bus groups according to the given boundary buses and internal buses. | 输入：net, boundary_buses, internal_buses, show_computing_time；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | time.perf_counter；set；len；create_nxgraph；_check_bus_groups；ValueError；logger.info；connected_components；sorted；net.res_bus.vm_pu.isnull；connected_component；next；round；str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.get_equivalent._get_buses_after_merge | (net_eq, net_internal, bus_lookups, return_internal) | pandapower/grid_equivalents/get_equivalent.py#L534 | Finds bus groups according to the new index after merge | 输入：net_eq, net_internal, bus_lookups, return_internal；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | net_eq.bus.index.tolist；net_internal.bus.index.tolist；bus_list_old.index；sum；enumerate；net_internal.bus.name.str.contains | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.get_equivalent.drop_repeated_characteristic | (net) | pandapower/grid_equivalents/get_equivalent.py#L401 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.grid_equivalents.get_equivalent.drop_repeated_characteristic 的实现与调用链确认。 | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | index.tolist；drop；duplicated；fillna | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.get_equivalent.get_equivalent | (net, eq_type, boundary_buses, internal_buses, return_internal=True, show_computing_time=False, ward_type='ward_injection', adapt_va_degree=False, calculate_voltage_angles=True, allow_net_change_for_convergence=False, runpp_fct=_runpp_except_voltage_angles, **kwargs) | pandapower/grid_equivalents/get_equivalent.py#L31 | This function calculates and implements the rei or ward/xward network equivalents. | 输入：net, eq_type, boundary_buses, internal_buses, return_internal, show_computing_time, ward_type, adapt_va_degree, calculate_voltage_angles, allow_net_change_for_convergence, runpp_fct, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | time.perf_counter；eq_type.lower；deepcopy；_check_network；logger.info；_determine_bus_groups；bool；_ensure_unique_boundary_bus_names；add_ext_grids_to_boundaries；len；match_cost_functions_and_eq_net；kwargs.get；ValueError；logger.warning；net.ward.bus.isin；net.xward.bus.isin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.get_equivalent.merge_internal_net_and_equivalent_external_net | (net_eq, net_internal, fuse_bus_column='auto', show_computing_time=False, **kwargs) | pandapower/grid_equivalents/get_equivalent.py#L311 | Merges the internal network and the equivalent external network. It is expected that the boundaries occur in both, equivalent net and internal net. Therefore, the boundaries are first dropped in the internal net before merging. | 输入：net_eq, net_internal, fuse_bus_column, show_computing_time, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | deepcopy；time.perf_counter；drop_internal_branch_elements；kwargs.pop；merge_nets；drop_assist_elms_by_creating_ext_net；drop_repeated_characteristic；str.contains；sum；drop_elements_at_buses；fuse_buses；index.values.max；dict；reindex_buses；logger.info；ValueError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
