# `pandapower.grid_equivalents.toolbox`

本模块共记录 8 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.grid_equivalents.toolbox.appendSetInDict | (dict_, keys, set_) | pandapower/grid_equivalents/toolbox.py#L27 | Use case specific: append existing value of type set in nested dict | 输入：dict_, keys, set_；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | getFromDict | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.toolbox.append_set_to_dict | (dict_, set_, keys) | pandapower/grid_equivalents/toolbox.py#L48 | Appends a nested dict by the values of a set, independant if the keys already exist or not. | 输入：dict_, set_, keys；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | ensure_iterability；enumerate；setSetInDict；isinstance；getFromDict；ValueError；keys；setInDict；dict；str；type | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.toolbox.getFromDict | (dict_, keys) | pandapower/grid_equivalents/toolbox.py#L17 | Get value from nested dict | 输入：dict_, keys；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | reduce | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.toolbox.get_boundaries_by_bus_zone_with_boundary_branches | (net) | pandapower/grid_equivalents/toolbox.py#L109 | Only in_service branches and closed switches are considered. | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pp_elements；net.bus.zone.unique；dict；set；branch_dict.items；list；boundary_branches.items；len；other_zone_cols.items；ValueError；element_bus_tuples；deepcopy；enumerate；logger.warning；isinstance；append_set_to_dict | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.toolbox.get_connected_switch_buses_groups | (net, buses) | pandapower/grid_equivalents/toolbox.py#L256 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.grid_equivalents.toolbox.get_connected_switch_buses_groups 的实现与调用链确认。 | 输入：net, buses；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | set；create_nxgraph；bus_dict.append；connected_component；list | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.toolbox.setInDict | (dict_, keys, value) | pandapower/grid_equivalents/toolbox.py#L22 | Set value to nested dict | 输入：dict_, keys, value；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | getFromDict | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.toolbox.setSetInDict | (dict_, keys, set_) | pandapower/grid_equivalents/toolbox.py#L32 | Use case specific: set new or append existing value of type set in nested dict | 输入：dict_, keys, set_；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | isinstance；getFromDict；ValueError；keys；setInDict；appendSetInDict；str；type | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.toolbox.set_bus_zone_by_boundary_branches | (net, all_boundary_branches) | pandapower/grid_equivalents/toolbox.py#L67 | Set integer values (0, 1, 2, ...) to net.bus.zone with regard to the given boundary branches in 'all_boundary_branches'. | 输入：net, all_boundary_branches；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | dict.fromkeys；include.keys；create_nxgraph；connected_components；len；enumerate；all_boundary_branches.keys；index.difference；ccl.pop；set_.intersection；list | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
