# `pandapower.grid_equivalents.ward_generation`

本模块共记录 5 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.grid_equivalents.ward_generation._calculate_ward_and_impedance_parameters | (Ybus_eq, bus_lookups, show_computing_time) | pandapower/grid_equivalents/ward_generation.py#L15 | calculates the wards and equivalent impedance to represent the external network | 输入：Ybus_eq, bus_lookups, show_computing_time；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | time.perf_counter；len；pd.DataFrame；range；Ybus_eq.sum；logger.info；reshape；np.abs；np.arange | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.ward_generation._calculate_xward_and_impedance_parameters | (net_external, Ybus_eq, bus_lookups, show_computing_time) | pandapower/grid_equivalents/ward_generation.py#L56 | calculates the xward and the equivalent impedance | 输入：net_external, Ybus_eq, bus_lookups, show_computing_time；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | time.perf_counter；_calculate_ward_and_impedance_parameters；logger.info | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.ward_generation._replace_external_area_by_wards | (net_external, bus_lookups, ward_parameter_no_power, impedance_parameter, ext_buses_with_xward, show_computing_time, calc_volt_angles=True, runpp_fct=_runpp_except_voltage_angles, **kwargs) | pandapower/grid_equivalents/ward_generation.py#L109 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.grid_equivalents.ward_generation._replace_external_area_by_wards 的实现与调用链确认。 | 输入：net_external, bus_lookups, ward_parameter_no_power, impedance_parameter, ext_buses_with_xward, show_computing_time, calc_volt_angles, runpp_fct, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | time.perf_counter；drop_buses；drop_internal_branch_elements；net_external.shunt.drop；net_external.res_ext_grid.copy；runpp_fct；pd.concat；len；net_external.shunt.bus.isin；create_ward；logger.info；create_impedance；create_switch；set；abs；net_external.bus.index.tolist | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.ward_generation._replace_external_area_by_xwards | (net_external, bus_lookups, xward_parameter_no_power, impedance_parameter, ext_buses_with_xward, show_computing_time, calc_volt_angles=True, runpp_fct=_runpp_except_voltage_angles, **kwargs) | pandapower/grid_equivalents/ward_generation.py#L192 | replaces the external networks by xwards and equivalent impedance | 输入：net_external, bus_lookups, xward_parameter_no_power, impedance_parameter, ext_buses_with_xward, show_computing_time, calc_volt_angles, runpp_fct, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | time.perf_counter；drop_buses；drop_internal_branch_elements；net_external.shunt.drop；net_external.res_ext_grid.copy；runpp_fct；pd.concat；len；net_external.shunt.bus.isin；create_xward；logger.info；create_impedance；create_switch；np.nan_to_num；set；abs | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.grid_equivalents.ward_generation.create_passive_external_net_for_ward_admittance | (net, all_external_buses, boundary_buses, calc_volt_angles=True, runpp_fct=_runpp_except_voltage_angles, **kwargs) | pandapower/grid_equivalents/ward_generation.py#L75 | This function replace the wards and xward in external network by internal elements, and replace the power injections in external area by shunts if necessary. | 输入：net, all_external_buses, boundary_buses, calc_volt_angles, runpp_fct, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | list；range；runpp_fct；len；drop；abs；create_shunt；bus.isin；np.nan_to_num | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
