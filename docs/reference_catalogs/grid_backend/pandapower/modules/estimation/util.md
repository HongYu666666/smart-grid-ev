# `pandapower.estimation.util`

本模块共记录 8 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.estimation.util._get_bus_ppc_mapping | (net, bus_to_be_fused) | pandapower/estimation/util.py#L58 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.estimation.util._get_bus_ppc_mapping 的实现与调用链确认。 | 输入：net, bus_to_be_fused；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | union；runpp；pd.DataFrame；bus_ppci.index.isin；sum；set；any；bus_ppci.groupby | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.estimation.util.add_virtual_meas_error | (net, v_std_dev=0.001, i_std_dev=0.01, p_std_dev=0.03, q_std_dev=0.03, dg_std_dev=0.1, with_random_error=True) | pandapower/estimation/util.py#L243 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.estimation.util.add_virtual_meas_error 的实现与调用链确认。 | 输入：net, v_std_dev, i_std_dev, p_std_dev, q_std_dev, dg_std_dev, with_random_error；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.random.normal；net.measurement.measurement_type.isin；AssertionError；abs | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.estimation.util.add_virtual_meas_from_loadflow | (net, v_std_dev=0.01, p_std_dev=0.03, q_std_dev=0.03, seed=14, with_random_error=False) | pandapower/estimation/util.py#L141 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.estimation.util.add_virtual_meas_from_loadflow 的实现与调用链确认。 | 输入：net, v_std_dev, p_std_dev, q_std_dev, seed, with_random_error；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.random.seed；net.res_bus.iterrows；remove_shunt_injection_from_meas；branch_meas_type.keys；add_virtual_meas_error；bus_meas_types.keys；float；iterrows；create_measurement | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.estimation.util.add_virtual_pmu_meas_from_loadflow | (net, v_std_dev=0.001, i_std_dev=0.1, p_std_dev=0.01, q_std_dev=0.01, dg_std_dev=0.1, seed=14, with_random_error=True) | pandapower/estimation/util.py#L192 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.estimation.util.add_virtual_pmu_meas_from_loadflow 的实现与调用链确认。 | 输入：net, v_std_dev, i_std_dev, p_std_dev, q_std_dev, dg_std_dev, seed, with_random_error；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.random.seed；branch_meas_type.keys；net.res_bus.iterrows；remove_shunt_injection_from_meas；bus_meas_types.keys；add_virtual_meas_error；np.conj；np.rad2deg；float；iterrows；np.exp；np.angle；create_measurement；np.deg2rad；meas_type.split | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.estimation.util.estimate_voltage_vector | (net) | pandapower/estimation/util.py#L15 | Function initializes the voltage vector of net with a rough estimation. All buses are set to the slack bus voltage. Transformer differences in magnitude and phase shifting are accounted for. :param net: pandapower network :return: pandas dataframe with estimated vm_pu and va_degree | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | DataFrame；create_nxgraph；net.ext_grid.iterrows；trafos.index.tolist；len；list；connected_component；notnull；isnull；trafo_index.remove；UserWarning；res_bus.vm_pu.isnull；res_bus.va_degree.isnull | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.estimation.util.remove_shunt_injection_from_meas | (net, type) | pandapower/estimation/util.py#L176 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.estimation.util.remove_shunt_injection_from_meas 的实现与调用链确认。 | 输入：net, type；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | index.tolist；tolist；range；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.estimation.util.reset_bb_switch_impedance | (net) | pandapower/estimation/util.py#L130 | Reset the z_ohm of the switch to its original state, undo the operation by set_bb_switch_impedance :param net: pandapower net :return: None | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | net.switch.drop | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.estimation.util.set_bb_switch_impedance | (net, bus_to_be_fused=None, z_ohm=0.1) | pandapower/estimation/util.py#L82 | Assuming a substation with multiple buses connected to each other with bb switch if multiple of them have an element (load, sgen usw.) then it caused problem on e.g. State Estimation, since they will be automatically fused in ppc A possiblity would be to properly seperate the substation to multiple clusters (ppc) bus according to the number of buses with elements, in order to estimate the p, q on all the busses. These method implemented a forward and backward substitution: 1. Find the mapping of pd2ppc bus, and identify the ppc bus, which contains multiple bus with elements 2. Skip the first bus with elements with the same ppc bus, and finding all the switch connected to the rest pandapower bus and set impedance 3. Update the pd2ppc lookup 4. if it caused some bus without elements to detach from any of the bus with connected element, then the switch caused it will be reset to fuse mode 5. Iterate this process until all buses with elements are seperated to their own ppc bus, while no bus is detached in order to get the voltage on bus right :param net: pandapower net :param z_ohm: float z_ohm to be setted for the switch :return: None | 输入：net, bus_to_be_fused, z_ohm；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _get_bus_ppc_mapping；range；int；switch_to_be_fused.values.any；lookup.elements_in_cluster.max；duplicated；net.switch.bus.isin；net.switch.element.isin；imp_switch.bus.isin；imp_switch.element.isin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
