# `pandapower.estimation.state_estimation`

本模块共记录 8 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | pandapower.estimation.state_estimation.StateEstimation | class | pandapower/estimation/state_estimation.py#L139 | Any user of the estimation module only needs to use the class state_estimation. It contains all relevant functions to control and operator the module. Two functions are used to configure the system according to the users needs while one function is used for the actual estimation process. | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | opt_vars.keys；pp2eppci；self.solver.estimate；self.estimate；np.dot；len；chi2.ppf；self.logger.debug；UserWarning；isinstance；set_bb_switch_impedance；eppci2pp；self.logger.warning；reset_bb_switch_impedance；datetime.now；hasattr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.estimation.state_estimation.StateEstimation.__init__ | (self, net, tolerance=1e-06, maximum_iterations=50, algorithm='wls', logger=None, recycle=False) | pandapower/estimation/state_estimation.py#L147 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.estimation.state_estimation.StateEstimation.__init__ 的实现与调用链确认。 | 输入：net, tolerance, maximum_iterations, algorithm, logger, recycle；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.estimation.state_estimation.StateEstimation.estimate | (self, v_start='flat', delta_start='flat', zero_injection=None, fuse_buses_with_bb_switch='all', algorithm='wls', debug_mode=False, **opt_vars) | pandapower/estimation/state_estimation.py#L164 | The function estimate is the main function of the module. It takes the inputs v_start and delta_start to initialize the state variables for the estimation process. Usually they can be initialized in a "flat-start" condition: All voltages being 1.0 pu and all voltage angles being 0 degrees. In this case, the parameters can be left at their default values (None). If the estimation is applied continuously, using the results from the last estimation as the starting condition for the current estimation can decrease the amount of iterations needed to estimate the current state. Returned is a boolean value, which is true after a successful estimation and false otherwise. The resulting complex voltage will be written into the pandapower network. The result fields are found res_bus_est of the pandapower network. | 输入：v_start, delta_start, zero_injection, fuse_buses_with_bb_switch, algorithm, debug_mode, **opt_vars；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | opt_vars.keys；pp2eppci；self.solver.estimate；UserWarning；isinstance；set_bb_switch_impedance；eppci2pp；self.logger.warning；reset_bb_switch_impedance；datetime.now；hasattr；now.strftime | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.estimation.state_estimation.StateEstimation.perform_chi2_test | (self, v_in_out=None, delta_in_out=None, calculate_voltage_angles=True, chi2_prob_false=0.05) | pandapower/estimation/state_estimation.py#L273 | The function perform_chi2_test performs a Chi^2 test for bad data and topology error detection. The function can be called with the optional input arguments v_in_out and delta_in_out. Then, the Chi^2 test is performed after calling the function estimate using them as input arguments. It can also be called without these arguments if it is called from the same object with which estimate had been called beforehand. Then, the Chi^2 test is performed for the states estimated by the funtion estimate and the result, the existence of bad data, is given back as a boolean. As a optional argument the probability of a false measurement can be provided additionally. For bad data detection, the function perform_rn_max_test is more powerful and should be the function of choice. For topology error detection, however, perform_chi2_test should be used. | 输入：v_in_out, delta_in_out, calculate_voltage_angles, chi2_prob_false；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.estimate；np.dot；len；chi2.ppf；self.logger.debug；J.item | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.estimation.state_estimation.StateEstimation.perform_rn_max_test | (self, v_in_out=None, delta_in_out=None, calculate_voltage_angles=True, rn_max_threshold=3.0) | pandapower/estimation/state_estimation.py#L339 | The function perform_rn_max_test performs a largest normalized residual test for bad data identification and removal. It takes two input arguments: v_in_out and delta_in_out. These are the initial state variables for the combined estimation and bad data identification and removal process. They can be initialized as described above, e.g., using a "flat" start. In an iterative process, the function performs a state estimation, identifies a bad data measurement, removes it from the set of measurements (only if the rn_max threshold is violated by the largest residual of all measurements, which can be modified), performs the state estimation again, and so on and so forth until no further bad data measurements are detected. | 输入：v_in_out, delta_in_out, calculate_voltage_angles, rn_max_threshold；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.estimate；self.logger.debug；np.linalg.inv；np.diag；np.sqrt；np.dot；np.absolute；max；self.net.measurement.drop；self.logger.error；np.argsort；item | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.estimation.state_estimation.chi2_analysis | (net, init='flat', tolerance=1e-06, maximum_iterations=10, calculate_voltage_angles=True, chi2_prob_false=0.05) | pandapower/estimation/state_estimation.py#L114 | Wrapper function for the chi-squared test. | 输入：net, init, tolerance, maximum_iterations, calculate_voltage_angles, chi2_prob_false；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | StateEstimation；_initialize_voltage；wls_se.perform_chi2_test | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.estimation.state_estimation.estimate | (net, algorithm='wls', init='flat', tolerance=1e-06, maximum_iterations=50, zero_injection='aux_bus', fuse_buses_with_bb_switch='all', debug_mode=False, **opt_vars) | pandapower/estimation/state_estimation.py#L32 | Wrapper function for WLS state estimation. | 输入：net, algorithm, init, tolerance, maximum_iterations, zero_injection, fuse_buses_with_bb_switch, debug_mode, **opt_vars；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | StateEstimation；_initialize_voltage；se.estimate；UserWarning；format | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.estimation.state_estimation.remove_bad_data | (net, init='flat', tolerance=1e-06, maximum_iterations=10, calculate_voltage_angles=True, rn_max_threshold=3.0) | pandapower/estimation/state_estimation.py#L87 | Wrapper function for bad data removal. | 输入：net, init, tolerance, maximum_iterations, calculate_voltage_angles, rn_max_threshold；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | StateEstimation；_initialize_voltage；wls_se.perform_rn_max_test | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
