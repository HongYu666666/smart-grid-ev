# `pandapower.build_branch`

本模块共记录 38 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.build_branch._branches_with_oos_buses | (net: pandapowerNet, ppc, dc: bool=False) | pandapower/build_branch.py#L1376 | Updates the ppc["branch"] matrix with the changed from or to values if the branch is connected to an out of service bus | 输入：net, ppc, dc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；np.setdiff1d；np.isin；np.any；np.hstack；np.zeros；np.arange；get_values；future_buses.append；np.vstack；np.nonzero；np.array；astype；np.logical_not | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._build_branch_dc_ppc | (net: pandapowerNet, ppc: dict) | pandapower/build_branch.py#L84 | Takes the empty ppc network and fills it with the branch values. The branch datatype will be np.complex 128 afterwards. | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _initialize_branch_lookup；np.zeros；np.array；_calc_line_dc_parameter | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._build_branch_ppc | (net: pandapowerNet, ppc: dict, sequence: int=1) -> None | pandapower/build_branch.py#L37 | Takes the empty ppc network and fills it with the branch values. The branch datatype will be np.complex 128 afterwards. | 输入：net, ppc, sequence；输出：None | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _initialize_branch_lookup；np.zeros；np.array；NotImplementedError；np.empty；branch_sc.fill；np.hstack；_calc_line_parameter；_calc_trafo_parameter；_calc_trafo3w_parameter；_calc_impedance_parameter；_calc_xward_parameter；_calc_switch_parameter | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._build_tcsc_ppc | (net: pandapowerNet, ppc: dict, mode: str) | pandapower/build_branch.py#L116 | Takes ppc and fills it with the values for tcsc. | 输入：net, ppc, mode；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；np.zeros；_calc_tcsc_parameter | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calc_branch_values_from_trafo_df | (net, ppc, trafo_df=None, sequence=1) | pandapower/build_branch.py#L467 | Calculates the MAT/PYPOWER-branch-attributes from the pandapower trafo dataframe. | 输入：net, ppc, trafo_df, sequence；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | get_trafo_values；_calc_tap_from_dataframe；_calc_nominal_ratio_from_dataframe；_calc_r_x_y_from_dataframe | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calc_impedance_parameter | (net, ppc) | pandapower/build_branch.py#L1142 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_branch._calc_impedance_parameter 的实现与调用链确认。 | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _calc_impedance_parameters_from_dataframe | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calc_impedance_parameters_from_dataframe | (net, zero_sequence=False) | pandapower/build_branch.py#L1193 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_branch._calc_impedance_parameters_from_dataframe 的实现与调用链确认。 | 输入：net, zero_sequence；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calc_line_dc_parameter | (net: pandapowerNet, ppc: dict, elm: str='line_dc', ppc_elm: str='branch_dc') | pandapower/build_branch.py#L280 | calculates the line_dc parameter in per unit. | 输入：net, ppc, elm, ppc_elm；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | values.astype；np.square；NotImplementedError；_end_temperature_correction_factor；np.sqrt | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calc_line_parameter | (net, ppc, elm='line', ppc_elm='branch') | pandapower/build_branch.py#L200 | calculates the line parameter in per unit. | 输入：net, ppc, elm, ppc_elm；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | values.astype；line.get；np.square；net._options.get；_end_temperature_correction_factor；np.sqrt；fillna | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calc_nominal_ratio_from_dataframe | (ppc: dict, trafo_df: pd.DataFrame, vn_hv_kv: NDArray[float], vn_lv_kv: NDArray[float], bus_lookup: dict) -> NDArray[float] | pandapower/build_branch.py#L1116 | Calculates (Vectorized) the off nominal tap ratio:: (vn_hv_kv / vn_lv_kv) / (ub1_in_kv / ub2_in_kv) | 输入：ppc, trafo_df, vn_hv_kv, vn_lv_kv, bus_lookup；输出：NDArray[float] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | get_trafo_values；get_values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calc_r_x_from_dataframe | (mode, trafo_df, vn_lv, vn_trafo_lv, sn_mva, sequence=1, characteristic=None, trafo_characteristic_table=None) | pandapower/build_branch.py#L1065 | Calculates (Vectorized) the resistance and reactance according to the transformer values | 输入：mode, trafo_df, vn_lv, vn_trafo_lv, sn_mva, sequence, characteristic, trafo_characteristic_table；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | get_trafo_values；np.sign；np.sqrt；np.array；warnings.warn；_get_vk_values；UserWarning；np.square；astype；any；_get_vk_values_from_table；DeprecationWarning；isinstance；np.any；np.isnan | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calc_r_x_y_from_dataframe | (net, trafo_df, vn_trafo_lv, vn_lv, ppc, sequence=1) | pandapower/build_branch.py#L513 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_branch._calc_r_x_y_from_dataframe 的实现与调用链确认。 | 输入：net, trafo_df, vn_trafo_lv, vn_lv, ppc, sequence；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | warnings.warn；_calc_r_x_from_dataframe；net._options.get；isinstance；_calc_y_from_dataframe；DeprecationWarning；get_trafo_values；_wye_delta；ValueError；net.get；_transformer_correction_factor；np.full_like | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calc_switch_parameter | (net: pandapowerNet, ppc) | pandapower/build_branch.py#L1471 | calculates the line parameter in per unit. | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.sqrt；np.square | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calc_tap_dependent_value | (tap_pos, value, tap_dependent_impedance, characteristic, characteristic_idx) | pandapower/build_branch.py#L1043 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_branch._calc_tap_dependent_value 的实现与调用链确认。 | 输入：tap_pos, value, tap_dependent_impedance, characteristic, characteristic_idx；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.zeros_like；np.vectorize；np.where；vec_get_char_for_tap_pos；np.isnan；item；characteristic | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calc_tap_from_dataframe | (net: pandapowerNet, trafo_df: pd.DataFrame) -> tuple[NDArray[float], NDArray[float], NDArray[float]] | pandapower/build_branch.py#L634 | Adjust the nominal voltage vnh, vnl and phase shift to the active tab position "tap_pos". If "side" is 1 (high-voltage side) the high voltage vnh is adjusted. If "side" is 2 (low-voltage side) the low voltage vnl is adjusted. | 输入：net, trafo_df；输出：tuple[NDArray[float], NDArray[float], NDArray[float]] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | copy.copy；astype；np.zeros；get_trafo_values；len；net._options.get；np.logical_and；any；float；np.array；np.any；warnings.warn；UserWarning；pd.DataFrame；net.trafo_characteristic_table.merge；dict | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calc_tcsc_parameter | (net, ppc) | pandapower/build_branch.py#L1161 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_branch._calc_tcsc_parameter 的实现与调用链确认。 | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；np.deg2rad；values.astype | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calc_trafo3w_parameter | (net: pandapowerNet, ppc: dict) -> None | pandapower/build_branch.py#L161 | Calculate 3 winding transformer parameters and add them to the PYPOWER Network | 输入：net, ppc；输出：None | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _trafo_df_from_trafo3w；astype；_calc_branch_values_from_trafo_df；get_trafo_values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calc_trafo_parameter | (net, ppc, sequence=1) | pandapower/build_branch.py#L364 | Calculates the transformer parameter in per unit. | 输入：net, ppc, sequence；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _calc_branch_values_from_trafo_df；any；UserWarning；index.tolist；trafo.query | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calc_xward_parameter | (net, ppc) | pandapower/build_branch.py#L1233 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_branch._calc_xward_parameter 的实现与调用链确认。 | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.square；get_values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calc_y_from_dataframe | (mode, trafo_df, vn_lv, vn_trafo_lv, net_sn_mva) -> NDArray[np.complex128] | pandapower/build_branch.py#L594 | Calculate the susceptance y from the transformer dataframe. | 输入：mode, trafo_df, vn_lv, vn_trafo_lv, net_sn_mva；输出：NDArray[np.complex128] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | get_trafo_values；np.square；np.sqrt | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calculate_3w_tap_changers | (t3, t2, sides) | pandapower/build_branch.py#L1761 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_branch._calculate_3w_tap_changers 的实现与调用链确认。 | 输入：t3, t2, sides；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；np.zeros；empty.fill；at_star_point.any；t2.update；np.array；empty.copy；np.any；np.abs；np.rad2deg；np.exp；np.angle；tap_mask.sum；np.deg2rad；float | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calculate_sc_voltages_of_equivalent_transformers | (t3, t2, mode, characteristic=None, net=None) | pandapower/build_branch.py#L1674 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_branch._calculate_sc_voltages_of_equivalent_transformers 的实现与调用链确认。 | 输入：t3, t2, mode, characteristic, net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | get_trafo_values；np.stack；z_br_to_bus_vector；np.sqrt；wye_delta_vector；np.any；np.array；any；warnings.warn；_get_vk_values；net._options.get；_transformer_correction_factor；np.sign；UserWarning；_get_vk_values_from_table；DeprecationWarning | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._calculate_sc_voltages_of_equivalent_transformers_zero_sequence | (t3, t2, case) | pandapower/build_branch.py#L1718 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_branch._calculate_sc_voltages_of_equivalent_transformers_zero_sequence 的实现与调用链确认。 | 输入：t3, t2, case；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.stack；z_br_to_bus_vector；_transformer_correction_factor；np.sqrt；wye_delta_vector；np.any；np.sign；UserWarning | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._end_temperature_correction_factor | (net, short_circuit=False, dc=False) | pandapower/build_branch.py#L1503 | Function to calculate resistance correction factor for the given temperature ("endtemp_degree"). When multiplied by the factor, the value of r_ohm_per_km will correspond to the resistance at the given temperature. | 输入：net, short_circuit, dc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | UserWarning；endtemp_degree.values.astype；temperature_degree_celsius.values.astype；np.nan_to_num；warnings.warn；alpha.values.astype | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._gather_branch_switch_info | (bus: int, branch_id: int, branch_type: Literal['l', 't', 't3'], net: pandapowerNet) -> tuple[str, int, int] | pandapower/build_branch.py#L1247 | determine at which end of a line or trafo the switch is located | 输入：bus, branch_id, branch_type, net；输出：tuple[str, int, int] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | int；ValueError；index.get_loc；UserWarning | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._get_trafo_shift | (trafo_df: pd.DataFrame, tap: str, mask, direction: int, vn=None, ideal: bool=True) -> tuple[NDArray, NDArray \| None] | pandapower/build_branch.py#L756 | Calculate the phase shift angle for transformer tap changers. | 输入：trafo_df, tap, mask, direction, vn, ideal；输出：tuple[NDArray, NDArray \| None] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | get_trafo_values；np.nan_to_num；np.sqrt；np.cos；np.sin；np.rad2deg；UserWarning；float；any；_arctan；np.deg2rad；np.arcsin；np.arctan；np.where；_arcsin；_sin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._get_vk_values | (trafo_df: pd.DataFrame, characteristic, trafotype: Literal['2W', '3W']='2W') -> tuple[str, ...] | pandapower/build_branch.py#L959 | get vk values from trafo table | 输入：trafo_df, characteristic, trafotype；输出：tuple[str, ...] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.any；enumerate；get_trafo_values；UserWarning；np.isnan；isinstance；trafo_df.keys；len；np.vstack；all；_calc_tap_dependent_value；astype | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._get_vk_values_from_table | (trafo_df: pd.DataFrame, trafo_characteristic_table, trafotype: Literal['2W', '3W']='2W') -> tuple[str, ...] | pandapower/build_branch.py#L888 | get vk values from trafo table | 输入：trafo_df, trafo_characteristic_table, trafotype；输出：tuple[str, ...] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | get_trafo_values；np.array；np.any；enumerate；np.isnan；UserWarning；copy；any；pd.DataFrame；trafo_characteristic_table.merge；dict；zip；vk_mapping.get；isinstance；pd.isna | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._initialize_branch_lookup | (net: pandapowerNet, dc: bool=False) | pandapower/build_branch.py#L134 | create the branch lookup map | 输入：net, dc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | net._impedance_bb_switches.any；len；net._impedance_bb_switches.sum | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._switch_branches | (net: pandapowerNet, ppc) | pandapower/build_branch.py#L1302 | Parameters: net: pandapower network ppc: PYPOWER network | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.count_nonzero；partial；np.array；astype；np.zeros；np.arange；np.vstack；np.unique；switch_mask.any；list；map；isinstance；real.astype | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._trafo_df_from_trafo3w | (net: pandapowerNet, sequence: int=1) -> dict | pandapower/build_branch.py#L1609 | translate trafo3w to trafo | 输入：net, sequence；输出：dict | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；_calculate_3w_tap_changers；np.zeros；np.full；np.where；np.ones；np.concatenate；lower；_calculate_sc_voltages_of_equivalent_transformers；net._options.get；_calculate_sc_voltages_of_equivalent_transformers_zero_sequence；UserWarning；trafo2.keys；NotImplementedError；net.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._transformer_correction_factor | (trafo_df, vk, vkr, sn, cmax, case) | pandapower/build_branch.py#L1566 | 2W-Transformer impedance correction factor in short circuit calculations, based on the IEC 60909-0:2016 standard. | 输入：trafo_df, vk, vkr, sn, cmax, case；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.sqrt；np.where；np.ones；values.astype；astype；len；np.zeros；trafo_df.power_station_unit.fillna | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch._wye_delta | (r, x, g, b, r_ratio, x_ratio) | pandapower/build_branch.py#L565 | 20.05.2016 added by Lothar Löwer | 输入：r, x, g, b, r_ratio, x_ratio；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.errstate；np.zeros_like | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch.get_is_lines | (net) | pandapower/build_branch.py#L1601 | get indices of lines that are in service and save that information in net | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | values.astype | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch.get_trafo_values | (trafo_df: pd.DataFrame \| dict, column: str, na_replacement: Any=pd.NA, raise_on_missing: Literal[True]=True) -> NDArray；(trafo_df: pd.DataFrame \| dict, column: str, na_replacement: Any=pd.NA, raise_on_missing: Literal[False]=False) -> NDArray \| None；(trafo_df: pd.DataFrame \| dict, column: str, na_replacement: Any=pd.NA, raise_on_missing: bool=False) -> NDArray \| None；(trafo_df: pd.DataFrame \| dict, column: str, na_replacement: Any=pd.NA, raise_on_missing: Literal[False]=False) -> NDArray \| None；(trafo_df: pd.DataFrame \| dict, column: str, na_replacement: Any=pd.NA, raise_on_missing: bool=False) -> NDArray \| None | pandapower/build_branch.py#L414；pandapower/build_branch.py#L420；pandapower/build_branch.py#L426；pandapower/build_branch.py#L420；pandapower/build_branch.py#L426 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_branch.get_trafo_values 的实现与调用链确认。；Get values from dataframe.；待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_branch.get_trafo_values 的实现与调用链确认。；Get values from dataframe. | 输入：trafo_df, column, na_replacement, raise_on_missing；输出：NDArray；输入：trafo_df, column, na_replacement, raise_on_missing；输出：NDArray \| None；输入：trafo_df, column, na_replacement, raise_on_missing；输出：NDArray \| None | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验；静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用；isinstance；astype；pd.isna；to_numpy；np.where；ValueError；series.infer_objects；静态扫描未发现直接函数调用；isinstance；astype；pd.isna；to_numpy；np.where；ValueError；series.infer_objects | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch.wye_delta | (zbr_n, s) | pandapower/build_branch.py#L1749 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_branch.wye_delta 的实现与调用链确认。 | 输入：zbr_n, s；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | np.array | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch.wye_delta_vector | (zbr_n, s) | pandapower/build_branch.py#L1755 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_branch.wye_delta_vector 的实现与调用链确认。 | 输入：zbr_n, s；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | np.array | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
| function | pandapower.build_branch.z_br_to_bus_vector | (z, sn) | pandapower/build_branch.py#L1744 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.build_branch.z_br_to_bus_vector 的实现与调用链确认。 | 输入：z, sn；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | np.array；min | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 抽检通过 |
