# `pandapower.pd2ppc`

本模块共记录 10 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.pd2ppc._build_gen_lookups | (net, element, f, t) | pandapower/pd2ppc.py#L459 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.pd2ppc._build_gen_lookups 的实现与调用链确认。 | 输入：net, element, f, t；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.arange；len；_init_lookup；element.split | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pd2ppc._check_line_dc_at_b2b_buses | (ppci) | pandapower/pd2ppc.py#L60 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.pd2ppc._check_line_dc_at_b2b_buses 的实现与调用链确认。 | 输入：ppci；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | astype；np.intersect1d；NotImplementedError；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pd2ppc._check_slack_at_vsc_bus | (ppci) | pandapower/pd2ppc.py#L83 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.pd2ppc._check_slack_at_vsc_bus 的实现与调用链确认。 | 输入：ppci；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.intersect1d；len；NotImplementedError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pd2ppc._check_vsc_different_ac_control_modes_at_same_bus | (ppci) | pandapower/pd2ppc.py#L70 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.pd2ppc._check_vsc_different_ac_control_modes_at_same_bus 的实现与调用链确认。 | 输入：ppci；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.hstack；len；NotImplementedError；np.intersect1d；combinations | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pd2ppc._init_lookup | (net, lookup_name, pandapower_index, ppc_index) | pandapower/pd2ppc.py#L470 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.pd2ppc._init_lookup 的实现与调用链确认。 | 输入：net, lookup_name, pandapower_index, ppc_index；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | _write_lookup_to_net；np.ones；max | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pd2ppc._init_ppc | (net, mode='pf', sequence=None) | pandapower/pd2ppc.py#L229 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.pd2ppc._init_ppc 的实现与调用链确认。 | 输入：net, mode, sequence；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.array；int | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pd2ppc._pd2ppc | (net, sequence=None, **kwargs) | pandapower/pd2ppc.py#L92 | 把 pandapower 元件表转换为完整 ppc 和只含在役元件的 ppci，并建立结果回写所需查找表。 | 输入：net, sequence, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _select_is_elements_numba；_init_ppc；_build_bus_ppc；_build_bus_dc_ppc；_build_branch_dc_ppc；_build_tcsc_ppc；_build_svc_ppc；_build_ssc_ppc；_build_vsc_ppc；_switch_branches；_branches_with_oos_buses；_build_gen_ppc；_replace_nans_with_default_limits；_ppc2ppci；_check_line_dc_at_b2b_buses；_check_vsc_different_ac_control_modes_at_same_bus | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 人工复核 |
| function | pandapower.pd2ppc._pd2ppc_recycle | (net, sequence, recycle) | pandapower/pd2ppc.py#L33 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.pd2ppc._pd2ppc_recycle 的实现与调用链确认。 | 输入：net, sequence, recycle；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _ppc2ppci；_pd2ppc；_calc_pq_elements_and_add_on_ppc；_build_gen_ppc；np.nan_to_num；net.get | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pd2ppc._ppc2ppci | (ppc, net, ppci=None) | pandapower/pd2ppc.py#L268 | Creates the ppci which is used to run the power flow / OPF... The ppci is similar to the ppc except that: 1. it contains no out of service elements 2. buses are sorted | 输入：ppc, net, ppci；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.vstack；astype；np.arange；np.zeros；_update_lookup_entries；copy；net._gen_order.items；np.any；ref_gens.astype；_init_ppc；len；_build_gen_lookups；run_userfcn；np.setdiff1d；np.array；np.append | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pd2ppc._update_lookup_entries | (net, lookup, e2i, element) | pandapower/pd2ppc.py#L452 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.pd2ppc._update_lookup_entries 的实现与调用链确认。 | 输入：net, lookup, e2i, element；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | _write_lookup_to_net | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
