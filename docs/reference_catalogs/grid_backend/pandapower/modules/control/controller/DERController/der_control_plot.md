# `pandapower.control.controller.DERController.der_control_plot`

本模块共记录 4 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.control.controller.DERController.der_control_plot._plot_pq_area | (min_max_q, title=None, ax=None, saturate_sn_pu: float=np.nan, circle_segment=90, p_samples=None, tex=False) | pandapower/control/controller/DERController/der_control_plot.py#L23 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.control.controller.DERController.der_control_plot._plot_pq_area 的实现与调用链确认。 | 输入：min_max_q, title, ax, saturate_sn_pu, circle_segment, p_samples, tex；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | ax.plot；ax.set_xlabel；ax.set_ylabel；ax.legend；ax.grid；ax.set_ylim；ax.fill_betweenx；plt.tight_layout；np.linspace；plt.figure；plt.gca；ax.set_title；p_samples.min；p_samples.max；np.isnan；generate_circle_segment | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.control.controller.DERController.der_control_plot.generate_circle_segment | (center_x, center_y, radius, start, stop, step) | pandapower/control/controller/DERController/der_control_plot.py#L100 | generates coordinates for a semicircle, centered at center_x, center_y generates x and y coordinates for a segment of a circle, centered at center_x, center_y. The segment (start, stop, step) is given in degree. Which degree means which direction? 0: "east", 90: "north", 180: "west", 270: "south" | 输入：center_x, center_y, radius, start, stop, step；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.array；np.radians；np.arange；np.cos；np.sin | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.control.controller.DERController.der_control_plot.plot_pq_area | (pq_area, **kwargs) | pandapower/control/controller/DERController/der_control_plot.py#L10 | Assumption: there is no q restriction at vm = 1 pu | 输入：pq_area, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | kwargs.pop；pq_area.q_flexibility；_plot_pq_area；np.linspace | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.control.controller.DERController.der_control_plot.plot_qv_area | (qv_area, title=None, ax=None, prune_to_flexibility=False, vm_samples=None, tex=False) | pandapower/control/controller/DERController/der_control_plot.py#L63 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.control.controller.DERController.der_control_plot.plot_qv_area 的实现与调用链确认。 | 输入：qv_area, title, ax, prune_to_flexibility, vm_samples, tex；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | qv_area.q_flexibility；ax.plot；ax.set_xlabel；ax.set_ylabel；ax.legend；ax.grid；ax.fill_between；plt.tight_layout；np.linspace；plt.figure；plt.gca；ax.set_title；min；max；np.arange；np.isclose | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
