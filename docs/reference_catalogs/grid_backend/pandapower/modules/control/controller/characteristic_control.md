# `pandapower.control.controller.characteristic_control`

本模块共记录 6 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | pandapower.control.controller.characteristic_control.CharacteristicControl | class(Controller) | pandapower/control/controller/characteristic_control.py#L11 | Controller that adjusts a certain parameter of an element in pandapower net based on a specified input parameter in pandapower net, according to a provided characteristic. The characteristic is specified by the index in the ``net.characteristic table``. Example: change the tap position of the transformers (``net.trafo.tap_pos``) based on transformer loading (``net.res_trafo.loading_percent``) according to a specified linear relationship. To this end, the input element is ``res_trafo``, the input variable is ``loading_percent``, the output element is ``trafo`` and the output variable is ``tap_pos``. The relationship between the values of the input and output variables is specified using the Characteristic class (or a scipy interpolator, e.g. ``scipy.interpolate.interp1d``). | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；_detect_read_write_flag；read_from_net；write_to_net；np.all；__str__；super；np.abs | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.control.controller.characteristic_control.CharacteristicControl.__init__ | (self, net, output_element, output_variable, output_element_index, input_element, input_variable, input_element_index, characteristic_index, tol=0.001, in_service=True, order=0, level=0, drop_same_existing_ctrl=False, matching_params=None, **kwargs) | pandapower/control/controller/characteristic_control.py#L41 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.control.controller.characteristic_control.CharacteristicControl.__init__ 的实现与调用链确认。 | 输入：net, output_element, output_variable, output_element_index, input_element, input_variable, input_element_index, characteristic_index, tol, in_service, order, level, drop_same_existing_ctrl, matching_params, **kwargs；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；_detect_read_write_flag；super | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.control.controller.characteristic_control.CharacteristicControl.__str__ | (self) | pandapower/control/controller/characteristic_control.py#L96 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.control.controller.characteristic_control.CharacteristicControl.__str__ 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | __str__；super | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.control.controller.characteristic_control.CharacteristicControl.control_step | (self, net) | pandapower/control/controller/characteristic_control.py#L90 | Set applied to true to make sure it runs at least once | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.control.controller.characteristic_control.CharacteristicControl.initialize_control | (self, net) | pandapower/control/controller/characteristic_control.py#L64 | At the beginning of each run_control call reset applied-flag | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.control.controller.characteristic_control.CharacteristicControl.is_converged | (self, net) | pandapower/control/controller/characteristic_control.py#L71 | Actual implementation of the convergence criteria: If controller is applied, it can stop | 输入：net；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | read_from_net；write_to_net；np.all；np.abs | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
