# `pandapower.control.controller.trafo.DiscreteTapControl`

本模块共记录 7 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | pandapower.control.controller.trafo.DiscreteTapControl.DiscreteTapControl | class(TrafoController) | pandapower/control/controller/trafo/DiscreteTapControl.py#L12 | Trafo Controller with local tap changer voltage control. | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | __init__；kwargs.get；np.array；cls；initialize_control；hasattr；self.nothing_to_do；read_from_net；copy；np.where；np.vstack；write_to_net；np.isnan；np.logical_or；np.all；np.full | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.control.controller.trafo.DiscreteTapControl.DiscreteTapControl.__init__ | (self, net, element_index, vm_lower_pu, vm_upper_pu, side='lv', element='trafo', tol=0.001, in_service=True, hunting_limit=None, level=0, order=0, drop_same_existing_ctrl=False, matching_params=None, **kwargs) | pandapower/control/controller/trafo/DiscreteTapControl.py#L29 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.control.controller.trafo.DiscreteTapControl.DiscreteTapControl.__init__ 的实现与调用链确认。 | 输入：net, element_index, vm_lower_pu, vm_upper_pu, side, element, tol, in_service, hunting_limit, level, order, drop_same_existing_ctrl, matching_params, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | __init__；kwargs.get；np.array；super；np.abs | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.control.controller.trafo.DiscreteTapControl.DiscreteTapControl.control_step | (self, net) | pandapower/control/controller/trafo/DiscreteTapControl.py#L93 | Implements one step of the Discrete controller, always stepping only one tap position up or down | 输入：net；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | self.nothing_to_do；read_from_net；copy；np.where；np.vstack；write_to_net；np.logical_and | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.control.controller.trafo.DiscreteTapControl.DiscreteTapControl.from_tap_step_percent | (cls, net, element_index, vm_set_pu, side='lv', element='trafo', tol=0.001, in_service=True, hunting_limit=None, level=0, order=0, drop_same_existing_ctrl=False, matching_params=None, **kwargs) | pandapower/control/controller/trafo/DiscreteTapControl.py#L49 | Alternative mode of the controller, which uses a set point for voltage and the value of net.trafo.tap_step_percent to calculate vm_upper_pu and vm_lower_pu. To this end, the parameter vm_set_pu should be provided, instead of vm_lower_pu and vm_upper_pu. To use this mode of the controller, the controller can be initialized as following: | 输入：net, element_index, vm_set_pu, side, element, tol, in_service, hunting_limit, level, order, drop_same_existing_ctrl, matching_params, **kwargs；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | cls | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.control.controller.trafo.DiscreteTapControl.DiscreteTapControl.initialize_control | (self, net) | pandapower/control/controller/trafo/DiscreteTapControl.py#L83 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.control.controller.trafo.DiscreteTapControl.DiscreteTapControl.initialize_control 的实现与调用链确认。 | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | initialize_control；hasattr；np.full；super；len；np.abs | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.control.controller.trafo.DiscreteTapControl.DiscreteTapControl.is_converged | (self, net) | pandapower/control/controller/trafo/DiscreteTapControl.py#L121 | Checks if the voltage is within the desired voltage band, then returns True | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | self.nothing_to_do；read_from_net；np.isnan；np.where；np.logical_or；np.all；np.logical_and | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.control.controller.trafo.DiscreteTapControl.DiscreteTapControl.vm_set_pu | (self)；(self, value)；(self, value) | pandapower/control/controller/trafo/DiscreteTapControl.py#L72；pandapower/control/controller/trafo/DiscreteTapControl.py#L76；pandapower/control/controller/trafo/DiscreteTapControl.py#L76 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.control.controller.trafo.DiscreteTapControl.DiscreteTapControl.vm_set_pu 的实现与调用链确认。 | 输入：无显式输入；输出：返回值未注解；输入：value；输出：返回值未注解；输入：value；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验；可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
