# `pandapower.control.util.auxiliary`

本模块共记录 15 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.control.util.auxiliary._controller_attributes_query | (controller, parameters) | pandapower/control/util/auxiliary.py#L72 | Returns a boolean if the controller attributes matches given parameter dict data | 输入：controller, parameters；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | parameters.keys；len；logger.debug；bool；set；ensure_iterability；str；controller.__getattribute__；all | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.control.util.auxiliary._create_shunt_characteristics | (net, shunt_index, variable, x_points, y_points) | pandapower/control/util/auxiliary.py#L348 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.control.util.auxiliary._create_shunt_characteristics 的实现与调用链确认。 | 输入：net, shunt_index, variable, x_points, y_points；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | copy；pd.DataFrame；hasattr；UserWarning；zip；SplineCharacteristic；len | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.control.util.auxiliary._create_trafo_characteristics | (net, trafotable, trafo_index, variable, x_points, y_points) | pandapower/control/util/auxiliary.py#L272 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.control.util.auxiliary._create_trafo_characteristics 的实现与调用链确认。 | 输入：net, trafotable, trafo_index, variable, x_points, y_points；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | copy；list；pd.DataFrame；hasattr；dict.fromkeys；UserWarning；zip；SplineCharacteristic；len | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.control.util.auxiliary._set_reactive_capability_curve_flag | (net, element) | pandapower/control/util/auxiliary.py#L387 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.control.util.auxiliary._set_reactive_capability_curve_flag 的实现与调用链确认。 | 输入：net, element；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | UserWarning；logger.info；astype；len；issubset；any；isin；notna | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.control.util.auxiliary.asarray | (val, dtype=np.float64) | pandapower/control/util/auxiliary.py#L32 | make sure the value is a numpy array of a certain dtype in contrast to np.asarray, it also converts a scalar value to an array, e.g. asarray(0) = array([0]) :param val: scalar or list-like or pandas index input :param dtype: np.dtype for the output array :return: numpy array of the dtype set | 输入：val, dtype；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.asarray；np.isscalar；np.ndim | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.control.util.auxiliary.create_q_capability_characteristics_object | (net) | pandapower/control/util/auxiliary.py#L405 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.control.util.auxiliary.create_q_capability_characteristics_object 的实现与调用链确认。 | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | time.time；_set_reactive_capability_curve_flag；pd.DataFrame；characteristic_df_temp.groupby；set_index；combine_first；logger.debug；net.keys；mydata_grouped.size；np.hstack；element_ids.append；q_min_indices.append；q_max_indices.append；map；pd.Series；Characteristic | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.control.util.auxiliary.create_shunt_characteristic_object | (net) | pandapower/control/util/auxiliary.py#L316 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.control.util.auxiliary.create_shunt_characteristic_object 的实现与调用链确认。 | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | sort_index；insert；time.time；logger.info；iterrows；pop；_create_shunt_characteristics；map；net.shunt.dropna；to_list；tolist；pd.notna | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.control.util.auxiliary.create_trafo_characteristic_object | (net) | pandapower/control/util/auxiliary.py#L215 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.control.util.auxiliary.create_trafo_characteristic_object 的实现与调用链确认。 | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | sort_index；insert；any；time.time；logger.info；iterrows；pop；_create_trafo_characteristics；map；notna；net.trafo.dropna；to_list；net.trafo3w.dropna；tolist；pd.notna | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.control.util.auxiliary.drop_same_type_existing_controllers | (net, this_ctrl_type, index=None, matching_params=None, **kwargs) | pandapower/control/util/auxiliary.py#L171 | Drops same type controllers to create a new controller of this type. INPUT: **net** - pandapower net | 输入：net, this_ctrl_type, index, matching_params, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | str；isinstance；get_controller_index；len；logger.info；net.controller.drop；logger.debug | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.control.util.auxiliary.get_controller_index | (net, ctrl_type=None, parameters=None, idx=[]) | pandapower/control/util/auxiliary.py#L104 | Returns indices of searched controllers. Parameters can specify the search query. | 输入：net, ctrl_type, parameters, idx；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | isinstance；len；list；Index；net.controller.object.apply；get_controller_index_by_typename；get_controller_index_by_type；idx.intersection；parameters.keys；set；_controller_attributes_query；net.controller.index.isin | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.control.util.auxiliary.get_controller_index_by_type | (net, ctrl_type, idx=[]) | pandapower/control/util/auxiliary.py#L51 | Returns controller indices of a given type as list. | 输入：net, ctrl_type, idx；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | net.controller.object.apply；list；len；isinstance；net.controller.index.isin | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.control.util.auxiliary.get_controller_index_by_typename | (net, typename, idx=[], case_sensitive=False) | pandapower/control/util/auxiliary.py#L60 | Returns controller indices of a given name of type as list. | 输入：net, typename, idx, case_sensitive；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；lower；typename.lower；split；str | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.control.util.auxiliary.get_min_max_q_mvar_from_characteristics_object | (net, element, element_index) | pandapower/control/util/auxiliary.py#L479 | Calculates the minimum and maximum reactive power (q_mvar) for a given element ('gen' or 'sgen') using its Q capability characteristic curve. | 输入：net, element, element_index；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | logger.warning；len；np.column_stack；logger.info；fillna；np.vectorize；np.any；pd.isna；func | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.control.util.auxiliary.log_same_type_existing_controllers | (net, this_ctrl_type, index=None, matching_params=None)；(net, this_ctrl_type, index=None, matching_params=None, **kwargs)；(net, this_ctrl_type, index=None, matching_params=None, **kwargs) | pandapower/control/util/auxiliary.py#L139；pandapower/control/util/auxiliary.py#L143；pandapower/control/util/auxiliary.py#L145；pandapower/control/util/auxiliary.py#L143；pandapower/control/util/auxiliary.py#L145 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.control.util.auxiliary.log_same_type_existing_controllers 的实现与调用链确认。；Logs same type controllers, if a controller is created.；待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.control.util.auxiliary.log_same_type_existing_controllers 的实现与调用链确认。；Logs same type controllers, if a controller is created. | 输入：net, this_ctrl_type, index, matching_params；输出：返回值未注解；输入：net, this_ctrl_type, index, matching_params, **kwargs；输出：返回值未注解；输入：net, this_ctrl_type, index, matching_params, **kwargs；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用；deprecated；str；isinstance；get_controller_index；len；logger.info；logger.debug；deprecated；str；isinstance；get_controller_index；len；logger.info；logger.debug | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.control.util.auxiliary.plot_characteristic | (characteristic, start, stop, num=20, xlabel=None, ylabel=None) | pandapower/control/util/auxiliary.py#L203 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.control.util.auxiliary.plot_characteristic 的实现与调用链确认。 | 输入：characteristic, start, stop, num, xlabel, ylabel；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.linspace；characteristic；plt.plot；soft_dependency_error；plt.xlabel；plt.ylabel；str；sys._getframe | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
