# `pandapower.shortcircuit.currents`

本模块共记录 8 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.shortcircuit.currents._calc_branch_currents | (net, ppci, bus_idx) | pandapower/shortcircuit/currents.py#L304 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.currents._calc_branch_currents 的实现与调用链确认。 | 输入：net, ppci, bus_idx；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | astype；np.conj；np.shape；np.zeros；enumerate；Yf.dot；Yt.dot；any；np.any；np.tile；abs；np.nan_to_num；np.real；ybus_fact；np.sqrt；np.isnan | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.currents._calc_branch_currents_complex | (net, ppci, bus_idx) | pandapower/shortcircuit/currents.py#L417 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.currents._calc_branch_currents_complex 的实现与调用链确认。 | 输入：net, ppci, bus_idx；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | astype；Yf.dot；Yt.dot；np.arange；flatten；nan_minmax；np.shape；np.exp；any；np.any；np.tile；enumerate；np.conj；len；np.nan_to_num；np.angle | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.currents._calc_ikss | (net, ppci, bus_idx) | pandapower/shortcircuit/currents.py#L25 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.currents._calc_ikss 的实现与调用链确认。 | 输入：net, ppci, bus_idx；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | net._options.get；_current_source_current；np.any；np.shape；np.full；abs；np.angle；np.sqrt；warnings.warn；np.zeros；enumerate；np.abs；np.isnan；np.exp；np.arange；np.deg2rad | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.currents._calc_ikss_1ph | (net, ppci, ppci_0, bus_idx) | pandapower/shortcircuit/currents.py#L103 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.currents._calc_ikss_1ph 的实现与调用链确认。 | 输入：net, ppci, ppci_0, bus_idx；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | abs；_current_source_current；np.sqrt | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.currents._calc_ip | (net, ppci) | pandapower/shortcircuit/currents.py#L217 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.currents._calc_ip 的实现与调用链确认。 | 输入：net, ppci；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.sqrt | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.currents._calc_ith | (net, ppci) | pandapower/shortcircuit/currents.py#L222 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.currents._calc_ith 的实现与调用链确认。 | 输入：net, ppci；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.nonzero；np.sqrt；np.exp；np.log | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.currents._current_source_current | (net, ppci, bus_idx) | pandapower/shortcircuit/currents.py#L131 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.currents._current_source_current 的实现与调用链确认。 | 输入：net, ppci, bus_idx；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.all；any；_sum_by_group；ikcv_pu.flatten；np.abs；len；pd.isnull；ValueError；np.deg2rad；np.where；np.angle；copy；_calc_zbus_diag；sgen.sn_mva.values.reshape；np.exp；np.dot | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.currents.nan_minmax | (a, rows, argminmax) | pandapower/shortcircuit/currents.py#L408 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.currents.nan_minmax 的实现与调用链确认。 | 输入：a, rows, argminmax；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | all；np.zeros；np.isnan；argminmax；np.abs | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
