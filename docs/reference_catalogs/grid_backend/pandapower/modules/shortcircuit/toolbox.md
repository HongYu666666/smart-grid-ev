# `pandapower.shortcircuit.toolbox`

本模块共记录 6 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.shortcircuit.toolbox._create_aux_net | (net, line_ix, distance_to_bus0) | pandapower/shortcircuit/toolbox.py#L95 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.toolbox._create_aux_net 的实现与调用链确认。 | 输入：net, line_ix, distance_to_bus0；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | deepcopy；create_bus；_create_element_from_exisiting；UserWarning；aux_net.switch.query | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.toolbox._create_element_from_exisiting | (net, ele_type, ele_ix) | pandapower/shortcircuit/toolbox.py#L84 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.toolbox._create_element_from_exisiting 的实现与调用链确认。 | 输入：net, ele_type, ele_ix；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pd.Series；pd.concat；_preserve_dtypes；to_dict；index.to_numpy；_get_index_with_check；ps.to_frame | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.toolbox.adjacency | (ppci) | pandapower/shortcircuit/toolbox.py#L202 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.toolbox.adjacency 的实现与调用链确认。 | 输入：ppci；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.flatnonzero；len；real.astype；np.zeros；np.diag_indices；matrix.sum | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.toolbox.adjust_V0_for_trafo_tap | (ppci, V0, bus_idx) | pandapower/shortcircuit/toolbox.py#L157 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.toolbox.adjust_V0_for_trafo_tap 的实现与调用链确认。 | 输入：ppci, V0, bus_idx；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.flatnonzero；real.astype；len；nx.MultiGraph；mg.add_nodes_from；mg.add_edges_from；zip；tolist；connected_component；np.intersect1d；astype | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.toolbox.calc_sc_on_line | (net, line_ix, distance_to_bus0, **kwargs) | pandapower/shortcircuit/toolbox.py#L132 | Calculate the shortcircuit in the middle of the line, returns a modified network with the shortcircuit calculation results and the bus added | 输入：net, line_ix, distance_to_bus0, **kwargs；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | _create_aux_net；rundcpp；calc_sc | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.toolbox.detect_power_station_unit | (net, mode='auto', max_gen_voltage_kv=80, max_distance_km=0.01) | pandapower/shortcircuit/toolbox.py#L27 | Identifies the power station units configuration (gen and trafo) according to IEC 60909. Updates the power_station_trafo in the gen table | 输入：net, mode, max_gen_voltage_kv, max_distance_km；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | logger.info；create_nxgraph；mode.lower；pd.Series；np.intersect1d；UserWarning；nx.single_source_dijkstra_path_length；len；logger.warning；sum；np.isin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
