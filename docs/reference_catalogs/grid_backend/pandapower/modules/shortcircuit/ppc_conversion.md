# `pandapower.shortcircuit.ppc_conversion`

本模块共记录 8 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.shortcircuit.ppc_conversion._add_gen_sc_z_kg_ks | (net, ppc) | pandapower/shortcircuit/ppc_conversion.py#L147 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.ppc_conversion._add_gen_sc_z_kg_ks 的实现与调用链确认。 | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.nan_to_num；np.sqrt；_sum_by_group；np.full_like；np.any；len；np.clip；values.astype；np.isnan；gen.power_station_trafo.isnull；np.square；pd.Series；np.abs；np.arange | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.ppc_conversion._add_kt | (net, ppc) | pandapower/shortcircuit/ppc_conversion.py#L65 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.ppc_conversion._add_kt 的实现与调用链确认。 | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _transformer_correction_factor；net._options.get；get_trafo_values | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.ppc_conversion._add_sgen_sc_z | (net, ppc) | pandapower/shortcircuit/ppc_conversion.py#L100 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.ppc_conversion._add_sgen_sc_z 的实现与调用链确认。 | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；_sum_by_group；np.sqrt；np.square | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.ppc_conversion._add_ward_sc_z | (net, ppc) | pandapower/shortcircuit/ppc_conversion.py#L78 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.ppc_conversion._add_ward_sc_z 的实现与调用链确认。 | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _sum_by_group；len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.ppc_conversion._check_sc_data_integrity | (net) | pandapower/shortcircuit/ppc_conversion.py#L49 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.ppc_conversion._check_sc_data_integrity 的实现与调用链确认。 | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.ppc_conversion._create_k_updated_ppci | (net, ppci_orig, ppci_bus, zero_sequence=False) | pandapower/shortcircuit/ppc_conversion.py#L274 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.ppc_conversion._create_k_updated_ppci 的实现与调用链确认。 | 输入：net, ppci_orig, ppci_bus, zero_sequence；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | deepcopy；np.any；np.isnan；np.argwhere；real.astype；np.ix_；astype；bus_ppci.update；np.isfinite；UserWarning | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.ppc_conversion._get_is_ppci_bus | (net, bus) | pandapower/shortcircuit/ppc_conversion.py#L24 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.ppc_conversion._get_is_ppci_bus 的实现与调用链确认。 | 输入：net, bus；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.unique；np.isin | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.shortcircuit.ppc_conversion._init_ppc | (net) | pandapower/shortcircuit/ppc_conversion.py#L30 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.shortcircuit.ppc_conversion._init_ppc 的实现与调用链确认。 | 输入：net；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _check_sc_data_integrity；_add_auxiliary_elements；_pd2ppc；_add_kt；_add_gen_sc_z_kg_ks；_add_sgen_sc_z；_add_ward_sc_z；_ppc2ppci | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
