# `pandapower.sql_io`

本模块共记录 17 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.sql_io.add_timestamp_column | (conn, cursor, table_name) | pandapower/sql_io.py#L214 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.sql_io.add_timestamp_column 的实现与调用链确认。 | 输入：conn, cursor, table_name；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | cursor.execute；conn.commit；format；to_sql_str；psql.SQL | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.sql_io.check_if_sql_table_exists | (cursor, table_name) | pandapower/sql_io.py#L56 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.sql_io.check_if_sql_table_exists 的实现与调用链确认。 | 输入：cursor, table_name；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | cursor.execute；cursor.fetchone；table_name.split | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.sql_io.check_postgresql_catalogue_table | (cursor, table_name, grid_id, grid_id_column, download=False) | pandapower/sql_io.py#L162 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.sql_io.check_postgresql_catalogue_table 的实现与调用链确认。 | 输入：cursor, table_name, grid_id, grid_id_column, download；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | check_if_sql_table_exists；get_sql_table_columns；format；cursor.execute；cursor.fetchone；UserWarning；to_sql_str；psql.SQL | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.sql_io.create_postgresql_catalogue_entry | (conn, cursor, grid_id, grid_id_column, catalogue_table_name) | pandapower/sql_io.py#L195 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.sql_io.create_postgresql_catalogue_entry 的实现与调用链确认。 | 输入：conn, cursor, grid_id, grid_id_column, catalogue_table_name；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | check_postgresql_catalogue_table；format；cursor.execute；conn.commit；cursor.fetchone；psql.SQL；to_sql_str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.sql_io.create_sql_table_if_not_exists | (conn, cursor, table_name, grid_id_column, catalogue_table_name) | pandapower/sql_io.py#L222 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.sql_io.create_sql_table_if_not_exists 的实现与调用链确认。 | 输入：conn, cursor, table_name, grid_id_column, catalogue_table_name；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | format；cursor.execute；conn.commit；psql.SQL；to_sql_str | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.sql_io.delete_postgresql_net | (grid_id: int, dsn: str, schema: str, grid_id_column: str='grid_id', grid_catalogue_name: str='grid_catalogue') -> None | pandapower/sql_io.py#L233 | Removes a grid model from the PostgreSQL database. | 输入：grid_id, dsn, schema, grid_id_column, grid_catalogue_name；输出：None | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | UserWarning；psycopg.connect；conn.cursor；check_postgresql_catalogue_table；format；cursor.execute；conn.commit；to_sql_str；psql.SQL | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.sql_io.download_sql_table | (cursor, table_name, **id_columns) | pandapower/sql_io.py#L73 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.sql_io.download_sql_table 的实现与调用链确认。 | 输入：cursor, table_name, **id_columns；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | check_if_sql_table_exists；cursor.execute；cursor.fetchall；pd.DataFrame；UserWarning；len；format；join；pd.option_context；infer_objects；df.set_index；df.drop；id_columns.keys；to_sql_str；psql.SQL；df.fillna | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.sql_io.from_postgresql | (grid_id: int, dsn: str, schema: str, grid_id_column: str='grid_id', grid_catalogue_name: str='grid_catalogue', empty_dict_like_object: Optional[dict]=None, grid_tables=None) | pandapower/sql_io.py#L457 | Downloads an existing pandapowerNet from a PostgreSQL database. | 输入：grid_id, dsn, schema, grid_id_column, grid_catalogue_name, empty_dict_like_object, grid_tables；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | UserWarning；psycopg.connect；from_sql | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.sql_io.from_sql | (conn, schema, grid_id, grid_id_column='grid_id', grid_catalogue_name='grid_catalogue', empty_dict_like_object=None, grid_tables=None) | pandapower/sql_io.py#L269 | Downloads an existing pandapowerNet from a PostgreSQL database. | 输入：conn, schema, grid_id, grid_id_column, grid_catalogue_name, empty_dict_like_object, grid_tables；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | conn.cursor；io_utils.from_dict_of_dfs；check_postgresql_catalogue_table；download_sql_table；tab.geo.replace；logger.debug；logger.info | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.sql_io.from_sqlite | (filename) | pandapower/sql_io.py#L390 | Loads a grid model from SQLite format | 输入：filename；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | UserWarning；sqlite3.connect；conn.cursor；cursor.execute；cursor.fetchall；io_utils.from_dict_of_dfs；pd.read_sql_query；format；t.replace | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.sql_io.get_sql_table_columns | (cursor, table_name) | pandapower/sql_io.py#L63 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.sql_io.get_sql_table_columns 的实现与调用链确认。 | 输入：cursor, table_name；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | cursor.execute；colnames.index；cursor.fetchall；table_name.split | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.sql_io.match_sql_type | (dtype) | pandapower/sql_io.py#L41 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.sql_io.match_sql_type 的实现与调用链确认。 | 输入：dtype；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | UserWarning | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.sql_io.to_postgresql | (net: pandapowerNet, dsn: str, schema: str, include_results: bool=False, grid_id: Optional[int]=None, grid_id_column: str='grid_id', grid_catalogue_name: str='grid_catalogue', index_name=None) -> int | pandapower/sql_io.py#L417 | Uploads a pandapowerNet to a PostgreSQL database. The database must exist, the element tables are created if they do not exist. JSON serialization (e.g. for controller objects) is not implemented yet. | 输入：net, dsn, schema, include_results, grid_id, grid_id_column, grid_catalogue_name, index_name；输出：int | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | logger.debug；UserWarning；psycopg.connect；to_sql | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.sql_io.to_sql | (net, conn, schema, include_results=False, grid_id=None, grid_id_column='grid_id', grid_catalogue_name='grid_catalogue', index_name=None) | pandapower/sql_io.py#L322 | Uploads a pandapowerNet to a PostgreSQL database. The database must exist, the element tables are created if they do not exist. TODO: JSON serialization (e.g. for controller objects) is not implemented yet. | 输入：net, conn, schema, include_results, grid_id, grid_id_column, grid_catalogue_name, index_name；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | conn.cursor；io_utils.to_dict_of_dfs；create_postgresql_catalogue_entry；pd.DataFrame；d.items；d.keys；create_sql_table_if_not_exists；upload_sql_table；logger.debug | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.sql_io.to_sql_str | (string: str) -> 'psql.Identifier' | pandapower/sql_io.py#L37 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.sql_io.to_sql_str 的实现与调用链确认。 | 输入：string；输出：'psql.Identifier' | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | psql.Identifier；string.split | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.sql_io.to_sqlite | (net, filename, include_results=False) | pandapower/sql_io.py#L369 | Saves pandapowerNet an SQLite format | 输入：net, filename, include_results；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | UserWarning；sqlite3.connect；io_utils.to_dict_of_dfs；dodfs.items；data.to_sql | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.sql_io.upload_sql_table | (conn, cursor, table_name, table, index_name=None, timestamp=False, **id_columns) | pandapower/sql_io.py#L99 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.sql_io.upload_sql_table 的实现与调用链确认。 | 输入：conn, cursor, table_name, table, index_name, timestamp, **id_columns；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | table.where；get_sql_table_columns；format；cursor.executemany；conn.commit；pd.notnull；match_sql_type；len；tuple；logger.info；cursor.execute；add_timestamp_column；to_sql_str；str；id_columns.keys；zip | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
