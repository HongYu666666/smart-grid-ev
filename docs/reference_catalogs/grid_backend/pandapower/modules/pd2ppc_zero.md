# `pandapower.pd2ppc_zero`

本模块共记录 8 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.pd2ppc_zero._add_ext_grid_sc_impedance_zero | (net, ppc) | pandapower/pd2ppc_zero.py#L495 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.pd2ppc_zero._add_ext_grid_sc_impedance_zero 的实现与调用链确认。 | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _sum_by_group；len；ValueError；np.sqrt | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pd2ppc_zero._add_gen_sc_impedance_zero | (net, ppc) | pandapower/pd2ppc_zero.py#L477 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.pd2ppc_zero._add_gen_sc_impedance_zero 的实现与调用链确认。 | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pd2ppc_zero._add_impedance_sc_impedance_zero | (net, ppc) | pandapower/pd2ppc_zero.py#L589 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.pd2ppc_zero._add_impedance_sc_impedance_zero 的实现与调用链确认。 | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _calc_impedance_parameters_from_dataframe；values.astype | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pd2ppc_zero._add_line_sc_impedance_zero | (net, ppc) | pandapower/pd2ppc_zero.py#L549 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.pd2ppc_zero._add_line_sc_impedance_zero 的实现与调用链确认。 | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | astype；np.square；_end_temperature_correction_factor | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pd2ppc_zero._add_trafo3w_sc_impedance_zero | (net, ppc) | pandapower/pd2ppc_zero.py#L615 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.pd2ppc_zero._add_trafo3w_sc_impedance_zero 的实现与调用链确认。 | 输入：net, ppc；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _trafo_df_from_trafo3w；astype；_calc_branch_values_from_trafo_df；np.arange；get_trafo_values；t3.vector_group.lower；join；product；UserWarning | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pd2ppc_zero._add_trafo_sc_impedance_zero | (net, ppc, trafo_df=None, k_st=None) | pandapower/pd2ppc_zero.py#L144 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.pd2ppc_zero._add_trafo_sc_impedance_zero 的实现与调用链确认。 | 输入：net, ppc, trafo_df, k_st；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | range；astype；trafo_df.groupby；np.ones；ValueError；values.astype；trafos.si0_hv_partial.values.astype；trafos.parallel.values.astype；_calc_tap_from_dataframe；_calc_nominal_ratio_from_dataframe；z_sc.astype；r_sc.astype；np.zeros；len；get_trafo_values；vector_group.lower | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pd2ppc_zero._build_branch_ppc_zero | (net, ppc, k_st=None) | pandapower/pd2ppc_zero.py#L105 | Takes the empty ppc network and fills it with the zero imepdance branch values. The branch datatype will be np.complex 128 afterwards. | 输入：net, ppc, k_st；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _initialize_branch_lookup；np.zeros；np.array；_add_line_sc_impedance_zero；_add_impedance_sc_impedance_zero；_add_trafo_sc_impedance_zero；np.empty；branch_sc.fill；np.hstack；_calc_switch_parameter；_add_trafo3w_sc_impedance_zero；NotImplementedError | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.pd2ppc_zero._pd2ppc_zero | (net, k_st, sequence=0) | pandapower/pd2ppc_zero.py#L63 | Builds the ppc data structure for zero impedance system. Includes the impedance values of lines and transformers, but no load or generation data. | 输入：net, k_st, sequence；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | _select_is_elements_numba；_init_ppc；_build_bus_ppc；_build_bus_dc_ppc；_build_gen_ppc；_build_svc_ppc；_build_tcsc_ppc；_build_ssc_ppc；_build_vsc_ppc；_add_gen_sc_impedance_zero；_add_ext_grid_sc_impedance_zero；_build_branch_ppc_zero；_build_branch_dc_ppc；_switch_branches；_branches_with_oos_buses；hasattr | 可参考：是否进入适配器边界由后续 PoC 决定 | 源码确认（静态 AST） | 自动生成 |
