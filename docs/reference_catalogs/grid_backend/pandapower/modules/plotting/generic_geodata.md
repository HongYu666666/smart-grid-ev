# `pandapower.plotting.generic_geodata`

本模块共记录 9 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.plotting.generic_geodata._get_element_mask_from_nodes | (net, element, node_elements, nodes=None) | pandapower/plotting/generic_geodata.py#L113 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.generic_geodata._get_element_mask_from_nodes 的实现与调用链确认。 | 输入：net, element, node_elements, nodes；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | astype；np.ones；np.isin；len | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.generic_geodata._get_switch_mask | (net, element, switch_element, open_switches) | pandapower/plotting/generic_geodata.py#L120 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.generic_geodata._get_switch_mask 的实现与调用链确认。 | 输入：net, element, switch_element, open_switches；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | np.isin | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.generic_geodata._igraph_meshed | (g) | pandapower/plotting/generic_geodata.py#L107 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.generic_geodata._igraph_meshed 的实现与调用链确认。 | 输入：g；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | range；g.vcount；len；g.get_all_shortest_paths | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.generic_geodata._prepare_geodata_table | (net, geodata_table, overwrite) | pandapower/plotting/generic_geodata.py#L234 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.generic_geodata._prepare_geodata_table 的实现与调用链确认。 | 输入：net, geodata_table, overwrite；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pd.DataFrame；drop；dropna；UserWarning | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.generic_geodata.build_igraph_from_pp | (net, respect_switches=False, buses=None, trafo_length_km=0.01, switch_length_km=0.001, dcline_length_km=1.0) | pandapower/plotting/generic_geodata.py#L29 | This function uses the igraph library to create an igraph graph for a given pandapower network. Lines, transformers and switches are respected. Performance vs. networkx: https://graph-tool.skewed.de/performance | 输入：net, respect_switches, buses, trafo_length_km, switch_length_km, dcline_length_km；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | igraph.Graph；len；g.add_vertices；list；dict；_get_element_mask_from_nodes；itertuples；_igraph_meshed；soft_dependency_error；np.array；_get_switch_mask；g.add_edge；zip；net.switch.closed.values.astype；str；range | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.generic_geodata.coords_from_igraph | (graph, roots, meshed=False, calculate_meshed=False) | pandapower/plotting/generic_geodata.py#L126 | Create a list of generic coordinates from an igraph graph layout. | 输入：graph, roots, meshed, calculate_meshed；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | list；range；graph.layout；graph.to_undirected；zip；len；graph.get_all_shortest_paths | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.generic_geodata.coords_from_nxgraph | (mg=None, layout_engine='neato') | pandapower/plotting/generic_geodata.py#L154 | Create a list of generic coordinates from a networkx graph layout. | 输入：mg, layout_engine；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | mg.edges；list；zip；get；int；values；nx.drawing.nx_agraph.graphviz_layout | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.generic_geodata.create_generic_coordinates | (net, mg=None, library='igraph', respect_switches=False, geodata_table='bus', buses=None, overwrite=False, layout_engine='neato', trafo_length_km=0.01, switch_length_km=0.001) | pandapower/plotting/generic_geodata.py#L174 | This function will add arbitrary geo-coordinates for all buses based on an analysis of branches and rings. It will remove out of service buses/lines from the net. The coordinates will be created either by igraph or by using networkx library. | 输入：net, mg, library, respect_switches, geodata_table, buses, overwrite, layout_engine, trafo_length_km, switch_length_km；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | _prepare_geodata_table；len；build_igraph_from_pp；coords_from_igraph；pd.Series；soft_dependency_error；coords_from_nxgraph；ValueError；map；create_nxgraph；copy.deepcopy；zip；geojson.dumps；geojson.Point | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.generic_geodata.fuse_geodata | (net) | pandapower/plotting/generic_geodata.py#L245 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.generic_geodata.fuse_geodata 的实现与调用链确认。 | 输入：net；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | create_nxgraph；set；connected_components；net.bus.dropna；len；apply；geojson.dumps；np.mean；geojson.Point；list | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
