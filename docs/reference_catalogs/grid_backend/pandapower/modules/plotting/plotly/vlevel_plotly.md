# `pandapower.plotting.plotly.vlevel_plotly`

本模块共记录 2 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.plotting.plotly.vlevel_plotly._draw_colored_bus_groups_plotly | (net, bus_groups, respect_switches=True, use_line_geo=None, on_map=False, projection=None, map_style='basic', figsize=1, aspectratio='auto', line_width=2, bus_size=10, filename='temp-plot.html', auto_open=True, zoomlevel=11) | pandapower/plotting/plotly/vlevel_plotly.py#L94 | Internal function of vlevel_plotly() | 输入：net, bus_groups, respect_switches, use_line_geo, on_map, projection, map_style, figsize, aspectratio, line_width, bus_size, filename, auto_open, zoomlevel；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | set；create_line_trace；create_trafo_trace；draw_traces；any；logger.warning；create_generic_coordinates；convert_crs；create_bus_trace；index.tolist；net.line.geo.isna；net.bus.geo.isna；tolist；net.line.index.difference；net.line.from_bus.isin；net.line.to_bus.isin | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotly.vlevel_plotly.vlevel_plotly | (net, respect_switches=True, use_line_geo=None, colors_dict=None, on_map=False, projection=None, map_style='basic', figsize=1, aspectratio='auto', line_width=2, bus_size=10, filename='temp-plot.html', auto_open=True, zoomlevel=11) | pandapower/plotting/plotly/vlevel_plotly.py#L20 | Plots a pandapower network in plotly using lines/buses colors according to the voltage level they belong to. If no geodata is available, artificial geodata is generated. For advanced plotting see the tutorial | 输入：net, respect_switches, use_line_geo, colors_dict, on_map, projection, map_style, figsize, aspectratio, line_width, bus_size, filename, auto_open, zoomlevel；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | create_nxgraph；connected_components；len；get_plotly_color_palette；_draw_colored_bus_groups_plotly；vlev_bus_dict.get；dict；logger.warning；unique；update；zip；vlev_bus_dict.items；vlev_bus_dict.keys；list | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
