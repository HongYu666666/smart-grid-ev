# `pandapower.plotting.plotly.get_colors`

本模块共记录 5 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.plotting.plotly.get_colors._to_plotly_color | (scl, transparence=None) | pandapower/plotting/plotly/get_colors.py#L65 | converts a rgb color in format (0-1,0-1,0-1) to a plotly color 'rgb(0-255,0-255,0-255)' | 输入：scl, transparence；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | soft_dependency_error；format；len；str；mplc.to_rgba；mplc.to_rgb；sys._getframe | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotly.get_colors._to_plotly_palette | (scl, transparence=None) | pandapower/plotting/plotly/get_colors.py#L44 | converts a rgb color palette in format (0-1,0-1,0-1) to a plotly color palette 'rgb(0-255,0-255,0-255)' | 输入：scl, transparence；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | soft_dependency_error；_out.append；format；str；mplc.to_rgba；AssertionError；sys._getframe | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotly.get_colors.get_plotly_cmap | (values, cmap_name='jet', cmin=None, cmax=None) | pandapower/plotting/plotly/get_colors.py#L81 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.plotly.get_colors.get_plotly_cmap 的实现与调用链确认。 | 输入：values, cmap_name, cmin, cmax；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | mplc.Normalize；soft_dependency_error；values.min；values.max；format；cmap；str；norm；sys._getframe | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotly.get_colors.get_plotly_color | (color_string) | pandapower/plotting/plotly/get_colors.py#L24 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.plotly.get_colors.get_plotly_color 的实现与调用链确认。 | 输入：color_string；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | soft_dependency_error；_to_plotly_color；mplc.to_rgba；str；sys._getframe | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotly.get_colors.get_plotly_color_palette | (n) | pandapower/plotting/plotly/get_colors.py#L34 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.plotly.get_colors.get_plotly_color_palette 的实现与调用链确认。 | 输入：n；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | soft_dependency_error；_to_plotly_palette；seaborn.color_palette；hsv；str；np.linspace；sys._getframe | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
