# `pandapower.plotting.plotly.traces`

本模块共记录 14 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.plotting.plotly.traces._create_branch_trace | (net, branches=None, use_branch_geodata=True, respect_separators=False, width=1.0, color='grey', infofunc=None, trace_name='lines', legendgroup=None, cmap=None, cbar_title=None, show_colorbar=True, cmap_vals=None, cmin=None, cmax=None, cpos=1.1, branch_element='line', separator_element='switch', node_element='bus', cmap_vals_category='loading_percent', hoverlabel=None, dash='solid') | pandapower/plotting/plotly/traces.py#L523 | Creates a plotly trace of branch elements. The rather generic, non-power net specific names were introduced to make it usable in other packages, e.g. for pipe networks. | 输入：net, branches, use_branch_geodata, respect_separators, width, color, infofunc, trace_name, legendgroup, cmap, cbar_title, show_colorbar, cmap_vals, cmin, cmax, cpos, branch_element, separator_element, node_element, cmap_vals_category, hoverlabel, dash；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | get_plotly_color；set；enumerate；create_edge_center_trace；branch_traces.append；soft_dependency_error；index.tolist；list；len；isinstance；get_plotly_cmap；branches_to_plot.iterrows；_get_branch_geodata_plotly；no_go_branches_to_plot.iterrows；format；branches_to_plot.index.tolist | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotly.traces._create_node_trace | (net, nodes=None, size=5, patch_type='circle', color='blue', infofunc=None, trace_name='nodes', legendgroup=None, cmap=None, cmap_vals=None, cbar_title=None, cmin=None, cmax=None, cpos=1.0, colormap_column='vm_pu', node_element='bus', branch_element='line') | pandapower/plotting/plotly/traces.py#L205 | Creates a plotly trace of node elements. In pandapower, it should be called by create_bus_traces. The rather generic, non-power net specific names were introduced to make it usable in other packages, e.g. for pipe networks. | 输入：net, nodes, size, patch_type, color, infofunc, trace_name, legendgroup, cmap, cmap_vals, cbar_title, cmin, cmax, cpos, colormap_column, node_element, branch_element；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | get_plotly_color；zip；soft_dependency_error；index.tolist；list；isinstance；pd.Series；Marker；to_list；len；cmap_vals.min；cmap_vals.max；str；ColorBar；apply；logger.error | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotly.traces._get_branch_geodata_plotly | (net, branches, use_branch_geodata, branch_element='line', node_element='bus') | pandapower/plotting/plotly/traces.py#L332 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.plotly.traces._get_branch_geodata_plotly 的实现与调用链确认。 | 输入：net, branches, use_branch_geodata, branch_element, node_element；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | branches.iterrows；tolist；zip；list；len；geojson.utils.coords；apply；np.array；T.flatten；geojson.loads | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotly.traces._in_ipynb | () | pandapower/plotting/plotly/traces.py#L38 | An auxiliary function which checks if plot is called from a jupyter-notebook or not | 输入：无显式输入；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | get_ipython | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotly.traces.create_bus_trace | (net, buses=None, size=5, patch_type='circle', color='blue', infofunc=None, trace_name='buses', legendgroup=None, cmap=None, cmap_vals=None, cbar_title=None, cmin=None, cmax=None, cpos=1.0, colormap_column='vm_pu') | pandapower/plotting/plotly/traces.py#L134 | Creates a plotly trace of pandapower buses. It is a wrapper function for the more generic _create_node_trace function. | 输入：net, buses, size, patch_type, color, infofunc, trace_name, legendgroup, cmap, cmap_vals, cbar_title, cmin, cmax, cpos, colormap_column；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | _create_node_trace | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotly.traces.create_dcline_trace | (net, dclines=None, width=1.0, color='grey', infofunc=None, trace_name='hvdc lines', legendgroup='dclines', cmap=None, cbar_title=None, show_colorbar=True, cmap_vals=None, cmin=None, cmax=None, cpos=1.1, cmap_vals_category='loading_percent', hoverlabel=None) | pandapower/plotting/plotly/traces.py#L449 | Creates a plotly trace of pandapower dclines. It is a power net specific wrapper function for the more generic _create_line_trace function. | 输入：net, dclines, width, color, infofunc, trace_name, legendgroup, cmap, cbar_title, show_colorbar, cmap_vals, cmin, cmax, cpos, cmap_vals_category, hoverlabel；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | _create_branch_trace | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotly.traces.create_edge_center_trace | (line_trace, size=1, patch_type='circle', color='white', infofunc=None, trace_name='edge_center', use_line_geo=False, showlegend=False, legendgroup=None, hoverlabel=None) | pandapower/plotting/plotly/traces.py#L74 | Creates a plotly trace of pandapower buses. | 输入：line_trace, size, patch_type, color, infofunc, trace_name, use_line_geo, showlegend, legendgroup, hoverlabel；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | center_trace.update；list；get_line_neutral；x.append；y.append；zip | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotly.traces.create_line_trace | (net, lines=None, use_line_geo=True, respect_switches=False, width=1.0, color='grey', infofunc=None, trace_name='lines', legendgroup='lines', cmap=None, cbar_title=None, show_colorbar=True, cmap_vals=None, cmin=None, cmax=None, cpos=1.1, cmap_vals_category='loading_percent', hoverlabel=None, dash='solid') | pandapower/plotting/plotly/traces.py#L370 | Creates a plotly trace of pandapower lines. It is a power net specific wrapper function for the more generic _create_line_trace function. | 输入：net, lines, use_line_geo, respect_switches, width, color, infofunc, trace_name, legendgroup, cmap, cbar_title, show_colorbar, cmap_vals, cmin, cmax, cpos, cmap_vals_category, hoverlabel, dash；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | _create_branch_trace | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotly.traces.create_scale_trace | (net, weighted_trace, down_shift=0) | pandapower/plotting/plotly/traces.py#L1050 | Create a scale (marker size legend) for a weighted_marker_trace. | 输入：net, weighted_trace, down_shift；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | zip；enumerate；max；min；scale_traces.append；apply；math.ceil；scale_info.get；mean；net.bus.geo.dropna | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotly.traces.create_trafo_trace | (net, trafos=None, color='green', trafotype='2W', width=5, infofunc=None, cmap=None, trace_name='2W transformers', cmin=None, cmax=None, cmap_vals=None, matching_params=None, use_line_geo=None) | pandapower/plotting/plotly/traces.py#L781 | Creates a plotly trace of pandapower trafos. | 输入：net, trafos, color, trafotype, width, infofunc, cmap, trace_name, cmin, cmax, cmap_vals, matching_params, use_line_geo；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | get_plotly_color；index.isin；enumerate；create_edge_center_trace；trafo_traces.append；soft_dependency_error；index.tolist；list；len；isinstance；get_plotly_cmap；trafos_to_plot.iterrows；net.trafo.hv_bus.isin；net.trafo.lv_bus.isin；UserWarning；pd.Series | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotly.traces.create_weighted_marker_trace | (net: ADict, elm_type: str='load', elm_ids: list[int] \| None=None, column_to_plot: str='p_mw', sizemode: str='area', color: str \| tuple[int, int, int]='red', patch_type: str='circle', marker_scaling: float=1.0, trace_name: str='', infofunc: pd.Series[Any] \| Callable \| None=None, node_element: str='bus', show_scale_legend: bool=True, scale_marker_size: float \| list[float] \| None=None, scale_marker_color: str \| None=None, scale_legend_unit: str \| None=None, trace_kwargs: dict \| None=None) -> dict | pandapower/plotting/plotly/traces.py#L911 | Create a single-color plotly trace markers/patches (e.g., bubbles) of value-dependent size. | 输入：net, elm_type, elm_ids, column_to_plot, sizemode, color, patch_type, marker_scaling, trace_name, infofunc, node_element, show_scale_legend, scale_marker_size, scale_marker_color, scale_legend_unit, trace_kwargs；输出：dict | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | any；apply；zip；unit.upper；index.tolist；list；logger.warning；column_to_plot.split；len；values_by_bus.apply；isinstance；pd.Series；marker_trace.update；sum；upper；str | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotly.traces.draw_traces | (traces, on_map=False, map_style='basic', showlegend=True, figsize=1, aspectratio='auto', filename='temp-plot.html', auto_open=True, **kwargs) | pandapower/plotting/plotly/traces.py#L1112 | plots all the traces to PLOTLY (see https://plot.ly/python/) | 输入：traces, on_map, map_style, showlegend, figsize, aspectratio, filename, auto_open, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | Figure；kwargs.get；soft_dependency_error；fig.add_annotation；_in_ipynb；init_notebook_mode；plot；_on_map_test；logger.warning；Layout；kwargs.pop；np.array；max；str；trace.keys；trace.pop | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotly.traces.get_line_neutral | (coord) | pandapower/plotting/plotly/traces.py#L57 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.plotly.traces.get_line_neutral 的实现与调用链确认。 | 输入：coord；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | len；sum_line_length；zip | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotly.traces.sum_line_length | (pts) | pandapower/plotting/plotly/traces.py#L50 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.plotly.traces.sum_line_length 的实现与调用链确认。 | 输入：pts；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | map；sum；zip；math.hypot | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
