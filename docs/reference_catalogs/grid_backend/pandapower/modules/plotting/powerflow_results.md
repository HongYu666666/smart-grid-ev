# `pandapower.plotting.powerflow_results`

本模块共记录 3 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.plotting.powerflow_results.plot_loading | (net, ax=None, element_type='line', box_color='b', median_color='r', whisker_color='k', index_subset=None, **kwargs) | pandapower/plotting/powerflow_results.py#L138 | Plot a boxplot of loadings | 输入：net, ax, element_type, box_color, median_color, whisker_color, index_subset, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | ax.boxplot；list；ax.set_ylabel；ax.set_xticks；warn_and_fix_parameter_renaming；soft_dependency_error；plt.figure；plt.gca；index.isin；boxplot.keys；plt.setp；locals；str；np.isnan；element_type.capitalize；sys._getframe | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.powerflow_results.plot_voltage_profile | (net, ax=None, plot_transformers=True, xlabel='Distance from Slack [km]', ylabel='Voltage [pu]', x0=0, line_color='grey', trafo_color='r', bus_colors='b', line_loading_weight=False, voltage_column=None, bus_size=3, lines=None, **kwargs) | pandapower/plotting/powerflow_results.py#L24 | Plot the voltage profile depending on the distance from the slack. | 输入：net, ax, plot_transformers, xlabel, ylabel, x0, line_color, trafo_color, bus_colors, line_loading_weight, voltage_column, bus_size, lines, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | warn_and_fix_parameter_renaming；np.union1d；soft_dependency_error；DeprecationWarning；plt.figure；plt.gca；ValueError；calc_distance_to_bus；iterrows；ax.set_xlabel；ax.set_ylabel；str；any；query；transformers.iterrows；ax.plot | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.powerflow_results.voltage_profile_to_bus_geodata | (net, voltages=None, root_bus=None) | pandapower/plotting/powerflow_results.py#L198 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.powerflow_results.voltage_profile_to_bus_geodata 的实现与调用链确认。 | 输入：net, voltages, root_bus；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | create_nxgraph；np.setdiff1d；mg.add_edges_from；pd.Series；pd.DataFrame；nx.single_source_dijkstra_path_length；ValueError | 需要适配：可作为新系统数据模型、求解、控制或错误契约的参考 | 源码确认（静态 AST） | 自动生成 |
