# `pandapower.plotting.to_html`

本模块共记录 8 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| class | pandapower.plotting.to_html.Raw | class(object) | pandapower/plotting/to_html.py#L42 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.to_html.Raw 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.plotting.to_html.Raw.__init__ | (self, html) | pandapower/plotting/to_html.py#L43 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.to_html.Raw.__init__ 的实现与调用链确认。 | 输入：html；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| class | pandapower.plotting.to_html.Tag | class(object) | pandapower/plotting/to_html.py#L47 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.to_html.Tag 的实现与调用链确认。 | 输入：构造参数由 __init__ 或工厂函数定义；输出：类实例 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | join；Raw；isinstance；escape；attr.rstrip；kwargs.items；str | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.plotting.to_html.Tag.__call__ | (self, *args, **kwargs) | pandapower/plotting/to_html.py#L51 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.to_html.Tag.__call__ 的实现与调用链确认。 | 输入：*args, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | join；Raw；isinstance；escape；attr.rstrip；kwargs.items；str | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| method | pandapower.plotting.to_html.Tag.__init__ | (self, name) | pandapower/plotting/to_html.py#L48 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.to_html.Tag.__init__ 的实现与调用链确认。 | 输入：name；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | 静态扫描未发现直接函数调用 | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.to_html._net_to_graphjs | (net, respect_switches=True, include_lines=True, include_trafos=True, images=None) | pandapower/plotting/to_html.py#L57 | Converts a pandapower network into a graph for vis.js which contains a simplified representation of a network's topology, reduced to nodes and edges. Busses are being represented by nodes (Note: only buses with in_service = 1 appear in the graph), edges represent physical connections between buses (typically lines or trafos). | 输入：net, respect_switches, include_lines, include_trafos, images；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | set；net.trafo3w.iterrows；int；str；images.get；zip；json.dumps；combinations | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.to_html._net_to_html | (net, respect_switches=True, include_lines=True, include_trafos=True, show_tables=True, images=None) | pandapower/plotting/to_html.py#L172 | Converts a pandapower network into an html page which contains a simplified representation of a network's topology, reduced to nodes and edges. Busses are being represented by nodes (Note: only buses with in_service = 1 appear in the graph), edges represent physical connections between buses (typically lines or trafos). | 输入：net, respect_switches, include_lines, include_trafos, show_tables, images；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | Tag；_net_to_graphjs；HTML；HEAD；BODY；SCRIPT；getattr；len；STYLE；DIV；Raw；TABLE；tables.append；TR；H2；map | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.to_html.to_html | (net, filename, respect_switches=True, include_lines=True, include_trafos=True, show_tables=True) | pandapower/plotting/to_html.py#L18 | Saves a pandapower Network to an html file. | 输入：net, filename, respect_switches, include_lines, include_trafos, show_tables；输出：返回值未注解 | 存在文件或序列化读写调用；具体路径和覆盖行为需按调用场景复核 | filename.endswith；Exception；open；_net_to_html；f.write；f.close | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
