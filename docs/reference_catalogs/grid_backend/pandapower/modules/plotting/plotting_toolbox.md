# `pandapower.plotting.plotting_toolbox`

本模块共记录 11 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.plotting.plotting_toolbox._get_coords_from_geojson | (gj_str) | pandapower/plotting/plotting_toolbox.py#L27 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.plotting_toolbox._get_coords_from_geojson 的实现与调用链确认。 | 输入：gj_str；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | pd.isnull；re.findall；len；ValueError；ast.literal_eval | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotting_toolbox._rotate_dim2 | (arr, ang) | pandapower/plotting/plotting_toolbox.py#L15 | Rotate the input vector with the given angle. | 输入：arr, ang；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | np.dot；np.array；np.cos；np.sin | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotting_toolbox.coords_from_node_geodata | (element_indices, from_nodes, to_nodes, node_geodata, table_name, node_name='Bus', ignore_no_geo_diff=True, node_geodata_to=None) | pandapower/plotting/plotting_toolbox.py#L144 | Auxiliary function to get the node coordinates for a number of branches with respective from and to nodes. The branch elements for which there is no geodata available are not included in the final list of coordinates. | 输入：element_indices, from_nodes, to_nodes, node_geodata, table_name, node_name, ignore_no_geo_diff, node_geodata_to；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | sum；node_geodata.apply；node_geodata_to.apply；np.sum；len；np.isin；logger.warning；isnull；zip；np.array | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotting_toolbox.get_angle_list | (angle, number_entries, name_entries='nodes') | pandapower/plotting/plotting_toolbox.py#L128 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.plotting_toolbox.get_angle_list 的实现与调用链确认。 | 输入：angle, number_entries, name_entries；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | get_list | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotting_toolbox.get_collection_sizes | (net, bus_size=1.0, ext_grid_size=1.0, trafo_size=1.0, load_size=1.0, sgen_size=1.0, switch_size=2.0, switch_distance=1.0, gen_size=1.0) | pandapower/plotting/plotting_toolbox.py#L42 | Calculates the size for most collection types according to the distance between min and max geocoord so that the collections fit the plot nicely | 输入：net, bus_size, ext_grid_size, trafo_size, load_size, sgen_size, switch_size, switch_distance, gen_size；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | sum；apply；map；net.bus.geo.apply；zip | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotting_toolbox.get_color_list | (color, number_entries, name_entries='nodes') | pandapower/plotting/plotting_toolbox.py#L121 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.plotting_toolbox.get_color_list 的实现与调用链确认。 | 输入：color, number_entries, name_entries；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | get_list；all；logger.info；len；isinstance；str | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotting_toolbox.get_index_array | (indices, net_table_indices) | pandapower/plotting/plotting_toolbox.py#L136 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.plotting_toolbox.get_index_array 的实现与调用链确认。 | 输入：indices, net_table_indices；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | np.array；np.copy；isinstance；list | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotting_toolbox.get_linewidth_list | (linewidth, number_entries, name_entries='lines') | pandapower/plotting/plotting_toolbox.py#L132 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.plotting_toolbox.get_linewidth_list 的实现与调用链确认。 | 输入：linewidth, number_entries, name_entries；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | get_list | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotting_toolbox.get_list | (individuals, number_entries, name_ind, name_ent) | pandapower/plotting/plotting_toolbox.py#L86 | Auxiliary function to create a list of specified length from an input value that could be either an iterable or a single value. Strings are treated as non-iterables. In case of iterables and the length not matching the specified length, the input values are repeated or capped to match the specified length. | 输入：individuals, number_entries, name_ind, name_ent；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | hasattr；isinstance；len；list；logger.warning；int | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotting_toolbox.position_on_busbar | (net, bus, busbar_coords) | pandapower/plotting/plotting_toolbox.py#L252 | Checks if the first or the last coordinates of a line are on a bus | 输入：net, bus, busbar_coords；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | deprecated；_get_coords_from_geojson；range；len | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.plotting_toolbox.set_line_geodata_from_bus_geodata | (net, line_index=None, overwrite=False, ignore_no_geo_diff=True) | pandapower/plotting/plotting_toolbox.py#L215 | Sets coordinates in net.line.geo based on the from_bus and to_bus coordinates in net.bus.geo :param net: pandapowerNet :param line_index: index of lines, coordinates of which will be set from bus geo (all lines if None) :param overwrite: whether the existing coordinates in net.line.geo must be overwritten :return: None | 输入：net, line_index, overwrite, ignore_no_geo_diff；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | coords_from_node_geodata；all；logger.warning；len；logger.info；net.bus.geo.isnull；net.line.geo.isnull；intersection；pd.Index | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
