# `pandapower.plotting.simple_plot`

本模块共记录 11 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.plotting.simple_plot._build_cmap_from_limits | (limits: tuple \| list, colormap_type: str, kind: str='line') -> list | pandapower/plotting/simple_plot.py#L215 | Build a pandapower ``cmap_list`` from sorted numeric breakpoints. | 输入：limits, colormap_type, kind；输出：list | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len；_pick_n_colors；range | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.simple_plot._extract_cbar_ticks | (cmap_list: list, colormap_type: str) -> list | pandapower/plotting/simple_plot.py#L244 | Extract tick positions from a ``cmap_list`` at the user-defined breakpoints. | 输入：cmap_list, colormap_type；输出：list | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | sorted；ticks.append | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.simple_plot._pick_n_colors | (n: int, palette: list[str]) -> list[str] | pandapower/plotting/simple_plot.py#L190 | Sample *n* colors evenly from *palette* using floor-based index mapping. | 输入：n, palette；输出：list[str] | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | len；math.floor；range；list | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.simple_plot._set_colormap_mode | (mode: str, state: dict, ax, normal_colls: list, cmap_colls: list, colorbars: list, btn_normal, btn_colormap) | pandapower/plotting/simple_plot.py#L271 | Switch the figure between ``"normal"`` and ``"colormap"`` display mode. | 输入：mode, state, ax, normal_colls, cmap_colls, colorbars, btn_normal, btn_colormap；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | btn_normal.ax.set_facecolor；btn_colormap.ax.set_facecolor；fig.canvas.draw_idle；c.set_visible；fig.set_size_inches；ax.set_position；zip；cbar.ax.set_visible；cbar.ax.set_position | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.simple_plot.bus_info | (bus) | pandapower/plotting/simple_plot.py#L47 | Return an info tuple identifying a bus element. | 输入：bus；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.simple_plot.calculate_unique_angles | (net: pandapowerNet) -> dict[int, dict[str, dict[str, float] \| float]] | pandapower/plotting/simple_plot.py#L929 | Calculate patch placement angles for sgen, gen, and load symbols at each bus. | 输入：net；输出：dict[int, dict[str, dict[str, float] \| float]] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | unstack；pd.Series；fillna；sum；defaultdict；loads.items；df.iterrows；float；size；net.load.bus.unique；pd.concat；patch_counts.ne；row.items；net.sgen.groupby；net.gen.groupby；isinstance | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.simple_plot.hover | (event, ax, net, hover_text) | pandapower/plotting/simple_plot.py#L95 | Update the hover text in an interactive pandapower plot based on mouse position. | 输入：event, ax, net, hover_text；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | hover_text.get_visible；getattr；collection.contains；hover_text.set_text；hover_text.set_ha；hover_text.set_va；hover_text.set_multialignment；hover_text.set_position；hover_text.set_visible；fig.canvas.draw_idle；isinstance；len；str | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.simple_plot.line_info | (line) | pandapower/plotting/simple_plot.py#L59 | Return an info tuple identifying a line element. | 输入：line；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.simple_plot.simple_plot | (net: pandapowerNet, respect_switches: bool=False, line_width: float=3.0, bus_size: float=1.0, ext_grid_size: float=1.0, trafo_size: float=1.0, plot_loads: bool=False, plot_gens: bool=False, plot_sgens: bool=False, orientation=None, load_size: float=1.0, gen_size: float=1.0, sgen_size: float=1.0, switch_size: float=2.0, switch_distance: float=1.0, plot_line_switches: bool=False, plot_bus_switches: bool=False, scale_size: bool=True, bus_color='#1c3f52', line_color='grey', dcline_color='c', trafo_color='k', ext_grid_color='#1c3f52', switch_color='k', library='igraph', show_plot: bool=True, ax=None, draw_by_type: bool=True, bus_dc_size: float=1.0, bus_dc_color='m', line_dc_color='c', vsc_size: float=4.0, vsc_color='orange', highlight_buses=None, highlight_lines=None, enable_hover=True, highlight_bus_size_factor=2.0, highlight_line_width_factor=2.5, highlight_color='red', colormap_type: str='continuous', line_limits: tuple=(0, 25, 50, 75, 100), bus_limits: tuple=(0.9, 0.95, 1.0, 1.05, 1.1), cmap_lines: list=None, cmap_buses: list=None, plot_colorbars: bool=True) | pandapower/plotting/simple_plot.py#L364 | Plot a pandapower network as simply as possible. | 输入：net, respect_switches, line_width, bus_size, ext_grid_size, trafo_size, plot_loads, plot_gens, plot_sgens, orientation, load_size, gen_size, sgen_size, switch_size, switch_distance, plot_line_switches, plot_bus_switches, scale_size, bus_color, line_color, dcline_color, trafo_color, ext_grid_color, switch_color, library, show_plot, ax, draw_by_type, bus_dc_size, bus_dc_color, line_dc_color, vsc_size, vsc_color, highlight_buses, highlight_lines, enable_hover, highlight_bus_size_factor, highlight_line_width_factor, highlight_color, colormap_type, line_limits, bus_limits, cmap_lines, cmap_buses, plot_colorbars；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | in_service_lines.difference；draw_collections；tuple；list；logger.warning；create_generic_coordinates；get_collection_sizes；len；create_bus_collection；collections.append；pd.Index；set；create_line_collection；create_dcline_collection；create_vsc_collection；create_ext_grid_collection | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.simple_plot.trafo3w_info | (idx) | pandapower/plotting/simple_plot.py#L83 | Return an info tuple identifying a three-winding transformer element. | 输入：idx；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.simple_plot.trafo_info | (idx) | pandapower/plotting/simple_plot.py#L71 | Return an info tuple identifying a two-winding transformer element. | 输入：idx；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | 静态扫描未发现直接函数调用 | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
