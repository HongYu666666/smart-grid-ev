# `pandapower.plotting.patch_makers`

本模块共记录 16 个函数、类或方法。

| kind | qualified_name | signature | source_anchor | summary | inputs_outputs | side_effects | dependencies | development_relevance | evidence_status | review_status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| function | pandapower.plotting.patch_makers.ellipse_patches | (node_coords, width, height, angle=0, color=None, **kwargs) | pandapower/plotting/patch_makers.py#L231 | Function to create a list of ellipse patches from node coordinates. | 输入：node_coords, width, height, angle, color, **kwargs；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | get_angle_list；soft_dependency_error；len；get_color_list；zip；patches.append；str；Ellipse；sys._getframe | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.patch_makers.ext_grid_patches | (node_coords, size, angles, **kwargs) | pandapower/plotting/patch_makers.py#L516 | Creation function of patches for external grids. | 输入：node_coords, size, angles, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | kwargs.get；get_angle_list；get_color_list；enumerate；soft_dependency_error；len；polys.append；lines.append；_rotate_dim2；Rectangle；str；np.array；sys._getframe | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.patch_makers.gen_patches | (node_coords, size, angles, patch_type, draw_by_type: bool=True, **kwargs) | pandapower/plotting/patch_makers.py#L361 | Creation function of patches for generators. | 输入：node_coords, size, angles, patch_type, draw_by_type, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | kwargs.get；get_angle_list；get_list；get_color_list；enumerate；soft_dependency_error；len；polys.append；lines.append；_rotate_dim2；Circle；Arc；str；np.array；sys._getframe | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.patch_makers.load_patches | (node_coords, size, angles, **kwargs) | pandapower/plotting/patch_makers.py#L324 | Creation function of patches for loads. | 输入：node_coords, size, angles, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | kwargs.get；get_angle_list；get_color_list；enumerate；soft_dependency_error；len；polys.append；lines.append；_rotate_dim2；RegularPolygon；np.array；sys._getframe | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.patch_makers.node_patches | (node_coords, size, patch_type, colors=None, **kwargs) | pandapower/plotting/patch_makers.py#L184 | Creates node patches from coordinates translating the patch type into patches. | 输入：node_coords, size, patch_type, colors, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | soft_dependency_error；kwargs.pop；ellipse_patches；patch_type.lower；rectangle_patches；startswith；str；len；int；polygon_patches；logger.error；ValueError；set；sys._getframe；kwargs.keys | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.patch_makers.polygon_patches | (node_coords, radius, num_edges, color=None, **kwargs) | pandapower/plotting/patch_makers.py#L293 | Function to create a list of polygon patches from node coordinates. The number of edges for the polygon can be defined. | 输入：node_coords, radius, num_edges, color, **kwargs；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | soft_dependency_error；get_color_list；zip；len；patches.append；str；RegularPolygon；sys._getframe | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.patch_makers.pv_patch | (node_geo, offset, size, angle, pv_rect_size, pv_tri_size, facecolor, edgecolor) -> tuple[list, list] | pandapower/plotting/patch_makers.py#L144 | Generate PV patch Items | 输入：node_geo, offset, size, angle, pv_rect_size, pv_tri_size, facecolor, edgecolor；输出：tuple[list, list] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | Rectangle；polys.append；lines.append；Polygon；soft_dependency_error；_rotate_dim2；np.array；str；sys._getframe | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.patch_makers.rectangle_patches | (node_coords, width, height, color=None, **kwargs) | pandapower/plotting/patch_makers.py#L263 | Function to create a list of rectangle patches from node coordinates. | 输入：node_coords, width, height, color, **kwargs；输出：返回值未注解 | 静态扫描未确认外部副作用；运行态副作用仍需在 PoC 中复核 | soft_dependency_error；get_color_list；zip；len；patches.append；str；Rectangle；sys._getframe | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.patch_makers.sgen_patches | (node_coords, size, angles, patch_type, draw_by_type: bool=True, **kwargs) | pandapower/plotting/patch_makers.py#L411 | Creation function of patches for static generators. | 输入：node_coords, size, angles, patch_type, draw_by_type, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | kwargs.get；get_angle_list；get_list；get_color_list；enumerate；soft_dependency_error；len；polys.extend；lines.extend；wye_patch；str；wp_patch；pv_patch；sys._getframe | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.patch_makers.storage_patches | (node_coords, size, angles, **kwargs) | pandapower/plotting/patch_makers.py#L477 | Creation function of patches for storage systems. | 输入：node_coords, size, angles, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | kwargs.get；enumerate；lines.append；_rotate_dim2；np.array | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.patch_makers.trafo_patches | (coords, size, **kwargs) | pandapower/plotting/patch_makers.py#L552 | Creates a list of patches and line coordinates representing transformers each connecting two nodes. | 输入：coords, size, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | kwargs.get；get_color_list；get_linewidth_list；list；enumerate；soft_dependency_error；len；map；np.array；np.all；np.sqrt；circles.append；lines.append；np.sum；Circle；str | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.patch_makers.vsc_patches | (coords, size, **kwargs) | pandapower/plotting/patch_makers.py#L696 | Creates a list of patches and line coordinates representing VSCs each connecting an AC and a DC node. | 输入：coords, size, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | kwargs.get；get_color_list；get_linewidth_list；list；enumerate；soft_dependency_error；len；map；np.array；np.all；np.sqrt；squares.append；lines.append；np.sum；Rectangle；str | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.patch_makers.ward_patches | (node_coords, size, angles, **kwargs) | pandapower/plotting/patch_makers.py#L607 | Creation function of patches for wards. | 输入：node_coords, size, angles, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | kwargs.get；get_angle_list；get_color_list；enumerate；soft_dependency_error；len；polys.append；lines.append；TextPath；rotate；PathPatch；_rotate_dim2；Rectangle；transform.transform_path；str；np.array | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.patch_makers.wp_patch | (node_geo: Point, offset: float, size: float, angle: float, facecolor: str, edgecolor: str, blade_coord1: float, blade_coord2: float, hub_size: float, path: any) -> tuple[list[Patch], list] | pandapower/plotting/patch_makers.py#L68 | Generate Patch for wind power plant. | 输入：node_geo, offset, size, angle, facecolor, edgecolor, blade_coord1, blade_coord2, hub_size, path；输出：tuple[list[Patch], list] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | zip；polys.append；lines.append；range；soft_dependency_error；_rotate_dim2；PathPatch；Circle；np.array；path；rotated_verts.append；str；math.cos；math.sin；sys._getframe | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.patch_makers.wye_patch | (node_geo, offset, size, r_triangle, angle, facecolor, edgecolor) -> tuple[list[Patch], list] | pandapower/plotting/patch_makers.py#L30 | 待人工补充：源码未提供可直接确认的功能说明；需结合 pandapower.plotting.patch_makers.wye_patch 的实现与调用链确认。 | 输入：node_geo, offset, size, r_triangle, angle, facecolor, edgecolor；输出：tuple[list[Patch], list] | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | polys.append；lines.append；soft_dependency_error；_rotate_dim2；Circle；RegularPolygon；np.array；str；sys._getframe | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
| function | pandapower.plotting.patch_makers.xward_patches | (node_coords, size, angles, **kwargs) | pandapower/plotting/patch_makers.py#L651 | Creation function of patches for xwards. | 输入：node_coords, size, angles, **kwargs；输出：返回值未注解 | 可能修改传入对象、网络表或仿真状态；调用前后状态需由适配器校验 | kwargs.get；get_angle_list；get_color_list；enumerate；soft_dependency_error；len；polys.append；lines.append；TextPath；rotate；PathPatch；_rotate_dim2；Rectangle；transform.transform_path；str；np.array | 仅内部参考：不进入首轮后端适配器 | 源码确认（静态 AST） | 自动生成 |
